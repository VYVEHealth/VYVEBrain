> **[PM-1222 · 2026-09-13 · DECISION: picker = the library over the builder, multi-add, Done]** Ready to build **Calum Batch 2** on "calum batch 2": (1) taxonomy normalisation migration; (2) library component in `132-shared-library.js` — rail with counts, search, video/sort chips, card grid, preview sheet — as the Exercise Library page for coach + physio; (3) the same component as a sheet over the day/programme builder with a running "in this session" strip and Done → rows into the block; (4) per-row tracking select in the builder (#13); (5) thumbnails in the Weekly workout view (#12); (6) search on the three template lists (#14); (7) builder as a page instead of an appended card (#15). ~2 sessions. Batch 3 (blocks, Mon–Sun/Session-N, days-directly programmes, tiers) stays talk-first.

