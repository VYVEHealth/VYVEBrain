> **[PM-1221 · 2026-09-13 · PROGRAMME BUILDER MOCKUP — CC `f49648d8`, `docs/mockups/programme-builder-mockup.html`]** **Open:** (1) **Dean:** click through the deployed mockup (chooser → Detailed → day tabs → + Add day → Add an exercise → drawer). (2) **Dean → Calum:** does the exercise picker need a browse rail, or is search-by-name enough (Kahunas has no library page in the picker)? Drawer is search-first with the rail folded, so either answer is one line. (3) **Decisions before build:** programme = days directly with day templates as an import source (the mockup) vs today's phases-of-day-templates; Simple programmes wanted or not; PDF upload parked. (4) **Batch 2 (coach-side, no contract change):** builder as a page (#15), drawer picker (#11), thumbnails in weekly workouts (#12), per-row tracking (#13 — `programme_json` already carries `exercise_type`/`duration_seconds`, the UI just never exposed it), template list search (#14), taxonomy normalisation (#17). **Batch 3 (contract change, member app renders it):** blocks (#18), Mon–Sun vs Session-N (#19), programmes from weekly workouts / days directly (#20), packages/access tiers (#21). (5) Both mockups to Calum before build.

