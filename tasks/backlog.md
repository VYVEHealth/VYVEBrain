> **[PM-1253 · 2026-09-13 · RACE DATE WINS + UNITS SHIPPED — vyve-site `ffe51b6c` (vbb 649), NOT OTA'd]** Race day asked before length; slider locked once a race exists; `fitPlanToRace()` gives exact / late-start (start moves later) / too-soon, each stated on screen. Km/miles toggle on the surface, written to `run_member_profile.units` (display only — SI storage untouched). Race day rendered on the calendar, the week tile and the header. **[DEAN] dev-shell check of 648 + 649 together, then OTA on your word.** **[DEAN] the late-start path needs a fresh build to see — 5k Improver with a race in mid-February.** **[DEAN] on your yes, running-plan.html soft-kill + drop `run: true` in exercise.html + remove the PM-1248 dev-panel row — all three go on the same commit, and that commit is what makes Running visible to members, so it waits on [LEWIS] strings.** [LEWIS] every string on running.html and the wizard.

> **[PM-1252 · 2026-09-13 · RUNNING WAVE 3 PART 1 SHIPPED — vyve-site `8374594e` (vbb 648), NOT OTA'd]** `running.html` — plan header, projection, training calendar, session detail with blocks and numbered steps. Running is a Body stream gated on an active plan (PM-1213 pattern). No engine call from the page; §23.349 enforced structurally. 94-assertion harness green against Dean's live plan. **NEXT: Wave 3 part 2 — the catalogue** (half marathon, marathon, 10 mile, first-5k/parkrun bridge, Return to Running; ~30 entries via length × level × days). **[DEAN] a CiRF reviewer is still blocking half and marathon before a member can start one** (open since PM-1241). [PHIL] injury wording. [LEWIS] strings + coach attribution.

> **[PM-1246 · 2026-09-13 · AVAILABILITY SHIPPED + CATALOGUE DECIDED]** `run-engine@0.6.0` takes `available_dows`; wizard mockup v2 has which-days, weekly running, session intensity and surface as real screens (CC `8de4fa79`). **[DEAN] deploy check + react to v2 — the build is still gated on it.** **NEXT AFTER THE WIZARD: five more templates** — half marathon, marathon, 10 mile, first-5k/parkrun bridge, Return to Running (pairs with physio) — nine total, ~30 browsable entries via length × level × days. **[DEAN] a CiRF reviewer is now blocking, not optional: half and marathon must not be startable by a member until someone qualified has read them** (open since PM-1241). [PHIL] injury wording. [LEWIS] strings + coach attribution.

> **[PM-1245 · 2026-09-13 · WIZARD MOCKUP SHIPPED — CC `df9d641c`, nothing built]** `docs/mockups/running-wizard-mockup.html`, twelve screens, phone frame, member tokens. **[DEAN] deploy check then react — the build is gated on this.** Open for him: (1) does the recommendation screen earn its place or should the goal answer go straight into the plan; (2) health pre-fill as its own screen, or folded into the race-time screen; (3) does the week-shape strip belong on the length screen or only on the plan surface at Wave 3; (4) whether a member with no health connection ever sees the pre-fill screen at all. [PHIL] injury wording + consent line + whether an injury answer may change training. [LEWIS] every string, the coach-attribution wording from PM-1242, and any trial framing. On his yes the build is: wizard page unlinked in vyve-site (lands on the dev shell, invisible to members until Wave 3 links it into Body), `run-engine` `build` on the recap, `persist` on Build my plan.

> **[PM-1244 · 2026-09-13 · WAVE 2 PART 1 SHIPPED — variable plan length, `run-engine@0.5.2`]** Templates author as head + repeating cycle + fixed tail; live ranges 5k 6–20, 10k 8–24, general 4–26, start-running fixed at 9 (NHS C25K verbatim). Tail inherits cycle growth so the peak is still the peak (§23.350); long-run ceiling is days-aware after the 3-day 10k was found shipping a "Long Run" shorter than its easy run (§23.351, pre-existing). **NEXT: wizard, recap, projection — mockup first, unlinked page.** [DEAN] the three calls made this session: injury question asked and stored but adapts nothing pre-Phil; projection shown on one race time with a wider range; derived knobs named on the recap. [PHIL] injury question + consent wording. [LEWIS] all wizard strings + any trial framing. [DEAN] CiRF reviewer still open from PM-1241.

> **[PM-1243 · 2026-09-13 · CATALOGUE BREADTH DECIDED — doc only]** Dean: the bar is "feels close to Runna", how we get there is Claude's call. **Finish Waves 2–4 first** — the wizard, recommendation, calendar, session page and completion loop are what feel premium; the member never sees the catalogue size until Wave 3. **Variable plan length moves INTO Wave 2** (phase-based expansion: repeating base block + fixed peak + taper, engine stretches the middle) because the wizard asks for a length and every plan is currently locked to one. **Catalogue expands in Wave 3** to ~10 templates where the training genuinely differs — half, marathon, first-5k/parkrun bridge, Return to Running, 10 mile — presented as ~30 entries via length × level × days. Not building Hyrox/triathlon/Functional Fitness. Pre-manufacturing plans to save API cost is moot: the engine is pure maths, zero per-plan cost.

> **[PM-1242 · 2026-09-13 · CALUM FRONTS THE RUNNING PLANS — migration `pm1242_run_catalogue_coach_name`]** `coach_name` = Calum Denham on all four plans; `authored_by` records the real provenance (Daniels VDOT + NHS C25K structure) so the two are never confused. **[DEAN] Calum still needs to read `docs/running-catalogue-review.html` once and say he is happy for his name on it — the sheet is now a five-minute endorsement ask, not a periodisation review.** [LEWIS] member-facing attribution wording ("your coach" vs "designed with" vs "reviewed by" are different claims).

> **[PM-1241 · 2026-09-13 · RUNNING CATALOGUE CORRECTED — `run-engine` v6 (`0.4.0`)]** Pace bands were a %VO2max-as-%velocity error and are now Daniels-accurate; Daniels' per-session ceilings (T 10% / I 8% + 10km / R 5% / long 30%) enforced by trimming rep counts, with time floors (T 20–30 min, I 12–15 min) so low-mileage weeks still get a real stimulus; Start Running re-authored on the NHS Couch to 5K 9-week shape (migration `pm1241_start_running_nhs_shape`). 22 session templates, 169 week rows, all active. **[DEAN] find a CiRF-qualified reviewer — Calum is a PT, not a running coach, and should not be the gate on endurance periodisation; he keeps the strength-alongside work.** [DEAN] Wave 3 must show Start Running in minutes, not "0 km". [PHIL] injury-history question. [LEWIS] strings. **Wave 2 (wizard, recap, projection) is the next build session.**

> **[PM-1241 · 2026-09-13 · SESSION TABS + CREATE A DAY IN A SLOT — CC `67c6c31b`]** Dean: reload → Weekly plans › + New → tabs across, + Add menu; Programmes › + New → slot select → Create a new day here… → editor over the top → Done → chip. (1) "Save as workout template" checkbox in the programme day sheet — next pass. (2) Programme days as Kahunas-style tabs — with Session-N (Batch 3). (3) Rest block button removal (PM-1240) was not yet deployed in Dean's 22:57 screenshots — same reload.

