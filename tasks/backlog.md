> **[PM-1201 · 2026-09-12 · AIRING PATH — NEXT SESSION (prompt written)]** Done this session: welcome video end-to-end (PM-1198), the three upload fixes (PM-1199), normalise-once-at-ingest worker live (PM-1200), runner padding + hang fix (PM-1201). **Remaining, in order:** (a) **stream-copy at airtime** — `run_occurrence` should `-c copy` when the occurrence's master is normalised (`partner_content_items.normalised_path`, or a session master that has been through a normalise pass), keep the re-encode path for anything else; this removes the §23.318 ceiling. (b) **normalise the 125 existing session masters** the same way (second mode of `vyve-video-normalise.py`: walk `VYVE_MEDIA_DIR`, write `<name>.norm.mp4` or replace in place after a verify, keep originals in cold storage) — 22 are 640×360, 16 are 48kHz, all carry ~10.4s GOPs. (c) **stream-key allocation at scheduling time** — same-category overlap collides on one key; model on the booking slot guard (`session_categories` → a pool of keys, allocate per occurrence). (d) partner scheduling mints a `calendar_occurrences` row (48h min already in the portal). (e) write `replay_video_id` back onto the content item once `refresh-replay-videos` brings the airing home. **Dean checks owed:** 06:00 12 Sep journal line (first padded airing — expect "starting ffmpeg push … [padded]", LIVE at ~06:00:00, "holding 20s", COMPLETE); CC deploy of `d52c7912` + an `IMG_`-file upload on the portal; OTA 642 when happy with the dev shell.
>
> **[PM-1198 · welcome video — small follow-ups]** (1) The Replays/Sessions tabs still route uploads through `partner-content-play` signed URLs — fine for now, but once the airing path lands, any upload that airs gets a `replay_video_id` and plays via YouTube; the signed-URL player then only serves never-aired items. (2) `vyve-video-publish` is welcome-only (`VYVE_YT_WELCOME_ONLY`); widen only if a non-airing YouTube route is ever wanted again. (3) `partner_content_items.duration_sec` is now filled for new uploads and back-filled by the normaliser — the profile cards can show it.
>
> **[PM-1197 · SMALLER, BANKED]** ~~Capture width/height + duration at upload~~ ~~Ask for a real title at upload~~ ~~`pillar` is defaulting~~ — all three SHIPPED PM-1199. ~~`storage-ingest` EF~~ DELETED PM-1201. Partner collections (the unused `program` type) when a library actually grows. **Correction to PM-1178: April's failed uploads were iCloud-resident files, not size** — the sweep will keep alerting on that shape.

> **[PM-1194 · 2026-09-11 · PARTNER DOCUMENT DOWNLOADS — Sharon's ADHD booklet (not built)]** `partner_content_items.type` is `video | live_session | program | post`; no PDF anywhere in the partner space (`partner-docs` is private, 15MB, qualifications only). Build: `type='document'` (CHECK widened), `partner-downloads` bucket (signed read, 25MB, PDF), `partner-content-upload` new slot + attempt tracking (PM-1178), portal Content library "Upload a document" on the same moderation path, member `partner-profile.html` Downloads section via signed URL (OTA), CC content-library chip + Play→Open. ~1 session, mockup first. Lewis reads clinical-adjacent documents before go-live (educational, not §23.84). Interim line to Sharon: "exactly the kind of thing — downloads slot is coming."
>
> **PARTNER PROVISION EMAIL v7 (carry-over from PM-1187 item 2):** add "forgot your password? tap Forgot on the portal sign-in" now that PM-1194 exists — stop sending partners to the app login page.

> **[2026-09-11 · PM-1193 — OTA APPLY-ON-NAVIGATION SHIPPED + PROVEN; PM-1182 SERVER HALF CLOSED]** Items (0) and (2) of the PM-1189 block below are DONE (vyve-site `5ab8b9d9` 637 / `17046c93` 638 / `5928b3fc` 639, prod deployment `cfaa576c`; `member-dashboard` v94); (1) was closed by PM-1189b and Dean's owed cold-start check is now moot — he has seen 638 and 639 on both store devices. **Remaining, small:** **(a) Dashboard-delete the `pm1190-tarball-relay` EF** (inert 410; redeploy with the new pinned SHA at the next push per §23.316). **(b) First real Android health sync owed from a device:** all 6 `health_connect` connection rows carry `total_synced 0` and there is not one `health_connect` row in `member_health_daily` — v94 is proven against a fixture, not a phone; Settings → Health on the Samsung, sync, then check `member_health_daily` for `source='health_connect'` and the home habit for the auto-tick. **Dean's plan: taking the Samsung out on 12 Sep, walking, then syncing.** Pass = a `health_connect` steps row for 2026-09-12 + the steps habit auto-ticked on Home. Fail = check whether the row exists at all (sync never reached the server → healthbridge/sync-health-data side) before touching v94. (Dean bought the Samsung purely for testing and does not carry it — Android proofs are scheduled events, not something that happens by itself.) **(c) PM-1189 items (3)–(7) unchanged:** vyve-capacitor native commit + `mode-ship.sh` `google-services.json` assert; Android "Enable notifications" card re-show after sign-out (`push-native.js` flag wiped by `vyveSignOut`); cron 18 header audit; Lewis copy pass; Play Console after 58 approves; App Review for iOS 2.0.2 (7). **(d) Hold-list note:** nav.js holds by pathname; a future in-flow page on a hub path should set `window.__vyveHoldUpdate=true` rather than widen the list.

> **[2026-09-11 · PM-1189 — ANDROID 1.0.12 (58) IN PLAY REVIEW; OTA APPLY-ON-NAVIGATION IS THE NEXT BUILD]** **(0) TOP, Dean's call tonight: OTA must not need a cold start.** Design agreed in principle: keep the background download + stage as now; on the next HUB-LEVEL navigation (nav.js tab tap: Home/Mind/Body/Connect/More) with a bundle fully downloaded, `LiveUpdate.reload()` into it and land on the tapped page via the existing return-to mechanism; never mid-workout-session / mid-check-in; first-run fast path unchanged. Files: `live-update.js` + `nav.js`; OTA-deliverable (existing members need ONE last cold start to receive it). ~1 session + device test both platforms. Write the session prompt. **(1) DONE — OTA pushed (deployment `7cf92bcd`, main `1c84d929`, vbb 636) and iOS 2.0.2 (7) submitted, both 11 Sep ~22:35–23:00.** Dean's cold-start check on the store iPhone (Settings → Update 636) still owed. Watch both store reviews; on iOS approval nothing changes for members (same code as the OTA). Note iOS dev shells at main had a dead Workouts tab from 8–11 Sep (PM-1184) — fixed on main. **(2) `member-dashboard` server half of PM-1182:** `health_connection_state`, `hasHealthkitConnection` (habit auto-tick evaluation) and the `member_health_daily source=eq.healthkit` filter must all count `health_connect`; check `sync-health-data` stamps `source` per platform and `_shared/achievements.ts` `isHkConnected` is platform-agnostic. Until then Android members never auto-tick habits. Multi-file EF — redeploy with `_shared/`. **(3) vyve-capacitor native commit owed (Mac-local):** 58/1.0.12 bump, CAMERA permission, `mode-dev.sh`/`mode-ship.sh` (PM-1120) + a `google-services.json` assert in `mode-ship.sh` (§23.309). **(4) "Enable notifications" card re-shows after sign-out on Android** — VYVE's own pre-prompt card keyed on a flag that `vyveSignOut` wipes; Android's system dialog can't re-ask once granted. Find the flag in `push-native.js`, persist across sign-out. Cosmetic. **(5) Cron job 18 (`process-scheduled-pushes`) carries the legacy service-role JWT in its header** — that EF accepts it today; `push-send-native` would not. Audit cron headers against the `sb_secret_…` key rotation. **(6) Lewis copy pass (non-gating):** "Create your programme / Create my programme", Health Connect strings, food log picker/report strings, ODbL privacy line. **(7) Play Console after 58 approves:** confirm Health apps declaration collapsed to 11 read permissions; Android vitals should show zero crashes on 58; lift the acquisition-push hold for Android. **(8) The PM-1041 backlog block below is superseded by this one** (walkthrough sweep DONE on 58; Health Connect parity SHIPPED).

> **[PM-1188 · 2026-09-11 · EMAIL DELIVERY BLIND SPOT — bounce webhook (option 3, not built)]** Verification catches typos at the wizard only. Brevo transactional webhook (hard_bounce / blocked / invalid_email / spam) → new `email-events` EF → `platform_alerts` keyed to recipient + red "email bounced" chip on the partner card (both CC copies, §23.306) and on Member Admin rows. Covers members too (welcome, trial-end, re-engagement). ~1 session + Brevo dashboard webhook config (Lewis/Dean login). `email-watchdog` claims to check hard bounces and fired nothing for Daniel — confirm what it actually queries while in there.
>
> **DEAN CHECKS OWED (PM-1187/1188):** (1) ~~Daniel Reay Provision~~ DONE by Claude via minted admin JWT — email delivered to the `.com`; if he still sees nothing it is spam/junk. (2) Cloudflare Pages deploy of Test-Site `0070a7a4`. (3) Phone run of the wizard: code arrives, wrong code bounces, "Wrong address? Change it" re-sends to the new one. (4) Reconcile vyve-site PM-1179→1186 into the brain.

> **[PM-1187 · 2026-09-11 · PARTNER COMP — TWO FOLLOW-UPS]** (1) **`comp_reason`** column (`partner` | `staff` | `test` | `manual`) so deprovision can revert partner-comp → trial/expired without touching staff; ~10 min + provision v7 + backfill of the 12 existing comp rows. (2) **partner-provision email v7** — Dean has seen the current copy (outputs/partner-provision-email-v5.html); add: referral link (live from Gate A), correct step 4 (welcome video → approval → live within 2 business days), bank-details line, `?` help drawer, video guidance (short, WiFi, keep the page open), what members will see. Dean's list → draft → Lewis pass. Also the `partners.vyvehealth.co.uk` PORTAL_BRAND_URL flip still waits on Dean's Cloudflare custom domain.

> **[PM-1178 · 2026-09-11 · APRIL'S TWO FAILED UPLOADS — OPEN]** Nothing reached storage; now instrumented (attempt rows + 10-min sweep → info alert + CC tile). When she retries, check CC › Partners › April Rosson › Content library for the warning tile, or platform_alerts type `partner_upload_failed`, and read the % reached: **0–5%** = never started (signed URL / CORS / file picker), **partway** = connection or backgrounding (wake lock now helps), **100% but no commit** = thumbnail or commit step. If it's the batch queue specifically (batch=true), that path has still never carried a real file from a phone. Consider chunked/resumable upload (tus — Supabase Storage supports it) if long clips keep failing.

> **[PM-1177 · 2026-09-11 · CANNED ANSWERS — NEXT STEPS]** (1) **Coach surface rows** — same table, ~20 from the coach corpus, once Calum's live use shows the real top questions. (2) **CC editor for `help_canned`** — today it's SQL only; a small admin table (question, keywords, answer fields, active, hits) belongs in the CC content area so Lewis can reword without a deploy. (3) **Watch `hits` + the `kind='canned'` share of the log** after a fortnight: rows with zero hits are either badly keyworded or unneeded, and any repeated model-answered question is a candidate for a new row. (4) Corpus and canned rows must move together — a label change (e.g. a renamed tab) invalidates both (§23.234 extended).

> **[PM-1175 · 2026-09-11 · HELP ASSISTANT: CANNED ANSWERS LAYER — proposed, awaiting Dean]** `help_canned` (surface, question, keyword sets, answer JSON in the drawer's shape, route) seeded with ~20 partner questions from the corpus; EF matches normalised typed questions against keyword sets BEFORE the gate/model and logs `kind='canned'` (needs the `coach_help_log_kind_check` widened); the drawer's opening screen lists them as **Common questions** answered client-side from one cached fetch (zero EF calls). Then the same for the coach surface. ~½ session. Also owed: Dean's coach-login check of the drawer (PM-1158).

> **[PM-1169 · 2026-09-11 · CONTENT LIBRARY — SHIPPED; TWO FOLLOW-UPS]** (1) ~~Multi-file upload queue~~ DONE PM-1170 — Dean's browser check owed; formerly: multi-file upload queue on My Content — pick several files, per-file title, sequential sign→upload→commit with progress, all land in In review (Dean's "upload a lot then push when I want"); the single-file form is untouched today. ~½ session. (2) **`stripe-webhook` severity `medium`** → its attribution-error alerts have never been written (§23.305); change to `high` on next touch of that EF and consider a backfill check of `partner_memberships` vs Stripe. Also: the PM-1158 coach-help coach-surface check and the PM-1164 View-my-page April check are still owed.

> **[PM-1164 · 2026-09-11 · VIEW MY PAGE SHIPPED — Dean check owed; OTA stack now carries vbb 627]** Members are still on the last pushed bundle; vbb 627 (partner-profile preview mode + auth.js one-liner) joins the unpushed stack. Nothing member-visible changed, so no urgency from this commit. Open: (1) Dean's browser check of View my page on a phone (My Space + Home); (2) April's onboarding login should preview despite not being live; (3) decision — show pending content greyed as "in review" in the preview?; (4) partner app wrap is its own session (bundle ID, keystore, Capawesome channel, store listings, native push + camera for Apple 4.2).

> **[PM-1161 · 2026-09-11 · PARTNER PORTAL MOBILE — SHIPPED (option A); FOLLOW-UPS]** Bottom tabs + More sheet + avatar menu live (CC `5e3204dde310ce7e659446d9d37830696ed069d2`). Left for the next mobile pass, in Dean's order once he's used it: (1) **PWA scaffolding** — manifest, icons, `apple-mobile-web-app-capable`, network-first SW (no offline claims), A2HS prompt, web push for booking requests / content approvals (iOS 16.4+ home-screen PWAs) — ~½ session; **do this before any Capacitor wrapper** (second bundle ID / keystore / listing / Apple minimum-functionality bar — decide after the first ten live partners ask for a store app); (2) ~~Gate B strip on Home~~ DONE PM-1163; (3) Live Sessions calendar under 720px — default to List view or fix the month grid; (4) Bookings availability day-pills wrap; (5) Dean's coach-login check of the coach-help surface (PM-1158) still owed.

> **[PM-1158 · 2026-09-11 · PARTNER PORTAL PREMIUM P0 SHIPPED — AI help drawer live; map moves to P1]** `coach-help` v6 serves both surfaces; `coach_help_corpus` has `coach` (v5) and `partner` (v1) rows; the drawer is in partner-portal.html (CC `82773323`). **Open on this:** (1) Dean opens the drawer once on a coach login to confirm the coach surface still answers (branch untouched, not re-tested); (2) the PM-1069 rollout item is now two-of-four — member app `help.html` (product help only, hard-separated from the personas, no Mind content) and the CC (team how-to from the brain) remain; (3) when P1 ships the type-aware shell, the corpus's "left menu order" line and the ambassador entry need a version bump (§23.234 rule extended to the partner row).

> **[PM-1156 · 2026-09-11 · APRIL ROSSON GATE B + DUPLICATE-ROW QUESTION]** Her link and code work now (earning from Gate A shipped — §23.299). To flip her live: (1) she uploads the welcome video; (2) **patch `partner_onboarding_progress.steps` with `modules_deferred:true`** — her row lacks the flag (applied 10 Aug via the 4-step wizard; Cole's 3 Sep row has it), so the PM-880 deferred confirm+stamp path won't offer and Go Live hits the hard safeguarding/GDPR/pct=100 gate; Alan Bird's row has the same gap; (3) CC Go Live (Lewis deferred-at-go-live nod still formally open, see PM-879 item below). **Dean to answer: is the ambassador row "April" (slug `april`, APRIL11, live since 25 Jun, no contact_email) the same person as April Rosson (`april-rosson-69839b`, APRIL10)?** If yes, merge — two rows will split her attribution and her old `/join/april` link is the one she may already have circulated. ~~Stale "being reviewed / 1–2 days" card~~ FIXED PM-1157 (CC `06cf4553`) — the card now lists outstanding items and the 2-business-day go-live promise; this closes item (1) of the PM-857 portal receiving-end scope.

> **[PM-1155 · 2026-09-10 · VYVE-SCOPE EXAMPLE FORMS SHIPPED]** `team@` sees Questionnaires / Check-in forms / Lead forms on the coach portal at null scope, one seeded Example per kind (CC `773e35c7`, migration `vyve_scope_example_forms`, `coach_forms.partner_id` now nullable = VYVE library). **Dean check owed:** team@ login → Forms → three Example rows with Default pills → open + save.
>
> **OPEN (not built): coaches cannot see VYVE example forms** — `cf_coach_all` is `partner_id = get_my_partner_id()`, NULL never matches (§23.298). Natural follow-on = a "VYVE examples" section in the form wizard's Set-up step: SELECT policy on `partner_id IS NULL` for coaches + a copy-to-my-partner action (INSERT with the coach's partner_id, never PATCH the null row). ~½ session. Also: the PM-1130 demo row `DEMO — Weekly Check-In` still uses `single_select`/`multi_select`/`longtext` — read-aliased, fine, but if it gets rewritten in the wizard it becomes canonical types; leave it.

> **[PM-1154 · 2026-09-10 · RENDER PATH VERIFIED — CLIFF IS A SOFT FAILURE]** PM-1152's open question is closed against the live file: `index.html` paints entirely from Dexie and **does not call `member-dashboard` at all**. A 7s server response is not a 7s member wait. **Severity downgraded; PM-1153 thresholds unchanged.** New §23.297 protects this — never add a blocking fetch to `boot()`.
>
> **TWO UNMEASURED GAPS worth a small session at some point (not urgent):** **(1) Cold-start exposure** — first open on a new device has no Dexie and no `vyve_home_v3` cache, so those members DO wait during saturation, and that is the onboarding path an employer cohort arrives through. Worth measuring what a cold first open actually costs under load. **(2) Other endpoints under saturation** — `my_challenges` and `daily_habits` timed out in the PM-1151 run; neither has been checked for local-first fallback the way index.html has. If they block their pages' first paint, the soft-failure conclusion does not extend to them.

> **[PM-1153 · 2026-09-10 · SCALING DECISION: NOTHING UNTIL 2,500 MEMBERS]** Dean asked whether this is being worried about too early. It is. At 1,000 members a simultaneous push is **~1.6 loads/sec against a measured ceiling of 8**; at 3,000 it is ~5/s. **No performance work is justified below 2,500 members.**
>
> **THRESHOLDS — act on these, not on anxiety:** **2,500** → build the performance posture monitor (p95, PostgREST pool, CPU → `platform_alerts`, cron-74 pattern). **5,000 or p95>1s** → collapse the outer 21 queries (22 internal calls → ~2 via `member_home_state`; ~5x, no spend, no visible change). **10,000 or CPU>60%** → compute tier (Medium $50 net). **10,000** → batch-path audit.
>
> **FREE AND NOW — (1) push jitter**, opportunistic next time anyone is in notification code; **hard rule: no synchronised 07:00 send to a cohort of several thousand until it exists**. **(2) STANDING DISCIPLINE: no new code that loops over members one at a time** (§23.296). Free now, expensive to retrofit.
>
> **BATCH PATH — the one that will actually surprise us (§23.296).** `daily-report`, `certificate-checker`, `re-engagement-scheduler`, `monthly-report`, push sender all iterate members and scale with **total membership**, not concurrency. **No compute tier fixes a per-member loop.** Needs engineering at tens of thousands. Nothing in read-path monitoring will ever warn about it, so it needs its own member-count trigger.
>
> **MILLION-SUBSCRIBER PATH (for Lewis conversations):** ~200-270 loads/sec required, ~30x from today, reachable via outer-21 + compute tier + read replicas, **no re-architecture**. ~$6-8k/month ≈ **0.04% of revenue**. Infrastructure cost is not the constraint at any size on the roadmap.
>
> **RECORDED PLAINLY: the model assumes 40% DAU; we are at ~6% (6.7 daily actives of 107 members). Capacity and engagement are the same question, and today the engagement number is the risk, not the capacity number.**

> **[PM-1152 · 2026-09-10 · CAPACITY MEASURED — CEILING IS ~8 LOADS/SEC, PLAN BROUGHT FORWARD]** New tool `tools/vyve-capacity-test.js` (arrival-rate, two scenarios: `knee` and `push`). **Knee: flat to 7.3/s at p95 497ms, cliff to p50 7.2s by ~12/s, zero errors throughout. Push: 8/s held two minutes at p95 692ms (= 5,000-member simultaneous push, PROVEN), broke at 17.94/s (= 10,000-member push, FAILS).** Estimate of 15-25/s was wrong; real is 8.
>
> **REVISED CAPACITY: ~25-30k members natural traffic, ~5,000 on simultaneous pushes.** Behaviour measured over 60 days: 1.94 sessions/active/day, 6.7 daily actives, **41.8% of activity in the 19:00 hour**, top-3 hours 74.3%. Small sample (17 members, 1,265 events) and activity logs are a proxy for opens — carry both caveats.
>
> **PRIORITY CHANGE vs PM-1151: jitter and the outer-21 collapse are the SAME trigger, and it is nearer than stated.** A few thousand members with a morning push is already the ceiling, not a comfortable distance from it. **(1) JITTER THE PUSH SEND WINDOW** — 10-15 min spread, free, no risk, ~10x on peak. **(2) COLLAPSE THE OUTER 21 QUERIES** — 22 internal calls → ~2 by reading `member_home_state`, which already holds most of what those queries re-derive; ~5x, no spend, no visible change to the screen. **(3) COMPUTE TIER** — Medium $50 net.
>
> **FIRST TASK OF THE NEXT SESSION, before any of the above: verify from code whether index.html genuinely paints from Dexie before the network call returns.** The brain says cache-first. If true, a 7-second server response is not a 7-second wait for the member and this whole cliff is far less user-visible than the numbers suggest — which would change the urgency of everything in this entry. Read the paint path; do not trust the note.
>
> **TEST FIDELITY, carry forward:** all runs hit one member on a hot row, so results are optimistic; `function_edge_logs` is lossy under saturation — use it for curve shape, k6 for totals.

> **[PM-1151 · 2026-09-10 · RE-TEST CONFIRMED THE FIX; CAPACITY PLAN NOW HAS TRIGGERS]** Post-fix 200-VU run vs the PM-1146 baseline: **50-VU stage avg 463ms / p50 400ms / p95 912ms** vs **avg 1.5-2.7s / p95 2.0-3.6s at only 30 VUs pre-fix**. Gateway traffic **79,513 → 44,184** on **17% more** client requests; failures **6.36% → 5.13%**. The 500-member goal is met with margin. §5C rewritten. **PM-1150's re-test item is CLOSED.**
>
> **NEXT SESSION (recommended, ahead of the query work) — PERFORMANCE POSTURE CHECK.** There is no performance alerting on this platform at all. Cron 74 covers security posture; nothing covers **p95 per endpoint, PostgREST pool utilisation, or CPU**. Every stage of the capacity plan below is gated on noticing a number cross a line, and right now that only happens if somebody goes looking. Build it the way cron 74 was built — scheduled check, rows into `platform_alerts`, alert on change rather than on absolute value.
>
> **THEN — COLLAPSE THE OUTER 21 QUERIES in `member-dashboard`.** 22 internal calls → ~4. Most of the 21 are per-table 30-day activity reads that `member_home_state` already materialises. ~5x throughput on the same hardware, no spend. Same discipline as PM-1150: byte-identical output, diffed against a live invocation before and after. **Trigger: ~5,000 members, or peak p95 crossing 1s, whichever comes first.**
>
> **CAPACITY PLAN — STAGES WITH TRIGGERS, NOT DATES.** (1) **Now → ~2,000 members: no action.** (2) **Before the first employer cohort goes live: JITTER THE PUSH WINDOW** across 10-15 minutes. Free, no risk, worth roughly **10x on peak load**, and it is the highest-return item on this entire list — pushes are the only thing that bunches thousands of opens into ninety seconds. (3) **~5,000 members or p95>1s: the outer-21 collapse** (above). (4) **~10,000 members or sustained CPU >60%: BUY COMPUTE.** Currently on **Micro** (2-core ARM, 1 GB RAM) — the instance the Pro credit covers. Small $15 / Medium $60 / Large $110 / XL $210; Medium is **$50 net**. Larger tier also means a deeper PostgREST pool, which is the actual constraint (§23.289 as corrected). At 5,000 B2C members this is a rounding error against ~£100k/month. **Make this decision early rather than under pressure.** (5) **Tens of thousands: read replica for the home read path, or an edge-cached home payload** — do not design it now.
>
> **THE SIZING ANSWER, for reuse (§23.295):** 1,000 members fine today, no work. 10,000 fine on natural traffic, **NOT** on a simultaneous push. 100,000 does not work today — that is the run that failed. 10,000 genuinely simultaneous requests fail at every tier before and after all planned work; what saves us is that people wake up over an hour, and the only thing that can manufacture a starting pistol is our own push scheduler.
>
> **FOR LEWIS (Dean relaying):** the app supports thousands of members; the limit is not headcount but how tightly their opens bunch, and the only thing that bunches them is a simultaneous push, which is a setting we control.

> **[PM-1150 · 2026-09-10 · MEMBER-DASHBOARD PERF FIX SHIPPED — RE-TEST AND TWO NEW PM-358 ITEMS]** `member-dashboard` **v91** live: the achievements payload is one `member_achievements_payload()` SQL call instead of ~26 sequential internal PostgREST requests. Home load **~45 → ~22** internal calls; 107 member payloads compute in **163ms total**. Output byte-identical, proven against a live v90 invocation before and after deploy. **No OTA, no vbb bump — server-side only.** Rollback = redeploy v90.
>
> **OPEN — DEAN: re-run k6.** `tools/vyve-load-test.js` (also in `~/Downloads`), 30 VUs first, then `k6 run --stage 1m:50 --stage 2m:200 --stage 3m:200 --stage 1m:0`. Fresh member token each run from the browser console — `JSON.parse(localStorage.getItem('vyve_auth')).access_token` — they expire hourly. Target: home load under ~300ms, 200-VU run clean. **Then `brain/security_questionnaire.md` §5C must be rewritten** — it currently carries the pre-fix figures and the named limit.
>
> **NEW FOR PM-358 (1) — `workouts_shared` is broken at the schema level and both halves must ship together.** The evaluator queries `shared_workouts.member_email`; the table keys on **`shared_by`**. It has 400'd on every home load since the overhaul and the tier is unearnable because `evaluateInline` fails identically. The perf fix **reproduces the skip on purpose** (§23.294) — fixing the read half alone shows an inflight bar that can never complete. Fix the column in `_shared/achievements.ts` in BOTH bundled copies (member-dashboard + member-achievements) and in `member_achievements_payload()` in one change, then backfill any tiers members should already hold.
>
> **NEW FOR PM-358 (2) — two thirds of the inline catalogue is inert.** `achievement_metrics` holds **75 rows with `source='inline'`**; the EF has evaluators for **24**. Never computed by anything: `movement_sessions_logged`, `mind_sessions_logged`, `breathwork_sessions`, `journal_entries`, `meditation_sessions`, `mind_minutes_total`, `affirmations_saved`, `streak_mind`, `meditation_streak`, `focus_cards_completed`, `streak_focus`, `daily_mood_checkins`, `checkin_text_responses`, `reps_lifted_total`, `sets_completed_total`, `distance_total`, `session_minutes_*` (four), the `first_*` set, `three_habits_day`, `five_habits_day`, `programme_week_complete`, `came_back`, `owl`, `first_light` and the rest. These are catalogued AND tiered, so members can see ladders that nothing will ever advance. Sizing the overhaul off "awarding isn't event-driven" alone understates it — the bigger half is that most of the catalogue has no evaluator at all.
>
> **NOT DONE, deliberately: no payload cache.** The session prompt asked for one; at ~1.5ms per member it buys one call and fights the instant-awarding requirement PM-358 is built on, and invalidation would add a write on every activity insert. If the home load needs further cutting, the target is the 21 outer queries in the `Promise.all`, not a per-member TTL.

> **[PM-1149 · 2026-09-09 · FULL BRAIN RECONCILIATION — DEAN'S CALL, START TOMORROW]** Dean: *"it's been a very long time since I've done that… we do need to do a full brain update."* Last full pass was **PM-603 (12 June)**. Evidence of drift already collected tonight, so tomorrow starts from a list rather than a hunt:
>
> **REGION ERROR FIXED TONIGHT — and the pattern is the point.** `brain/security_questionnaire.md` §6 said member data rests in **West EU / Ireland (eu-west-1)**. Live is **eu-central-1, Frankfurt** (verified via management API). **PM-767 corrected this in July across master.md, the privacy policy and the sub-processor register — and missed the one prospect-facing document.** It has been answering DPO questions with the wrong country for two months. Fixed here, along with §12 and `policies/key-person-handover.md` §2 (which I propagated Ireland into tonight, from the stale register). **STILL TO CHECK:** Lewis's Complete Knowledge Base still says "West EU / Ireland" — PM-767 flagged it and it was never fixed; and confirm the sub-processor register copy Lewis holds is the corrected one, since the v1 in Dean's project files still says Ireland.
>
> **KNOWN-STALE COUNTS in master.md, all verified live tonight:** §5/§6 say **133 tables** → live **232**. §24-era figures say **47 members** → live **107** (108 with achievements rows). **41 cron jobs** → live **75** (74 posture + 75 prune added tonight). §11A achievements **32 metrics / 327 tiers** → live **107 / 538**. Also verify: **150 Edge Functions**, storage buckets (**21** live), `admin_users` (**13** active).
>
> **DOCS KNOWN TO BE WRONG OR SUPERSEDED:** `prompts/cold-start.md` (banded PM-1148 — 36 tables, retired Composio path, MCP read-only, Capacitor wrap as priority). `reports/13-enterprise-readiness-audit-2026-09.md` — **three statuses wrong in one day** (B1 closed, B5 said CSP "Absent" when it was on 66/96, C6 re-rated), banner added but the remaining rows are unverified. `reports/09-enterprise-readiness.md` (superseded, corrected). `reports/02-backup-dr.md` (PITR inference, corrected).
>
> **THE STANDING LESSON to write into §23 tomorrow:** four times today a hand-maintained summary of live state was wrong — CSP coverage, PITR, MFA method, and the region. In every case the live system was one query away. The reconciliation should not just refresh numbers, it should **remove cached numbers from the brain wherever a query can answer instead**, which is already the stated §6 policy and is not being followed.

> **[PM-1148 · 2026-09-09 · TRIGGER INDEX AT THE TOP OF master.md]** Dean asked for phrase-triggered loading — say "load brain, start the dashboard fix" and the right doc opens without hunting. Table sits immediately under the master.md H1 so it is the first thing any session reads, and covers all three new session prompts plus the enterprise playbook, employer cohort floor, partner portal, Trainerize, member admin, CC design system, partner onboarding, live sessions/runner, load test, questionnaire, GDPR, DR and schema snapshot. **Standing rule written into the table itself: any new wave map, playbook or session prompt adds a row — a doc nobody can trigger is a doc nobody reads.** **ALSO: `prompts/cold-start.md` marked SUPERSEDED** (not deleted, soft-kill) — it claimed 36 tables vs 232 live, the retired Composio GitHub path, GitHub MCP read-only, and Capacitor wrap as current priority; anyone pasting it inherited a wrong rule set. Banner points at "load brain" instead.

> **[PM-1147 · 2026-09-09 · THREE SESSION PROMPTS WRITTEN — new `prompts/` folder]** Dean is running these as separate chats (this session's context is spent). Each carries its own facts so nothing is re-derived. **(1) `prompts/session-member-dashboard-performance.md` — DO THIS FIRST.** Collapse the achievements payload into ONE SQL call + cache; explicitly warns that parallelising alone is the wrong fix (fixes 30, not 200 — §23.292). Same output shape so `achievements.js` is untouched; `volume_lifted_total` to SQL sum. Design talk before code, deploy via `Supabase:deploy_edge_function` with all `_shared/` files, then Dean re-runs both k6 profiles and §5C gets the new figures. **(2) `prompts/session-achievements-overhaul.md`** — PM-358: event-driven awarding ("if something's complete it should be instant"), the seen/unseen race, and the 107-metric/538-tier correction; must not flood existing 1,349 earned rows with re-fires; member-facing copy is Lewis's. Builds ON the perf fix if that has already landed. **(3) `prompts/session-credential-rotation.md`** — enterprise B3, own session by design: full consumer checklist BEFORE rotating (EFs, crons, Hetzner `159.69.95.90`, Vault, OTA-held client bundles), plus the Google backup codes and the MFA-method audit from PM-1141/1142. **CONTEXT SET IN ALL THREE: target is 500 members by end of month ≈ 25–50 concurrent at peak, which tonight's 30-VU run already handles cleanly — so this is about not shipping a two-second home screen to 500 people, not about survival.**

> **[PM-1146 · 2026-09-09 · B7 CLOSED — LOAD TESTED AT 30 AND 200, BOTTLENECK NAMED]** **30 VUs: 2,520 reqs, 0 failures, p95 3.15s, median 107ms.** **200 VUs: 4,856 reqs, 6.36% failed — 309 failures, ALL of them `member-dashboard` 60s timeouts; `my_challenges` and `daily_habits` both 100% success throughout.** Questionnaire §5C written with both figures and the named limit. **THE FINDING: `member-dashboard` fans ONE home load into ~16–45 internal PostgREST calls** — gateway logged **29,752 REST requests in 14 min while k6 sent ~3,200** (§23.292). 21 outer queries in Promise.all + ~24 sequential inside `getMemberAchievementsPayload`. **REVISED RECOMMENDATION — supersedes PM-1145's "just parallelise":** parallelising shortens each invocation but issues the SAME number of internal calls, so it fixes the 30-user latency and barely touches the 200-user saturation. **The right fix is cutting round-trip COUNT: collapse the achievements payload into ONE SQL function (or cache it), which fixes latency and amplification together.** Do that, then re-run both profiles — script is `tools/vyve-load-test.js`, and 200 is `k6 run --stage 1m:50 --stage 2m:200 --stage 3m:200 --stage 1m:0`. **Bellway context: a 30-person trial is comfortably inside the clean band.** Scaling levers in order: the endpoint fix, then compute tier (connections 60, 21 idle), then function scaling.

> **[PM-1145 · 2026-09-09 · ACHIEVEMENTS — OVERHAUL CONFIRMED NEEDED, WITH MECHANISMS FOUND (Dean's call: backlog, not now)]** Feeds PM-358. Three findings from tonight's load test + Dean's observations, all diagnosed rather than assumed:
>
> **(1) EARNING IS NOT EVENT-DRIVEN — this is the "nothing popped up when I logged food" complaint.** `getMemberAchievementsPayload` (the one member-dashboard calls) **only reads** already-awarded rows. The function that actually awards tiers is `evaluateInline`, and it is **not** on the food-log path — nor on most activity paths. So crossing a threshold awards nothing until something else happens to trigger evaluation. Dean's requirement is explicit: **"if something's complete, it should be instant."** That is an architecture change (award at write time — trigger or EF hook on each activity insert), not a popup fix.
>
> **(2) RE-FIRING SEEN ACHIEVEMENTS IS A RACE, NOT A BROKEN FLAG.** `seen_at` marking works — live: **1,223 of 1,349 rows marked seen, 126 unseen across 108 members** (Dean 75 earned / 3 unseen). The unseen set is read in more than one place and marked *after* display, so two overlapping loads can both claim the same row. The dev loop makes it more likely but does not cause it.
>
> **(3) `getMemberAchievementsPayload` IS THE MEMBER-DASHBOARD LATENCY.** It loops all 24 INLINE metrics and **awaits each sequentially** — 24+ round trips per home load. Measured: member-dashboard **p50 2,104ms / p95 3,541ms / max 6,523ms at 30 concurrent** while every other call sat at p95 <90ms. **Parallelising that loop is a contained, identical-output fix (~2.1s → ~200ms) and does NOT need the overhaul — it can ship on its own.** Deeper wins for the overhaul: cache the payload (recomputed every home load though it barely changes) and stop `volume_lifted_total` pulling every `exercise_logs` row to sum in JS — it degrades as members log more sets.
>
> **BRAIN CORRECTION:** catalogue is **107 metrics / 538 tiers** live, not the 32/327 recorded against PM-358.

> **[PM-1144 · 2026-09-09 · SYNC CURSOR BUG FIXED + TWO 404s LOGGED]** vyve-site `b3cf615c` **vbb 626** (sw `v2026-09-09c-pm1144-syncfix`). **`monthly_checkins` delta cursor was `logged_at`, a column that does not exist** — every delta cycle 400'd on that one table while full hydrate looked healthy, because PM-112 fixed the hydrate path and missed the `CURSOR_COL` entry three hundred lines below it (§23.291). Now `created_at`. **All 14 CURSOR_COL entries checked against live schema — 13 were correct**, so no wider sweep needed. Found in Dean's browser console during the load-test setup, not by any monitoring, which is the point worth noting. **OPEN — LEWIS/CALUM:** `sessions-data.js` references `thumb-podcast.jpg` and `thumb-education.jpg`; **neither exists in the repo** (six sibling thumbnails do), so those two session cards render broken images. Both are dead streams awaiting content per Dean's earlier call, so the right fix is two real images rather than substituting an existing thumbnail — deliberately not guessed. **B7 LOAD TEST PARKED (not blocked):** `tools/vyve-load-test.js` is committed and also written to Dean's `~/Downloads`; the remaining step is a **five-minute run**, not a session — export the anon key + a fresh member token and `k6 run`. Everything else from B7 is already answered (member path 3–56ms measured, connection ceiling 60 with ~39 spare).

> **[PM-1143 · 2026-09-09 · B7 LOAD TESTING — MEASURED WHAT I COULD, SCRIPT DELIVERED FOR THE REST]** The sandbox cannot reach supabase.co, so live load must be generated from Dean's Mac: **`tools/vyve-load-test.js`** (k6, read-only by design so it is safe against prod). Two profiles — `pilot` ramps to 30 concurrent (Bellway-shaped, everyone opening the app in one morning window) and `PROFILE=rollout` ramps to 200. Thresholds p95<2s, p99<5s, errors<1%; prints a summary block to paste into the questionnaire. **DEAN: `brew install k6`, export VYVE_URL / VYVE_ANON_KEY / VYVE_JWT (use a TEST member token, expires hourly), `k6 run tools/vyve-load-test.js`.** **MEASURED TONIGHT, no load needed:** member hot path is healthy — `compute_engagement_components_v2` 56ms, `refresh_member_home_state` 45ms, member lookup 8ms, activity log 3ms, `get_charity_total` 1ms. **THE CEILING IS CONNECTIONS, NOT SPEED:** max_connections **60**, **21 held at idle** with nobody on the platform (§23.289) — that is the number that decides concurrency, and it is a budget nobody has been tracking. **FIXED IN PASSING:** `cron.job_run_details` had **318,213 rows, zero index scans, nothing pruning it** — cron 75 `prune-cron-history` added (03:40 UTC, 7-day retention), backfill left 25,097 (§23.290). **STILL OPEN:** `get_cron_health_snapshot()` averages **7.6s** and an internal-dashboard aggregate query averages **9.7s** — both admin-path, neither member-facing, but both worth a look before the CC gets busier.

> **[PM-1142 · 2026-09-09 · MFA POSITION CORRECTED — stronger than recorded, concentrated differently]** `team@vyvehealth.co.uk` is a **Google account** and most of the estate is reached by *Sign in with Google*; second factor is a **device-approved Google prompt**, which is phishing-resistant and materially stronger than the email-delivered codes PM-1141 wrongly recorded (§23.288 rewritten, questionnaire §5B rewritten, report 13 C6 row updated). **Both directors' devices are signed in and can approve prompts — CONFIRMED, so continuity works end-to-end, not just to the password step.** **THE REAL EXPOSURE IS RECOVERY: a consumer Google account has no administrator.** A lock-out, compromise or automated flag has no human remedy — recovery is an automated web form and accounts are lost to it permanently; the entire production estate sits behind it. **ACTIONS:** (1) **generate Google backup codes, store outside Google** (device approval fails if both devices are unavailable together or the account is flagged) — small, do it; (2) **C6 migration re-framed for Lewis as continuity/security, not GDPR** — the one-liner is *"if that account goes we don't lose our email, we lose access to the company's entire infrastructure, and there's nobody to call"*; a managed tenant adds admin recovery, enforced 2SV, hardware keys and audit logging. (3) MFA audit should record **method** per account, not just state.

> **[PM-1141 · 2026-09-09 · C6 RE-RATED — the email tenant is the root of trust, not housekeeping]** Dean confirmed MFA **and** account recovery for every system resolve to `team@vyvehealth.co.uk`. Continuity is complete (Lewis reaches everything unaided) — but that mailbox is now the second factor for the entire estate: reach it and you reach Supabase, GitHub, the stores and Stripe together. **C6 moves from a data-protection item to the highest-value security item we have** (report 13 row re-rated; §23.288 written; questionnaire gains §5B stating the trade-off proactively). **ACTIONS:** (1) **the mailbox itself must carry an authenticator app or hardware key, never SMS, satisfiable by both directors** — email recovery cannot protect the email; (2) C6 migration to a managed tenant, now sequenced as security not GDPR; (3) one-off MFA audit across every account recording state *and method*, then annual; (4) the 15-minute continuity dry run (`policies/emergency-continuity-prompt.md` §5 still reads "Last tested: never"). **NEXT TECHNICAL ITEM: B7 load/concurrency testing** — the last open Dean item with a genuine unknown in it; 107 members proves nothing about a 30-person Bellway trial arriving at once, and "never tested" is a worse answer than any number we'd actually measure.

> **[PM-1140 · 2026-09-09 · KEY-PERSON POSITION CORRECTED + EMERGENCY CONTINUITY PROMPT]** PM-1139's runbook assumed Dean was the sole access holder and called "give Lewis org access" the highest-value item. **Wrong — Dean corrected it: every account is registered to the shared business email team@vyvehealth.co.uk, Lewis already has access, holds repo access, and his Claude is linked to the same VYVEBrain repo.** Key-person risk is therefore a **capability** risk, not an **access** risk — a materially better position than most single-technical-founder companies, and it should be put to vendor management in those words. `policies/key-person-handover.md` §1/§3/§5 rewritten. **NEW `policies/emergency-continuity-prompt.md`** — a paste-into-Claude prompt written for Lewis, non-technical, under pressure: opens with "nothing is on fire" and the reasons why (67 crons, automated, weeks of unattended survival, the danger is a well-meaning intervention), then brain load, then four questions (live state vs docs — trust live and say so if they disagree; what was in flight; what Lewis must do this week in plain English; what to tell an incoming engineer). **THE RESIDUAL RISK AND THE ONLY REAL ACTION: second factor.** If MFA for those accounts resolves to Dean's phone or authenticator, the shared mailbox is not sufficient on its own — **15 minutes today, impossible later.** Recommended dry run: Lewis signs into Supabase, GitHub and one store account from his own device, unaided, and pastes the prompt to confirm his Claude can genuinely read the brain. Record the date in §5 of the prompt doc ("Last tested: never"). Also confirm per-provider recovery routes to the shared mailbox, not a personal address — which makes the C6 email tenant migration a continuity issue as well as a data-protection one.

> **[PM-1139 · 2026-09-09 · FIVE ENTERPRISE DOCUMENTS DRAFTED — new `policies/` folder]** Closes scorecard **D4** (`policies/uptime-sla.md`), **D5** (`policies/support-model.md`), **C5** (`policies/records-retention-schedule.md`), **E3** (`policies/key-person-handover.md`), **H2** (`policies/access-mfa-offboarding.md`). All DRAFT, none issued. **LEWIS SIGN-OFF REQUIRED before any of these leave the building:** (1) the uptime figure — recommended 99.5%, and the service-credit schedule, which is a contractual liability not a marketing number (a legitimate alternative is no credits at pilot stage); (2) support response times per severity; (3) confirmation that the privacy policy wording covers crisis-record retention under vital interests. **DEAN ACTIONS SURFACED BY THE DRAFTING, in value order:** (a) **Lewis needs organisation-owner access to Supabase, GitHub, Apple, Google Play, Stripe and the registrar — £0, one hour, and it removes the sharpest edge of key-person risk. Supabase org access is the master key to the whole estate because Vault sits inside it.** (b) External uptime monitoring must exist before the SLA is issued — committing to a number nobody measures is worse than committing to nothing. (c) One-off MFA audit across every account in the estate inventory, then annual. (d) First quarterly access review due within 30 days of approval — the *record* of it is the evidence, not the policy. (e) Email tenant migration (C6) blocks the support model being issued, since the support inbox also receives crisis alerts. **Retention gaps stated in the doc rather than hidden:** declined partner drafts (no rule), CRM prospect data (unenforced), some telemetry tables (no ageing), `bookings`/`booking_events`/`coach_leads` still absent from `gdpr_table_policy`. **Report 13 status corrections applied — B1 and B5 closed, and a warning banner added that other statuses in that report were asserted rather than verified.**

> **[PM-1138 · 2026-09-09 · EMPLOYER DASHBOARD COHORT FLOOR — PARKED, DECISION-READY]** Read `tasks/employer-dashboard-cohort-floor.md` at the start of the employer dashboard overhaul (Dean has it queued behind the partner portal back end). **Trigger is live, not hypothetical: a ~30-member Bellway trial.** Dean's policy: no aggregated data for any group below a minimum cohort size, working figure **20** (exact number TBC) — a four-person team gets nothing, a 100-person Sales department is fine. **The trap that matters: count RESPONDERS, not headcount** — a 100-person department where six people did a check-in is six people's data wearing a department's name. Also: suppression must be sticky (a team flickering above/below the line is itself a signal), complementary disclosure via subtraction (company minus one department), and per-metric not per-view suppression. Floor belongs **server-side in `employer-dashboard` EF**, never client-side. **With 30 members and a floor of 20 Bellway will only ever see the whole-group figure — Lewis should sell that upfront rather than let them discover it.** **Open questions for Lewis/Bellway are listed in the doc** (one group or split by site/team; who gets the login — HR lead vs line managers; wellbeing scores or activity only, and what has Lewis already promised; volunteers or nominated; start date; confirm the number). **NOT blocked by the Wave 3 deferral (L12)** — this applies to employers of any size.

> **[PM-1137 · 2026-09-09 · WAVE 3 DEFERRED — LEWIS QUESTION L12 ADDED]** Dean's read of the Sage arrangement: VYVE offered to **Sage's customers** (small businesses on Sage), likely embedded in the Sage portal with sign-in through Sage. That is a distribution channel, not an enterprise sale, and it breaks three Wave 3 assumptions — tenant is many small employers not one big one (self-serve provisioning, not CSV import of 200); identity is probably OIDC via a Sage developer programme, not SAML (Supabase Auth does SAML natively but not arbitrary OIDC — token exchange in an EF); and **aggregate-only reporting stops protecting anyone at nine-person companies, so a minimum cohort floor is needed on the employer dashboard**. **LEWIS (L12 in `playbooks/enterprise-readiness.md`):** confirm with Sage whether this is an app-marketplace listing, an embedded integration or a reseller arrangement — three different builds. Alan to help shape the question. **Wave 3 build work stops until answered.** **Nothing is at risk meanwhile:** live check confirms no real employer accounts exist (86 real members carry no company; BT/Sage/Individual rows are test). The cohort floor is the one item to build *before the first real employer of any size*, not before the first enterprise one — B2B at £10/user sells to small employers too.

> **[PM-1136 · 2026-09-09 · PORTAL CONTROL-COVERAGE CHECK]** `tools/check-portal-controls.py` shipped (vyve-site `bc7a45c6`, vbb 625): CSP presence + CSP position ahead of the first `<script>` + vbb marker agreement. First run found `movement.html`/`movement-plans.html` loading `/theme.js` above their CSP meta (§23.287) — both fixed. **DEAN ACTION:** the Actions workflow could not be committed — `GITHUB_PAT_CLAUDE` lacks `workflow` scope, 403 on any tree touching `.github/workflows/` (§23.286). Either add the scope (then I commit `portal-controls.yml` next session) or drop the delivered yml in by hand. **Until then the check is manual, which is the exact weakness §23.285 describes — don't let it sit.** Remaining CSP work: canonicalise the six policy variants across the 66 older pages; size the nonce migration off `'unsafe-inline'` (199 inline blocks + 883 inline handlers).

> **[PM-1135 · 2026-09-09 · CSP COVERAGE CLOSED]** vyve-site `9885cbf3` vbb 624 — CSP added to the 30 portal pages that had none (Mind, Connect, journal, mental-fitness, challenges, sessions, replays, podcast, onboarding, settings-account, partner-space). Device-confirmed clean. `api.brevo.com` + `www.googleapis.com` dropped (unused); `youtube-nocookie` + `open.spotify.com` added. §23.284 (report-only impossible via meta — header-only, GitHub Pages sets none, so portal CSP always enforces), §23.285 (per-file controls decay silently on new surfaces; ship a coverage check with the control). **OPEN:** (1) **CI check on vyve-site that fails if any `.html` ships without a CSP meta** — recommended, not built; this is the only thing stopping 30 becoming 40. (2) Canonicalise the six CSP variants across the other 66 pages. (3) **Nonce migration off `'unsafe-inline'` — 199 inline script blocks + 883 inline handlers, needs proper sizing before it is scheduled.** (4) This is commit 35 in the unpushed vyve-site queue; OTA channel still held at 581 pending the iOS approval continuity check.

> **[PM-1134 · 2026-09-09 · ENTERPRISE WAVE 2 — DOC TRACK DONE, PITR BLOCKED ON DEAN]** False backup claim purged from `reports/09-enterprise-readiness.md` and from its actual origin `reports/02-backup-dr.md` (dated correction banners, original lines struck not deleted); `brain/security_questionnaire.md` §12 rewritten honest. §23.283: never infer a capability from a config flag — `wal_level=logical`/`archive_mode=on` are Supabase defaults and are not PITR. **DEAN ACTION (blocks the rest of Wave 2):** (1) Supabase dashboard → Project Settings → Add-ons → enable **Point-in-Time Recovery** (paid, ~$100/mo Pro tier for 7-day — confirm live pricing; recurring cost, worth a word with Lewis). (2) Tell me when it's on and I'll verify from the DB side and update the questionnaire. (3) Then: restore into a scratch project, **timed**, and minute the result — that replaces the unevidenced 4-hour RTO with a measured number and answers "when did you last test a restore?", which is a standard question we currently have to answer "never". **REMAINING WAVE 2 AFTER PITR:** service_role + anon key rotation with a written cadence — **bigger blast radius than anything in Wave 1**, service_role picked up an extra consumer on the Hetzner runner at PM-714, so build the full consumer checklist (every EF, every cron, the box, any local tooling) before touching anything, and treat it as its own session; then CSP headers on the portal pages + a localStorage inventory.

> **[PM-1133 · 2026-09-09 · ENTERPRISE WAVE 1 CLOSED]** SECURITY DEFINER remediation shipped: anon-executable non-trigger definer functions **25 → 0** (migrations `enterprise_w1_definer_lockdown`, `enterprise_w1_security_posture_monitor`), **cron 74 `security-posture-check`** 03:15 UTC alerting into `platform_alerts` on any posture change and naming new exposures. Root cause was Supabase default privileges, not a coding mistake (§23.280); one guard failed on NULL three-valued logic (§23.281); policies must be retargeted `TO authenticated` before revoking helper EXECUTE (§23.282). **OPEN ITEMS:** (1) **DEAN CALL — `ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE EXECUTE ON FUNCTIONS FROM public, anon, authenticated`**: the structural fix for §23.280, deliberately not applied because it changes the default for every parallel session's new RPC and would make a legitimately member-callable function fail until granted; cron 74 is the compensating control meanwhile. (2) 123 `TO public` RLS policies remain (all `auth.email()`-keyed, no helper dependency) — safe, untidy, sweep when convenient. (3) `challenge_partner`/`thread_partner` return null today only because `coach_challenges`/`coach_threads` are empty; they will leak `partner_id` to any authenticated caller once populated — add self-scoping when challenges go live. (4) Dean device check requested at apply time: Challenges, food log, coach automations tab. (5) **NEXT: Wave 2** — PITR (off; real RPO 24h vs quoted 4h RTO), false backup claim in `reports/09-enterprise-readiness.md`, service_role + anon key rotation with a documented cadence, CSP headers.

> **[2026-09-08 · PM-1130 — COACHING DEMO SEEDED; OTA STOOD DOWN; TRAINERIZE CONFIRMED COMPLETE]** State: **no repo commit** — vyve-site HEAD still `24c915c6` vbb 623, CC still `bd7b5647`. Migrations `demo_seed_m1..m4`. **THE OTA WAS PREPARED AND CANCELLED BY DEAN** — nothing pushed, nothing committed; a `vyve_is_coached_` TTL fix + vbb 624 triple bump were staged in the sandbox as a pre-push fold-in (an overstep — Dean had listed it as an *after* item) and discarded. **CORRECTION TO THE BRAIN: members are on 589b (`df06c5ba`, deployment `51e4c156`, 19:32), not 589a** — two hotfixes went out that evening and only one was recorded. **The unpushed gap is 34 commits, not eleven waves** — vbb 590 (PM-1023), 591 (PM-1039) and **592–611, the entire food-log/nutrition rebuild (PM-1042→1065)**, which is dark behind the PM-374 cover (`log-food.html` gated, Body-hub row `dev:true`) but is 1,767 changed lines plus two new sw-precached files riding the same push. **The two `PM-XXX` commits (621/622) are NOT new member code** — blob-compared file-by-file against `ota/589b-video-fix`: ten identical, `workouts-programme.js` differs only by W6 part 2's additions with the wrapper block intact. **TRAINERIZE COMPLETE:** 39 of 44 gap-map rows (#62–#112) shipped, every split wave (W6/W7/W9) has both parts landed; the five open rows are all gated — **#87** + **#111** (Calum go/no-go), **#106** (built dark behind `W9_HEALTH_VISIBLE`, Lewis's consent wording), **#110** (parked into Achievements; coach view shipped as #101), **#100** (WD, mockup gate). **The only unbuilt piece is W7 part 2b's member-side import sheet** — the `recipe-import` EF is live and proven, the button still fires a placeholder toast. **DEMO SEED — live now:** 10 clients on partner `a23478c8` (8 active / 1 invited / 1 archived+lapsed), 216 habits, 134 workouts, 95 cardio, 296 exercise logs, 46 check-ins, 24 form responses, 15 biometrics, 8 goals, 11 messages, 73 weekly rows, 8 applied programmes, **0 rows into `activity_dedupe`**. Compliance: Sarah 88 · Tom 75 · James 71 · Grace 46 · Nadia 42 · Carl 36 · Priya 35 · Dev 25 · Marcus 13. **Teardown = one predicate** (`members.email LIKE '%@vyve-demo.invalid'` + the `coach_forms` row `DEMO — Weekly Check-In`); script delivered to Dean, `member_activity_log`/`member_home_state`/charity counter all self-restore. **Charity counter 1056 while the demo stands, 565 before, and 565 is itself 364 real + 201 from `is_test` members** — the home screen reads 18 donated months where the truth is 12. Dean ruled leave it for now; **the 201 overstatement predates the seed and survives the teardown — LEWIS CALL, still open.** **DEAN'S WALKTHROUGH IS TOMORROW** — 69-step doc delivered covering all eleven sections in portal order. **OWED, unchanged:** the OTA (623, 34 commits, blast-radius order needed before it lands — the yt-embed wrapper first, since §23.276 means the dev shell cannot test it); `re-engagement-scheduler` **v15 written and verified but NOT deployed** (md5 `7e62976b`, adds the `is_test` gate — the fixture is safe without it only by the `'trial'` coincidence, §23.277); `challenges` missing from coach-help's `HASH_OK`; the `vyve_is_coached_` one-liner; `bookings`/`booking_events`/`coach_leads` absent from `gdpr_table_policy`; `recipe-photo` still `verify_jwt:false` with no auth check. **New §23.274** (charity counter is cached + healed nightly; triggers and reconcile move together), **§23.275** (`coach_apply_assignments` silently skips a foreign-partner template and returns jsonb rather than raising), **§23.276** (the yt-embed wrapper iframes the remote origin, so the dev shell cannot validate it), **§23.277** (`'trial'` is the fixture-safe subscription status). **NEXT: Dean's walkthrough, then the OTA.**

> **[2026-09-08 · PM-1128 — CORRECTION: THE POST-WORKOUT FEEDBACK CAPTURE IS NOT BROKEN. IT HAS NEVER BEEN ELIGIBLE TO FIRE]** PM-1127's backlog entry called for an hour diagnosing why `workouts.difficulty_rating` is null on all 82 rows. **Do not spend it.** The capture is wired end to end: `workouts.html` line ~451 holds `#comp-coach-feedback` (1–5 scale + note), and `workouts-session.js` calls `maybeShowCoachFeedback(_workoutClientId)` from the completion screen (~line 1043); submit PATCHes the `workouts` row by `client_id` with a 3-second retry for the un-awaited POST still in flight. **It is gated on `coach_clients?status=eq.active` for that member and returns early otherwise.** There is exactly **one** active coach client in production — `deanonbrown12@hotmail.com` — and that account has logged **zero** workouts. All 82 workouts belong to members with no coach, so not one has ever been eligible to see the card. **The zero is correct behaviour on a platform where coaching has one test client who doesn't train**, and the #109 feedback panel needs no change — it populates the moment a coached member finishes a session. Prove it in one pass by logging a workout on deanonbrown12.

**One real bug found while checking, not yet fixed:** the coached check caches to `sessionStorage` as `vyve_is_coached_<email>`, so a member who opened the app **before** being added as a client keeps the cached `'0'` for the rest of that session and will not see the card even after the coach adds them. One-line fix (clear the key on coach-link, or drop the cache to a short TTL) — **ride it on the next member-side commit**, it does not deserve its own.

**Method note for §23.263:** the rule fired correctly — a column that is null everywhere IS worth stopping for — but the diagnosis "the capture is dead" was an assumption. An all-null column has two shapes: nothing writes it, or **nothing is eligible to write it**. Check the gate before the writer.

> **[2026-09-08 · PM-1125–1127 TRAINERIZE W9 PART 2 SHIPPED — W9 COMPLETE; OTA STACK NOW TEN WAVES + TWO]** State: vyve-site HEAD `24c915c6` **vbb 623** (sw `v2026-09-08l-pm1126-qtypes`), CC HEAD `bd7b5647`. **No migration, no EF, no cron.** **Dean's pass, in order:** (1) coach portal → a client → **Overview** — the part 1 strip is still on top, everything under it is now a grid; check the **notes box still saves** and W2's **alert toggles still toggle** (they were moved, not rebuilt — if either is dead, that is the thing to report). (2) **Edit layout** → drag a widget, toggle one off, press Done → reopen another client and confirm the layout followed you (it is per coach, not per client). (3) **Habits** tab → one row per habit, 28 days, then **12 weeks**; a recently assigned habit should show dashed not-assigned cells before its start, not orange misses. (4) **Logs** → scroll to **Review by workout** → Open a session → prescribed sits beside logged, and anything you did off-plan is in **Also logged this session**. (5) **Forms** → edit a check-in form → the type list now has **Multi-select**, and both choice types offer **Allow "Other"**. (6) **phone:** open your coach check-in — the `longtext` question ("Injuries or anything I should know?") should now be a proper multi-line box, and a multi-select question should give you chips you can tick several of. Then **"OTA 623"** — that push carries W3 + W4 + W5 + W6 parts 1 and 2 + W7 parts 1 and 2a + W8 + W9 parts 1 and 2, plus the two YouTube-wrapper builds (621, 622). **OPEN QUESTION, worth an hour: `workouts.difficulty_rating` is null and `member_note` empty on ALL 81 rows.** PM-956 shipped the post-workout feedback capture and **nothing has ever come through it** — either the sheet is unreachable or nobody finishes a session that way. The #109 feedback panel is built and honest about being empty, but the capture itself needs diagnosing before that column means anything. **Also still owed:** `challenges` missing from coach-help's `HASH_OK` (seven waves old); `bookings`/`booking_events`/`coach_leads` absent from `gdpr_table_policy` (Lewis call); **vyve-site 621 and 622 shipped with a literal `PM-XXX` in their commit messages** — the §23.24/25 sweep did not run, so neither has a number in the namespace; Lewis copy on W4 opt-in, W5 gate labels, W6 finish sheet, W7 recipe strings, W8 prospect-booking lines. **#106 stays dark behind `W9_HEALTH_VISIBLE`** — `member_health_daily` holds 2,102 rows, so the only remaining cost is Lewis's consent wording. **NEXT: clear the OTA stack, then WD light-first skin (mockup-gated) or W10.**

> **[2026-09-08 · PM-1124 — SCOPE CORRECTION: RECIPE SEARCH + FILTER IS **NOT** W9 PART 2]** PM-1121 folded the recipe search box and filter drawer into W9 part 2. **That was wrong** — the recipe library is the W7 / nutrition workstream, running in a different session, and it was raised here only because two chats got crossed. **W9 part 2 is: #107 Insights widget grid · #109 review-by-workout · per-habit heatmap · #91 form question types. Nothing about recipes, meals or nutrition.** The two findings PM-1121 recorded are real and stay on record for whoever owns the recipe surface — **`payload.shared` is false on all 175 stock recipes so the seeded library is invisible to members** (one UPDATE, gated on Lewis's copy, not a build item), and **the tag vocabulary carries `High protein`/`High-protein`, `Low carb`/`Low-carb`, `High-fibre`, `Sulphites` filed as a tag rather than an allergen, and the four slot names duplicated as tags** (normalise before any facet UI, or the drawer ships duplicate checkboxes). The facet mapping in PM-1121 also stands, with the PM-1123 correction that Status is `payload.shared` and its chip already exists. **Neither finding is a W9 item and neither should be picked up by a W9 session.**

> **[2026-09-08 · PM-1122–1123 — W9 PART 1'S PORTAL HALF WAS CLOBBERED BY PM-1115 AND HAS BEEN RE-APPLIED]** CC `836f35f3` restores it; **jsdom 55/55; PM-1115–1118's Meals library, stock-library scoping and recipe detail view are all preserved untouched.** What happened: W9 part 1 committed coach-portal.html at 17:43 (`33746395`, 975,129 bytes); PM-1115 at 19:12 wrote the same file from a base fetched before that commit and main dropped to 912,240 — the W9 zone, the `TABS` Progress entry, the `w5Tab` dispatch route and the dead-column header fix all silently gone, with no conflict and no error. **The database was never affected** — `tz_w9_biometrics_goals`, `tz_w9_badges_last_activity_fix`, `coach_client_badges()` and the RLS have been live throughout, and vyve-site `36a4467d` (vbb 620) was never touched. New **§23.265**: re-fetch the FILE immediately before blob creation, grep the committed blob for a marker from the previous commit as well as your own, and treat a byte count that drops across consecutive commits to a growing file as the tripwire. **Also open, from the parallel sessions:** vyve-site `41459f58` (vbb 621) and `401621d6` (vbb 622) both shipped with a literal **`PM-XXX` in the commit message** — the placeholder sweep (§23.24/25) did not run, so those two commits have no PM number in the single namespace and the changelog cannot reference them. **OTA stack is now vbb 622 — eleven waves owed.** **NEXT: W9 part 2** (#107 widget grid, #109 review-by-workout, per-habit heatmap, #91 form types, recipe search + filter drawer) — all portal-side, one session, no OTA. Correction to PM-1121: the **Status facet DOES have a source** — PM-1115 already ships a Draft/Shared chip on the Meals grid, so the facet is `payload.shared`, not a missing concept.

> **[2026-09-08 · PM-1121 — W9 PART 2 SCOPE ADDED: RECIPE SEARCH + FILTER DRAWER. TWO LIVE FINDINGS ON THE RECIPE LIBRARY]** Dean showed Trainerize's Meals library (search box · sort by Last Modified/Name/Calories · filter drawer: Status, Library, Meal type, Total prep time, Dietary preference, Excludes, Tags) and asked for the same on ours. **Bundled into W9 part 2** — it is portal-side and needs no OTA, so part 2 stays one session alongside #107, #109 and the per-habit heatmap.

**FINDING 1 — the stock recipe library exists and is INVISIBLE.** `coach_templates` holds **175 rows at `kind='meal'`, all `partner_id IS NULL`, all `is_recipe`, all `active`, all carrying a `method` and `prep_mins`, 100 with `image_path`** — the seed the backlog has been describing as pending since W7 has in fact happened. But **`payload.shared` is `false` on all 175**, and that is the flag the member side reads (W7: "`shared` defaults OFF on every pre-W7 row"). So "From your coach" is empty for every member **not because nothing was seeded but because nothing was flipped**. This is one UPDATE and zero code. It is a **Lewis gate, not a build item** — he has not passed the recipe copy. Flip it the moment he does; do not flip it as part of a build wave.

**FINDING 2 — the tag vocabulary is dirty and would render as duplicate checkboxes.** Live distinct tags include **`High protein` AND `High-protein`**, **`Low carb` AND `Low-carb`**, plus `High-fibre` (hyphenated where the others are not). `Sulphites` sits in **tags** when it belongs in **allergens**. The four slot names (`Breakfast`/`Lunch`/`Dinner`/`Snacks`) are duplicated as tags when `payload.slot` already carries them. **Normalise as a migration BEFORE the drawer is built** — it is a data fix every tag reader benefits from, and a facet list built on today's values ships visible duplicates on day one.

**Facet mapping, verified against live data:** Meal type → `payload.slot` (Breakfast/Lunch/Dinner/Snacks, clean). Total prep time → `prep_mins + cook_mins` (all 175 populated). Dietary preference → tags Vegan/Vegetarian, after the merge. Excludes → `payload.allergens`, which is **already the full UK FSA 14** (Celery, Cereals containing gluten, Crustaceans, Eggs, Fish, Milk, Molluscs, Mustard, Nuts, Peanuts, Sesame, Soya, Sulphites) and needs no work. Tags → Batch cook, Budget, Family, Freezer-friendly, Make-ahead, Meal prep, No-cook, Quick. Library → stock vs the coach's own via `partner_id IS NULL`. **Status has no clean source** — we have `active`, not Trainerize's published/draft, so either drop the facet or decide what draft means for us. Sort: name and calories direct; last-modified → `updated_at`.

**Build order when part 2 opens:** ① tag-normalisation migration ② search + drawer on the coach recipe library ③ the same on the member recipe tab (that one IS member-facing and carries an OTA, so it can split to part 3 if the OTA stack is still owed). **`nutrition_recipes` is 0 rows** — that table is the member's own saved recipes and is a different surface from the stock library; do not confuse them.

> **[2026-09-08 · PM-1120 — LEWIS ON THE `.dev` SHELL; MODE SWITCHERS REPAIRED (Mac-local, uncommitted)]** No repo commit outside the brain. **`mode-dev.sh` / `mode-ship.sh` were no-ops from PM-974 to now** (hardcoded `cd ~/Projects/vyve-capacitor` + `set -e` → exit at line 3); repointed at `~/vyve-capacitor-git`. **Consequence to act on: `mode-ship.sh`'s pre-archive LiveUpdate grep — the PM-703 guard — did not run for iOS 2.0 (5), 2.0.1 (6), Android 1.0.9 (55), 1.0.10 (56) or 1.0.11 (57).** Nothing bad shipped (589a reached the fleet) but **the next archive is the first execution of that guard since PM-703 — read its output, do not skip it.** This session's `./mode-ship.sh` run is **Dean-confirmed, not Claude-verified** (output not pasted back). **TO DO, small:** ① **commit `mode-dev.sh` + `mode-ship.sh` into `vyve-capacitor`** — they are gitignored Mac-local and a Mac wipe loses the only copy; add a loud working-directory assert at the top of each per §23.273(a) while you are in there. ② `capacitor.config.dev.json`'s `appId` reads `co.uk.vyvehealth.app` — cosmetic on iOS (identity is `PRODUCT_BUNDLE_IDENTIFIER`) but it audits as ship config; make it `.dev`. ③ Confirm the exact expiry date on the new `GITHUB_PAT_CLAUDE` for §24 (still open from PM-1041). **LEWIS'S DEV SHELL — standing facts:** `.dev` bundle coexists with his App Store install; `server.url` = live, so it is a **one-time install that never needs repeating** and tracks main at the 2–15 min WKWebView lag; **no native push** (APNs topic = ship bundle ID) so push copy must still be checked on his store app; **§23.214** — he is `admin`, so his phone is NOT a valid test of member visibility. He is on **vbb 622** vs **589a** on his store app, which puts every owed string in his hand: W4 opt-in consent sheet, W5 gate labels + member strings, W6 finish-sheet logging line, W7 recipe strings + share-toggle wording, W8 prospect-booking lines. **Watch:** `[VYVELocalDB] open failed: {}` on his first boot — expected once on a fresh container, but if it persists it is the §23.231 layer and the food log will misbehave for him. Xcode also held **no Apple ID account** at session open; if that recurs, check Manage Certificates for the 01/09 Apple Distribution cert before the next archive.

> **[2026-09-08 · PM-1105–1107 + PM-1115–1118 (parallel session, brain-committed late as PM-1119) — W7 PART 2b: `recipe-import` EF LIVE + 24 STOCK RECIPES SEEDED; THE MEMBER IMPORT SHEET IS NOT BUILT]** State: CC HEAD `4fded171`, **no vyve-site commit this session**. Migration `tz_w7_recipe_import`; `recipe-import` v4 (platform v5, `verify_jwt:true`, source `scripts/ef/recipe-import/index.ts`); NEW `recipe-photo` v1; NEW ledger `nutrition_recipe_imports` (+ purge row); `nutrition_recipes.source_url`; policy `tpl_vyve_stock_read`. **NEXT, in Dean's order: (1) the other 76 stock recipes** — same SQL block, new rows, ingredient names constrained to exact `nutrition_common_foods` matches (that constraint is the whole reason the macros need no parser), then fire `recipe-photo` **one invocation per recipe** (§23.269); keep them `shared: false`. **(2) the member-side import sheet** — mockup signed off (paste screen stating the allowlist up front, two-phase loading state, review step showing the original page text in italics above the parsed fields with matched / check this / no macros per row; **the review step is the feature**). That finishes W7 2b. **DECISION OWED BEFORE ANYTHING ELSE SHIPS ON THIS SURFACE: `recipe-photo` is `verify_jwt:false` with no auth check in its body** — anyone with the URL can spend the Gemini key or enumerate models. Bounded (writes only `vyve/<id>.png` on `partner_id IS NULL` meal rows) but open. One flag flip + `is_admin_or_team()`. Its header also still reads `PM-1119`. **DON'T START WITHOUT ASKING:** per-ingredient verified ticks + a print button on the recipe modal; `challenges` into coach-help's `HASH_OK` (now SEVEN waves old — fold into whatever deploys `coach-help` next); **§23.254** — `recipe-photos` and `coach-challenge-art` still survive a GDPR erase because `gdpr_table_policy` covers tables only, and note the new **`vyve/` prefix in `recipe-photos` is VYVE-authored, not member-authored, so it must be EXCLUDED from any purge**. **OTA — CORRECTED AGAINST LIVE: members are on 589a, not 589.** The parallel session shipped an Error 153 YouTube-embed hotfix as Update **589a** off branch `ota/589a-video-fix` (built from `db348511` so 590–620 stayed unpushed), and vyve-site HEAD is now **`401621d6` vbb 622**. So the push Dean is holding is **622, not 618** — it carries W3 + W4 + W5 + W6 parts 1 and 2 + W7 parts 1 and 2a + W8 + W9 part 1 + the two embed-fix waves. Dean says the number when the device pass is done; no partial pushes. Two vyve-site commits (621, 622) carry a literal `PM-XXX` in the message — placeholder sweep did not run. **LEWIS (none gating):** W4 opt-in consent sheet, W5 gate labels, W7 recipe strings + share-toggle wording. **Corpus:** `coach_help_corpus` not bumped for the Meals photo grid / recipe detail view — owed on the next `coach-help` deploy alongside `challenges`.

> **[2026-09-08 · PM-1112–1114 TRAINERIZE W9 PART 1 SHIPPED — client workspace depth; OTA STACK NOW NINE WAVES DEEP]** State: vyve-site HEAD `36a4467d` **vbb 620** (sw `v2026-09-08i-pm1113-measure`), CC HEAD `33746395`. Migrations `tz_w9_biometrics_goals` + `tz_w9_badges_last_activity_fix`; NEW table `member_biometrics`; NEW RPC `coach_client_badges()`; NEW page `measurements.html`; **no EF, no cron**. **The recon finding: `members.last_active_at` is dead** — 1 of 97 populated, newest 25 May 2026, and the client workspace header has been printing "Not active yet" for essentially every client since W5. Fixed in both places; the strip reports last activity off `member_activity_log` (§23.263). **Dean's pass, in order:** (1) coach portal → Clients → your test client → **Overview** — the new strip sits above the tiles: last activity, active days, messages out/in (amber if they've been quiet 5+ days), workouts, cardio, check-ins, and recent badges. (2) **Goals** → + New goal → type **Body weight**, target 78, save → confirm it lands in **Current** with a bar, and that a body-weight goal with no target is refused before it tries to save. (3) add a goal with a **start date in the future** → it should land in **Upcoming**, not Current. (4) Mark one achieved → it drops to **Past**, struck through; **Remove** still works. (5) **Progress** (new twelfth tab) — with only a weight log you should see exactly one chip, not fourteen. (6) **phone (server.url loop):** Body → **Measurements** → log a body fat with method Calipers → the tile shows it, BMI and lean/fat appear once your height is in (it asks inline), the history chart draws on the second entry. (7) log a second body fat the **same day** → it should correct the day, not add a second row. (8) log a **blood pressure** → both numbers required. (9) back in the portal, Progress → **Body fat** → your entries are there with the method column; log one as **DEXA** and confirm the mixed-method warning appears. (10) Home → the goal card now shows the bar with current/start/target. Then **"OTA 620"** — that push carries W3 + W4 + W5 + W6 parts 1 and 2 + W7 parts 1 and 2a + W8 + W9 part 1. **W9 PART 2 (not started):** #107 Insights widget grid replacing the Overview static blocks (widget set belongs in `coach_ui_prefs`, NOT `coach_profile` as the wave brief says — §23.237); #109 review-by-workout (join `exercise_logs` to its prescribed `programme_json` day + the PM-956 `workouts.difficulty_rating`/`member_note` feedback); per-habit heatmap on the Habits tab; **#91 is much smaller than the brief** — `textarea` and `scale` (1–10) already exist in `QTYPES` and in both renderers, so what is actually new is **multi-select and an "other" option**, plus a `longtext` alias: **the one authored `coach_forms` row in production uses `type:"longtext"`, which is in neither `QTYPES` nor the member renderer, so it degrades to a single-line input today.** #106 is built dark behind `W9_HEALTH_VISIBLE=false` — `member_health_daily` already holds 2,102 rows, so **the only remaining cost is Lewis's consent-card wording + a version bump**, not a build. **Still owed from earlier waves:** `challenges` missing from coach-help's `HASH_OK` (six waves old — needs a `coach-help` deploy); `bookings`/`booking_events`/`coach_leads` absent from `gdpr_table_policy` (Lewis call); Lewis copy on W4 opt-in sheet, W5 gate labels, W6 finish-sheet line, W7 recipe strings, W8 prospect-booking lines (none gating). **NEXT: clear the OTA stack, then W9 part 2 or WD light-first skin (mockup-gated).**

> **[2026-09-08 · PM-1108–1111 TRAINERIZE W8 SHIPPED — scheduling on four surfaces; OTA STACK NOW EIGHT WAVES DEEP]** State: vyve-site HEAD `fd81b3b4` **vbb 619** (sw `v2026-09-08h-pm1110-sched`), CC HEAD `77c1dfc8`, Test-Site HEAD `e214ffdf`. Migration `tz_w8_scheduling`; `create-booking` v4; `coach-lead-submit` v3; NEW `coach-calendar-ics` v1; no cron. **Dean's pass, in order:** (1) coach portal → **Scheduling** (new sidebar item under Calendar) → New service, set **Places per slot** to 2, save → add hours for a day you can test → confirm the row shows "Class · 2 places". (2) Try to save it back to 1 place once two people are on a slot — it should refuse and say why. (3) Tick **Show on my public lead page** with self-booking off — refused; with it on — saved. (4) **Availability** → add usual hours → **Apply to all services**, then re-run it and confirm it says nothing to add rather than duplicating. (5) **Time off** → block a three-day range → the chip reads the service count → remove it. (6) **Your calendar feed** → Get my link → subscribe it in Apple Calendar on the Mac and confirm your events and confirmed bookings appear; **Reset link** and confirm the old one 404s. (7) phone: partner profile → Book → the class slot reads "2 of 2 left", book it, then book from a second account and confirm the slot disappears at full. (8) portal **Calendar** → that class is ONE tile reading "2 of 2" — open it, see the roster, remove one person, confirm the place frees. Then **"OTA 619"** — that push carries W3 + W4 + W5 + W6 parts 1 and 2 + W7 parts 1 and 2a + W8. **#96 prospect booking cannot be tested end to end until a coach has a lead form** (0 exist live) — make one under Forms › Lead forms first, then mark a free consultation service public and open `www.vyvehealth.co.uk/lead/<your slug>`. **Owed:** Lewis copy on the prospect-booking lines + the W8 portal strings (non-gating); `challenges` still missing from coach-help's `HASH_OK` (five waves old now — needs a `coach-help` deploy); **`bookings`, `booking_events` and `coach_leads` are not in `gdpr_table_policy`** and W8 adds guest emails to them — retain-as-financial-record vs purge is a Lewis call. **Parallel-session note:** VYVEBrain HEAD carried PM-1105–1107 (`recipe-import` EF, W7 part 2b) at W8 open with no changelog entry — that session owes its own close. **NEXT: W9 client workspace depth, or WD light-first skin (mockup-gated).**

> **[2026-09-08 · PM-1102–1104 TRAINERIZE W6 PART 2 SHIPPED — video pipeline (#88); W6 COMPLETE; OTA STACK NOW SEVEN WAVES DEEP]** State: vyve-site HEAD `3f364e09` **vbb 618** (sw `v2026-09-08g-pm1103-exvideos`), CC HEAD `19e66771`. **No migration, no EF** — `cex_admin_all` already carried the write, proven live with a minted `team` JWT (3 stock rows attached incl. an alt, reverted, residue zero, library back to 128/977). **The recon finding: filling `coach_exercises` would have reached ONE member** — 84 active `workout_plan_cache` rows, exactly 1 `source='coach'`, the other 83 read video from `workout_plans` by name; `coach_exercises.video_url` only reaches a member by being snapshotted into `programme_json` at apply time (§23.256). So the member side gained a live fallback read of the stock library, which also made the alt free (no second snapshot, no re-apply, works on AI plans). **Honest count: 849 unfilmed, not 866** — 17 rows are `media_url`-only and already play, so every "filmed" test is the COALESCE. **Dean's pass, in order:** (1) CC → coach portal at VYVE scope → Exercise Library → the **Video** select → Missing a video should read 849; Has an alt should read 0. (2) **Bulk attach** → paste two or three real lines (`Exercise Name, https://…/file.mp4` and one `…, alt, Female`) → **Check rows** → confirm the verdicts read true → **Attach** → the grid chips and the count line move without a refresh → **Undo this batch** → back to where you started. (3) paste a deliberately bad list — a made-up exercise, an `http://` url, an alt with no label, a name already filmed — and confirm each says why. (4) **phone (server.url loop):** open a session containing an exercise you just attached and confirm the demo plays; attach an alt labelled `Female` to that same exercise and confirm the toggle appears over the fullscreen video, swaps, and is still on your choice next time. `window.vyveRefreshExerciseVideos()` clears the 6 h cache if the phone is stale. Then **"OTA 618"** — that push carries W3 + W4 + W5 + W6 parts 1 and 2 + W7 parts 1 and 2a. **CONTENT IS THE GATE:** the tool is ready and the library is empty — 849 rows need filming. **4 already have footage** in `exercise-videos` that W0's exact-name join missed (`incline-push-up.mp4`, `jogging-treadmill.mp4`, `recumbent-bike.mp4`, `walking-treadmill.mp4`); the other 61 unlinked bucket objects are near-duplicates of already-filmed rows, so it is 4, not a batch. **Recorded, not a defect:** every authenticated member can read the 977-row stock `coach_exercises` library — a side effect of `cex_coach_read`'s `partner_id IS NULL` arm, harmless (public-domain exercise catalogue, no member data) and it is what makes the live read free, but nobody decided it. **Still owed:** `challenges` missing from coach-help `HASH_OK` (four waves old — no `coach-help` deploy happened this session, it needs one); Lewis copy on the W6 finish-sheet logging line, W5 member strings + gate labels, W4 opt-in sheet, W7 recipe strings (none gating). **New §23.256** (snapshot-at-apply reaches only rows applied since — trace the read path backwards before building the write path), **§23.257** (`pg_net` cannot PATCH; PostgREST ignores `X-HTTP-Method-Override` and the request becomes an INSERT), **§23.258** (a default off a free-text column must match the value explicitly and fall back safe). **NEXT: clear the OTA stack, then W8 scheduling or W7's `recipe-import` EF (talk-first, SSRF shape).**

> **[2026-09-08 · PM-1100–1101 W7 PART 2a SHIPPED — recipes survive the assignment boundary; OTA STACK NOW SIX WAVES DEEP]** State: vyve-site HEAD `da779b94` **vbb 617** (sw `v2026-09-08f-pm1100-planrecipes`), CC HEAD `3f79ce7b`. Part 1's bug: both plan-day drop points copied `{name, note, items}`, so an assigned recipe reached the member as a bare ingredient list — fixed via `w7PlanMealFrom`/`w7PlanMealKeep` + `recipe_ref` (provenance only); `nutrition.html` renders photo/prep/servings/method; **Save as a meal** added. **The meal-plan PDF slot needed no work — already shipped (§23.255).** **OTA: last production push was 4 Sept, `32ab5a88`, vbb 589. Members are on 589; 590→617 unpushed. Clear this before another member-facing wave.** **W7 LAST UNGATED ITEM — `recipe-import` EF, NOT STARTED, TALK-FIRST:** an EF fetching arbitrary member-supplied URLs from inside Supabase's network is the risk, not the Haiku parse. Shape to agree before writing: scheme/host allowlist, no redirects into private IP ranges, response size cap, per-member daily quota, own ledger table (§23.245), cost cap; UI is a two-step sheet (paste URL → review parsed ingredients → confirm before any write) and **gets a mockup first**. **Gated:** #87/#111 AI drafts (Calum go/no-go, ship behind `AI_DRAFT_VISIBLE=false` regardless); ~100 stock recipes drafted from `food_products` → Lewis/Calum approve → seed at `partner_id IS NULL` with `shared:true` — **until this lands "From your coach" is empty for every member without a coach, which is currently every member**. **Still owed:** `challenges` missing from coach-help `HASH_OK` (four waves old); Lewis copy on W4 opt-in sheet, W5 gate labels, W7 recipe strings + share-toggle wording (none gating).

> **[2026-09-08 · PM-1097–1099 TRAINERIZE W7 PART 1 SHIPPED — recipes on both sides; OTA STACK IS NOW FIVE WAVES DEEP]** State: vyve-site HEAD `4fd5bc79` **vbb 616** (sw `v2026-09-08e-pm1097-recipes`), CC HEAD `2fe25d32`, migrations `tz_w7_recipes` + `tz_w7_recipe_photo_member_prefix`, new table `nutrition_recipes`, new public bucket `recipe-photos`, new functions `my_recipes()` + `coach_meal_items_to_member()`. **The W7 brief was wrong against live schema:** `coach_foods` is the private *food* library (0 rows) and the coach's **meal** is a `coach_templates` row `kind='meal'` — so recipes are additive payload keys (`image_path`/`method[]`/`prep_mins`/`cook_mins`/`servings`/`tags[]`/`shared`), **no coach table, no coach migration**, and they land in nutrition plan days for free. `shared` defaults **OFF** on every pre-W7 row. **OWED — OTA 616 carries W3+W4+W5+W6+W7 member code (612/613/614/615 all still unpushed). Clear this before another member-facing wave: five waves behind one device pass is the blast radius, not the convenience.** **W7 PART 2 (not started):** `recipe-import` EF (Haiku parse of a fetched page → ingredient rows the member confirms, ~0.13p/import, server-side only — this IS the PM-1063 "Import from link" spec; the button ships today as an honest "next update" toast); `coach-ai-draft` v1 kind=workout|meal_plan (#87/#111) **behind `AI_DRAFT_VISIBLE=false` until Calum says yes**; ~100 stock recipes drafted from `food_products` macros → Lewis/Calum approve → seeded at `partner_id IS NULL` with `shared:true`; nutrition-slot `attachment_path` for a PDF meal plan. **Still owed from earlier waves:** `challenges` missing from coach-help `HASH_OK` (three waves old, corpus not bumped this session — fold into the next `coach-help` deploy); Lewis copy on the W4 opt-in sheet, W5 gate labels, and now the W7 recipe strings + share-toggle wording (all non-gating). **New §23.253** (a page's top-level `let`/`const` are not on `window` — zone code touching page state stays inline) **and §23.254** (`gdpr_table_policy` covers tables only; public-bucket member uploads survive an erase — open tail on `recipe-photos` AND `coach-challenge-art`, decide it before the next bucket).

> **[2026-09-08 · PM-1093–1096 TRAINERIZE W6 PART 1 SHIPPED — circuit + interval workout types (#86) on both sides (migration `tz_w6_workout_types`, `coach_build_program_json` v3, zero EF changes; CC `6466664a` PM-1094; vyve-site `9bfd6895` PM-1095 vbb 615 NOT OTA'd); NEXT = DEAN'S DEVICE PASS, THEN "OTA 615", THEN W6 PART 2 (#88)]** Dean's pass, in order: (1) coach portal → Workouts › Day templates › New → **Session type › Circuit** → rounds 4, rest between rounds 90 → add a reps exercise, a duration exercise and a **+ Rest block** → save → reopen and confirm it comes back on Circuit with its numbers. (2) a second day template on **Interval** (rounds / work / rest). (3) drop both into a programme week — the week slot should chip "Circuit · 4 rounds" — and assign the programme to your test client. (4) **phone (server.url loop):** open the circuit session — round strip, **Start**, Done on the reps block, the timed block runs itself, the rest block counts down, chime + haptic at every change; lock the screen mid-block and confirm the clock is right on return; **Finish here and log** → kg/reps sheet → Finish and log → completion screen, and your exercise history shows it as rounds-as-sets (`4 × 12`). (5) same again on the interval session. Then **"OTA 615"** — that push carries W3 + W4 + W5 + W6 member code. **W6 part 2 (#88 bulk video attach) is next**: `video_url_alt` + `alt_label` columns are already live and empty, 866 stock `coach_exercises` rows unfilmed, CC tool + "missing video" filter + member alt toggle still to build. Owed alongside: [LEWIS COPY PASS] on the W6 finish-sheet logging line, the standing W5 member strings + gate labels, and the W4 opt-in sheet; `challenges` STILL missing from coach-help's `HASH_OK` allowlist (three waves old).

> **[2026-09-08 · PM-1089–1091 TRAINERIZE W5 SHIPPED — programme scheduling live on all three surfaces (migration `tz_w5_programme_scheduling`, `coach-provision-client` v15, `coach-weekly-snapshot` v2 + projection v2, cron 73; CC `653a53fa` PM-1090; vyve-site `65627aa2` PM-1091 vbb 614 NOT OTA'd); NEXT = DEAN'S DEVICE PASS, THEN "OTA 614", THEN W6]** Dean's pass, in order: (1) coach portal → Workouts › Programmes → open a programme → **+ Add phase**, rename it, **+ Add week to phase**, **Import phase from…** another programme → save → reopen and check the phases survived. (2) Clients › a client › **Plans** — queue a next phase (on-end and on-a-date), set a couple of permissions, Save; check the programme-position line reads right. (3) same client › **Calendar** — drag a session to another day (and onto an occupied day, which should swap), click one for Skip then Restore, set **Programme starts**, use the inline **Add next phase**. (4) Profile › **Settings › Client permissions** → Save defaults → Apply to all current clients. (5) Clients list, Summary view → **Phase** and **Next phase** columns populate; a programme ending within 7 days with nothing queued shows amber. (6) phone (server.url loop): Workouts shows the phase label + weekday strip, a session you moved in the portal shows on its new day with a Moved pill, a past unlogged session shows Missed, the look-ahead preview matches the gate, and **Move** opens the sheet and moves a session (flip that client to strict in the portal and confirm the button disappears and a hand-rolled RPC would refuse). (7) coach-messages with `messaging` set to one_way → composer gone, note shown; off → coach thread gone. Then **"OTA 614"** — that push carries W3 + W4 + W5 member code. Owed alongside: Lewis copy on the W5 member strings (missed / moved / preview / move sheet / one-way note) + gate labels + the standing W4 opt-in sheet; `challenges` still missing from coach-help's `HASH_OK` allowlist. **W6 (client workspace depth) is next** — `tasks/coach-portal-trainerize-waves.md`.

> **[2026-09-08 · PM-1085–1088 TRAINERIZE W4 SHIPPED — coach challenges live (CC `3a6b27bd`, vyve-site `289a006d` vbb 613 NOT OTA'd, migrations `tz_w4_coach_challenges` + `tz_w4_board_null_partner_fix`, `challenge-score` EF v3 + cron 72 hourly :40, corpus v2); NEXT = W5 programme scheduling]** Owed: Dean device pass (coach: New challenge → Go live; phone: Join → board → workout → Recompute now → points move) then "OTA 613" (carries 612's W3 member side too); Lewis copy pass on the member opt-in sheet [LEWIS COPY PASS]; add `challenges` to coach-help `HASH_OK` on its next deploy (corpus task currently routes to `clients`); employer-portal launch UI has no rules editor — employer launches score on STOCK_RULES; portal Realtime on `coach_challenge_participants` not subscribed (publication is on). §23.246 landed.

> **[2026-09-08 · PM-1082–1084 TRAINERIZE W2b SHIPPED — coach help assistant live (CC `a8b03c33`, migrations `tz_w2b_coach_help` + `tz_w2b_coach_help_log`, `coach-help` EF v3, corpus v1 in `coach_help_corpus` + mirror `playbooks/coach-help-corpus.md`, no OTA); NEXT = W4 challenges]** Own ledger `coach_help_log` (not `ai_interactions` — §23.245). **Dean checks owed:** coach login → `?` → chip → Open Clients route → thumbs → off-topic refusal. **Lewis:** corpus tone pass in Studio (non-gating). Standing rule: every wave close updates the corpus table + mirror. The PM-1069 rollout line below (member app / partner portal / CC) now has a proven pattern to copy — parameterise `surface`, one corpus row each.

> **[2026-09-08 · PM-1079–1081 TRAINERIZE W3 SHIPPED — group threads + attachments + voice + Realtime (CC `9b6c26f0`, vyve-site `65bee552` vbb 612 NOT OTA'd, migrations `tz_w3_threads_media` + `tz_w3_media_coach_delete`, zero EF changes); NEXT = W2b coach help assistant, then W4 challenges]** Fan-out rows (§23.243) — direct readers filter `thread_id=is.null`. **Dean checks owed (coach login on Mac + phone):** New message → two clients (invite a second test account first — the test coach has one; a single pick opens the direct thread) → group → send text / photo / voice note → phone group tab shows names, image, audio plays → reply from the phone → portal updates without waiting for the poll + Seen by 1/2 → Manage rename → Archive. Then say "OTA 612". **Carry-forward:** #60 workspace popover + W1 recent-activity feed show group copies (one-line `thread_id=is.null` each); scheduled sends not offered on groups; member voice recording = next binary (recorder plugin); thread previews in the list; W4 challenge threads = `coach_threads.kind='challenge'` + `challenge_id` already in place.

> **[2026-09-08 · PM-1077–1078 TRAINERIZE W2 SHIPPED — Automations live (CC `3e43fca7`, migration `tz_w2_auto_messages` + two follow-ups, `coach-automations` v1 on cron 71, `coach-provision-client` v14, no OTA); NEXT = W2b coach help assistant]** Auto-message sequences with the eleven-rule stock set, scheduled messages, sweep-detected membership lapse (`coach_clients.lapsed_at` + `is_coach_of` gate), per-client calorie/meal alerts, health-connection status + Ask to connect, four welcome attachments. **Dean checks owed:** phone — the stock Welcome push to deanonbrown12 (already sent by the first real sweep); desktop coach login → Automations three tabs + Edit → Send me a test (lands in deanonbrown2's inbox), Messages ⏱ schedule + cancel, client Overview cards (Ask to connect), Add client with two attachments → invite email links both, Settings › Notifications two new rows. **Carry-forward:** timed triggers (day 3/7/14, inactive, health-not-connected, birthday) code-reviewed only — first real day-3 for deanonbrown12 is 10 Sep 09:00 London; member-side lapse gate (coach-nudge.js / coach-content still render for a lapsed client) rides W3 with the standing OTA; `SWEEP_ENABLED` const in the EF is the kill switch. **Lewis:** stock message copy pass, non-gating. **W2b brief unchanged** (PM-1068/1070/1071): `playbooks/coach-help-corpus.md` first (fold W1 + W2 tasks in — automations, scheduling, alerts, attachments, lapse), then `coach-help` EF + portal drawer.

> **[2026-09-08 · PM-1075–1076 TRAINERIZE W1 SHIPPED — roster + insights live (CC `9e403569`, migration `tz_w1_leads_convert`, zero EF changes, no OTA); NEXT = W2 automations]** Dashboard 12-week strip + needs-attention chips; eleven auto tags with thresholds in `coach_ui_prefs.auto_tags` (Settings › Auto tags); roster views + multi-select bulk actions + segment sends; Booking clients → Invite; Leads → Convert; Subscribers panel; feed widened. **Dean checks owed (coach login, desktop):** Dashboard strip + chips (expect zeros — your client has 0/2 both weeks), chip → filtered roster, Clients view switcher + tick a row → bulk Message → send, Settings › Auto tags save. **Carry-forward:** invited clients never get snapshot rows (EF computes `status=eq.active` only) — roster shows "—" for them; the snapshot's `last_msg_*` are all-time so not-messaged/not-responded need at least one snapshot row (active clients only); `missed_workouts` dropped as a duplicate of low compliance. **W2 brief unchanged**: `coach_auto_messages` + `coach-automations` EF (cron */15) + `coach_messages.deliver_at` + `coach_clients.lapsed_at`; stock bodies [LEWIS COPY PASS]; then W2b help assistant (PM-1068). Corpus rule from W2b onward: update `playbooks/coach-help-corpus.md` at every wave close (W1 items to fold in when it exists).

> **[2026-09-07 late · PM-1072–1074 TRAINERIZE W0 SHIPPED — backbone live (migration `tz_w0_coach_client_weekly`, `coach-weekly-snapshot` v1 + `_shared/programme_projection.ts`, cron 70 Sun 23:30 UTC, 111-row video backfill, CC `8f2a0abd`); NEXT = W1 roster + insights — DONE PM-1075]** `coach_client_weekly` fills every Sunday for active clients (this week + last); on-demand coach path = `POST coach-weekly-snapshot` with the coach JWT `{member?, weeks?}` → returns `data[]` — **W1's Insights strip is the first caller** (12-week default). `active_days` not sign-ins (none exist). Projection contract: `scripts/ef/coach-weekly-snapshot/_shared/programme_projection.ts` — W5 edits it there and redeploys every consumer. `coach_ui_prefs` is the portal pref bag (§23.237) — W1's auto-tag thresholds can live in `coach_notification_prefs.auto_tags` ONLY via a `save_notify_prefs` extension (whitelisted), else use `coach_ui_prefs`. **#71 verdict for W3:** add `coach_messages` to `supabase_realtime`, subscribe client-side, keep the 15 s poll as fallback. **Dean checks owed (coach login on desktop):** Quick add ×4, Get-Started 2/4 + Hide, Messages archive/restore, Settings › Calendar tiles → name. **Parked from W0:** archived threads move from `coach_ui_prefs.archived_threads[]` to `coach_threads.archived` in W3; the deployed EF header says PM-1068 (parallel-session collision, PM-747 precedent — no redeploy).

> **[2026-09-08 · PM-1069 FOLLOW-ON: HELP ASSISTANT ROLLOUT TO ALL SURFACES — after the Trainerize waves complete]** Dean: "need to add this to our endpoint once all waves sorted". Once W2b's `coach-help` (#112) is proven with Calum, roll the same pattern out with a corpus per surface: member app (help.html — product help ONLY, hard-separated from the coaching personas, no Mind-pillar content, Phil not in the path), partner portal, CC (team/admin how-to from the brain). Same EF shape parameterised by `surface`, same per-user cap, same corpus-update-at-wave-close rule. Not before the waves finish.

> **[2026-09-07 late · PM-1066 TRAINERIZE GAP MAP + WAVES WRITTEN — nothing built; W0 NEXT]** Read `tasks/coach-portal-trainerize-gap-map.md` (#62–#111) + `tasks/coach-portal-trainerize-waves.md` when Dean says "do Trainerize wave N". Order W0 backbone → W1 roster/insights → W2 automations → W3 messaging → W4 challenges → W5 programme scheduling → W6 workout types + video pipeline → W7 nutrition/recipes (+ gated AI drafts) → W8 scheduling → W9 client workspace → WD light skin; 15–18 sessions. **Protected-key rule:** `workout_queue`, `alerts`, `schedule_overrides`, `welcome_attachments`, new `gates` keys go on coach-provision-client's protected list before any writer. **Coordination:** W7's member Recipes tab IS the PM-1063 "My recipes" next step — ship once. W4's member surface also renders `employer_challenges` (closes employer B2). **Owed:** Alan + Lewis ruling on Stripe Connect coach billing (not a build item); Lewis: stock auto-message bodies, challenge opt-in copy, prospect-booking copy, #106 consent wording + version bump; Calum: AI draft go/no-go (#87/#111), video filming for #88. **Standing:** OTA 591+ still not pushed; every member-facing wave inherits it.

> **[2026-09-07 late · PM-1063 OFF UK MIRROR LIVE — `food_products` 148,565 rows, off-proxy v12 local-first + write-back, `vyve-off-mirror.timer` WEEKLY (Sun 03:30 UTC) on the Hetzner box, `food_lookup_misses` telemetry live; cover still ON → NEXT SESSION = MY RECIPES, THEN UNLOCK]** State: vyve-site HEAD `b20e9ed5` vbb 610 (no site commit this session), off-proxy v12 (platform v36), migrations `pm1063_food_products_uk_mirror` + `pm1063_search_food_products_v2_sanitise`, box loader `/opt/vyve/off-mirror-load.py` (VYVEBrain `scripts/`), Dean's demo seed `off_id='demo-pm1058'` (201 rows — delete when done). **Dean check owed:** two cupboard barcode scans (instant; new items mirror on first scan). **OFF MIRROR — DONE (PM-1063).** Truth: 148.5k loggable UK rows, not 300–400k (§23.236); refresh = every Sunday 03:30 UTC (PM-1064), manual `systemctl start vyve-off-mirror.service`. **NEXT SESSION (own session, ~1) — MY RECIPES:** `nutrition_recipes` (ingredients [{food,qty,unit}], servings, per-serving totals) — port the W4 coach meal-builder maths; strip = Create recipe · Import from link (Haiku parse of the fetched page → ingredient rows the member confirms; ~0.13p/import; server-side EF only); Recipes tab currently shows an honest "next update" state; gdpr purge row; ingredient search goes through off-proxy v12 (local-first now, so the builder's typeahead is cheap). **THEN UNLOCK (separate commits):** cover block + inline style + 4 dev-bypass early-returns + bypass script + the PM-1042 nutrition.html door OUT; `exercise.html` Food log row `dev:true` + filter OUT; More-menu Food Log + nutrition.html food-log card BACK + a Nutrition-hub card for `nutrition-progress.html`; `common_foods` seed from CoFID (McCance & Widdowson, OGL) 125→~3,000; **ODbL attribution line on the food-log footer + privacy page [LEWIS COPY — gating for the privacy page, wording: "Food data from Open Food Facts, licensed under the Open Database License"]**; Lewis sees log-food + the report before members [LEWIS COPY PASS non-gating: picker strings, Your week/report labels, kbd-done none]; tell Calum his clients can log (adherence card live-by-consequence); `kbd-done.js` roll-out to other input-heavy pages is one script line each — Dean names them. **PARKED / DECIDED:** voice log + meal scan OUT; Monday digest push (scheduled_pushes rail exists) waits for Lewis's one-line persona copy; a separate "Progress" nav tab NOT built (steps/sleep live under Body/Home); custom week start exists in Your week, not on the report page; `loadRecent` still Dexie-first (harmless, §23.231); kJ-only OFF rows (119) not converted. **BINARY ITEMS (next Android/iOS build):** `@capacitor-mlkit/barcode-scanning` native scanner (keep ZXing as web fallback), Android `CAMERA` permission + Play declaration. **DROPPED:** FatSecret Premier application — only if `food_lookup_misses` says so at the 60–90-day review (best-guess £350–450/mo net); AI photo/text logging — costed, not requested.

> **[2026-09-07 · PM-1042–1048 FOOD LOG DEVICE PASS RUN — scanner/sheet fixed live, first real `nutrition_logs` rows, MFP-shape picker APPROVED → NEXT SESSION]** State: vyve-site HEAD `e4f2f891` vbb 598, cover still ON, Dean's dev door = long-press nutrition.html WATER TRACKER eyebrow 1.2s. **NEXT SESSION (build, mock-up approved — dark theme on the live page, talk-first only on schema):** (1) **History + header meal selector + strip (~0.5):** sheet header gets a meal dropdown (breakfast/lunch/dinner/snacks — today the slot is fixed by which diary "+" was tapped; `currentMeal`/`fd-meal` become the single source), tabs History / My meals / My recipes / My foods, History strip = Barcode scan · Quick add, "Recently logged" sorted most-recent with the per-row + (one-tap log at last-used qty/unit); keep §23.195 registry rows; NO auto-focus anywhere (PM-1048). (2) **My meals (~0.5):** new `nutrition_meals` (member_email, name, items jsonb [{food snapshot, qty, unit}], totals, created_at, client_id) + RLS own + `gdpr_table_policy` purge row + Dexie table if PF-8 wants it; strip = Create meal (pick from today's diary rows or search) · Copy previous meal (yesterday's same slot → today, one tap); logging a meal expands to N `nutrition_logs` rows (one client_id each). (3) **My foods:** existing `nutrition_my_foods`; strip = Create a food (full form: name/brand/serving/per-100g or per-serving macros/barcode) · Quick add. (4) **My recipes (~1, second session):** `nutrition_recipes` (ingredients [{food, qty, unit}], servings, per-serving totals) — port the W4 coach meal-builder maths; strip = Create recipe · Import from link (Haiku parse of the fetched page → ingredient rows the member confirms; ~0.13p/import; server-side EF only). Voice log + Meal scan are OUT (Dean). **THEN UNLOCK (separate commits):** cover block + inline style + 4 dev-bypass early-returns + bypass script + the PM-1042 nutrition.html door OUT; More-menu Food Log + nutrition.html food-log card BACK; `common_foods` seed from CoFID (McCance & Widdowson, OGL) 125→~3,000 instead of the hand-picked 300; `food_lookup_misses` telemetry (barcode miss + zero-result search, no member id) — review at 60–90 days = the FatSecret decision; Lewis sees the page before members [LEWIS COPY PASS non-gating]; tell Calum his clients can log (adherence card live-by-consequence). **BINARY ITEMS (next Android/iOS build):** `@capacitor-mlkit/barcode-scanning` native scanner (keep ZXing as web fallback), Android `CAMERA` permission + Play declaration. **DROPPED / PARKED:** FatSecret Premier Free application + UK quote — only if the miss telemetry says so (best-guess £350–450/mo net); AI photo/text logging — costed (≈0.5p/photo, ≈23p/member/mo), not requested. **Dean's remaining pass items:** grams vs serving totals sanity, forced miss → Quick Add → rescan resolves locally.

> **[2026-09-06 · PM-1041 ANDROID 1.0.11 (57) IN PLAY REVIEW — Health Connect minimum-scope fix after 1.0.10 (56) rejection]** (1) **Watch the review** (~1-5 days on precedent); if Google bounces again it names the type — reply by removing it from the manifest AND the Health apps declaration, never by justifying it in prose. (2) On approval: Health apps declaration step 2 should self-collapse to 11 read permissions — verify, and re-run the PM-978 item-5 **Android walkthrough sweep on 57**. (3) **healthbridge Android branch** — manifest is done, so Health Connect parity is now OTA-only work: lift the iOS gate at healthbridge line 34 for android, request the 12 scopes, map to the same sync-health-data contract; own session, mockup-first on the rv3 Connect step wording (Lewis copy — it says "Apple Health" today). (4) **Resting HR + HRV** permissions are queued on the readiness/recovery feature — next binary after that feature exists, + declaration update. (5) PM-980 optimisation pass (R8, edge-to-edge, large-screen) still rides the NEXT binary, not this one. (6) Dean confirms the exact expiry date shown on the new `GITHUB_PAT_CLAUDE` → §24 row.

> **[2026-09-05 · PM-1039/1040 MEMBER ADMIN W6 SHIPPED — VYVE-side messages + PT-role check; programme W0–W6 + W4b CODE-COMPLETE (migrations `w6_vyve_messages` + `w6_vyve_messages_sender_email`, vyve-site `d470f7f3` vbb 591, CC `94f00a63`)]** VYVE thread on `coach_messages` = `partner_id IS NULL` + `sender_scope='vyve'`; member page shows coach + VYVE threads with a switcher; CC Messages tab sends as "VYVE team" with `sender_email` for the staff view; team may write only into VYVE threads. **NOT DONE / OWED:** ① **OTA 591 not pushed** — Dean device pass first (CC → your test account → Messages → send → push → bell → tap → reply → pill/dot), then "OTA 591"; ② Cloudflare deploy `94f00a63` (Dean); ③ [LEWIS COPY PASS] three VYVE-thread strings (`coach-messages.html` VYVE empty state + subtitle, nudge card line) — non-gating; ④ `role='pt'` deliberately not added (team already cannot reach Admin tab / audit / billing) — add when a hire needs less than team; ⑤ `sender_email` is member-readable over REST (row-scoped SELECT) — acceptable today, a definer view is the fix if it ever matters; ⑥ `coach_clients` / `coach_templates` have no explicit `gdpr_table_policy` row (default purge on member_email applies) — register explicitly next time that table is touched. **Standing from W5:** six plan-change strings [LEWIS], then flip `PLAN_CHANGE_NOTIFY` ×3 + `NOTIFY_DEFAULT`; `partner-notify` `data.route`→`data.url` five-minute fix. **With W6 shipped the Member Admin thread is closed — next thread is Dean's call.**

> **[2026-09-05 · PM-1037/1038 MEMBER ADMIN W5 SHIPPED — plan-change notification, push + bell, admin + coach (`admin-member-programme` v6, `admin-batch-assign` v2, `coach-provision-client` v13, CC `ab4a1e6d`)]** One `scheduled_pushes` row per changed member; `send-push` makes the bell row + push from it (§23.223). Flag `PLAN_CHANGE_NOTIFY=false` in all three EFs + `NOTIFY_DEFAULT=false` in member-admin.html until Lewis approves the copy; the CC checkbox / `{notify:true}` override works now. **DEAN CHECKS OWED (phone):** bell already has a proof entry on deanonbrown1 → tap → Workouts; CC assign to yourself with the box ticked → push ≤5 min → bell → tap; batch confirm box → rows say notified. Cloudflare deploy of `ab4a1e6d` is yours. **LEWIS OWES [LEWIS COPY PASS]:** six plan-change strings (admin programme/habits; coach workout/habits/nutrition/supplements) — then one change flips the four flags. **NEW SMALL BUG (own session, ~5 min):** `partner-notify` queues `data.route` instead of `data.url`, so every partner bell entry has `route=null` — rename the key. **W6 (VYVE-side messages + PT role) is optional — Dean confirms before it starts.**

> **[2026-09-05 · PM-1035/1036 COACH PORTAL W4b SHIPPED — batch assign + client tags (migration `w4b_coach_client_tags_and_batches`, `coach-provision-client` v12, CC `555ba2d5`)]** Batch assign under Clients: pick who (status/tag/search, cap 100) → pick what (one slot, own plan/form) → preview (today's plan + week, pushes-now vs lands-on-accept, already-on-it) → confirm w/ optional reason → per-client report + Revert; Recent batches w/ Revert / Revert remaining. Tags on the roster (chips + count-filter bar) and the client Overview (Tags box). Revert restores the client's PRIOR workout row + week; anything changed since is skipped. Both list Assign-to modals fixed (`merge_slots:true` — they used to wipe every other slot). Live-proven with a minted coach JWT; fixture cleaned to zero. **DEAN CHECKS OWED (coach login on deanonbrown2, your own client row):** Clients › Batch assign renders your client + the four-step strip; pick your Upper/Lower Starter → preview names today's plan → apply (reason optional) → report + Revert → Recent batches shows it; open the client → Overview → Tags box add `pilot` → chip on the roster + the Tags filter bar narrows; Assign to… from a plan list no longer clears the other slots (check the Plans tab after). Cloudflare deploy of `555ba2d5` is yours. **DECISION OWED: `coach_apply_assignments` resets the workout to week 1 on every apply (any Plans-tab save) — guard at the RPC (skip the workout section when the active wpc `source_id` = template)? Production RPC, benefits Calum, ~20 min. NEXT: W5 plan-change notifications (Lewis copy + OTA).**

> **[2026-09-05 · PM-1032/1033/1034 MEMBER ADMIN W4 SHIPPED — tags + batch assign + revert (migration `w4_member_tags_and_audit_batch_id`, `admin-batch-assign` EF v1, CC `239015a3` + `f3106ab1`)]** Batch assign on the member-admin rail: facets → names/emails/tags → template (programmes + habit sets from the VYVE library) → dry run → confirm with reason → per-member report with Revert; Recent batches with revert-remaining. Tags on Overview, list facet, Settings › Tags. Server-side loop, one audit row per member with `batch_id`, cap 100, empty picker refused, revert skips anything changed since the batch. Live-proven with a minted team JWT (batch `9e502a94`). **DEAN CHECKS OWED (dark first, desktop then phone):** CC → Members → open test1 → Tags box: add `pilot`, see it as a `# pilot` chip on the list and a chip on the row; Settings → rename it, delete it. Batch assign → tick a facet, watch the count, add yourself by email, remove one from the list, Choose template → pick `Beginner Full Body Circuit`, Dry run → the table names today's programme per member, Apply (reason optional) → report shows the batch id, spot-check two in the app, Revert → they are back, Recent batches shows it reverted. Log in as `team` → the same surfaces work (spec role model). Cloudflare deploy of `f3106ab1` is yours. **NEXT ON DEAN'S CALL: W4b — the same batch assign + tags in the PT area** (coach portal, a coach's own consented clients; multi-select on the existing Assign-to client picker or a Batch item under Clients, applying through `update_assignments`/`coach_apply_assignments` per client with the same dry-run → apply → per-client report → revert shape; client tags scoped by partner_id — own table or `member_tags.partner_id`, decide at mock-up). Own session, mock-up first, Calum-unchanged proof. Then W5 plan-change notifications.

> **[2026-09-05 · PM-1030/1031 MEMBER ADMIN W3 SHIPPED — `pages/member-admin.html` live on the coach-portal chrome (CC `ee93b90b`), admin-console re-soft-killed (CC `7c2e8358`, byte-untouched), admin-dashboard v25 `member_health`]** Members tab in the CC now lands on member-admin: list over all 95 (search, facets, sort) + w5 workspace with Overview (field_schema-driven SAFE inline / SCARY reason modal) · Habits · Workouts (wpc rows by surface, pause/resume/set-week, assign VYVE template, preview) · Goals · Nutrition · Check-ins (scores only) · Logs · Admin (admin role only — absent for team). Library items link out to coach-portal at null scope. **DEAN CHECKS OWED (dark first, desktop then phone):** CC → Members → looks like the coach portal; search, open test1, SAFE edit, SCARY edit with reason, habit fix, assign a template → renders in the app, Admin tab shows the audit rows; log in as `team` → seven tabs, no Admin tab. Cloudflare deploy of `7c2e8358` is yours. Not wired on purpose: staff-initiated GDPR export/erase (member-initiated pipeline only). **NEXT SESSION = W4: tags + batch assign + revert** — `member_tags` + `gdpr_table_policy` purge row + `admin_audit_log.batch_id` migration, `admin-batch-assign` EF (dry_run → apply per-member via `vyve_apply_template` → revert by batch_id), Batch page on the member-admin rail (facets first, cap 100, empty picker ≠ everyone).

> **[2026-09-05 · PM-1029 MEMBER ADMIN W2 COMPLETE — step 3 shipped, CC `4fceb967`]** coach-portal.html null scope live: staff (admin/team, no partner row) get the VYVE library (Nutrition/Workouts/Exercise Library/Habits/Settings) instead of "Not linked"; `pscope()`/`pprefix()` sweep, Calum unchanged by construction. Migration `w2_null_scope_staff_write` (team write on exercises/foods, `coach-content` `vyve/` staff policies). **DEAN CHECKS OWED (desktop, dark first):** log in to the coach portal as yourself (admin, no partner): sidebar shows only Nutrition/Workouts/Exercise Library/Habits/Settings, "VYVE library" badge, lands on Programmes with 33 templates; open Exercise Library → VYVE rows show Edit/Remove; author + save a VYVE programme template; assign it to test1 via admin-console → renders in the app. Then log in as Calum (or ask him): nothing changed. Cloudflare deploy of `4fceb967` is yours. **NEXT SESSION = W3: `pages/member-admin.html` on the coach-portal chrome (mock-up first, dark first), re-retire admin-console (same three lines), repoint its dropdown from `admin-programme-library` to `list_templates`.** Known interim gap: templates authored in the portal after today do not appear in admin-console's assign dropdown until W3.

> **[2026-09-05 · PM-1028 MEMBER ADMIN W2 STEP 1+2 SHIPPED — step 3 shipped same day, see line above]** `vyve_apply_template()` live (SQL, service_role only), `programme_library` migrated into `coach_templates` partner_id NULL (35 rows), admin-member-programme v19 assigns through it (`list_templates` added). Dean confirmed direction: keep going, keep everything admin-console does, fix the look at W3. **NEXT SESSION = W2 STEP 3: coach-portal.html null scope** — `pscope()` over 55 `partner_id=eq.` filters + 24 inserts, `init()` null-partner branch for admin/team (no more "Not linked"), coach-only sections (Clients/Leads/Messages/Automations/Forms/Profile) hidden at null scope, "VYVE library" badge; **Calum's REST calls must diff byte-identical before/after**; revert to known-good first if a live staff surface breaks. Then W3 = `pages/member-admin.html` on the coach-portal chrome (mock-up first, dark first), re-retire admin-console, repoint its dropdown from `admin-programme-library` to `list_templates`.

> **[2026-09-05 · PM-1027 MEMBER ADMIN W0 + W1 SHIPPED — W2 step 1+2 followed same day, see line above]** Security gate live on all six admin-* EFs (admin/team only, team split, `members_staff_view` + `members_admin_read_w0`), admin-member-programme v4 on the live wpc contract, `create_library_habit`, admin-console.html RESTORED as interim (CC `75688462`). All live-proven with minted partner/team JWTs (§23.216). **HARD GATE REACHED: Dean decides whether W2 starts** (SQL `vyve_apply_template()` + null-scope coach portal ~2–3 sessions; W3 = `pages/member-admin.html` on the coach-portal chrome, the look Dean asked for, re-retires admin-console). **NEW WARM SECURITY DEBT from the PM-1003 grep (no `?email=` read fallbacks remain):** (a) seven cron sweeps accept an unauthenticated trigger — `comms-dispatcher` (also honours body `at`/`dry_run`), `habit-reminder`, `streak-reminder`, `session-reminder-cron`, `process-scheduled-pushes`, `partner-health-sweep`, `platform-watchtower` — fix = a shared `VYVE_CRON_KEY` header check + cron job header update, one short session; (b) `platform-alert` open intake accepts `member_email` in the payload (rate-limited; anyone with the anon key can file an alert against any member) — consider JWT-when-present + drop unauthenticated member_email. **Open decision:** add `injuries`/`exercises_to_avoid` to `members_staff_view` for programme safety (currently excluded as health free text). Known: admin-console Raw tab → 403 for team logins (intended).

> **[2026-09-05 · PM-1026 MEMBER ADMIN — spec v2 + 7 wave briefs after Fable audit; W0+W1 SHIPPED 2026-09-05 (PM-1027), see line above]** v1 spec was wrong in load-bearing places (blocker 1 false — inbox exists; `admin-member-programme` v17 broken vs live wpc contract; LIVE security hole — 4 `partner` admin_users rows pass every admin-* EF gate; `team` reads all 142 members columns). Waves v2: **W0 security gate (0.5) → W1 programme-EF rewrite + admin-console restore (1) → reassess → W2 null-scope library + `vyve_apply_template` + portal `pscope()` (2–3) → W3 `member-admin.html` on coach-portal chrome (1.5–2) → W4 tags + batch + revert (1.5–2) → W5 notifications (0.5–1) → W6 VYVE messages/PT role (1.5–2)**. Total 8–10. **W0 should ship before any other CC work — the partner-login hole is live now.** Dean: "load the brain, do wave N".

> **[2026-09-04 · PM-1003 — DONE: hk_connected revoked-filter fix (member-achievements v15 + log-activity v47) + unauth email fallbacks removed (leaderboard v28, monthly-checkin v37 — JWT-only, 401 otherwise; live-verified 3× against production)]** Follow-on queued: one-pass grep of remaining legacy EFs for the same v17-era email-fallback pattern.

> **[2026-09-04 · PM-1001/PM-1002 CORS SWEEP — DONE, all 16 fixed + eszip-verified]** §23.189 landmine class fully cleared: notifications v25, log-activity v46 (PM-1001) + the remaining 14 (PM-1002 — allowlist adds, pinned pair converted to allowlist, anthropic-proxy both natives). All verify_jwt flags preserved. apply-trial stays pinned www by design. Bonus: April-audit 89-EF graveyard confirmed already purged (142 live fns, zero graveyard slugs).

> **[2026-09-04 · PM-1000 — CALENDAR RUNWAY WATCHDOG LIVE (PM-833 closed)]** calendar-runway-watch v1.1 + cron 69 (07:30 daily): runway <14d/<7d + next-7-days thin/empty checks → platform_alerts + email (team@+Dean) + push (Dean+Lewis), daily nag while unhealthy, lands in the 08:00 digest. Both paths live-verified (healthy 57.1d/41; simulated trip proved rows+push+[TEST] email to Dean, fixtures cleaned). simulate knob = permanent alarm-test capability. Current schedule healthy to 31 Oct — next extension is Lewis/ops, the watchdog now covers the forgetting.

> **[2026-09-04 · PM-998 B2B S3 — billing + wizard SHIPPED; reactivation arc + SSO remain]** Recovered from the safeguard-killed session (backend had landed unlogged: pm998 migration, b2b-billing-run v2 + cron 68, employer-provision v4) — all live-verified this session incl. create_employer→validate_employer round-trip — then CC frontend shipped at `80412591` (employers.html Organisations + New Employer wizard; invoicing.html B2B chips/Send/Run-billing). NEW §23.199: bill-before-sweep cron order on the 1st is the arrears mechanism. **Dean checks owed:** Organisations table + wizard invite-link copy + Activate/Pause; Invoicing Run-billing all-trial honest state. **[LEWIS COPY PASS]** invoice email + confirm dialogs, non-gating. **S3 CLOSED at PM-999 (same day):** day-6 arc live (trial-end-email v3, stream trial_d6, first sends 5 Sep 01:10 cron); returning-trialist attach found ALREADY BUILT in stripe-webhook v10-16 (gap-map lag). SSO/SAML stays optional-if-Sage-asks. **Dean pricing note:** employer billing likely flat licence fee not per-user — PM-998 auto-run dormant pending Lewis; manual invoices cover flat fees today; adaptation ~1h when confirmed. NEW §23.200 (deploy-then-verify, no exceptions).

> **[2026-09-04 · PM-991 PARTNER PORTAL OVERHAUL — ALL SIX WAVES SHIPPED — overhaul CODE-COMPLETE; sole leftover = partners domain (Dean)]** Method = PM-982 redux, benchmark our own coach portal; map at `tasks/partner-portal-gap-map.md`. W1 ✅ PM-991 (CC `ba038b87`) · W2 ✅ PM-992 (CC `bf83f48d`) · W2b ✅ PM-993 (CC `2f9fca6a` — Go Live gated on 7 profile checks + approved welcome video, advisory first-content/bank, kanban path covered, PM-880 posture untouched) · W3 ✅ PM-994 (CC `5a012fcb` — calendar month/week/list over occurrences+bookings, badge-counted requests inbox riding booking-paid-confirm, content library lanes/folders/stats; content_folders jsonb migration, partner-content-upload v11, partner-notify v2 self-notify fix) · W4 ✅ PM-995 (CC `cf235032` — self-billing statements w/ print-to-PDF, £25 rollover as payment discipline, human promo codes Piece 1 COMPLETE incl. app-signup v8 code-or-slug + 17/17 backfill, CC payable rollup; funnel + CC payout run found already shipped — gap-map lag). W5 ✅ PM-996 (CC `c0a2bdd2` + Test-Site `feab7e25` — leads for every partner incl. mini form editor + lead.html neutral copy, ratings surface, source split via extended RPC, checkout-session EF all-£10-paths live-verified, CC manual attribution w/ attribution_admin stamp; gap #7 likes/replies found already shipped PM-866). W6 ✅ PM-997 (Test-Site `10874add` + partner-onboarding v20 — cropper at source, photos public + url-first submit stamping, honest upload errors, wizard reportError + global hooks; 4 dead one-shot EFs DELETED via Management API/MGMT_PAT — no-MCP-delete constraint retired). REMAINING (Dean, non-code): partners.vyvehealth.co.uk Cloudflare custom domain on the CC Pages project → then the one-line PORTAL_BRAND_URL flip in partner-provision so partner emails stop pointing at admin. (backlog line: 'partners.vyvehealth.co.uk' item). **Lewis owes (W4, non-gating):** statement copy pass [LEWIS COPY PASS], self-billing agreement clause + VAT position (statement stays statement-not-invoice until confirmed); W5 adds lead.html neutral-copy pass + continue.html already-active line [LEWIS COPY PASS]. **Dean device passes owed (accumulated W1→W4):** W1 tiles/routing/notifs · W2 Account tabs/prefs/password · W2b CC minimums panel + disabled Go Live · W3 calendar/requests/lanes/folders + v11 authed content list w/ stats · W4 statement modal+print, rollover states, ALEX10 in share copy, CC Revenue rollup. · W5 leads view+form editor+real lead submit on a non-coach page, ratings card, source split, admin-console attribution set/clear/locked (also verifies admin-member-edit v7 — no admin JWT in-session), continue.html £10 checkout on a real conversion. · W6 fresh wizard run — cropper on photo pick, submit lands PUBLIC avatar/cover URLs on the partner row, huge-PDF honest reject.

> **[2026-09-03 · PM-988 DIRECTION — COMMAND CENTRE "PREMIUM TIER" OVERHAUL + APP]** Dean (post-coach-portal gap map): elevate the CC to premium-premium quality and ship it as an app. Method = the Kahunas pattern aimed at design quality: Dean supplies (1) benchmark screenshot passes of 2-3 tools he rates premium (Linear/Stripe Dashboard/Vercel/Notion class — his picks; nav/home, dense table, detail view, command palette, settings per tool) and (2) an iPhone-width pass of the current CC with one-line "feels cheap" annotations (CC is desktop-first; app = mobile-first is the gap). Claude pre-work needing NO input: full CC repo surface inventory, component/spacing/typography drift audit (post-PM-9xx bolt-ons), design-token system definition. Ship shape: design system → mobile-first restructure → Capacitor wrap (= existing Watchtower Phase 2 backlog item, native push payoff) → internal distribution. Run as its own session thread; do NOT interleave with the coach-portal build waves (PM-983+ in flight). **[PRE-WORK DONE 2026-09-04, PM-1004: inventory at `tasks/cc-overhaul-inventory.md`, drift audit at `tasks/cc-overhaul-drift-audit.md`, token proposal at `tasks/cc-design-tokens-proposal.md`. Remaining gate CLEARED 2026-09-04 (PM-1005): Dean locked the design without the benchmark pass — five calibration calls decided, executable spec at `tasks/cc-design-system-locked.md`, Waves 0-4 delegated to Opus one-per-session (W0 prompt delivered). Fable retains: portal CSS consolidation, mobile-first restructure, monolith absorb/iframe decision, command-palette promotion, Capacitor wrap. Dean device check between waves, dark first.]** **[WAVE 0 SHIPPED 2026-09-04, PM-1006, CC `c6a9fb49`: tokens.css v3 + components.css v2 (modal/toast absorbed out of shell) + shell.css slimmed to chrome + density boot in index.html; four files md5-perfect, zero undefined var refs, selector set frozen across the components+shell pair, all inline script blocks node-parsed. Two logged deviations: shell's breakpoint ladder left for the mobile-first restructure, and the page-specific `.ap-*`/`#page-settings` blocks left verbatim until Wave 4 moves them to action-plans.html. `--fs-xxs`/`--fs-hero` kept as compat aliases → retire in Wave 4 (new §23.201). NEXT: **Wave 1 — Insights family, 8 pages**, own session, after Dean's device pass.]** **[WAVE 1 SHIPPED 2026-09-04, PM-1007, CC `ca188b5e`: the 8 Insights pages (app-health, usage, retention, activity-depth, wellbeing, platform, revenue, ai-usage) off their local dual-theme blocks + document-wide resets and onto tokens v3/components v2; primitives (.btn/.card/.card-title/.pill/.grid-2/.tab-*/.page-title/.page-sub) handed to components, font-size + radius literals snapped, breakpoints on the 640/900/1280 min-width ladder, zero JS changes, DOM byte-identical, 8 files md5-perfect. Five behaviour-preserving scope calls logged; one exposed a genuine gap in components.css (no `.btn:disabled`) → new §23.202. Inline style= attributes and raw colour literals deliberately untouched. NEXT: **Wave 2 — pages/partner-management.html** (partners.html stays BYTE-UNTOUCHED), own session, after Dean's device pass.]** **[WAVE 2 SHIPPED 2026-09-04, PM-1008, CC `44367135`: partner-management.html off its two scoped `html[data-theme]` blocks, all 222 rules on canonical token names, surface tiers realigned one step, breakpoints on the 640/900/1280 min-width ladder, zero JS changes, DOM byte-identical, zero undefined var() refs, both inline scripts node-parsed, 1 file md5-perfect. `partners.html` byte-untouched (note: it is at repo ROOT, not `pages/`). Three Part C deviations logged — the retired-name table's `--mint`/`--mint-light`/`--dark` rows are wrong for this page's values, so Part C must be checked against each page rather than applied blind on W3/W4. A scoped 12-name compat-alias block stays until this page's JS can be touched (new §23.203); `--amber` fixed. OPEN, reported not fixed: renderer modals `appendChild`'d to `document.body` sit outside the page scope and resolve their var() refs against nothing — pre-existing, needs a JS change. NEXT: **Wave 3 — admin-console.html**, own session, after Dean's device pass.]** **[WAVE 2 REVERTED THEN RE-LANDED 2026-09-04, PM-1009/PM-1010, CC `0102d872`: Dean's device check showed the page badly broken; reverted to known-good bytes first, and it came back identical — both faults were pre-existing specificity bugs, not the migration. `.app` was a flex row because `showApp()` sets an inline `display:flex` that PM-924's `.app{display:block}` could never beat, and the `#page-partner-management *` reset (1-0-0) was stripping padding/margin off every components.css primitive used on the page. Fixed with `flex-direction:column` and `:where()` respectively; new §23.204. Wave 2 content unchanged from 44367135.]** **[WAVE 3 SHIPPED 2026-09-04, PM-1011, CC `0c5811f2`: `admin-console.html` off its local 28-var system (a `:root` the brief said didn't exist, plus both `html[data-theme]` blocks) and onto tokens v3 + components v2 via `<link>`; clean delete, **no compat-alias block needed** — all 63 var() refs outside the `<style>` block resolve from tokens. Five local names (`--teal`, `--teal-lt`, `--teal-xl`, `--teal-dark`, `--font-mono`) had zero consumers; only `--row-hover`→`--hover-overlay` was live, so Part C's admin-console rows never fired. Primitives handed to components (incl. `.page-header`/`.hdr-row` — first page whose frozen DOM has the child components expects); `.error-banner` kept page-local because components' copy is light-theme-only (second §23.202 gap). Grid clamps: components' `!important` on `.grid-3`/`.grid-4` at base+640 meant the page rules were never in control below 900, so the shared ladder was adopted. One breakpoint inverted to a single min-width:900 rung. §23.204 checked and clears both halves — bare `*` reset, no `#id *`, and the inline `display` on `#login` is PM-888's deliberate pre-paint handover. One JS change only (Law 6 theme-key migrate); `'dark'` default retained, aligning it to the shell's system default is a Wave 4 one-liner. shell.css deliberately NOT linked (standalone doc, own chrome, 22 colliding class names) so the tokenised `html,body` + `*` reset stays. NEXT: **Wave 4 — small-page sweep (~24 pages)**, own session, after Dean's device pass; it also retires `--fs-xxs`/`--fs-hero` and moves `.ap-*`/`#page-settings` out of components.css.]** **[2026-09-04 · PM-1012 — `admin-console.html` SOFT-KILLED at Dean's instruction right after the Wave 3 device pass, CC `1d8ecd6c`. Delinked from `sidebar-config.js` (Member Admin item + Members blurb), `pages/home.html`, `pages/links.html`, and `partners.html` (its "Command Centre" item repointed to `index.html` — the only edit to that file since PM-1008 froze it). File preserved, restore = uncomment three lines. `platform-health.html`'s LABELS entry left in place: watchtower still probes the target, so the preserved file keeps it green and the row is now a tripwire. ~~**OPEN: the CC has no member list/detail/edit surface any more**~~ **DECIDED 2026-09-04 (PM-1020): STAYS RETIRED — Dean is building a brand-new back end** — **now SPECCED at PM-1024/1025: `tasks/vyve-member-admin-spec.md` (design + keep/kill verdicts) and `tasks/vyve-member-admin-waves.md` (W0-W6 briefs, hard gate after W2). Backend largely exists already — admin-member-edit v21 + 4 sibling EFs; the UI is the gap. NOT STARTED.**, so the member surface is not being restored to the shell and is not being rebuilt against admin-console. Member editing and the audit log remain direct-URL-only in the interim. The Wave-3 carry-over to align admin-console's `'dark'` theme default with the shell's system default was **dropped with it** — styling a page being replaced buys nothing (its two `?v=` stylesheet links WERE bumped in PM-1020, since a direct-URL visit would otherwise serve a stale cached components.css). **Also for Wave 4: ask whether a surface is still wanted BEFORE migrating it** — this page was migrated and retired within the same session.]** **[WAVE 4 SHIPPED + MIGRATION COMPLETE 2026-09-04, PM-1016, CC `6fb80be4`→`6b9c2b16` in five batched commits: 34 files, 396 literals snapped, all 23 max-width queries inverted onto the 640/900/1280 ladder, `--fs-xxs`→`--fs-2xs` and `--fs-hero`→`--fs-4xl` retired, `.ap-*` MERGED into action-plans.html and `#page-settings` DROPPED as redundant (settings.html never touched), zero selectors lost, zero JS changes. Brief corrections: the 7 alias pages + action-plans are DEAD ROUTES (§23.209), not off-nav surfaces, so they got var-reference edits only. **PM-1017 `1160efcd` fixed two faults from Dean's device pass** — a Wave-4 source-order bug on tasks.html (§23.205) and a Wave-0 `!important` cascade bug that had left every `.grid-3`/`.grid-4` in the CC 2-up since `c6a9fb49` across 14 surfaces (§23.206); **PM-1017b `4972e80a`** bumped index.html's `?v=` stylesheet links so the fix propagates (§23.207). **THE PM-988 DESIGN-SYSTEM PHASE IS CLOSED.** Next in the CC programme: mobile-first restructure → Capacitor wrap → internal distribution, all Fable's. ~~OPEN one-liners reported but not taken~~ **ALL CLOSED 2026-09-04 by PM-1020, the components pass** (`c4a4d35f`→`31f322d7`): `index.html:143`, `--vyve-teal-dark`, `--vyve-coral` and the four shortcuts, plus the two components.css gaps (`.btn:disabled`, unthemed `.error-banner`) and **nine more light-only hardcoded text colours the waves never spotted** — `.badge-*`/`.pillar-tag.*`, one pair of which renders on LIVE settings.html. partner-management's body-mounted modals also fixed, with zero renderer changes, by widening the alias block to `body` (§23.212). **Dead-route list corrected to NINETEEN** (Wave 4's eight + activity/clients/competitors/compliance/intel/intel-hub/sessions/social-blueprint/strategy/team/trash); Dean's ruling = keep every file, remove every link, repoint the shortcuts. **STILL OPEN, deliberately deferred:** partner-management's 12-name alias block (177 refs across the 124KB renderer — invisible tidiness, do it when something else forces that JS open); `pages/social-blueprint.html`'s inline script has a pre-existing genuine syntax error and has never parsed (dead route, Law 1); restoring `lib/entities.js` to the shell's script list would light up six "Open in …" buttons pointing at dead routes (§23.213).]** **[2026-09-04 · PM-1018/1019 — Meetings page, CC `938bf7da` + `06842471`: past meetings in month folders nested under year folders (current year's months at top level, older years wrapped, Undated last); `When` is now a Right away / Schedule toggle. No backend change. Year tier is fixture-verified only — all live rows are August 2026, so it first renders in January.]**

> **[2026-09-04 · PM-982→990 COACH-PORTAL OVERHAUL — DONE (7 waves, 6 sessions, 3-4 Sep); portal back to normal feature work]** Gap map `tasks/coach-portal-gap-map.md` fully dispositioned. Ship chain: W1 PM-983 (CC `9da8c820`, cpc v9, cron 65) IA/dashboard/clients/wizard → W2 PM-984 (CC `fc073da7`, site 584) form+habit wizards/banks/T&C/habit notes → W3 PM-985 (CC `76f735d4`, site 585) workout depth/TVS/duration/alternatives → W4 PM-986 (CC `844af645`, site 586) nutrition builders → W5 PM-987 (CC `ef324d3d`, site 587, cpc v10) 10-tab workspace + slim gates → W6 PM-989/989b (CC `adef98a2`, site 588; split-ship, §23.197/198 born) calendar/inbox/content+drip → W7 PM-990 (CC `f8e5aefd`, site `db348511` vbb 589, OTA artifact `41c3308e` deployment `32ab5a88`, migration `pm990_wave7_settings_compliance`, coach-notify v1 + cron 66, cpc v11) Account area + member #47/#48. **PARKED (pull on demand):** #24 circuits; #16 days-library (one-line kind widen); full #46 gate matrix (slim load_calc+exercise_library live); #11 nutrition IA depth; invite/apply-page coach profile card (get_my_coach_profile exists — small job); coach email-notify live-fire verification (path structurally verified, Brevo code identical to proven senders; no email-on coach existed at fixture time); inline video/audio + NEW pill on coach-content.html (PM-989b); `pm985-enrich-exercises` EF deletion; template-edit re-apply behaviour (unchanged: re-save assignments or gate pass propagates). **DEAN CHECKS OWED (one accumulated portal+device pass):** W7 — portal Account tabs (profile save + photo upload + completion bar; notify prefs save; theme/signout/T&C editor still work reparented; vyve_w7dc='1' consent preview), member 589 (e1RM '· Best' on a beaten history, coach photo+about on coach-checkin + coach-content); plus the standing W3-6 lists in changelog. **LEWIS OWES (consolidated; only ① gates anything):** ① Data Consent wording (#49) → flip `W7_DC_VISIBLE` in coach-portal.html, one-line CC commit; ② coach T&C boilerplate review (W2); ③ member coach-content copy (PM-989); ④ W7 profile/notify/consent copy pass; ⑤ prior-wave member strings (gate, check-in, builders). **Permanent exclusions (Dean):** coach payment/invoicing, impersonation, coach self-serve deletion, AI-training-consent patterns.

> **[2026-09-03 · PM-981 FOOD LOG — Dean device pass gates member unlock]** 583 on device (OTA `9170b0da` for store 2.0.1; dev shell direct) → long-press 'Food log — coming soon' title 1.2s to unlock (per-device, member-invisible). Pass list: (1) search 'chicken breast' / 'greek yogurt' / an own-brand item — UK results, no French noise; (2) tap an apostrophe brand (Sainsbury's/Kellogg's) — sheet must open (§23.195 class); (3) log one food by GRAMS and one by SERVING — diary totals sane; (4) scan 5-10 real cupboard barcodes = the true OFF hit-rate sample AND the camera check (ZXing/getUserMedia in the store binary UNVERIFIED — camera dead = permission missing = next-binary item, manual barcode entry is the interim); (5) force a miss → Quick Add prefilled → 'Save to My Foods' → rescan same item resolves instantly/locally. THEN separate commits: (a) UNLOCK — delete cover block + inline style + 4 dev-bypass-aware early-returns + bypass script (cover comment documents removals), restore More-menu Food Log entry + nutrition.html food-log card; member-facing → Lewis sees the page before members, copy pass on food-log strings non-gating; (b) common-foods seed 125→300+ UK staples (single insert, prepared on request); (c) coach nutrition-adherence card goes live-by-consequence — tell Calum once clients can log. PARKED: FatSecret Premier Free application (£0, Lewis 5-min form) = the upgrade path to a licensed UK dataset when revenue justifies; OFF stays until then.

> **[2026-09-03 · NEXT ANDROID BINARY (1.0.11/2.0.2 era) — Play Console optimisation pass (PM-980, non-blocking, Google fix-by Feb 2027)]** (1) Enable R8 minification+shrinking in the Capacitor Android build (AGP 9.0 upgrade per console hint); webview-centric app = low keep-rule risk, but test a release build on-device before submitting — obfuscation currently 2%, Google warns <25% 'may impact visibility and publishing capabilities'. (2) Modernise edge-to-edge (deprecated APIs/params flagged on release 56) — pairs with the existing safe-area work. (3) Drop resizability + orientation restrictions for large-screen devices (tablets/foldables free-resize). Ride the same binary as the Health Connect wiring + LU key-rotation pubkey — one Android build collects all three.

> **[2026-09-03 · BOTH STORES SUBMITTED (PM-974)]** iOS 2.0.1(6) Waiting for Review + Apple expedite requested (auto-release on approval); Android 1.0.10(56) full rollout in review (~24h per 1.0.9 precedent). Bake `a0d24ce1` vbb 581, verified at four checkpoints. RISK WINDOW: both stores serve broken binaries until approval — Lewis to HOLD acquisition pushes ~48h (Dean to flag). POST-APPROVAL SEQUENCE (order is load-bearing): (1) fresh-install smoke on store 2.0.1 = the clamp verification (expect Body 0/30 day one + promoted.skipped_prejoin > 0 in sync response); (2) OTA continuity check — store-installed 2.0.1 pulls+applies a post-581 deployment on the CURRENT key; (3) ONLY THEN LU key rotation, correct sequence: mint key → bake new pubkey into 2.0.2/1.0.11 → submit → switch OTA signing after fleet converges (rotating before the new-pubkey binaries are live strands the fleet — window for riding 2.0.1 closed at archive time, Dean asked, deferred deliberately); (4) DONE PM-974b — fixture sweep executed same night (9 dummies erased, 4,352 rows, verified clean; real signups azu.coco0804@gmail.com + cheskriabin@yahoo.co.uk PRESERVED — probable broken-2.0 victims, recovery email once 2.0.1 live = Lewis copy; cross-check cheskriabin vs the 3f4f26df customer). OTA channel hold LIFTED (PM-979 pushed 582, deployment `7a46c797` — doubles as the step-(2) continuity vehicle; Dean confirms a store-installed 2.0.1 applies 582 on the current key). Small new item: live-update.js shows 'Updating VYVE' overlay on the dev shell (no LiveUpdate config) — skip overlay when config absent. Still queued: store-first customer funnel P0 (spec 3f4f26df — awaiting customer email from Dean), EF CORS sweep, achievements.ts hk_connected inversion, Lewis copy pass on new onboarding strings, HAVEN/Phil unchanged.

> **[2026-09-03 · superseded gate entry — was 581 pre-pass]** (PM-972: rv3 auth root cause fixed; supersedes 580 run)]** Criterion #1 (Dean's design contract): accept Apple Health at rv3 → banner NEVER appears, anywhere, including after force-quit. Then the rest of the 10: intro/loading/rv2/hub videos autoplay (device-proven on 579, must not regress), docked video, centred pages, Home populated, reopen keeps connected state, View health data renders (~month of steps proven server-side for a@a.com — 7,176 on 2 Sep). Clean sweep → bake `a0d24ce1` → Xcode 2.0.1 build 6 / Android 1.0.10 code 56 + REQUEST EXPEDITED REVIEW. Also queued: LU key rotation rides the binary (key transited transcripts repeatedly); _shared/achievements.ts hk_connected query inverted (checks revoked NOT NULL — low stakes); EF CORS sweep; HealthKit backfill milestone product ruling (Dean→Lewis); test-fixture cleanup + comps (test1, test6, a@a.com + tonight's other signups).

> **[2026-09-03 · SHIP GATE — superseded, was 580 (PM-971 supersedes the 579 run; autoplay + Body card already device-confirmed on 579)]** Focus points: (7) land Home with NO banner after rv3 connect; (banner button, if ever seen: single tap → 'Connecting…' → resolves); (9) force-quit+reopen → connected persists; (10) View health data loads (~month of steps for the test account). Clean sweep → bake `148b53cb` → Xcode + expedited review.

> **[2026-09-03 · PM-970 — SHIP GATE RE-RUN (superseded by 580 above)]** Dean's 10-point checklist on **579** (open→10s→close→open, Settings must read 579; long-press logo to reset first-visit flags; fresh account): (1) intro video plays or honest one-tap; (2) Lewis-as-loading-screen; (3) plan-ready button under video ~15s; (4) tap mid-video → docked video keeps playing; (5) rv2/rv3 centred; (6) rv3 health connect clean; (7) land Home, NO health banner; (8) Mind/Body/Connect cards all autoplay muted w/ chip; (9) force-quit+reopen → still connected everywhere; (10) View health data loads. Clean sweep → bake `00fb6100` → Xcode + expedited review. Any fail → number to Claude, nothing ships.

> **[2026-09-03 · PM-969 follow-ons]** (1) **Dean device pass on 578** (two launch cycles): reopen shows Apple Health CONNECTED everywhere (banner gone, settings green) with no re-connect needed; "View your health data" loads; Body hub via nav shows the intro card; hub/rv videos autoplay muted (or honest play button only on genuine failure). (2) **EF CORS sweep NOW CONCRETE (§23.189):** three patients found by symptom — remaining first-party EFs the app calls (notifications, monthly-checkin, workout-library, log-activity, leaderboard, wellbeing-checkin, off-proxy, anthropic-proxy, onboarding, app-signup, certificate-serve, coach/partner EFs) need a one-pass CORS audit before more store-binary surprises; anything allowlisted must include the native origins, anything pinned goes to *. (3) **Product ruling needed (Dean → Lewis):** HealthKit 365-day backfill promotes pre-VYVE workouts into milestone progress (11/30 Body on day one) — intended generosity or inflation? Affects certificates + charity counters. (4) vyve-site `d3e8435b` = accidental no-op commit (documented, not rewritten — §23.190).

> **[2026-09-02 · PM-968 follow-ons]** (1) **Dean retry Apple Health connect** (Settings or home banner) on v25 — confirms CORS fix + the verify_jwt true/false flip is benign; banner should self-resolve. (2) **CORS sweep (§23.189):** grep all first-party EFs the native app calls for pinned Allow-Origin — any pinned to online.vyvehealth.co.uk is store-broken. (3) **verify_jwt drift:** MCP deploys default verify_jwt true — check/restore intended flag after every MCP EF deploy (sync-health-data was false, now true; assessed OK but confirm). (4) Dean device pass on 577: stalled videos show honest play button w/ sound; View-my-plan mid-video docks the playing video atop the reveal.

> **[2026-09-02 · PM-966 REVEAL-FLOW — follow-ons]** (1) **Dean device pass on 575** (force-quit ×2 or fresh install w/ patience): video keeps playing at plan-ready + View-my-plan button, rv2/rv3 centred, hub videos muted-autoplay w/ sound chip, lands on Home, Body hub video appears via nav. (2) DONE PM-967 (design call resolved by field data — retry not block; site `a89daae5` vbb 576 live). **SUPERSEDING P0: BINARY REBUILD iOS 2.0.1(6)/Android 1.0.10(56) — URGENT, checklist with Dean (vyve-binary-rebuild-2.0.1.md), bake ≥ `a89daae5`, request Apple expedited review.** Until approved every fresh install is a first-run coin flip. (3) **Store-first customer funnel (P0 tomorrow — spec widened by Dean's resilience requirement):** design principle = NO device state may lack a visible path to BOTH sign-in AND create-account; the vyve_device_has_account flag may change emphasis, never remove exits (anti trial-cycling is enforced server-side via the 409→sign-in door, so the hidden signup row was only nudging). Build: (a) login.html — signup row shows ALWAYS, flagged devices included; (b) set-password.html — honest post-submit copy ('if an account exists, an email is on its way — nothing after a few minutes? check spam, or you may have joined with a different email') + create-account link; keep the anti-enumeration silence but give every silence an exit ramp (also fixes the reset-email-in-spam infinite-silence mode for real members); (c) signup door = universal recovery surface (email-first routing new→create / existing→sign-in) reachable from everywhere. Root-cause context: customer's phone carried the flag (only set by successful auth — all 4 write sites verified), got 'Welcome back' + hidden signup + funnelled into set-password's silent no-op. Dean still to forward her email address for near-miss account check + reply. (4) Lewis copy pass owed on new strings: 'Your plan is ready', loading subtext, 'View my plan →', 'Tap for sound'.

> **[2026-09-02 · PM-964 ONBOARDING-SUBMIT FIX — follow-ons]** (1) **Dean on-device retest (P0, tonight/next):** fresh install or reuse test1@gmail.com (routes back to onboarding, onboarding_complete=false) → submit should 200 in ~10–15s on EF v123; force-quit+reopen twice first to pull+apply OTA 573 if the session predates it. Clean test1 fixtures after. (2) **Onboarding telemetry (1 short session):** un-`false` the posthog init on onboarding.html (or wire the client-error-report shim) — the page was totally blind through a 16-day flagship outage; pair with a welcome_submit/welcome_error funnel check in PostHog. (3) **Client-path smoke ritual:** after ANY commit touching onboarding.html/signup.html script blocks, one real device or browser end-to-end submit (§23.187) — pg_net green is not enough. (4) **Next store binaries bake `26b982c6`+** (fold into the existing PM-954 on-approval checklist; no emergency resubmit — first-run OTA self-heal proven). (5) **Workbench key hygiene:** confirm/delete lu_key.pem on the Composio sandbox (delete unconfirmed — connection erred); LU signing key has now transited transcripts — cheap rotation available (`apps:liveupdates:generatesigningkey`, new public key rides the next binary), decide alongside the standing service_role/Daily rotation op. (6) DONE PM-965 — resume-sync shipped (was: staleness gap for never-close-app members). (7) **Channel-lag awareness:** parallel sessions landing vyve-site commits after an OTA push leave the channel behind main (573 collected PM-960/961/963) — pushing sessions should check for post-push commits before closing.

> **[SPRINT WINDOW: Thursday-reset coaching sprint — STATUS after PM-956/956b/958 (2 Sep): gate ✅ apply ✅ WORKOUT BUILDER v2 ✅ (exercise library + day templates + programmes + prescription depth + coach video in player) trial-wall routing ✅ workout summaries ✅ check-in bucket ✅ Calum provisioned ✅. REMAINING: Dean device tests (gate walk as deanonbrown12, then build day template → programme → assign → verify in app incl. video); ExerciseDB.io media purchase decision (Dean/Lewis, $299–599 one-off, then ONE ingestion job attaches ~1,400 animated demos to the 876 stock rows via storage-ingest); stretch if usage allows: coach messaging / automations / content library / notifications feed (all honest coming-soon in the portal sidebar). Lewis copy passes on gate + trial emails + builder copy non-gating PM-958e adds: notifications feed ✅ check-in review timeline ✅ bucket photo-only ✅. member weekly check-in ✅ (PM-958f, mockup approved + shipped same day). automations ✅ (PM-958g — plan-change emails, default-on, editable templates). Calum batch ✅ (PM-959 — supplements link/duration, lead forms + public page, stars, calendar v1 external-link calls). habits library picker + step auto-track ✅ (PM-959b). messaging ✅ (PM-960 — coach_messages + RLS, portal view, member page + nudge entry points, push chain trigger→relay→send-push PROVEN live; coach payment tooling ruled OUT permanently same session). automations channel='message' ✅ (PM-960d — coach-provision-client v8, portal Send-as selector; live exercise rides Dean's next assignment save). photo compare ✅ + nutrition-adherence card ✅ (PM-960d, client-detail). TYPED DAILY HABIT FORMS ✅ (PM-961 — mockup approved, input_rule/value schema, apply-RPC v3, builder typed rows, member typed surface w/ weigh-in→weight_logs mirror, coach 7-day values grid; answered-typed-habits-always-complete decision logged). NUTRITION BUILDER V2 ✅ (PM-963, renumbered from 962 — TDEE calc, meal plans, PDF plans via coach-content bucket, apply RPC v4, member plan card). Then: automations v2 (missed check-in/inactivity/birthdays/scheduled sequences — message channel now exists for it), check-in metric charts, client self-booking (booking engine owned, unwired), voice notes (rides messaging), coach content library (last SOON pill — coach-content bucket now exists for it), supplements text/PDF mode. Dean device chain test now spans gate → builders (incl. typed habit rows) → assign (automation fires as in-app message + push) → member typed daily form (weigh-in mirrors to weight log) → check-in → review (values grid, photo compare, nutrition card) → MESSAGING (portal thread ↔ app reply ↔ push). Exercise media SETTLED PM-960c: external purchases (ExerciseDB/RepDB) parked INDEFINITELY — 128 VYVE demo videos are first-class stock rows w/ picker badges; coach uploads fill gaps; free-exercise-db photos remain the zero-cost fallback if the videoless browse ever becomes an objection.]**
> **[2026-09-01 · P0 NEXT SESSION — COACHING FIRST-LOGIN GATE + APPLY PIPELINE (PM-955 follow-on; the one unbuilt door in the coaching platform)]** vyve-site work — vbb triple-bump discipline applies (index.html + settings.html + sw.js CACHE_NAME, monotonic from live HEAD). **(1) Gate:** billing_tier='coaching' members at first app login → accept VYVE terms + their coach's coach_terms (versioned; stamp coach_terms_version/accepted_at) + explicit data-sharing consent (writes consent_accepted_at, flips coach_clients invited→active — this is what arms is_coach_of) + render the coach's assigned onboarding questionnaire (coach_forms; client-read RLS already in place) — bypassing AI onboarding entirely. **(2) Apply pipeline:** materialise coach_clients.assignments → member_habits (assigned_by='coach:<partner_id>'), workout_plan_cache (source='coach' so AI regen never clobbers; expand repeating week), nutrition targets on member row, coach_supplements rows, weekly check-in schedule; must also handle re-assignment pushes for already-active clients (portal saves assignments today; materialisation queued on this). **(3)** Workout-completion summary to coach (Kahunas: difficulty slider + note on finish) surfacing in coach detail view. **(4)** `coach-checkins` storage bucket for photo/video check-in answers. **(5) Trial-wall routing:** coaching-tier day-7 wall → £10/mo Checkout Session (extend partner-join-redirect pattern); stripe-webhook sets partner_memberships subscription_value=10 → £5/mo flows through run_partner_payouts. **Also pending:** provision Calum (auth user + admin_users partner row + set-password link — 2-min job, say go); coach content library on partner_content_items w/ YouTube unlisted (sidebar slot live, PM-955j); calendar wiring (booking engine exists; sidebar slot live); **coach messaging** (portal↔app client threads — sidebar slot live); **coach automations** (triggered emails, e.g. workout-updated → client; sidebar slot live); **notifications feed** (client activity — sidebar slot live); coach profile editing (photo/bio/display name); Simple/PDF programme variants (nice-to-have, later).

> **[2026-09-02 · PM-957 FREE-YEAR OFFER — follow-ons (analysis session, Lewis go/no-go pending)]** (1) **Tighten coach-checkins bucket to image-only MIME types** (drop video/mp4|quicktime|webm) — photo-only is currently client-enforced only; one-line migration makes the cost ceiling storage-layer-enforced. Fold into next migration touch. (2) **Lewis product call:** trial-status members get ZERO re-engagement emails (scheduler filters subscription_status='active') — decide whether a free-year cohort needs an engagement email stream, and if yes whether AI lines stay off (they do by default: persona/goal_focus NULL). (3) **coach-checkins 12-month media retention policy** — media is cumulative; storage doubles yr-2 without it. Worth adopting regardless of the offer. (4) **PostHog cost controls before member scale:** billing cap + event diet (or anonymous-mode for coaching cohort) — the dominant scaling line at 5k (~£440/mo uncapped → ~£30). (5) If offer goes ahead: implement as 365-day trial_end on the existing coaching trial rail (no new build); confirm trial-end-email v2 copy suits a 12-month (not 7-day) horizon before the first cohort's year ends.

> **[2026-09-01 · STORE SUBMISSION NIGHT (PM-954) — follow-ons + closures]** **CLOSED: the recurring "vyve-capacitor git init" P0** — repo existed and was current; canonical checkout is `~/vyve-capacitor-git`, old `~/Projects/vyve-capacitor` retired (keystore artefacts copied out; the keystore+password→1Password co-location item STILL OPEN and now the only survivor of that cluster). **On approval (both stores in review since 1 Sep):** (1) Dean updates his store devices, confirms 564 renders (header included — his last check likely predated propagation; any residual header polish ships via OTA, §23.177 in hand); (2) OTA discipline resumes against the 2.0/1.0.9 cohort. **Parked by Dean, ride the NEXT native release or a quiet session:** (3) Android assetlinks.json — Play Console app-signing SHA-256 page unfindable after console reorg; alternative: pull the signing cert SHA-256 from a Play-installed APK (`keytool -printcert`); (4) iOS Associated Domains entitlement (AASA web-side LIVE + Apple-CDN-verified on all 3 domains — entitlement is the only missing half of password-autofill association); (5) Cloudflare in front of online.vyvehealth.co.uk + purge-on-deploy GitHub Action — cuts Dean's dev loop ~2-15min → ~1-3min (distinct from the existing vyve-site→CF-Pages migration line, either subsumes it); (6) delete the `~/vyve-site-latest` scratch clone after next store build or fold it into the checklist. **Still open from earlier tonight:** PM-943 s3 device check (rate physical 9 → no skip prompt; empty Continue → field errors; activity cards render) + Lewis q-hint copy pass (PM-943 block above).

> **[2026-09-01 · PM-943 ONBOARDING AUDIT DEBT — next onboarding session]** (1) **Quick Start / skipped sections write fake 5/10 baselines** — submit reads every slider regardless; null unseen scores + `path` flag in payload, EF treats missing as missing (touches EF — pair client+server). (2) **Consent race** — health_data_consent PATCH can fire with undefined JWT (boot async) and swallow the failure; also carry consent in submit payload + EF stamp. (3) **Boot-fail dead end** — hidden email field fails validation invisibly when the session boot fails open; surface an error or bounce to login. (4) Sliders min=0 vs "1–10" labels — set min=1 (check EF/score consumers first). (5) DOB caps at 80 with misleading 16+ error copy. (6) Dead payload fields smartwatch/bedtime — remove or add the questions. (7) "Prefer not to say" gender silently disables TDEE — show an explanatory line. (8) Draft-restore rebuilds no sectionHistory (Back → s0). (9) Quick-path progress bar jumps 20%→100%. [LEWIS COPY PASS owed, non-gating: new q-hint slider copy + keep-going recap line, PM-943.]

> **[2026-08-17 · VIDEOS LIVE (PM-937–940) — follow-ons]** (1) **STORE-BUILD CHECKLIST (next resubmission, Dean machine):** mkdir vyve-capacitor www/videos/ + drop the 7 app-videos bucket files named by key (onboarding_intro/onboarding_welcome/hub_mind/hub_body/hub_connect/checkin_intro/monthly_checkin_intro .mp4); from then on every Capawesome OTA zip EXCLUDES videos/ (§23.175). (2) **Offline-signup fail-safe** — Dean flagged, explicitly separate: someone installing with no connectivity can watch bundled videos but the questionnaire SUBMIT needs the network; scope an offline queue/retry for onboarding (draft already survives in vyve_onb_draft — the missing piece is submit-when-connected + honest UI). (3) **give_back.mp4 placement — LEWIS CALL:** parked in bucket; candidates give-back page / employer demos / social. (4) **LEWIS (from filming day):** partner niche-expert map (addiction, suicide prevention, weight loss, female+male fertility, diabetes, sleep, performance nutrition, everyday nutrition), tiered influencer rev-share model (anchor 50% / recruits 40% + 10% override), podcast-alumni outreach (employer-embedded guests first), define employer-referral rev share. (5) Optional UX tweak raised: step-zero path cards → full-questionnaire primary with quick as a subtle skip link (Lewis leaning that way on camera; mockup-first if picked up). (6) **PM-941 test loop LIVE** — Dean to run cycle one (long-press step-zero heading as deanonbrown2; reset = long-press Settings App-version row); account already reset server-side and comp'd, land on step-zero at next login. (7) Optional: mirror the reset long-press onto the trial-ended wall page itself — moot for allowlisted accounts now all comp, only worth doing if non-comp test flows emerge. (8) Standing: any NEW test account → `account_type='comp'` at creation (§23.176).

> **[2026-08-11 · MEET PAGE FOLLOW-UPS (PM-933)]** (1) meet.vyvehealth.co.uk → add as custom domain on the CC Cloudflare Pages project (guest links become meet.vyvehealth.co.uk/?m=..). (2) Delete ct-probe EF (one-shot diagnostic). (3) Dean: re-create the Vanessa client meeting — the original ended before she used the link.

> **[2026-08-11 · INBOUND FOLLOW-UPS (PM-929/931)]** (1) LEWIS: Brevo → Security → disable Authorised-IPs restriction so inbound-digest's direct Brevo email works (bells already live); alternatively read a send-email caller EF's source to learn its internal auth and route through it. (2) LEWIS: told about the contact-form void — any expected-but-missing enterprise enquiries are explained. (3) Mirror crisis + manual-onboard alert emails into cc_inbound as records (emails STAY immediate — never digest-gated). (4) Careers JOBS array is hardcoded in careers.html — optional later: move roles to a table so Lewis edits without a repo push. (5) Consider intake for partner applications currently outside the wizard.

> **[2026-08-10 · MEETINGS — SHIPPED COMPLETE as PM-915/915b/916 (same night). SURVIVING OPEN ITEMS:]** (1) Daily API key rotation at desktop (key in two chat transcripts, carded account) — mint new key in Daily dashboard → UPDATE vault DAILY_API_KEY → delete old (joins the service_role rotation op). (2) Register (Lewis): Daily = new sub-processor (US, SCCs via Daily DPA) + ADD HETZNER (hosts live runner + meeting audio transit). (3) Lewis copy pass: guest-page transcription notice (non-gating). (4) Optional: meet.vyvehealth.co.uk vanity guest domain; reportError() wire on the meetings CC page; per-participant track capture → speaker-labelled transcripts; 'Ask Claude about this meeting' EF; 'Regenerate summary' button (EF already accepts admin JWTs). AI summaries SHIPPED PM-917. Original scope for reference below.
> **[2026-08-10 · MEETINGS SESSIONS 2–3 (PM-915 V1 shipped — video live pending DAILY_API_KEY)]** (1) **Bot capture + transcription worker on the Hetzner box (~1 session):** serve-transcribe-installer EF (one Dean paste, vyve-agent precedent) installs venv + daily-python + faster-whisper and RUNS THE BENCHMARK (small int8, RTF on the real 2 vCPU) before enabling; worker watches cc_meetings for live meetings → daily-python bot joins as silent participant → WAV on disk → meeting end → claims cc_transcription_jobs via UPDATE…SKIP LOCKED → whisper nice-19 → transcript to cc_meetings.transcript → audio deleted → systemd Restart=always + capture heartbeat; crash mid-meeting = transcript_status 'incomplete', honest never silent. (2) **Hardening (~0.5):** attempts/retry, reportError() wire on the meetings page, Daily webhook as belt-and-braces meeting-end signal. (3) Optional: meet.vyvehealth.co.uk vanity guest domain (guest links currently on the functions/v1 URL — works, ugly). (4) **Register (Lewis):** Daily = new sub-processor line (US, SCCs via Daily DPA) + ADD HETZNER (absent today, hosts the live runner + soon meeting audio). (5) Later: per-participant track capture → speaker-labelled transcripts; 'Ask Claude about this meeting' EF over stored transcripts; per-meeting transcript ACL if ever needed.

> **[2026-08-10 · EXTEND THE FAILURE BEACON (PM-902 follow-ons)] — (1) IS NOW NEXT-SESSION PRIORITY after PM-903: April's 429 wall was invisible to us for an hour.** (1) Wire `reportError()` into the WIZARD (Test-Site partner-onboarding.html) upload paths — the pre-auth funnel is where Vanessa's cover actually died and it's still unreported; the EF is already verify_jwt:false for exactly this. (2) Wire it into the member app (vyve-site) for failed logs/uploads. (3) CC surface for `client_errors` (list + mark-resolved) — today it's email/push + SQL only. (4) Consider TUS resumable uploads for partner video (Dean's 800MB single-shot PUT succeeded 10 Aug, but there's no resume — a partner on mobile data has no recovery). ~1 session each.

> **[2026-08-10 · WIZARD UPLOAD ERRORS MUST BE HONEST (PM-893, Vanessa's cover image)]** A storage 413 (file over the bucket cap) leaves the wizard recording client-side metadata with path:null and no clear user message — the partner thinks it uploaded then finds it "disappeared". Surface the storage error at upload time: "file too large — max 30MB for images" (+ mime rejections similarly). partner-docs cap now 30MiB. Check both wizard upload paths (docs slots + starter video) and the portal upload for the same class. ~0.5 session, rides the next wizard touch.

> **[2026-08-10 · MEMBER DISCOUNTS FOLLOW-ONS (PM-893 shipped)]** (1) **Seed the Newcastle businesses** — zero build: one INSERT per business into `member_perks` (business_name, headline e.g. "20% off food", redemption_type code|show_app|link, code/instructions, optional logo → perk-logos bucket, website). Awaiting Dean's list. (2) **Lewis copy pass** on perks.html member-facing copy incl. the third-party-offers disclaimer (shipped placeholder-honest, non-gating). (3) ~~CC perks admin surface~~ **DONE PM-975** — pages/perks.html: full CRUD + logo upload (perk-logos bucket now active) + the per-business 30d usage line. (4) Later: per-member unique codes if a business wants true redemption verification. (5) **DONE PM-1022:** perks are deletable from inside the CC Edit modal, behind a confirm that states the real cascade cost (`perk_events.perk_id` is ON DELETE CASCADE) and offers Hide as the non-destructive exit. (6) **DONE PM-1023 (vyve-site `4fe2133b`, vbb 590):** perks.html stated no visibility predicate and leaned on RLS alone — and RLS policies OR, so every staff account matched `perks_admin_all` and saw the unfiltered catalogue in the member app (members never affected; see §23.214). `perkVisible()` now mirrors `perks_member_read` on both the fetch and the localStorage paint. **Worth a sweep: any other member-facing read that relies on RLS as its display filter has the same staff-side blindness.**

> **[2026-09-03 · iOS 2.0.1 APPROVED — POST-APPROVAL CHECKLIST + ANDROID SWEEP]** (1) Fresh-signup clamp check on clean iOS install (v26 behavioural). (2) Victim recovery email — Lewis copy (azu.coco0804 / cheskriabin). (3) Acquisition-push hold lifts only when Android 1.0.10 ALSO approves. (4) LU key rotation after fleet convergence (PM-974 sequence). (5) **Android walkthrough sweep (Dean now has a device):** fresh install first-run OTA race behaviour, full signup→onboarding→reveal flow, video autoplay under Chromium (the PM-966/969/970 watchdog/source-probe work was iOS-verified only), FCM push receipt (register + a real broadcast/coach push), deep links (vyve:// + https intent-filter), safe-area rendering, and — the known intentional gap — what rv3's Connect step shows on a device with NO HealthKit: it should degrade gracefully, not dead-end. (6) Android Health Connect parity: UNPARKED (device now exists) — scope after the walkthrough sweep.

> **[2026-09-03 · WATCHTOWER FOLLOW-ONS (PM-977 shipped)]** (1) **Phase 2 — CC Capacitor wrap** (existing backlog item, now with a concrete payoff: native push on the business app itself; today alerts ride the member-app channel, which works but means Dean/Lewis get platform alerts inside VYVE-the-member-app). (2) **Phase 3 — VYVE Partner app** (Dean direction, working assumption: SEPARATE app, not in-member-app; thin Capacitor shell over partner-portal/coach-portal + ambassador kit + discount-partner stats; revisit when partner count justifies). (3) Watchtower target list is inline in the EF by design — adding a surface = one MCP redeploy; move to a table only if the list starts churning. (4) Optional: probe latency trend chart on Platform Health once a few weeks of data exist.

> **[2026-09-03 · AMBASSADORS FOLLOW-ONS (PM-975 shipped)]** (1) **Lewis: ambassador agreement wording** (owed since PM-868) — the CC go-live tap assumes a signed agreement exists offline. (2) **Lewis: chase agreements for the 4 pre-gate live ambassadors** (antonia/april/david/ivan — flagged in-UI “no signed agreement on record”). (3) Lewis copy pass on both new CC pages’ member-adjacent strings (share text esp.) — non-gating. (4) ~~human_promo_code wiring~~ SHIPPED PM-995 — validate_code accepts code OR slug (app-signup v8), all 17 partners backfilled (ALAN10 etc.); ambassador spoken codes are now the human codes, slugs still work.

> **[2026-08-11 · CC → PHONE APP BY END OF WEEK (Dean, PM-920/923) — AUDIT DONE, wrap gate = playbooks/cc-mobile-audit.md checklist]** Systemic fixes SHIPPED PM-920/921/923 (topnav, checkboxes, all tables self-scroll, .tab-bar swipes, tasks board snap-scrolls, media clamp). REMAINING before wrap: (1) Phase 2 partners port SHIPPED PM-924 — Dean QA checklist in the changelog gates it (rollback = one sidebar line); (2) Phase 3 admin-console port + mobile pass (5 tables); (3) partner-portal OUT of wrap scope (PM-925 Dean decision — long-term home is the customer-facing app, one-identity model makes it additive; CC keeps sidebar link + Preview for viewing; migration sequenced post-wrap, alongside/after bookings-through-VYVE); (4) calendar.html mobile month view (agenda list ≤640); (5) chart canvas device sweep; (6) Dean full-sidebar iPhone walk logging against the playbook. THEN Capacitor wrap pointing at admin.vyvehealth.co.uk (no sw/vbb ritual; reuse vyve-capacitor patterns — note vyve-capacitor git-init debt). Conventions for all new/ported pages are §Conventions in the playbook.

> **[2026-08-08 · CC STREAMLINE — PHASES 2+3 (Dean-approved sequence, PM-883)]** Phase 2: port partners.html (152KB, most-touched CC surface) into the SPA as Partners-domain pages under the new rail (~1–2 sessions; own auth bootstrap + helpers to dissolve). Phase 3: port admin-console.html into the Members domain (~1 session). Then analytics consolidation with Dean's page-by-page review now data flows again. Riding items: IIFE-wrap usage.js + retention.js (§23.169 latent re-entry bug); alert hygiene (info severity never counts toward Needs Attention, self-healed alerts auto-resolve at write); SPA login magic-link → password to match every other surface (identity model PM-859).

> **[2026-08-08 · ACCESS-MODEL SESSION (PM-884 remaining holes — close BEFORE any invite outside the inner circle)]** (1) admin-member-* EFs (habits/programme/weekly-goals/programme-library) verify admin_users.active ROLE-BLIND — a team-role user can hit member-management server-side; add role='admin' (or explicit admin|team decision) to their in-code check. (2) members-table is_admin_or_team READ policy (PM-796, powers Active Users) lets team query member rows via PostgREST regardless of page acl — Dean+Lewis decision: keep (team sees member activity) or narrow to a purpose-built view. (3) Vicki has no admin_users row yet — invite = role='team' insert once holes closed. (4) Brevo authorised-IPs security setting: Lewis to disable (Supabase egress IPs rotate; already 401'd one GDPR confirmation email 4 Jul).

> **[2026-08-07 · GO-LIVE GATE vs DEFERRED COHORT (PM-879 finding — WILL BLOCK the first real go-live)]** CC `advancePartner('live')` gates on safeguarding_passed && gdpr_passed && pct_complete=100; the PM-659/857 deferred-assessment cohort can never satisfy it. Blocked on the outstanding Lewis safeguarding/duty-of-care decision (deferred-at-go-live + visible "assessments deferred" stamp in CC is the standing rec). GATE REWRITTEN PM-880 (allow + explicit confirm + permanent stamp, per the PM-857 rec) — remaining: the Lewis posture decision itself; if he declines deferred-at-go-live, revert the deferred branch and unwind any stamped partners. Also at payout-run build time: unify portal MRR (currently partner_memberships.subscription_value) with the members-table RPC so earnings have ONE source.

> **[2026-08-07 · PARTNER PAYOUT RUN + BOOKINGS-THROUGH-VYVE (Dean decisions, PM-876)]** (1) **Finance payout run (~1 session):** monthly statement per partner (line items: attributed paying members × share; paid bookings at 100% minus Stripe fees), self-billing invoice generated by VYVE on the partner's behalf (needs the agreement clause — add to Lewis's owed wording; confirm VAT status per partner), payment-date field, rollover threshold ~£25, mark-paid flow already in CC. Bank details capture SHIPPED PM-876 (partner_bank_details, portal card, CC readiness line). Finance director executes transfers. (2) **Bookings payments through VYVE (~2 sessions, sequence AFTER attribution Piece 2 — reuses its EF-minted Checkout Session machinery):** replace the partner-payment-link step with a VYVE Stripe Checkout Session (booking + partner in metadata), webhook marks booking paid/confirmed, refund path driven by the per-service cancellation-notice hours, ledger rows accrue to the partner at 100% minus Stripe fees (commission % = later commercial call, Lewis). Retire the bsf-paylink field once live.

> **[2026-08-07 · vyve-site → CLOUDFLARE PAGES MIGRATION (Dean call, ~0.5 session)]** Move online.vyvehealth.co.uk off GitHub Pages onto Cloudflare Pages (same repo, same main-branch deploys). Motive: GitHub Pages build issues on 6 Aug + multi-minute build latency on the host the member app loads from, vs CC's sub-minute Cloudflare deploys. Scope: CF Pages project on vyve-site, custom-domain swap for online.vyvehealth.co.uk (DNS already Cloudflare), verify 404 behaviour / headers / sw.js serving parity, then retire the GitHub Pages config. NOTE for the session: dev-loop and member OTA are unaffected (they load by URL, not by host) but §23.x cache rules re-verify against CF's edge cache — may need a cache rule for sw.js.

> **[PM-843 · 2026-08-06 · PARTNER FUNNEL P0 — SHIPPED as PM-844/845/846 (same day)]** Items 1–3 all live: wizard re-sequenced 5 steps + optional video + on-screen resume card (`9a3af440`); stalled-draft 48h/7d nudges + onboarding/declined stage emails in partner-draft-purge v2 (live-proven); pre-filled invite links via partner-onboarding v13 `mint_invite` + CC "Mint & send invite link" (`4700aab2`). Invite→submit ≥80% metric now trackable via `_system.invited` cohort. **SURVIVING OPEN ITEMS:** **DECISIONS OWED (Dean+Lewis, block nothing):** (a) photo-ID policy — wizard requires a passport under "VYVE verifies your identity" but NO verification process exists and submit auto-stamps identity_verified:true (GDPR minimisation; PM-634 recorded ID as removed, live wizard disagrees) — either an explicit admin "ID checked" pipeline action feeds the flag, or collection moves to contract stage; (b) quiz coherence — 3-question safeguarding/GDPR gate quizzes on hidden training modules — restore modules (Lewis/Phil content) or reframe as declaration. **Lewis copy pass owed (non-gating):** 48h/7d nudge emails, onboarding/declined stage emails, Lewis-voiced invite email. **NOW, zero build (updated PM-855):** Vanessa RESET to 'applied' (Dean's onboarding flip sent nothing — provision never fired). Lewis sends her resume link ?resume=13f61ee9-a991-4a2d-8b34-a096c914cb5a with 'video now optional, ~5 min left' — she submits (signed-agreement copy now emails+files automatically, PM-855) → Dean approves in CC → provision emails her login. **Lewis copy pass list grows:** agreement-copy wrapper email (PM-855) joins the 48h/7d nudges, stage emails, and invite email. **PM-867 PARTNER ATTRIBUTION + IN-APP ONBOARDING — SPEC LOCKED, BUILD NOT STARTED.** Full spec: `playbooks/partner-attribution-and-onboarding-spec.md` (v2). Pieces: **(3) SHIPPED PM-869** (`join.html` account creation, hard-stop on invalid slug, 404 rewrite, app-signup v4 account_state/attribution columns/web_join confirmation email, re-engagement v14 shell routing); **(4) SHIPPED PM-870** (three-state email-first door live, vbb 534, employer mode preserved); remaining order 1→2→5 — **(1) HALF-SHIPPED PM-874** (portal card: derived link + QR/PNG + funnel counts via partner_referral_stats; left: joins-by-source split only (human promo codes + backfill + validate_code code-or-slug + share text SHIPPED PM-995), PII call SETTLED PM-875 = nudge-on-behalf shipped, Lewis nudge-email copy pass gates Men Together provisioning): **(1)** ~~human_promo_code generation + backfill + validate_code code-or-slug + share card code display~~ SHIPPED PM-995 (17/17 backfilled, app-signup v8, live-verified 6/6); ~~joins-count-by-source split~~ CLOSED PM-996 (RPC by_link/by_code/by_admin + portal line); **(2)** ~~continue.html → EF-minted Checkout Session~~ SHIPPED PM-996 (checkout-session v1.2, all price paths live-verified, link fallback kept); **(5)** ~~SHIPPED PM-996~~ (admin-console card + admin-member-edit v7, attribution_admin stamp, converted-lock) — was: manual attribution grant in CC only — set/clear a member's partner pre-conversion, `attribution_source='admin'` + approving admin, blocked once converted, help-area line → team@vyvehealth.co.uk (PM-868: the member-facing picker was WITHDRAWN — a self-service route teaches partners the link doesn't matter) ~0.5s; **(6)** autofill association (AASA webcredentials + Associated Domains entitlement + assetlinks.json) — RIDES THE NEXT NATIVE RELEASE, cannot go via Capawesome; ship `autocomplete` semantics on join page + app sign-in immediately regardless. Settled: 7-day trial for everyone, no PIN, identical price on every route, NO per-partner rates for now (£10 universal), NO self-declared route, partner bio/why/feel limits 60/400/200/160. **Lewis still owes: agreement wording (rev share on attributed members; attribution via link or code; anything else a manual exception at VYVE's discretion) + partner-portal copy line telling partners the link is how they get paid.**

**PM-859 CARRY-OVER (2026-08-07 night) — do these before a real partner is provisioned:**
- **Wizard photo handling at source (~0.5 session):** the apply wizard still uploads profile/cover photos to the PRIVATE partner-docs bucket as bare paths, so the next partner's photos will again show as "Click to upload" until manually published, AND the wizard has no cropper — they arrive centre-cropped. Publish to `partner-thumbnails` at submit + port the PM-860 cropper into the wizard's Build your space step.
- **Delete the one-shot EF `pm859-publish-partner-photos`** (§23.154 — token-gated, already run; no delete tool in the MCP surface, so do it from the Supabase dashboard).
- **`partners.vyvehealth.co.uk`:** add the Cloudflare custom domain to the CC Pages project, then flip `PORTAL_URL` → `PORTAL_BRAND_URL` in partner-provision (one line) so partner emails stop pointing at an `admin.` URL. Portal file should then live at that root rather than as a sub-path.
- **Vanessa Sturman was never provisioned** despite the 6 Aug audit row saying so (PM-859 bug 1) — nothing was sent to her. Lewis should know before he follows up.
- **`deanonbrown2+b2binvite@gmail.com` password was silently reset twice** by the old provisioning — Dean to reset it if he still uses it for B2B testing.
- **Partner member rows now carry `account_type='partner'`** — check whether any dashboard counts members without filtering account_type before partners start inflating the member count.
- **In-app door to the partner portal (later, Dean's steer):** app shows "Your partner space" for accounts with the partner role, opening the portal — NOT a second implementation of moderation/scheduling/payouts, which are desktop-shaped.

**WIZARD SIDE DONE (PM-856, `85b500c3`) + CC review honesty DONE (PM-857, `d52b499d`).** REMAINING = portal receiving end (~1 session, scope grew with the agreed go-live model): (1) post-approval checklist card in partner-portal.html (record welcome video → polish space → schedule launch); (2) dedicated welcome-video upload skipping PM-684's 48h publish_at rule (welcome video live at approval); (3) **admin Go Live from CC partner detail** gated on portal minimums met (welcome video uploaded + profile complete), deferred assessments allowed — this is the PM-659 HELD-migration posture, **Lewis nod owed** (wizard landing publicly claims safeguarding review; rec: allow + visible "assessments deferred" stamp on the partner record). SEQUENCE WITH Gate-A provisioning + contact_email backfill (0 portal logins ever provisioned, 7/9 missing emails — without it the portal promise is empty). Upload machinery itself already exists (partner-content-upload v6, JWT, in_review moderation).

> **[PM-836 · 2026-08-06 · POST-INCIDENT]** (1) **Runner fail-fast patch** (~0.5 session, via agent/SSH): refresh loop currently swallows fetch/heartbeat failures silently — the 23:12 5 Aug 17h blindness. Log every failure; sys.exit after N consecutive (systemd Restart=always turns a wedge into a 10s blip). (2) **service_role key rotation** (PM-714 carried, now doubly warranted — key in a chat transcript): planned op touching runner creds (location TBD — not under /srv/vyve), /srv/vyve/agent/env, and any other embeds. (3) Calendar-runway watchdog (PM-833) still open — next cliff 30 Sep. (4) PM-834 missing-file thread CLOSED (all 91 filenames on box). (5) CORS-pin EF sweep (PM-834) still open.

> **[PM-835 · 2026-08-06 · AGENT INSTALL + AFTERMATH]** (1) Dean-at-Mac: run vyve-agent-install.sh on the box (restores tonight's sessions; runner hardened; Claude gets remote hands). Capture journalctl of the 23:12 UTC runner death FIRST — empty-schedule crash ⇒ Claude patches a guard into the runner. (2) Claude post-install: remote check-cycle via runner_commands + ls_masters verdict on the 9 never-box-aired mp4s (PM-834 thread — now unverified secondary risk, not the cause). (3) See NEW playbooks/runner-agent.md for the drive-the-box mechanics + incident decision tree.

> **[PM-834 · 2026-08-06 · OPEN THREADS]** (1) **Dean SSH on the live box** (before 7 Aug 08:30 BST): journalctl since 12:55 + masters ls for the 9 never-box-aired filenames (list in PM-834 changelog); rsync missing mp4s from Mac ~/vyve-live media, or Claude swaps affected rows to box-proven titles. (2) **CORS-pin sweep across EFs**: grep deployed EF sources for pinned Access-Control-Allow-Origin values — any pin breaks the bundled native fleet (origin capacitor://localhost); mark-seen was the one found, siblings may lurk (~0.5 session, pairs with the EF-source-backup snapshots). (3) Optional client hardening later: cap replayUnseen bursts (e.g. 3 newest + mark_all the rest) — not needed while backlog is zero.

> **[PM-833 · 2026-08-06 · CALENDAR-RUNWAY WATCHDOG — NEW, ~0.5 session]** The live calendar ran dry 3–6 Aug and nothing alerted (broadcast-watchdog covers dead broadcasts, not an empty schedule). Build: small cron EF (daily, platform_alerts + team@ email pattern) that fires when future calendar_occurrences coverage < 7 days. Next cliff: current schedule ends 30 Sep — regen or watchdog must land before then. Session-regen rules now banked as §23.156–158 (trigger disable pattern, bounded pool sweep, DELETE…RETURNING repairs).

> **[PM-820 · 2026-07-23 · S1 SHIPPED — carried items]** (1) **Capawesome OTA publish of vbb 523 to the member fleet — DEAN CALL** (Dean's iPhone sees it now via server.url; members bundled until OTA; new-signup path reachable only via login.html link, existing members unaffected pre-OTA). (2) **[LEWIS COPY PASS]** on everything member-facing shipped placeholder-honest: signup screen, onboarding step-zero consent + reveal/tour, trial-ended screen, trial-end email (incl. partner-framed variant). (3) **Videos — DONE PM-939 (17 Aug): all slots (incl. NEW weekly+monthly check-in) filmed by Lewis, ingested, url'd, LIVE.** (4) Optional: trial-end-email copy branch for the legacy '14day' cohort (currently says "free week"). (5) welcome.html slim to pay-and-password — deferred BY DESIGN until app completion rate proves out. (6) S3 scope reminder: returning-trialist checkout ATTACH (webhook/reconcile branch, email join key) + day-6 trial arc upgrade of the v1 email. (7) First app-signup cohort watch: app-signup EF logs + trial-end-email daily counts are the funnel telemetry until PostHog events are added (no analytics on signup/onboarding pages v1 — marketing PostHog deliberately neutralised in-app).
> **[PM-819 · 2026-07-23 · APP-FIRST FUNNEL + B2B — S1 SHIPPED as PM-820, S2 SHIPPED as PM-828/PM-829 (24 Jul: employers registry + provision-first invites + portal Members tab + removal sweep, all live-verified; Sage row seeded, invites gated on Lewis copy pass of the invite + £20 continue emails). S3 money+wizard SHIPPED as PM-998 (4 Sep: b2b-billing-run v2 + cron 68 arrears billing w/ §23.199 cron-order rule, employer-provision v4 create_employer, CC Organisations table + New Employer wizard + invoicing B2B send/run surfaces, CC `80412591`). S3 CLOSED as PM-999 (4 Sep: day-6 arc trial-end-email v3 + returning-trialist attach found already shipped in stripe-webhook); SSO/SAML optional add-on remains (~1 session, Pro plan incl. 50 SSO MAU — Sage-deck line)]** One programme, three sessions, locked order. **S1 (~1.5): APP-FIRST ONBOARDING + APPLE DE-STEER** — in-app signup; questionnaire moves INSIDE the app (new vyve-site onboarding page, JWT → same onboarding EF, `onboarding_complete` flag + first-login routing, resumable/retry-safe; existing members backfilled true; web welcome.html slimmed to pay-and-password; web form soft-killed only after app completion rate proves out); 7-day trial, **clock starts at onboarding complete** (freemium REJECTED — decision recorded); video slots ship DARK (questionnaire step-zero Lewis intro + first-visit cards on mind/exercise/connect, Dexie per-hub flags, storage-bucket hosted, no OTA to swap); **HARD GATE: de-steer the live in-app trial-end→Stripe surface in this session, BEFORE any binary submission** (in-app signup hands reviewers a fresh trial account; current cover = Dean's non-trial demo login only). Compliant loop: trial-end screen says check-your-email only; email carries Stripe link + coupon; returning-trialist checkout attaches to EXISTING member (webhook/reconcile branch, email join key). **S2 (~1): B2B DOOR** — `employers` registry (name-keyed joins preserved; seat_cap, price_per_seat_pence, billing cycle, invite token); `welcome.html?c=TOKEN` → set-password, no payment, no trial, b2b_active, seat-cap enforced; employer portal **Members tab** (roster name/email/joined/status, Brevo invites + copyable link, Remove = period-end → b2b_removed → B2C-continue email); PII principle: employer sees WHO is enrolled, never what anyone does. **S3 (~1): MONEY + WIZARD** — CC New Employer wizard on pages/employers.html (registry row + employer-provision admin login + token + admin welcome); billing cron 1st-of-month snapshots active B2B seats → structured invoice (cc_invoices + employer_name/period/seat_count/unit_price) → CC send/Mark-paid; full trial-end/reactivation day-6 arc on re-engagement rails. **DEAN CONFIRMED (23 Jul, executive decision) + [LEWIS REVIEW] before first employer contract (commercial calls, S2/S3 scope — none block S1):** arrears-on-actuals billing · roster-identity-visible/health-never · period-end removal · invite link+email w/ seat-cap guard. **LEWIS DOC OWED (Claude drafts next session):** trial mechanics, coupon + conversion email framing, 4 video scripts (Dean dictated ~80% of hub scripts — in changelog PM-819), copy pile (invite / admin welcome / removal / invoice / trial-arc emails, slimmed web page). Content ask Lewis/Calum: film 4 videos (~60s intro + 3×30–45s hubs); build never waits on them.

> **[PM-818 · 2026-07-19 · PARTNER LIVE STREAMING — VYVE-NATIVE, DESIGNED, ~5–6 sessions, NOT started]** Dean: partners go live from the partner area straight into the app, and NOT via YouTube — "manage this ourselves". Locked architecture: partner browser WebRTC/WHIP publish → MediaMTX (free OSS, new systemd service on the existing Hetzner box 159.69.95.90) → HLS out → VYVE-branded hls.js player in the app; MediaMTX records → EF pipeline → mp4 to storage (recommend Cloudflare R2, free egress) + replay row. Zero new recurring cost; LiveKit Cloud = fallback only (per-minute fees, Dean re-consult first). Member side mostly exists (live pages / chat / session_live_views / replay surfaces). Build shape: ingest + orchestration EFs, portal Go Live tab, branded player, recording→replay pipeline, TLS + monitoring on the box. HARD REQUIREMENTS baked into scope: (a) monitoring day-one — we become the broadcaster (PM-658 four-day silent outage is the precedent); watchdog-pattern extension + player status probe; (b) cron 50 / runner coexistence with partner-originated streams (touches outage detection — test properly); (c) MODERATION GAP: live bypasses in_review — Gate-B partners only + partner-agreement conduct clause + admin kill-switch EF; LEWIS must see this framing before any partner gets the button. Existing daily simulated-live stays on YouTube; platform-wide migration off YouTube = separate later decision after this runs clean. Sequencing note: pairs naturally with PM-816 partner-area-in-app port (Go Live tab should land wherever that port puts the partner surface).]**

> **[PM-816 · 2026-07-14 · PARTNER AREA IN THE MEMBER APP — DESIGNED, ~2–2.5 sessions, gated on Gate-A provisioning]** Dean: make the partner area fully manageable as an app. Locked approach = ladder, NOT a second App Store listing: **(1) Partner push** (~0.5–1) — booking landed / payment to confirm / employer request confirmed / content moderated, fired into the existing APNs + scheduled_pushes spine against the partner's member identity (early partners are members). **(2) Partner mode in the member app** (~1–1.5) — PORT partner-portal.html onto vyve-site (same origin ⇒ the app's existing Supabase session just works, no second login), swap CC shell for theme.css/nav.js, entry = a "Partner" row gated on get_my_partner_id() resolving. Dean corrected sizing down from 4–6 (that assumed a native rebuild; it's a port). This pattern is the CC-v2 Team App precedent. **(3) Standalone branded partner app** — packaging exercise later, only if partner recruitment demands it (30+ partners). **PRECONDITION: Gate-A provisioning + contact-email gap (3/5 visible partners) — otherwise an app area with two eligible users.**

> **[PM-811 · 2026-07-14 · BOOKING SYSTEM COMPLETE — B1+B2+B3+B4 ALL SHIPPED (PM-812/813/814/815 same evening), member loop device-verified; employer loop VERIFIED 14 Jul late (Dean confirmed Emma Clarke request — both emails + .ics received, diary + portal reflect). Remaining crumbs: delete pm814-commit-runner one-shot EF (dashboard/CLI — no MCP delete), page-docs (§11B) help.md/partner-profile.md/partner-space.md, Gates before member promotion: Lewis copy ~14 email variants (complaints+bookings, all [LEWIS COPY PASS]), Phil therapy category, Lewis/Alan launch-partner list.]** **v1.1 AMENDED post-close (Dean):** (i) experts deliver online OR in person — `delivery_mode` on service + venue/address fields, confirmation carries link or address+maps; (ii) video ownership ladder reframed — Dean wants sessions under VYVE's roof 'not Google Meet or Teams': v2 embedded rooms (Daily/Whereby/LiveKit Cloud, fully VYVE-branded in-page — this ACHIEVES the ownership goal, member never sees a provider) → v3 self-host LiveKit (open source, same client code, migrate only if per-minute fees beat ops cost). Building a Teams-alike from scratch stays off the table; recorded hosting already owned via YouTube plumbing. Design doc delivered to Dean (`vyve-booking-system-design-2026-07-14.md`). One engine, two doors: member 1:1s with partner experts (pay on partner's OWN Stripe link — partners keep 100%, `booking_share_pct` dormant at 100; 30-min pending_payment hold; external browser per no-IAP rule; expert-supplied meeting link v1, Daily/Whereby embed v2, NEVER build own video) + employer expert-booking (request-based v1: portal tab → CC queue → confirm+invoice). Tables: booking_services/availability/exceptions/bookings/booking_events. Therapy category dark behind `requires_clinical_signoff` until Phil. Refund wrinkle: booking-dispute refunds are VYVE-instructed, PARTNER-executed (they hold the money) — partner-agreement line needed. ~4.5 sessions, order B1 engine → B3 employer (Sage line) → B2 member → B4 polish. GATES before build: (1) Dean confirms employer-v1 shape, (2) Lewis ×6 email copies + agreement line, (3) Phil therapy pathway (blocks category only), (4) Lewis/Alan: which partners offer 1:1s at launch — empty booking shelf = freshness failure mode again.

> **[PM-803–809 · 2026-07-14 · FRESHNESS + COMPLAINTS FOLLOW-ONS]** (a) **Lewis/Calum content asks:** breathwork `mind_videos` rows (page runs on fallback only — 3–4 rows makes it real); monthly Studio ritual proposed in freshness doc (theme now self-rotates PM-803; podcast/mind catalogues still manual). (b) **Lewis:** `checkin_questions` rails still 0 rows — sliders-mirror-onboarding remains open, NOT done. (c) **Lewis copy pass** on complaint emails (submit-complaint ack ×2 variants + team notify, resolve-complaint ×4 resolution types) — gates member promotion of Help & Support. (d) **Dean:** dead streams (Workouts/Therapy/Events/Podcast — 0 replays, 0 occurrences) deliberately LEFT per Dean, content coming; revisit if still empty in 2–3 weeks. (e) **Dean device checks open:** help.html restyle (PM-808, post-cache), featured partner flag (Lewis's face call), employer Comms tab, carousel taste. (f) 'New partner'[contract] row: Dean to name or kill.

> **[PM-810 · 2026-07-14 · COMPLAINT REPLY-INGEST — BACKLOGGED, pair with email tenant migration (finding U)]** Replies to complaint resolution emails currently land in the team@ inbox (by design — sender is team@; Dean device-verified and expected them in CC). Build when picked up (~1 session + DNS): dedicated reply address on a subdomain (e.g. reply.vyvehealth.co.uk), MX → Brevo inbound parsing, webhook EF matches complaint id (Reply-To plus-addressing or embedded token), appends to a complaint_messages thread, flips status resolved→open, CC complaints detail grows a thread view. DO NOT build on the current consumer-grade inbox — sequence it with/after the managed-tenant migration flagged pre-Sage, or the DNS work gets done twice. Until then: team inbox is the accepted v1 reply path.

> **[PM-797 · 2026-07-13 · ALAN CATCH-UP 12 JUL — CLOSED at PM-811 (14 Jul eve): 1 SHIPPED (PM-798) · 2 SHIPPED (PM-806/807, device-verified — [LEWIS COPY PASS] gate on emails before member promotion) · 3 SHIPPED (PM-800) · 4 DONE (doc) · 5 = LEWIS · 6 SHIPPED (PM-799, nobody flagged) · 7 DESIGNED (PM-811 booking doc w/ Dean — real bookings not save/remind; build gated on Dean employer-v1 confirm + Lewis copy + Phil therapy gate + launch-partner list) · 8 DONE (PM-803/804 fixes + doc) · 9 DONE (doc) · 10 SHIPPED (PM-801, taste pending). Hygiene sweep DONE PM-805 (3 test rows deleted; placeholders hidden; 'New partner'[contract] still needs Dean name-or-kill).]** From Alan's writeup of his catch-up with Lewis. Triage vs live state: **(1) Partner ratings — NEXT UP (build opened this session, ~1 session).** Real feature behind the column PM-762 display-killed: member star ratings on partner profile/content, VYVE+partner visible from rating #1, member-facing publish gated at N ratings (configurable, default 20). `partner_ratings` table + threshold trigger onto `partner_partners.rating`/`rating_count` + partner-profile.html capture/display + CC partners detail surface. **(2) Complaint automation loop** (~1–2 sessions): complaints table + EF workflow — complaint → auto partner notify → resolution menu (refund/apology/free session) → automated comms; escalation-only human touch. v1 links occurrence/timestamp, no clip splicing. **(3) Employer portal "Comms & engagement" tab** (~1 session): record + asset store of everything pushed to a corporate's internal comms (Workplace etc.); v1 manual post log + copy/embed assets, automation later. Slots into PM-789–791 portal. **(4) Go-live partner coverage matrix — LEWIS/ALAN decision doc**; Dean generates the current breadth×depth grid from live `partner_partners` as the starting truth (note: 5/7 live partners lack contact_email, PM-694 ops debt). Defines gates for (a) present to Sage (b) trial go-live (c) first scaled corporate. **(5) Deck onboarding/in-life detail — LEWIS**; slides on request. **(6) Featured partner slot** (~0.5 session): `featured` flag + pinned top slot in partner-space Discover; selection algorithm explicitly deferred per Alan. **(7) "My booked sessions" calendar view — TALK FIRST**: no booking concept exists (sessions are drop-in; session_live_views = views). Needs save/book action + member calendar view — scope with Dean before building. **(8) Static-vs-changing site map** (analysis session): freshness map per page + what drives each update; page-docs (§11B) head start. **(9) API spec pre-Sage** (~1 session + Lewis deck slide): document real integration surface (SSO, provisioning, export, webhooks — mostly aspirational today) so Dean/Lewis can talk detail with Sage. Pairs with (4) as the Sage gates. **(10) Connect bottom blocks UI pass** (~0.5 session): carousel/imagery treatment on connect.html bottom tiles, follows PM-720/724 hub work.

> **[PM-796 · 2026-07-13 · CC ACCESS SESSION CLOSE]** Device check DONE (nav pill + Active Users). **NEW ITEMS:** (1) **Stripe Customer Portal self-serve billing — Dean: BACKLOGGED, not urgent** ("not fussed about easy cancel"). Spec when picked up (~1 session): `stripe-billing-portal` EF (member JWT → stripe_customer_id → portal session URL) + "Manage subscription" row in settings.html (external browser, continue.html IAP pattern); webhook already handles the downstream status flips; default cancel-at-period-end; Lewis copy pass. Context: Paige Coult's 13 Jul renewal failed with `revocation_of_authorization` — blocked at her bank because no in-product cancel path exists. (2) **LEWIS: personal outreach to Paige** (sole paying member; card valid, block deliberate; if she wants out, cancel cleanly in Stripe so smart-retries/dunning emails stop — she keeps access via past_due grace meanwhile). (3) Optional: paid tile shows "N past due" alongside the active count (offered, not taken). (4) Pages sharing the getCurrentAdmin gate (calendar/tasks/documents/sessions/settings) were ALL silently deny-bricked pre-PM-796 — re-walk them during the standing PM-779 verification. (5) acl.js resetToDefaults is now local-cache-only (server untouched; hydrate re-pulls) — fine, noted. **CARRIED from PM-779:** cc_kv smoke (Competitors/Clients edit → row in cc_kv), device check vs Update 512 (settings marker + test check-in in PostHog WITHOUT health props). **DECISIONS STILL OWED (Dean):** PostHog historical health-props deletion request, Demo Co synthetic roster, register row 2 → Anthropic Ireland, Limited.

> **[PM-779 · 2026-07-12 · CC REPAIR NIGHT — VERIFY FIRST NEXT SESSION]** Dean checks: (1) CC Calendar renders month grid (was blank — stub-wrapper clobber, fixed PM-779); (2) CRM/Finance still fine post single-client swap (one magic-link re-login possible); (3) Competitors/Clients edit → confirm row in cc_kv (PM-777 loop proof); (4) device check now vs **Update 512**: settings marker + test check-in in PostHog WITHOUT health props. **NEW BUILD (agreed, one session each):** (a) CC Calendar session management — edit title/desc/host photo/cover on calendar_occurrences, storage-upload images (Calum-banner pattern), pull/cancel WITH runner cancelled_at pre-check (MANDATORY before a cancel button ships), mockup-first; (b) meeting invites — Send-invites on cc_calendar_events via send-email EF + ICS attachment (any pasted link); Meet-link GENERATION blocked on Google OAuth Testing mode (rides that line). **PROMOTIONS QUEUE:** cc_kv collections → first-class tables, Strategy + Intel first (cc_kv = whole-collection last-write-wins, client-side audit — fine for 2 people, not forever). **PM-780 addendum:** deanonbrown2@gmail.com now on admin_users (admin); avatar user-menu + Sign out live; last stub client() calls purged from lib/auth.js. **DECISIONS OWED (Dean):** PostHog historical health-props deletion request (recommended); Demo Co synthetic roster; register row 2 → "Anthropic Ireland, Limited (UK/EEA contracting entity of Anthropic PBC)".

> **[PM-771 · 2026-07-12 · DEAN DECISION]** PostHog holds HISTORICAL events with health properties (score/feeling/energy/weight/weight_kg/food_kcal) from before the `0a616ca4` allowlist fix. Options: (a) deletion request to PostHog support citing our DPA — RECOMMENDED, (b) let retention age out, (c) document as remediated gap. Also optional: print-to-PDF the two canonical Anthropic legal pages to upgrade the position-note filing to verbatim evidence.

> **[PM-764 · 2026-07-12 · MOBILE M3]** Standalone monolith mobile passes, one session each: admin-console.html (131KB, 1 media query — biggest job), partners.html (3 queries, partial coverage), partner-portal.html (1 query — EXTERNAL users, PM-763 low-key discipline applies).

> **[PM-762 · 2026-07-12]** Attendances-by-week REMOVED from partners.html analytics (was a hardcoded mock). Realise later as an EF aggregation over session_live_views once live sessions have volume — table currently 3 rows with member-scoped RLS, so a client-side chart is impossible and an EF is the only honest path. Sonnet-safe when picked up.

> **[PM-751 · 2026-07-12]** Live-mode device fixes shipped (`6d7e6508`): renderDeepDive demo-only guard, sign-in button reset, loadLive catch split (network vs display errors — display errors now console.error'd, never again mislabelled as connection failures). **NEW (optional, Sonnet-safe): seed VYVE Demo Co fixture — 12 wks of wellbeing check-ins across 10+ test members + recent activity spread — to visually verify the UNSUPPRESSED rendering path (never yet seen on a device) before a real client hits it.**

> **[PM-750 · 2026-07-12 · EMPLOYER PORTAL END-TO-END LIVE]** Strategy V2 commitment closed: employer-provision EF v1 (member-safe password/deprovision semantics — deliberate fixes over the partner-provision pattern; auth matrix fully live-verified incl. JWT happy path 200 scoped + 403 not_employer), employer-portal EF v2/v3 (insights[] rule engine, sessions byCategory+mins, byTypeAllTime, thisMonth), employer-portal.html live mode (`e1cbfe9b`: publishable-key sign-in + refresh retry, real hero/insights/benchmarks, fictional surfaces hidden in live, ?demo=1 = PM-740 demo untouched). **DEAN device check: DONE (PM-751 — logged in, live data confirmed; renderDeepDive live-mode crash fixed at `6d7e6508`).** **NEW ITEMS:** (1) ~~CC admin UI for employer provisioning~~ CLOSED PM-761 (pages/employers.html — Sage row is now one modal when Lewis has the contact); (2) Lewis copy pass — login screen + live hero/insights copy + PM-789–791 additions (Timetable/Impact/Challenges/Ask + challenge consent line); (3) Lewis/Alan: curated benchmark figures into employer_benchmark_figures (metrics engagement_rate / activities_per_member, scope uk/eu/global, source + year — externals render ONLY from this table); (4) ~~employer_metrics_weekly snapshot table~~ CLOSED PM-789 (table + Mon 05:40 cron + real Δ in EF v4; deltas render from 20 Jul); (5) PM-740 spec remainder: ~~employer challenges (B1 skeleton)~~ CLOSED PM-790 (EF v5 + employer_challenges/optins; **B2 member-side surfacing OPEN** — vyve-site challenges.html workplace card + opt-in consent write, ~1 session), ~~board pack v2~~ CLOSED PM-791 as live-data print-to-PDF (server-side PDF deprioritised), ~~Ask VYVE form→send-email~~ CLOSED PM-789 (EP-ref relay verified), ~~per-employer timetable~~ CLOSED PM-789 v1 (public community schedule; per-employer custom sessions need calendar_occurrences audience scoping — open); (6) ~~'See your impact' giving feature~~ CLOSED PM-789 (Your Impact tab); (7) HARD pre-Sage-diligence: prospectus states clinical sign-off IN WRITING — Phil signs HAVEN or it comes out (sharpens the standing Tier-0 E item).

> **[PM-749 · 2026-07-12 · GDPR BUCKET SWEEP + EMPLOYER PORTAL BACKEND]** PM-718 hard blocker CLOSED (gdpr-erase-execute v6: challenge-photos + certificates + member-avatars swept as step 2e — the last two were undocumented PUBLIC-bucket erasure gaps; rule: new member-email-keyed buckets go in MEMBER_BUCKETS). PF-NEXT-7 CLOSED (revenue_share_pct default already 50 — brain drift). Employer Portal backend v1 LIVE: employer_admins + is_employer() + employer_benchmark_figures (EMPTY by design) migration; `employer-portal` EF v1 (server-derived scope, Wellbeing Index 30/30/20/20, MIN_COHORT=8 suppression+renormalise, teams <8→Other); 'VYVE Demo Co' fixture; verified 401/400/200 incl. suppression arithmetic. **NEXT:** wire employer-portal.html to the EF (Supabase Auth login, Lewis copy pass), employer Gate-A provisioning flow, Lewis/Alan curated benchmark sourcing, then board pack / Ask VYVE / employer challenges per PM-740 spec. **NEW ITEMS (Alan doc gaps — Lewis/Alan prioritise):** (1) partners@vyvehealth.co.uk printed in the welcome pack — confirm mailbox exists or fix the pack (Lewis); (2) 1-2-1 booking promised in the playbook — no feature exists; (3) monthly partner statements promised — earnings is read-only today; (4) partner-portal metrics (weekly-active %, 30-day retention, avg rating) promised in playbook, not computed. JWT happy-path + 403 device check rides with front-end wiring.

> **[PM-748 · 2026-07-09 · PARTNER FUNNEL SESSION 4 CLOSED — AUDIT COMPLETE]** Launch materialization live: `partner-launch-materialize` EF (admin-gated) wired into partners.html go-live (`d70786fa`) — calendar occurrence (tz-correct, pillar→category, past-date guard) + pinned welcome post, idempotent. Vestiges: EF v10 drops `dbs`; wizard `494e6476` fixes the MISSING qualifications ref in the submit payload. Playbook reconciled (override SQL pattern documented). **GOTCHA:** admin_audit_log shape is admin_email/member_email/action/table_name/new_value — partner-provision v1 audit writes failed silently since ship; v2 fixed. **PM-747 duplicated** by parallel vyve-site ship (Mental Fitness boot fix) — repo+SHA disambiguates. **All four partner-funnel sessions closed.** Remaining partner items: Lewis CTA gate (S1, GATING), Gate-A emails Men Together/Alex/Nicola (Dean), declined-draft purge policy (Dean/Lewis), Lewis privacy-notice pass (non-gating), modules un-deferral post-trial.

> **[PM-747 · 2026-07-09 · PARTNER FUNNEL SESSION 3 CLOSED]** Abuse hardening live: `ef_rate_limits` + `check_rate_limit()` (fail-open) — start 5/hr/IP, global 120/hr/IP, assessment 15/hr/draft, uploads 30/hr/draft, 429 `rate_limited`; `partner-draft-purge` EF v1 + **cron 54** (03:30 daily, 30-day retention — Dean may override, storage-first); GDPR: `partner_draft_erase(p_email)` RPC (applied/declined only — live/onboarding contractual, excluded by design) wired into `gdpr-erase-execute` **v5** (platform v17) as best-effort+HIGH-alert step 2d; wizard `2f5275ec` privacy notice (30-day retention stated). **Email-verification gate: built v8, SHELVED v9 (Dean: friction over-engineered)** — dormant behind `REQUIRE_EMAIL_VERIFICATION=false`, one-const re-arm; `_system` stripping from resume responses stays live regardless. EF source headers for partner-draft-purge v1 / gdpr-erase-execute v17 say PM-744 — STALE, session landed PM-747 (parallel sessions took 744-746). **NEW ITEMS:** (1) declined-draft auto-purge policy — Dean/Lewis call; Dean's 3 `declined` test rows (`dean-brown-*`) sit indefinitely (purge sweeps only `applied`, erase RPC fires only on request); (2) Lewis copy pass on wizard privacy notice — non-gating; (3) DPIA/register line: live/onboarding partners excluded from draft erasure (contractual basis). **Session 4 CLOSED — see PM-748 banner above.**

> **[PM-743 · 2026-07-08 · PARTNER FUNNEL SESSION 2 CLOSED]** Gate integrity live: INSERT+UPDATE go-live trigger w/ audited GUC override (any future admin/playbook partner creation at `live` MUST route through an audited EF action setting `vyve.partner_golive_override` — raw SQL inserts now blocked; fold into PF-NEXT-15 audited action surface + update playbooks/partner-onboarding.md in Session 4); 9 pre-gate live partners backfilled honest progress rows (`admin_onboarded_pre_gate`); assessment scoring server-side (EF v7 + wizard `e5205090` — QUIZ_KEY in EF must stay in lockstep with wizard quizData order). **DEAN:** contact emails for Men Together CIC / Alex (alex-movement) / Nicola (nicola-yoga) → then Gate-A provision via partner-provision EF (confirm send list first; 4 referral placeholders antonia/april/david/ivan SKIPPED by agreement). **Session 3 CLOSED — see PM-747 banner above.** **Then Session 4 — completion plumbing:** launch details → calendar_occurrences, welcomePost seeding, playbook reconcile (incl. the new override path), photoId/dbs vestige sweep. Lewis gates from Session 1 unchanged (CTA gating, email copy non-gating).

> **[PM-739 · 2026-07-08 · PARTNER FUNNEL SESSION 1 CLOSED]** Funnel integrity live (EF v6 + wizard `cb340d44`). **LEWIS:** (1) email copy pass on resume + confirmation emails — non-gating (PM-722 precedent); (2) **GATING: "Become a VYVE partner" CTA** — Dean's recommendation is footer link + about.html placement; nothing gets committed without Lewis sign-off on copy AND placement. **APPROVED REMAINING SESSIONS:** **Session 2 — gate integrity:** extend go-live trigger to fire on INSERT as well as UPDATE + audited admin-override path; Men Together forensic (sitting at pct 25) against the HELD deferred-aware migration — migration lets a deferred partner go live WITHOUT safeguarding/GDPR assessment, stays HELD until Dean + Lewis explicitly approve; move assessment scoring server-side; backfill the 7/9 NULL `contact_email`s then Gate-A provision logins via `partner-provision` EF. **Session 3 — abuse hardening:** rate limiting on the public EF (closes the v6 known gap), email verification before `upload-url` is handed out, abandoned-draft purge, wire partner draft data into the GDPR erasure pipeline, privacy notice on the wizard. **Session 4 — completion plumbing:** launch session details → `calendar_occurrences`, welcomePost seeding into the community, reconcile `playbooks/partner-onboarding.md` with the flow as it now works, sweep vestigial photoId/dbs references.

> **[PM-718 · 2026-07-07 · CHALLENGES SESSION CLOSE]** Feature live end-to-end (PM-707→718, vbb 480→491). NEW ITEMS: (1) ~~**HARD PRE-LAUNCH BLOCKER: wire `challenge-photos` bucket into the GDPR erasure pipeline**~~ **DONE PM-749 (gdpr-erase-execute v6 sweeps challenge-photos + certificates + member-avatars)** (member progress photos = special-category-adjacent body imagery; erasure executors are table-catalog-driven and do NOT touch storage). (2) **Lewis gates:** naming/copy pass on the 60-challenge catalogue (Dean-draft placeholders live in prod), diet/"no alcohol" task framing (never "diet" in copy), privacy-policy line for progress photos. (3) **Quit tracker (Mind hub)** — Lewis's core ask: addiction/sobriety tracking with anti-challenge psychology (total days never reset, compassionate slip flow wired to crisis-scan, craving tools, milestone markers, alcohol/benzo abrupt-stop medical warning). Spec next session; **Phil clinical gate BEFORE ship — do not repeat the HAVEN open-gate pattern.** (4) Five dormant challenges need v2: `pb:achieved` publish (workouts-session PB detection point), `podcast:played` publish, consecutive_weekends / every_other_day / monthly_plus_weeklies evaluator rules. (5) Enrolment sync is last-write-wins (fire-and-forget PATCH) — reconcile pass later, PM-704 family. (6) Day view photo strip/history + non-flagship day view polish. (7) 75-day tail: expiry currently marks `expired` — decide completion-grace copy. Dean's own enrolment: abandon+rejoin done (old 5-key shape). Standing queue unchanged (runner now OFF Mac per parallel PM-714/716 — Tier 1 I CLOSED).

> **[PM-714 · 2026-07-07 · TIER 1 I CLOSED]** Live runner migrated to Hetzner CX23 `vyve-live-runner` 159.69.95.90 (systemd, vault VYVE_RUNNER_SSH_KEY, masters 124 mp4s at /srv/vyve/masters/media). Mac launchd retired — ~/vyve-live is cold backup; new-video flow = drop in Mac media dir + incremental rsync. First unattended box air 8 Jul 06:00Z — verify + also eyeball tomorrow's 12:00Z Midweek Reset (first fresh-content air). CARRIED: rotate exposed service_role key (now also in box /opt/vyve/vyve-runner.env — one more consumer). NEW: ~~August calendar regen must carry the Calum Wed/Fri rotation forward (Weds resume reset 2,3,1…; Fris resume review 2,3,1…)~~ **DONE PM-833 (6 Aug — regen through 30 Sep, rotation carried; Dean to confirm reset/review 2+3 mp4s on the box before Fri 7 Aug 20:30).** Optional later: turn OFF session-publish cron 27 once box-created broadcasts proven over a week (CAS-safe to co-run). Tier 1 queue is now EMPTY.

> **[PM-706 · 2026-07-07]** iOS 1.9 LIVE on the App Store; signed 479 (fast-path live-update.js) on production channel. NEW: (1) next binary 1.10/1.0.9 ships CURRENT www at archive time (fast path removes the rationale for aged snapshots); (2) device-verify the first-run fast path on the next natural fresh install; (3) **Lewis: live App Store description opens with stale PWA copy ("loads content from our secure web portal") — §23.1, fix with the HAVEN copy item**. Dean's store app converges to 479 via one legacy double-restart (60–90s dwell on first open). Standing queue unchanged: Tier 1 **I (runner off Mac)**, dashboard-delete ota-canary-watch slug, commit Mac vyve-capacitor state to remote, Android 1.0.8 review pending.

> **[PM-704 · 2026-07-05]** Android 1.0.8 SUBMITTED (both stores in review). NEW backlog: Settings 'resync my data' (nuke Dexie → re-hydrate) + reconcile-on-hydrate for track counters (immortal-Dexie property: container persistence + additive-pull; bites on GDPR rectification or unflushed PF-4 writes). Dean device chore: delete+reinstall store app to converge. Standing queue: Tier 1 **I (runner off Mac)**, dashboard-delete ota-canary-watch slug, commit Mac vyve-capacitor ship-state + mode scripts to remote, 477-tree startup SyntaxError check, Lewis items (HAVEN gate E, store copy claims, PostHog DPA, retain legal bases, sub-processor register actions).

> **[PM-703 · 2026-07-05]** Dev shell back (.dev bundle ID, "VYVE Dev", inverted icon) via mode-dev.sh/mode-ship.sh; ship state verified post-switch. Backlog adds: commit vyve-capacitor Mac state to remote (configs, mode scripts, 1.9/1.0.8 bumps); delete stale .bak-pf14b/.bundled-backup configs; stale APNs token rows from Dean's reinstall churn will 410-auto-revoke (no action). Awaiting: Apple review (1.9), Play upload confirm (1.0.8 AAB built).

> **[PM-702 · 2026-07-05]** iOS 1.9(4) SUBMITTED; Android 1.0.8(54) AAB built — **Dean: confirm Play Console upload**. On approval the frozen cohort self-heals via OTA (477). Queue after: Tier 1 **I (runner off Mac)**, dev-shell rebuild (.dev bundle ID), dashboard-delete ota-canary-watch slug, commit Mac vyve-capacitor ship-state to remote, **Lewis: HAVEN/group-therapy claims in live store description vs open clinical gate**.

> **[PM-701 · 2026-07-04 · TIER 1 K CLOSED §23.106]** OTA verified on-device (476 on-screen). Shipped binaries were BROKEN (embedded config stripped + publicKey:"" signing enforcement) — store installs can never OTA. NEW TOP NATIVE PRIORITY: **archive + submit iOS 1.9 / Android 1.0.8** (Mac already in verified archive-ready state). Also: rebuild Dean's dev shell with `.dev` bundle ID (store-config build overwrote it); dashboard-delete `ota-canary-watch` EF slug; check 477 for the bundled-tree startup SyntaxError (index.html:2857, seen in 455 native console); PM-700 race fix live in signed 477 bundle. Remaining Tier 1: **I (runner off Dean's Mac)**.

> **[PM-699 · 2026-07-04 · TIER 1 K SUBSTANTIALLY DONE]** First-ever OTA bundle live on production (Update 476, deployment `383c172e`, 100%). Server half proven: real-device probes + checksum-matched artifact download. On-device apply auto-verifies via `ota-canary-watch` EF/cron → team@ email on first device flip (then DELETE the EF + cron `ota-canary-watch-15min` — temporary). CAPAWESOME_TOKEN vaulted + `capawesome_token()` accessor. Capawesome plan is PAID. Remaining Tier 1: **I (runner off Dean's Mac)**. §23.106 closes on tripwire fire.

> **[PM-698 · 2026-07-04 · TIER 1 J CLOSED]** Google OAuth consent screen published to production (publish-not-verify) — 7-day refresh-token expiry GONE (keepalive live-verified: refresh_token_expires_in absent). New token minted via localhost:8765 redirect (oauthplayground URI replaced), proven against the VYVE channel, vault-swapped; keepalive + runner read vault per-run so zero consumer changes. Crons 25/35 stay as belt-and-braces. PF-PARK-3's rotation-chore half is dead — separate-partner-channel question is now pure isolation. Tier 1 remaining: **K OTA canary (pre-Sage gate), I runner off Dean's Mac**. H drafted PM-697 (Lewis has 5 DPA actions).

> **[PM-696 · 2026-07-04 · TIER 0 COMPLETE except E]** Findings C, F, G CLOSED. F: 66 dead EFs deleted (163→96; ref-checks wider than register: 52 crons, DB fn sources, 3 repo greps, all 95 keep-set eszip bodies; `achievement-earned-push` KEPT — live dependency of achievement-claim/achievements-sweep cron 15/log-activity). G: PM-695 replay mask + 10% client-side sampling (phx key lacks project:write). C: both PM-689 slugs deleted. CRITICAL incident found+fixed en route: signup DEAD since PM-689 — welcome.html hard-gated its success screen on the stubbed set-member-password EF; onboarding v99 now sets the password at auth-user creation + welcome.html `547ad7b` gates on `password_set`; live-verified both paths incl. sign-in; zero members harmed (sole signup since, reece-calvert, beat the stub by ~30 min). Remaining Tier 0: **E HAVEN routing (Lewis)**. Lewis also: confirm PostHog EU DPA (project 138491). Tier 1 **J (Google OAuth verification) STARTING — external clock**. Then: K OTA canary, I runner off Dean's Mac, H sub-processor register.

> **[PM-694 · 2026-07-04 · TIER 0 PROGRESS]** Finding D DONE (partner-content-upload v6 JWT + server-derived partner; portal PM-693 real login, shared password deleted). Finding B FULLY verified (Dean's live check-in; crisis-scan v2.2 pattern fix — "feeling"-form phrases now match). OPS: backfill contact_email for 5 live partners then Gate-A provision logins (partner-provision EF) before any real partner uses the portal. Remaining Tier 0: C dashboard-delete 2 slugs (Dean), E HAVEN routing (Lewis), F 68-slug EF sweep, G PostHog mask. Tier 1 J (Google OAuth) to start.
> **[PM-692 · 2026-07-04 · TIER 0 PROGRESS]** Finding B internal-alerting half DONE (crisis-scan v2.1 wired: onboarding + weekly [incl. HAVEN] + monthly + journal + connect; persistent platform_alerts rows; VYVE_INTERNAL_KEY trigger path). Verify: Dean submits one weekly check-in from phone with a benign concern phrase (member-JWT path). B's member-facing half (crisis copy, persona suppress/re-route on critical, persistent signposting) = Phil + Lewis gate. Gotcha logged: platform_alerts.severity CHECK critical/high/info only ('warning' silently rejected; gdpr-erase v4.1 fixed). Remaining Tier 0: C dashboard-delete 2 slugs (Dean), D partner-content-upload JWT, E HAVEN routing (Lewis), F 68-slug EF sweep, G PostHog mask. Tier 1 J (Google OAuth) should start.
> **[PM-691 · 2026-07-04 · TIER 0 PROGRESS]** Finding A DONE (catalog-driven enumeration in BOTH export+erase, verified live; no re-run needed — the 2 executed erasures were TEST accounts with zero residue). Finding V DONE (orphan purge fn dropped). NEW CRITICAL found+fixed: GDPR crons 21/22 sent no auth header vs CRON_SECRET gate — every tick 401'd, pipelines silently dead; revived via vault GDPR_CRON_KEY + x-vyve-cron-key (gate now always enforced). Erase v14 also purges subject's gdpr-exports artefacts (Art 17 residual). Remaining Tier 0: B crisis-scan wiring (Phil copy gate), C dashboard-delete set-member-password/create-test-user slugs, D partner-content-upload JWT, E HAVEN routing (Lewis), F 68-slug EF sweep, G PostHog mask. Lewis: confirm retain legal bases (stripe_events/admin_audit_log/gdpr_erasure_requests) for DPA. Tier 1 J (Google OAuth verification) should start.

> **[PM-689 · 2026-07-03 · SECURITY]** Two unauthenticated account-takeover EFs killed this session (`set-member-password`, `create-test-user`) — now 410+JWT, verified 403. **TODO: dashboard-delete both slugs.** EF delete sweep pending: 68-slug list at `reports/ef-delete-list-2026-07-03.txt`, needs CLI + ~5 admin-ref checks first. Also queued from audit: erasure-gap fix (PM-688 §1.1), close partner-content-upload (PM-688 §2.1), HAVEN interim routing (Lewis), admin-member-edit mandatory-reason on SCARY fields before enterprise DPA.

## NEXT — Employer Portal productionisation (PM-740 demo = the spec, 2026-07-08)
The Sage demo (employer-portal.html) is now the agreed shape of the employer product. Lewis has socialised these promises with a live prospect — treat as committed scope, sequence post-Sage-feedback:
- **Wellbeing Index** (employer barometer, was already "scoped not started") — company-level composite: engagement + anonymised avg check-in + pillar balance + momentum. Needs real formula + EF support. Wellbeing NEVER below company level (PM-738 discipline).
- **employer-dashboard EF extensions:** per-team aggregates (activity/engagement ONLY, min-cohort threshold ≥8 → "Other"), company-level wellbeing avg series, benchmark payload (VYVE community avg is computable today; UK/EU/Global = curated published figures).
- **Employer challenge launch** — templates → real Challenges feature (exists, PM-707–719); needs employer-scoped challenge creation + opt-in join flow + team-aggregate leaderboards (HealthKit data stays member-private; only opted-in team aggregates surface).
- **Expert/workshop booking** — real booking flow (enquiry → team@ is fine as v1; expert roster is Lewis commercial work: real names, prices, contracts).
- **Board pack** — server-side or print-route quarterly report; monthly snapshot email (Brevo).
- **Ask VYVE** — v1 = form → send-email EF; chat answers later (Anthropic EF, aggregate-context only).
- **Per-employer auth-gated portal URL** (e.g. /sage) — was already planned for first enterprise trial; the demo is its front-end.
- **VYVE Average / VYVE Best benchmarks** — once >3 employer accounts (Lewis's "eventually", teased in demo copy).

## NEW IDEAS + structural risk (captured PM-658, 2026-06-22)

**Feature ideas (Dean brain-dump — unspecced, talk-first before build):**
- **Challenges** — subscribable, saveable, time-boxed challenges. "75 Hard" is trademarked → build a VYVE-branded variant (multi-task daily checklist, reset-on-miss model). Step-count challenges need native HealthKit / Health Connect step data (native dependency). Lewis copy gate on naming + framing.
- **Content library expansion** — more videos / education / support content; partly exists already, needs an audit of what's there vs. the gap before scoping.
- **Global app-wide search** — single search across sessions, content, exercises, etc.
- **Fasting feature (nutrition)** — 16:8 / 24 / 36 / 48h fasting tracker. Extended-fast (36/48h) guidance needs a health disclaimer + Phil clinical gate before shipping to members.

**Structural risk — live-session runner single point of failure (from PM-658):**
- ~~`vyve-live-runner.py` runs as a launchd agent on Dean's personal Mac — sleep-prone, no redundancy. Move to an always-on box~~ **DONE PM-714** — Hetzner CX23 `vyve-live-runner` 159.69.95.90, systemd.
- ~~Google OAuth app is in "Testing" mode → refresh token expires ~every 7 days~~ **DONE PM-698** — app published to production, token non-expiring.
- Detection is now handled (broadcast-watchdog EF, cron 50, alerts email+push). These two items are the remaining *structural* fixes.


## CC OVERHAUL CAMPAIGN (PM-752 opened 2026-07-12 — ACTIVE, supersedes PM-639 Layer 2)

**Spec: `playbooks/cc-overhaul.md`** (Phase 0 audit + disposition tables + phase plan). Locked: VYVE-brand dual light/dark design system on every CC surface (partner-portal/admin-console lineage); 5-domain IA (Run the Business / Members / Partners / Employers / Analytics); curated Run-the-Business rebuild on the empty cc_* tables; kill list PENDING LEWIS CONFIRM (nothing deleted before his nod).

- [ ] **Phase 1** — design system + shell mockup, both themes (Dean gate, then Lewis look)
- [x] ~~**Phase 2**~~ **SHIPPED PM-753 (2026-07-12, `06ac8085`)** — tokens v2 + shell + 8 Analytics re-skinned; auth fail-closed; single supabase-js; theme toggle; Employers nav live (portal links). Dead-lib load trim deferred to Phase 3 (Legacy pages still depend on the lib layer)
- [x] **Phase 3 CLOSED PM-759** — Home launchpad SHIPPED PM-755 (absorbs Brief + dashboard/inbox/activity jobs; cc-home EF v1). CRM SHIPPED PM-756 (cc_leads live, RLS fixed, real pipeline seeded). Finance+Invoicing SHIPPED PM-757 (incl. won-lead→cc_clients flow + the cc_team_only 15-table dead-policy class sweep). Investors+Grants / Content / Podcast SHIPPED PM-758 — RtB domain COMPLETE, all live-data. Phase 3 CLOSED PM-759 — SOFT-KILL (Dean policy, hard-delete off the table): 10 dead lib loads removed, files kept; acl/ui/widgets/integrations/targets/cc-adapter KEPT as live deps (settings rebuild retires targets/cc-adapter later). Restore recipe: nav line (pages) / script tag (libs)
- [~] **Phase 4 step 1 SHIPPED PM-760** — admin-console re-skinned (tokens v2, Playfair zero, theme key unified); broadcast/active-users audited already-clean. Remaining (later strangler steps, refactor-value): extract console views into shell pages, retire monolith sidebar/login
- [ ] **Phase 5** — Partners: partners.html re-skin + kill/realise the two mocks (attendances-by-week, engagement scorer)
- [~] **Phase 6 PM-761 IN FLIGHT** — recon done (see changelog 12 Jul): employer-provision v2 +list, benchmark write policy, pages/employers.html, nav slug, pg_net verify. Handed to fresh session
- [ ] **Phase 7** — partner-portal.html adopts the system LAST (external users)
- [x] **Wrapper consolidation SHIPPED PM-779** (`9bda443a`) — lib/supabase.js stub soft-killed, single VYVE_SUPABASE (§23.146), 9 Phase 3-6 pages patched off client() calls
- [x] **cc_kv strangler SHIPPED PM-777** (`f8759820`/`04ff7fe7`) — all 16 Legacy pages server-backed (hydrate/write-through/focus-pull/fill-gaps seed + graduated-table mirrors into VYVE_DATA); promotions queue: Strategy, Intel first
- [x] **Desktop shell fix SHIPPED PM-776** (`b1783492`) — topnav tabs live >900px, drawer closed by default, home tiles de-nested (§23.145)
- [x] ~~Lewis: confirm kill list~~ RESOLVED PM-754 — Dean soft-killed (nav delink, files kept). Lewis: look at the new skin; flag if any Legacy page held data he typed (his-browser localStorage only)
- Folded in from PM-639 Layer 2: seed-data.js audit (DONE — it's a 13-May Make snapshot, retires Phase 3), partners.html mock kill (Phase 5), 4 unreachable hub pages sweep (Phase 3 kill list).

## Command Centre IA + Team App build (PM-639 spec — Phase A SHIPPED PM-685)

IA + spec done as artefacts (`CC-information-architecture.md`, `CC-team-app-spec.md`); §13 in master carries the summary.

~~**Phase A — CC nav reorg (4 domains):** restructure~~ **SHIPPED PM-685 (2026-07-03)** — 4-domain nav live, 11 dead pages killed, `#/performance` links repointed. Remaining from Phase A scope: sweep the 4 unreachable old hub pages (`pages/commercial|marketing|delivery|org.html`) in the CC Layer 2 pass (seed-data.js audit + kill the partners.html mocks). Original scope for reference: restructure `assets/sidebar-config.js` to Run the Business / Analytics / Members / Partners; retire the duplicate `VYVE_NAV_TOP` layer. Cross-domain moves: App Health Delivery→Analytics, Broadcast Delivery→Members, Active Users Org→Members. KILL old Commercial Partners skeleton, Performance, Brand, the 7 orphan `pages/partner-*.html` (grep-confirm first), root `Dashboard.html` (verify first). Reconcile Partner Space onto the `partners.html` monolith.

**Team App build phases:**
1. ~~**Foundation** — `admin_users` roles (admin/team), `is_team()` SECURITY DEFINER capability, RLS on `cc_tasks` / `cc_calendar_events` / new `cc_task_attachments`.~~ **SHIPPED PM-640 (2026-06-16)**
2. ~~**Tasks** — shared-list UI + per-assignee completion (`completed_at`/`stage=done`) + `cc_task_attachments` table + private `cc-task-docs` bucket + signed upload/download EF (partner-docs/partner-file-url pattern).~~ **SHIPPED PM-641 (2026-06-16)**
3. ~~**Calendar** — unified 3-source read view (`cc_calendar_events` + `calendar_occurrences`, union-at-read, colour by `source`) + meeting create/edit.~~ **SHIPPED PM-642 (2026-06-16)**
4. ~~**Scheduler** — `calendar_occurrences` write form, capability-gated (admin + `lives`), confirm-step (member-facing publish).~~ **SHIPPED PM-643 (2026-06-16)**
5. **Wrap** — deferred until content signed off. 2nd Capacitor binary off `vyve-capacitor`, own bundle id, points at role-gated CC, TestFlight internal (~6 emails).

Open (non-blocking): app name/bundle id, team route/subdomain (only matters at wrap), scheduler capability holders, gcal 2-way sync (lean internal-only v1), meet_url auto-gen vs paste (lean paste v1).


## Partner Space — build spec LOCKED (PM-660, 2026-06-22) → Sonnet builds

**PM-683 status (2026-06-25) — referral system built out + partner upload working. Full map: `playbooks/referral-system.md`.** SHIPPED: tier links £20/£15/£10 (shared coupons `VYVE_TIER15`/`VYVE_TIER10`, per-partner coupons dead, identity via `metadata.partner_slug`); 3-tier attribution (stripe-webhook v13, stripe-reconcile v5); partner trials (`trial_days` col, `apply_trial_campaign` slug fallback, £20→£10 conversion); admin payouts rewired to net-paid model (both `partners.html` + `partner-revenue.html`); 4 live partners (antonia/april/david/ivan); partner-portal VIDEO UPLOAD working end-to-end (`partner-content-upload` EF, x-portal-key gate, in_review row). OPEN: (1) real per-partner portal auth — replace shared password + `DEMO_PARTNER_ID` with real logins (`admin_users` role='partner' + `partner_partners.contact_email`, §23.132/133), flip EF back to JWT, decide which partners first; (2) raise Dashboard global storage upload limit (§23.131) + resumable/TUS for big video; (3) real role_title/pillar + per-partner trial_days for the 4 new partners; (4) payout churn filter; (5) real "Attendances by week" (hardcoded mock in partners.html) + engagement-segment scorer (hardcoded 'regular'); (6) Lewis: UK trial pre-charge disclosure + ~3-day reminder. Throwaway EFs to sweep next dead-EF pass: setup-tier-coupons, verify-tier, test-storage-sign, test-upload-put.

**PM-684 status (2026-06-26) — partner content scheduling SHIPPED.** Approved ≠ live: partner sets a mandatory go-live (≥48h ahead) at upload; live = `approved AND publish_at IS NOT NULL AND publish_at<=now()` (member RLS + portal chips, no cron). Editable title/desc/go-live ONLY while in_review (EF `update` 409s after approval). `published`→`approved`. Video thumbnails: client-side poster-frame capture → PUBLIC `partner-thumbnails` bucket → `thumbnail_url`, shown in portal + admin. `partner-content-upload` v5 (sign/sign-thumb/commit/list/update). Bugs fixed: loadContent race (§23.135), partner-file-url bucket-prefix (§23.137). See §23.135-139, `playbooks/referral-system.md`. **HEADLINE NEXT: partner-portal login + account creation folded INTO partner onboarding** — replace shared password + `DEMO_PARTNER_ID` with a real auth user + `admin_users` role='partner' matched on `partner_partners.contact_email`, flip EF to JWT; TUS/resumable uploads ride with this build. STILL OPEN from PM-683: real role_title/pillar for antonia/april/david/ivan; payout churn filter; real Attendances-by-week + engagement scorer (both mocked in partners.html); Lewis trial pre-charge disclosure.


**Full spec: `playbooks/partner-space-build.md`.** End-to-end map done this session. Decisions LOCKED: two-gate model (A=approved→web login+build space; B=Phil safeguarding/GDPR→member-visible); partner mgmt WEB-ONLY (`is_partner()` CC-slice, extend `admin_users.role`); community join FREE (payment is the VYVE membership; partner earns via existing 50% referral; no Stripe at join); simulated-live not genuinely-live.

**NEXT-UP (one schema, three deliverables — `partner_memberships` is the hinge):**
- **PF-NEXT-11: Partner web backend** (`is_partner()` CC-slice) — Account/Profile, My Content (upload+moderation status), My Community (feed posts to `partner_community_posts`, see/message subscribers, "we're live" trigger), My Sessions, My Earnings (read-only). Login provisioned at Gate A. Heavy back-office stays web.
- **PF-NEXT-12: Member-facing Partner Space (in-app)** — discover partner, JOIN free (`partner_memberships` row), view feed + sessions + library. Read-and-join only. Visible only after Gate B. Community-scoped sessions live here filtered by `partner_id`, NOT global `sessions.html`.
- **PF-NEXT-13: Community subscription + notifications** — free join; subscribers = new `partner_subscribers(partner_id)` audience shape over the EXISTING push spine (`send-push`/APNs/scheduled). Partner triggers notes/messages/"live 30-min-before" from web backend. Hard part already built.
- **PF-NEXT-14: Audience scoping** — `calendar_occurrences` add `visibility` (`public`|`community`) + `partner_id`. Member session reads filter: `public` OR (`community` AND member in `partner_memberships` for that `partner_id`). Also the guardrail against the Team-App-scheduler global-list footgun.

- **PF-NEXT-15: Claude-driven audited admin actions (Workstream 4).** Read half ALREADY LIVE (Supabase MCP). Write half = thin audited action surface graduated by risk: low-risk stage advances (say-so) / Gate A provision-login (confirm) / Gate B go-live (HARD confirm — verify Phil safeguarding+gdpr first, §23.84, never casual). All → `admin_audit_log`; reuse audited EF pattern (`admin-member-edit`) not raw writes. Generalises to members + admin console; partners first consumer. Full detail: playbook Workstream 4.

**Gate mechanics:** A = admin advances `partner_partners.status`→`onboarding` → provision `is_partner()` login + send creds. B = `trg_assert_partner_golive` + PM-659 HELD migration (deferred-aware) — needs safeguarding+gdpr (Phil) AND pct=100; members see partner only at `live`.

**PARKED (mapped PM-660, NOT next — videos may not even go live yet):**
- **PF-PARK-1: QC/ingest airing pipeline.** Exception-only, minimum Dean work. Watcher launchd agent in `~/vyve-live` (TCC-safe §23.89, py3.9-safe §23.88): download master → transcribe → rubric transcript → ffmpeg-probe → frame-sample → write `calendar_occurrence`. **Transcript-first + mechanical QC, NOT "AI watches the video"** (burned Dean before): transcript-vs-rubric = load-bearing safeguarding check (§23.84); ffmpeg = resolution/bitrate/silence/black-frame; optional vision-on-stills only. Clean→auto-schedule; flagged→hold+notify+10s tap. Download permanent until runner fetches from Storage. Inherits runner SPOF.
- **PF-PARK-2: YouTube channel/key model.** ONE channel today, 9 reusable keys (concurrency lanes) + 8 category playlists (replay split by playlist). 15 partners = slot allocation not key/channel scarcity (reuse 9 keys, ceiling 9 concurrent, add keys free, no-double-book check). Leaning separate "VYVE Partners" channel for isolation; costs 2nd OAuth + channel-aware runner — build runner channel-aware from start if doing it.
- **PF-PARK-3 (UPDATED PM-698): token-expiry half RESOLVED** — consent screen published, refresh token non-expiring, rotation chore dead. Remaining question is only whether partner streaming warrants a separate "VYVE Partners" channel (+2nd OAuth client, one localhost re-consent) — now a pure isolation decision.

**Carried/superseded from prior PF-NEXT list:**
- PF-NEXT-1 (onboarding journey) — SHIPPED PM-631/634/635; modules hidden PM-659.
- PF-NEXT-2 (member-facing space) — now PF-NEXT-12 above; no longer blocked on a Lewis demo file (spec'd from scratch).
- PF-NEXT-3 (curriculum persistence) / PF-NEXT-4 (programs sub-tab) — still open, fold into PF-NEXT-11 backend.
- PF-NEXT-5 (revenue attribution) — DONE PM-633.
- PF-NEXT-6 (CC overhaul) — tracked under PM-639 CC IA.
- PF-NEXT-7 (`revenue_share_pct` default 30→50) — ~~DECISION STILL PENDING DEAN~~ **DONE pre-PM-749 (found live at 50, all rows 50; closed PM-749).**
- PF-NEXT-8/9 (agreement brackets + Lewis live-origin review) — still open, Lewis.
- PF-NEXT-10 (silent PDF download) — optional.

## SHIPPED PM-559 — App Health Dashboard v1
### Live at admin.vyvehealth.co.uk/#/app-health (2026-06-08)

---

## BACKLOG — PM-591 — Staff account_type flag (exclude staff data from analytics)

**Problem:** Analytics (retention, wellbeing, activity depth, all Command Centre pages) are skewed by heavy test usage from Dean, Lewis, Calum, Kelly and other team members. Real member signal is diluted — day-N curves, wellbeing averages, and engagement metrics are not representative.

**Solution:** Add `'staff'` to `members.account_type` enum. Exclude staff rows from all analytics by default.

**Scope:**
- DB: add `'staff'` to `account_type` check constraint
- `compute_cc_wellbeing()`, `compute_cc_retention()`, `compute_cc_activity()` — add `AND account_type != 'staff'` to all member queries
- `employer-dashboard` EF — exclude staff
- `expire_lapsed_trials` cron (pg_cron job 34) — staff never expire
- `auth.js` `vyveCheckAccess` — staff always pass access gate regardless of subscription status
- Command Centre admin UI — toggle to include/exclude staff data per page (default: exclude)

**Members to flag on deploy:** Dean, Lewis, Calum, Kelly + any other team test accounts identified at time of build.

**Effort:** ~half day.

## NEXT — VYVE Command Centre (post-App Store resubmission)
### Internal ops, admin CRM, document hub, app health monitoring

**Trigger:** Start during Apple review window after app resubmission.

**v1 modules (priority order):**
1. Admin CRM — view/edit member profiles, habits, workouts, personas. Critical before first enterprise contract.
2. Task & role management — assign work to the 11-person team, status visibility, output upload.
3. Document hub — Supabase Storage, upload/categorise/notify. Replaces Google Drive for internal docs.
4. App health dashboard — VYVE error feed, key metrics, alerts surfaced here not email inbox.
5. Meeting scheduling — Riverside link generation, calendar invites.

**Stack:** GitHub Pages + Supabase Auth + Supabase Storage + Supabase DB + Edge Functions + Anthropic API. New repo: vyve-command-centre (admin.vyvehealth.co.uk already exists).

**Future phases:** PT/partner portal (B2B2C), SSO integration, API access for enterprise data export.

**Architecture note:** Design for multi-tenancy from day one — PT portal and employer access require role-based permissions that must be in the schema before v1 ships.

**Session start:** Load VYVEBrain, claim PM number, spec DB schema first before any parallel build sessions open.

---

## BACKLOG — PF-23 v2 First-Run Experience (action-tutorial + per-hub contextual tours)

**Depends on:** Achievements overhaul (per-step achievement unlocks), Lewis copy sign-off.

**Two-track design (Dean + Lewis agreed 2026-06-07):**

**Track A — Action-tutorial home tour (replaces v1 explanatory tour)**
Each step prompts a real action. Member finishes the tour with actual activity credited.
- Mood tap → logs to `daily_mood_checkins` → fires mood achievement toast
- Focus tap → completes focus activity → counts toward rings
- Habit tap → logs the habit → the spotlit circle is tappable-through (pointer-events allowed on that element during that step)
- Tour ends with rings visibly ticked up

Implementation note: spotlight needs tap-through mode for specific steps — remove pointer-events block from the spotlit element only, listen to VYVEBus for `mood:logged` / `focus:completed` / `habit:logged` to auto-advance.

**Track B — Per-hub contextual first-visit tooltips**
Each hub page shows a 2-3 step spotlight the first time a member visits. Independent localStorage gate per page (`vyve_seen_body`, `vyve_seen_mind`, `vyve_seen_connect`). No cross-page navigation. Member can dismiss at any step.

- Home: already covered by v1 (or v2 Track A)
- Body: workout card, movement ring, exercise library
- Mind: today's focus, tools grid (breathwork/journal/meditation), progress counter
- Connect: sessions calendar, community feed, weekly check-in

**Sequencing:** Build Track B first (simpler, no Achievements dependency). Track A after Achievements overhaul lands.

## SHIPPED PM-554 — PF-23 v1 First-Run Experience
### Intro slides + in-context spotlight tour (explanatory; first-login once, never again)
### Shipped PM-554 (2026-06-07). Copy DRAFT in COPY object at top of firstrun.js — Lewis edits in place.

**Ship target:** in the next binary OR first Capawesome OTA (`--rollout 0.1` canary) — Dean's call. Decoupled from Achievements (explanatory v1, no per-step achievement).

**Production change (approved):** `ALTER TABLE members ADD COLUMN tour_completed_at timestamptz;` + add `tour_completed_at` to member-dashboard EF snapshot payload. Bundle together; verify via real invocation.

**Component approach:** `firstrun.js` + `firstrun.css`, self-contained. Loaded on index.html, mind.html, sessions.html only (add to all three script stacks; confirm haptics.js present — §23.44). Add both new files to sw.js precache, same commit (§23.76). Inert on mind/sessions unless tour active.

**Trigger / gate:**
1. index.html boot: `if (localStorage['vyve_firstrun_done']) return;`
2. else read `tour_completed_at` from home snapshot / hydrated Dexie members row -> if set, write `vyve_firstrun_done`, return.
3. else (null + past consent gate) -> run Part 1.

**Resume cursor:** `vyve_tour_active` (bool) + `vyve_tour_step` (int). firstrun.js on mind/sessions: if active and page matches the current step's host, anchor-ready wait then draw; else inert.

**Dismissal (skip any step OR final Done — identical):** set `vyve_firstrun_done=1`, clear cursor, remove overlay, fire UN-AWAITED `supabase.from('members').update({tour_completed_at:new Date().toISOString()}).eq('email',<authEmail>)` (member-scoped RLS on auth.email(); no EF). §23.31 — never await on dismissal path. Reinstall self-heals via cold hydrate.

**Overlay mechanics:** full-viewport scrim; transparent cutout element over the anchor's bounding rect via large-spread box-shadow; tooltip card = copy + Next/Skip + step dots. Auto-scroll anchor into view; reposition on scroll/resize. Anchor-ready guard per step (selector exists AND non-empty; rAF poll ~1.5s; on timeout anchor to section container) — never spotlight an empty skeleton (§23.36/§23.47). Safe-area insets on all fixed controls (§23.58). VYVEHaptics.selection() on advance. Suppress web tells (§23.59).

**Copy is a swappable table (basics-first):** every slide + spotlight string lives in ONE `COPY` object at the top of firstrun.js (mirror the hydration.js COPY_TABLE pattern). Build the mechanism solid once; treat all strings as DRAFT and iterate wording in place — Lewis edits the object, never the logic. Do not block the build on copy being final.

**Part 1 — 4 intro slides (swipeable, Skip always visible):**
1. What VYVE is — "Most health apps wait for something to go wrong. VYVE helps you build health before it breaks — a little, every day."
2. Five areas — "Five places to look after yourself: Body to move, Mind to settle, Nutrition to fuel, Sessions to join live, and Connect for the people around you." (Areas, not nav — bottom nav is Home/Mind/Body/Connect/More; Nutrition+Sessions live under More; spotlight teaches the real nav.)
3. Help yourself, help others — "Help yourself, help others. Every habit, workout and session you log builds toward a certificate — and each one you earn donates a free month of wellness to someone who needs it." (Echoes the VYVE tagline: Help yourself. Help others. Change the world. THIS is the "why" of the cap — see step 5.)
4. Your day — "A few minutes is enough. Check your focus, tick a habit, drop into a session. Come back tomorrow and keep it going." CTA "Show me around" (-> Part 2) / "Skip" (-> done-flag, home).

**Part 2 — 7 spotlight steps (host / selector / anchor-ready / copy). Path: home(1-4) -> mind(5) -> sessions(6-7), 2 hops.**
1. index `#focus-carousel` (label `#focus-section-label`) — ready: has `.focus-card` — "Your daily focus — one small guided action, picked for you. Tap to begin."
2. index `#habit-list .habit-row:first-child .habit-check` (fallback `#habit-empty`) — ready: `#habit-list` visible & has a row, else empty-state — "Tap the circle to tick off a habit. That's a log — it counts."
3. index `#pills-row` — ready: has `.pill` — "Five rings, one per area. Each fills toward your next certificate at 30."
4. index `#live-carousel` (header 'Up next'; View all -> /sessions.html) — ready: has `.scroll-card` — "What's on this week. Tap a card to join a live session, or View all for everything."
5. mind `#today-progress` (in `.progress-card`) — ready: textContent matches /\d \/ 2/ — "Two a day per area count toward your certificate. The cap is the point — it rewards turning up daily, not grinding, and those steady days are what donate months to others. Log as much as you like."
6. sessions `#sessionList` + first `.status-badge.live` (up next = `#nextSession`) — ready: `#sessionList` has a card — "Every session in one place. Live ones carry a red badge; the next one's always up top."
7. sessions first `.card-replay` (-> replay-category/replays.html) — ready: a `.card-replay` exists — "Missed one? Every session becomes a replay you can watch any time." -> Done.

**Copy notes for Lewis:** live label says "Today's focus" not "Tonight's" — recommend mirroring it. All copy emoji-free. This IS the ~35-line copy doc PF-23 was waiting on.

**Build checklist:** §23.72 vbb-marker (index + settings) + sw.js CACHE_NAME bump, same atomic commit; §23.24/25 recompute PM across vyve-site + VYVEBrain at commit time; §23.76 precache both new files; honour §23.36/§23.47/§23.58/§23.59. Budget ~84s followed fully (4x5s slides + 7x8s spotlights + 2 hops + fades).

---

## IN PROGRESS — Check-in merge (PM-484 / PM-478 spec)
### Weekly deepened + monthly merge + AI debrief engine
### Structure shipped PM-484 (2026-06-05). Pending Lewis copy + Phil clinical sign-off for question wording.

**Status:** DB migration + EF v22 + HTML branching flow LIVE. Question copy is placeholder pending Lewis/Phil sign-off. Build in a fresh session. Member-facing change on the whole server.url cohort. Claim fresh PM number(s) at commit time.

**Blocked on:**
- Lewis: member-facing copy for dimension labels, branch prompts, improvement question
- Phil: negative branch prompt + stress dimension clinical review
- Dean: monthly cadence trigger decision (calendar-anchored rec, not locked)

Everything else is build-ready. Structure, data sources, AI rules, fallbacks all locked.

---

### WHY

The current weekly check-in is one mood slider + optional free text. It produces thin AI recommendations that don't reflect what the member actually did, how their week compared to their normal, or what's driven their mood. A member can score 4 after a strong week because of one bad day — the AI has no way to know. The monthly check-in is substantially better but lives as a separate surface duplicating the entry point. This build collapses both into one intelligent, branching, data-rich check-in that produces a genuine weekly debrief.

---

### BUILD ORDER

**1. DB — Add columns to wellbeing_checkins**

Add: `check_in_type` (text, default 'weekly', values: weekly|monthly), `dimension_energy` (smallint, nullable), `dimension_sleep` (smallint, nullable), `dimension_stress` (smallint, nullable), `dimension_body` (smallint, nullable), `branch` (text, nullable), `drivers` (text[], nullable), `improvement_focus` (text, nullable). All nullable — existing rows unaffected.

VERIFY: migration applied, existing rows intact.

**2. DB — checkin_questions new weekly version**

Add new weekly question set version to `checkin_questions` reflecting deepened structure (dimension taps + branching driver + improvement question). Keep existing monthly set intact. New weekly version active from build date forward.

**3. wellbeing-checkin EF — enriched signal assembly**

Before the Anthropic call, assemble full signal block. Sources (all existing tables, no new infrastructure):

- `member_home_state` WHERE member_email — this_week counts (habits/workouts/cardio/sessions), goals (target vs done), streaks (current + best per type)
- `member_stats` WHERE member_email — baseline_30d averages per activity type, at_risk, needs_support, programme name + week + active flag
- `wellbeing_checkins` ORDER BY created_at DESC LIMIT 4 — weekly_mood_last_4 scores
- `daily_mood_checkins` WHERE mood_date >= today-7 — daily_mood_7d array (null for missing days)
- `monthly_checkins` ORDER BY created_at DESC LIMIT 1 — last monthly dimension scores (sleep/stress/energy/social/physical)
- `member_health_daily` WHERE date >= today-7 — steps per day (HealthKit members only)
- `member_health_samples` WHERE sample_type='sleep' AND start_at >= today-7 — sleep per night (HealthKit only)
- `session_views` + `replay_video_views` WHERE logged_at >= week_start — session_types watched this week

HealthKit null-safety (applied during assembly, before prompt build):
- Sleep: count nights with data in the week. If < 3 → sleep_avg_hrs: null, sleep_data_confidence: 'insufficient'
- Steps: if weekly sum < 500 (phone left home) → steps_this_week: null
- No HealthKit connection → health_kit: null
- AI prompt instruction: never reference any field that is null — skip it entirely

Derive server-side before call:
- tone_required: positive branch → 'affirming', negative → 'empathetic', neutral → 'balanced'
- returning_member: days_since_last_activity >= 7
- branch: mood >= 7 AND >= 2 positive dims → positive; mood <= 4 OR stress=high OR energy=low → negative; else neutral

Pre-filter habits list to 6-8 relevant to branch + improvement_focus (do not pass all 34 to the model). Pre-filter content list to 4-5 relevant options.

VERIFY: real invocation against a member with full data. Check null fields absent from output. Check tone on negative branch.

**4. wellbeing-checkin EF — Anthropic call + response parsing**

Model: claude-sonnet-4 (keep — quality matters on empathetic cases).
Max tokens: 600 (up from previous cap).

Update system prompt with full AI hard rules:
1. tone_required mandatory — never open with positivity when empathetic
2. Compare against member's own baseline_30d, never an abstract ideal. If baseline cardio = 0, never flag missing cardio.
3. daily_mood_7d contrast detection — mostly high trend + low weekly mood = likely one hard day, say so. Consistently low trend = acknowledge and support, no contrast framing.
4. monthly_scores_last for pattern detection — sleep low 2+ months = name the pattern. Improved = acknowledge progress.
5. Reference this_week vs baseline: "two workouts is ahead of your usual" not "you only did two workouts"
6. Active streaks are motivational fuel — reference them. Never mention a streak of 0.
7. Null fields are invisible — never reference absent data.
8. at_risk / needs_support flags silently shift tone before member answers anything.
9. 5-8 sentences flowing prose — no bullet points, no headers, no lists.
10. End with one concrete suggestion tied to improvement_focus.
11. New line: HABIT: [name from filtered list] — [one sentence why]
12. New line: CONTENT: [name from filtered list] — [one sentence why]

Parse response server-side: split on HABIT: and CONTENT: lines, return as structured fields (debrief_text, habit_name, habit_reason, content_name, content_reason).

**5. wellbeing-checkin.html — Question flow rebuild**

Replace single-slider flow with 5-step branching flow:

Step 1: mood slider 1-10 (unchanged UI)
Step 2: dimension taps — 4 cards (Energy/Sleep/Stress/Body), 3 options each, single-tap select. Labels per Lewis sign-off.
Step 3: branching driver question — multi-select chips. Prompt text + options set by branch computed from steps 1+2. Same branch thresholds as server.
Step 4: improvement question — single-select chips. Prompt adapts (positive vs neutral/negative). Positive branch includes "Nothing — I'm happy with everything" option.
Step 5: free text optional (unchanged).

Progress indicator shown across all steps.

New member grace: if days_since_joined < 7 → show "come back on [date]" state, no flow.
Returning member: if days_since_last_activity >= 7 → show wrapper card before step 1: "Welcome back — we've missed you. Take a moment to check in."

**6. wellbeing-checkin.html — Results screen**

Three components rendered after EF response:

6a — Activity recap strip: habits/workouts/cardio/sessions this week vs goals. Read from EF response. Simple count display, no AI.

6b — 7-day mood graph: bar chart from daily_mood_checkins last 7 days. Y axis 1-5 (Not great/Meh/Good/Great/Amazing). Graceful "no data" state if < 3 entries.

6c — AI debrief: prose paragraph. Below it two cards:
  - Habit card: habit_name + habit_reason + "Manage my habits →" deep link to settings habits section
  - Content card: content_name + content_reason + link to sessions page

**7. sw.js + vbb-marker atomic bump**

CACHE_NAME suffix bump + vbb-marker +1 in index.html AND settings.html in same commit. PM-299 invariant.

**8. VERIFY end-to-end**

- New member (< 7 days joined): grace state shown, no flow
- Returning member (>= 7 days inactive): wrapper shown before step 1
- Positive branch: affirming opener, "nothing" option present, habit from movement/nutrition pot
- Negative branch: empathetic opener, never upbeat, habit from sleep/mindfulness pot
- HealthKit member, patchy sleep (< 3 nights): sleep not referenced in debrief
- Non-HealthKit member: no HK fields referenced anywhere
- at_risk=true member: tone pre-shifted before questions answered
- Monthly cadence slice: SKIP until Dean locks cadence decision

---

### KEY FILES

- wellbeing-checkin.html (full page rebuild)
- wellbeing-checkin EF (signal assembly + Anthropic call + response parsing)
- DB migration (wellbeing_checkins new columns)
- checkin_questions (new weekly version row)
- sw.js + index.html + settings.html (vbb-marker + cache bump)

---

### COST

~0.68p per check-in on Sonnet 4 (~1,100 input + ~350 output tokens). Capped once/week. ~£14/month at 500 members. No optimisation needed until 5,000+ active check-ins/month.

---

### DECISIONS LOCKED 2026-06-05

- One auto-routed entry replaces separate weekly + monthly surfaces
- One store (wellbeing_checkins) + type discriminator — no forked storage
- Weekly deepened to branching multi-dimension flow (~45 seconds)
- Branch thresholds: mood >= 7 + 2 positive dims = positive; mood <= 4 OR stress overwhelming OR energy drained = negative; else neutral
- Habit recommendation: suggest only (Option A) — one tap to settings habits section. No auto-swap, no cap enforcement in this flow.
- Content recommendation: one link to relevant session/replay category
- AI signal enriched with full member context: home_state + stats + trends + HealthKit
- HealthKit null-safety: < 3 nights sleep = don't reference; < 500 steps = don't reference; null health_kit = skip entirely
- Baseline = member's own 30d average, never an abstract ideal
- tone_required derived server-side from branch
- 5-8 sentence debrief, flowing prose, no bullet points
- Sonnet 4 for all check-in AI calls
- New member grace: < 7 days joined, no check-in
- Returning member: >= 7 days inactive, wrapper before step 1
- Monthly cadence: LOCKED — calendar-anchored. First check-in on/after 1st of month = monthly (absorbs that week's weekly). Late-join grace: joined within last 14 days of month → skip that monthly, weeklies only, first monthly at next 1st.

---

### DEFERRED

- Monthly cadence routing — LOCKED calendar-anchored (see decisions). Build this slice in same session as weekly build.
- Live/replay merge — separate gated backlog item, do not touch


## QUEUED BUILD — Habits autotick v2 (Dexie-first instant + server history-backfill) — spec'd PM-477, build pending

**Status:** fully spec'd, NO code shipped. Build in a fresh session. Production-affecting (a live hub page on the whole server.url cohort + a production EF + a new Dexie store + a habit_library data change + history-backfill). Execute in order below; talk-first only if something here is genuinely unclear. Claim fresh PM number(s) at commit time. Full reasoning in changelog PM-477.

WHY: home shows a fake "8,000 steps" under the 10-min walk and no live Apple Health progress (both = the subForHabit stub); habit autotick is today-only while activity credit already backdates. We're making habits tick instantly from Dexie and backdate like workouts do.

BUILD ORDER:
1. WALKING-WORKOUT RULE (data + server parity).
   - habit_library "10-minute walk": replace health_rule {op:gte,agg:sum,value:1,metric:distance_km,source:daily,window:today_local} with a discrete walking-workout rule — member_health_samples sample_type='workout', workout_type='walking', duration (end_at-start_at) >= 10 min (600s), window today_local. Define the new shape (e.g. source:'samples_workout', workout_type:'walking', agg:'duration_minutes', op:'gte', value:10).
   - member-dashboard EF: add evaluator branch for the new workout-duration shape so server reconciliation matches client. VERIFY by real invocation against a member with a walking workout today (188 walking samples exist to test).
2. LOCAL EVALUATOR (new shared JS, e.g. health-eval.js).
   - Pure fn (health_rule, hk_snapshot, dexie_local) -> {satisfied, progress:{value,target,unit}}. Mirror member-dashboard logic EXACTLY (JS<->SQL parity). Handle all live shapes: steps gte (daily), distance, sleep duration last_night, walking-workout duration today, plus activity-table rules (30-min cardio, complete-a-workout) read from Dexie cardio/workouts (already local).
   - Sleep progress emitted in HOURS (value/target 1dp + 'hrs'), never minutes (rule value stays 420 internally).
3. DEXIE HK SNAPSHOT STORE (NEW store — never re-key an existing store, §23.83).
   - healthbridge.js caches today's aggregates each sync: steps, distance, sleep_minutes, walking-workout total/longest duration. New Dexie store + additive db.version bump. FALLBACK_* const for cold paint. Lets index/habits paint progress instantly from local before the plugin re-reads.
4. WIRE index.html + habits.html TO THE LOCAL EVALUATOR.
   - Both pages: read HK snapshot from Dexie -> run local evaluator -> instant progress + optimistic autotick write (daily_habits Dexie-first, REST sync background; reuse existing write path + synthetic key member|date|habit).
   - DELETE subForHabit() in index.html (~L2176) and its call site; home habit rows render real health_progress via formatProgress parity with habits.html.
   - sw.js precache + vbb-marker (index + settings) + CACHE_NAME bump in the SAME commit (PM-299 invariant).
5. SERVER HISTORY BACKFILL (reconciler).
   - On sync and/or a member-dashboard pass: walk synced HK history (member_health_daily per-date steps; member_health_samples dated workouts/sleep) and write backdated daily_habits autotick rows per qualifying day.
   - ANCHOR floor = max(member join, habit_assigned_at, FIRST GENUINE ENGAGEMENT). First engagement = earliest real VYVE activity (first daily_habits/workout/cardio/session/checkin), NOT first login, NOT the Stripe date.
   - Only write (member, day, habit) with NO existing daily_habits row (never override manual yes/no/skip). Respect 1/day cap (over-cap -> activity_dedupe).
   - UNIFORM across B2C/B2B (§23.93). Backfill retroactively rebuilds streak/score/certificates/charity for those days (desired — real activity; a returning member may get a streak/certificate on login).
6. VERIFY end-to-end: member-dashboard invocation for the walking rule; home shows live progress + auto-ticks on open; backfill writes only within the engagement window and skips manual rows.

KEY FILES: index.html (subForHabit ~L2176, loadHabits ~L2031, renderHabitList ~L2108), habits.html (runAutotickPass, formatProgress ~L680, member-dashboard v51 fetch), healthbridge.js (foreground sync, queryAggregated/queryWorkouts, _localTodayDate), db.js (Dexie SCHEMA_V22 — add new store as next version), member-dashboard EF (rule evaluator + health_progress), habit_library (the walk rule row). Autotick habits with rules: 8k steps, 10k steps, sleep 7+ (420min/last_night), 30-min cardio (activity_tables), complete-a-workout (activity_tables), + new walking-workout walk.

## Thumbnails / Storage / deployment cleanup (added 2026-06-04, PM-475)

- **Delete one-shot seed-host-thumbnails EF.** Seeded the session-thumbnails bucket from GitHub-pinned bytes (commit 153567ad); job done, EF is now dead weight. No MCP delete tool — remove via Supabase dashboard or CLI.
- **Remove dormant /assets/hosts/*.jpg repo copies** from vyve-site — now unreferenced (image_url points at Storage). Tidy, low priority, harmless if left.
- **Calum / Alan / Shan host thumbnails** are staged in Storage but unused until those hosts are scheduled in calendar_occurrences.
- (carried) rotate the exposed service_role key; deactivate ~40 stale empty-notes calendar_occurrences rows; move the live runner off Dean's Mac to a real 24/7 box. (Calendar regeneration DONE PM-686 — next regen due before 2 Aug.)

## Replays follow-ups (added 2026-06-04, PM-465)

- **Move playlist-insert pre-create → completion (optional hardening).** `session-publish` adds each broadcast to its playlist at creation (best-effort). The §23.91 duration guard already stops not-yet-aired (P0D) items surfacing, so this is belt-and-braces: inserting at the broadcast-complete transition (runner / broadcast-status) means playlists only ever contain aired sessions.
- **Replay ordering.** `refresh-replay-videos` labels "latest" by `published_at` = playlist-ADD time, not air time. The just-backfilled older Yoga `d-dNe6W-o4I` now ranks above today's `9b-xSEfEIKc`. If air-time order matters, sort by a stable recorded/scheduled date.
- **Dev-test record.** `JEFNPGKhQqY` ("PM-327 Device Walk") still carries a broadcast_id on its occurrence; excluded from replays by the title filter — null it for full detachment if desired.
- **`replay-playlist-backfill` EF** retained (idempotent, `?dry=1`, eligibility-guarded). Delete when no longer needed.

## Live content go-live (added 2026-06-02, PM-437)

### NEXT — live content go-live (PM-439, 2026-06-02) — continue here
- [x] Masters renamed via ~/vyve_rename.py (truncation gotcha logged).
- [x] 30-day calendar WIRED + CONTENT APPLIED PM-451: all 116 active rows (4 Jun–2 Jul) carry final titles (Part->Session; Flexibility Route/Routine->Session), Dean-approved live-voiced session_description, host_name filled (Alex/Nicola/Lewis/Lucy/Jamie), and image_url thumbnails. 5 host/type cards committed to vyve-site assets/hosts/ (commit cecf2f7f). Thumbnails mapped by session TYPE not per-host (Nicola's card is breathwork-branded but she hosts most yoga/pilates).
- [x] `*-live.html` YouTube broadcast-status probe (override clock-only LIVE gate) — SHIPPED PM-445 (broadcast-status EF + session-live.js effectiveState; §23.65 resolved). live:true branch needs real-push device walk.
- [x] Token-health monitor — LIVE PM-447: EF `youtube-token-health` + cron job 35 (`0 4 * * *`), refresh+authed-call probe, send-email alert to team@ on failure. Real-send branch unexercised.
- [ ] Stand up the always-on box; place vyve-live-runner.py; turn OFF session-publish hourly cron once box owns creation.
- [ ] PM-451: confirm the 5 host-card thumbnails render in-app (sandbox can't reach online.vyvehealth.co.uk — 403 even on logo.png; files verified present via raw GitHub API). Do NOT re-host on a sandbox 403 (§23.86).
- [ ] PM-451 decision: keep TYPE-based thumbnails or flip to strict per-host (one UPDATE on calendar_occurrences.image_url); also decide whether to drop numeric labels on flows/flexibility titles.
- [ ] PM-451: wire runner/session-publish so each created YouTube broadcast sets title + description from calendar_occurrences AND uploads the matching assets/hosts/<card>.jpg as the YouTube thumbnail (currently uploads the per-session frame card).
- [x] PM-453: home "live sessions this week" carousel now reads real upcoming sessions from calendar_occurrences (titles/hosts/thumbnails); static sessions-data.js fallback on cold boot, upgrades once Dexie hydrates (vyve-site 44de2cf9). node --check passed.
- [x] PM-455: FIX home carousel — PM-453 read calendar_occurrences via `.all()` (undefined on catalogue stores → threw → silent static fallback = the "old sessions" Dean reported). Corrected to `.allFor(null)` (vyve-site 0a242751). §23.87 added. Still gated on device loading the new index.html + Dexie having synced the calendar.
- [x] PM-456: FIX home carousel thumbnails — blank because a parallel session made calendar_occurrences.image_url root-relative (/assets/hosts/..) and the thumb builder prepended a 2nd slash (//assets.. = broken host). Builder now leaves leading-slash paths intact (vyve-site 63c73dbd). node --check passed. image_url is root-relative going forward.
- [ ] 3 missing talk videos: "Why I Founded VYVE", "Doing Hard Things", "Not Drinking Alcohol".
- [x] Place vyve-live-runner.py into a repo — DONE PM-446: VYVEBrain scripts/vyve-live-runner/ (runner + systemd + launchd + env.example + README; reconstructed from the PM-439 autostart-dead model). Not yet smoke-tested on the box.


- [ ] Resolve a category RTMP ingest+key live via YouTube Data API liveStreams.list(part=cdn) (Yoga/Pilates & Stretch first).
- [ ] ONE test simulated-live push from Dean's Mac -> confirm session-publish binds broadcast + playlistItems.insert + replay appears.
- [ ] Seed Replays: upload back-catalogue (53 Movement -> Yoga/Pilates & Stretch playlist, 21 Mind -> Mindfulness & Mindset playlist) so Replays is populated for sales now.
- [ ] Stand up always-on ~GBP4/mo box (Dean creates/pays; Claude configures); get riverside_ masters onto it once.
- [ ] Wrap simulated-live-worker.py into a multi-session scheduler reading calendar_occurrences.
- [ ] Wire the 30-day calendar into calendar_occurrences (map readable titles -> riverside_ filenames).
- [ ] BLOCKED: Suicide and Men airing -> Phil clinical sign-off + crisis signposting (§23.84).
- [ ] PENDING: 6th Lewis video; morning_stillness classification.

## Trial / membership (added 2026-06-03, PM-438)

- [ ] DEAN (Stripe dashboard, closes the loop): register webhook endpoint `https://ixjfklpckgxrwjlfsaaz.supabase.co/functions/v1/stripe-webhook` (events `checkout.session.completed` + `customer.subscription.deleted`) and set its signing secret as `STRIPE_WEBHOOK_SECRET` in Supabase Edge Functions secrets. Until set, payments succeed but membership won't flip (webhook fails safe).
- [ ] DEAN: confirm "Allow promotion codes" stays ON for the conversion Payment Link (`bJeeVe0Cs1Em53D5oh93y01`) else £20 is charged instead of £10.
- [ ] Read-path EF enforcement: add the membership gate to `member-dashboard` (and any other read EFs) so an expired member can't pull data even if the client gate is bypassed. Deferred at PM-438 (write-guard + client gate judged sufficient for trial; core EFs are 400+ lines, hand-redeploy = transcription risk).
- [ ] Extend the write-guard (`assert_member_not_expired`) beyond daily_habits/workouts/cardio/session_views to: mind_activities, movement_activities, connect_checkins, wellbeing_checkins, monthly_checkins.
- [ ] Web "Start free trial" sign-up flow (inverts the current pay-first signup) — NOT built (was step 6). Needed before public trial marketing.
- [ ] `continue.html` no-`cid` path = manual reconcile (rare; member reaches checkout without a bound id). Decide auto-handling later.
- [ ] GITHUB_PAT_CLAUDE expires 20 Jun 2026 — rotate before expiry (Vault `GITHUB_PAT_CLAUDE` on project ixjfklpckgxrwjlfsaaz).
- [ ] PM-476 (design locked, BUILD PENDING): Blended "easy first" welcome milestone — ANY 30 activities (sum of the five `get_certificate_buckets` buckets) funds the first donated month. New one-shot derived metric; NO new daily/weekly cap (capped buckets give 7/day ceiling, ~5-day floor). Framed openly as easy-first; per-pillar rule resumes after.
- [ ] PM-476: Milestone CELEBRATION sheet on threshold-cross (Member Prompts infra PM-375) — charity win + offer AVAILABLE ("lock in whenever ready"), NOT a hard ask. Keen members can self-convert here.
- [ ] PM-476: Day-~24 timed conversion nudge before the wall — urgency-led, NO "you earned a free month" claim (members may not have hit the milestone).
- [ ] PM-476 DECIDED: 30-day trial length stays (not 14); £10-off-FOREVER is the standing conversion price (resolves §22); check-ins count toward the blended 30 (recommendation, Lewis to confirm).
- [ ] PM-476 GATE: named charity partner unconfirmed — plural "charity partners" copy claim is load-bearing; settle named partner or soften wording before member-facing. Lewis.
- [ ] PM-476 ENG (Dean, future price rise): keep the £10-off lifetime lock by updating price on the SAME subscription / persisting subscription-level discount — never migrate converters to a fresh Stripe Price without re-attaching VYVE10.

# VYVE Health — Backlog

Last triage: 2026-05-26 (Chat 3 of brain overhaul). Recomposed from ~5,923 lines / 206 headings / 480 sub-headings of chronological journal into surface-bucketed live items. SHIPPED blocks dropped against changelog. Stale "Added" items pre-14 May dropped per triage rule. Bus-campaign chronicle (PM-30 → PM-65, 08–11 May) dropped — all shipped, surface state in master §19.

## NEXT FOCUS — Movement V2 CLOSED (28 May). Pick next from queued items below (PM-308 cardio walking removal, Achievements post-trial overhaul, In-App Tour, Layer 2 Realtime bridge).

**Movement V2 is member-reachable.** Step 4a-pre-2, 4a-page (Chunk B render scaffolds), 4b (multi-active-by-surface), and the plan-picker all SHIPPED 27-28 May (see changelog). Live flow: Body → Movement → State 5 "Start a movement programme" → `movement-plans.html` picker → pick Just Steps / Foundation / Distance Builder → set target → Start → movement.html renders step ring + week card. Members can hold a workouts plan AND a movement plan concurrently.

**SHIPPED 28 May:** PM-420 4c — Today's Movement card (`dc1108c5`), Sport pill (`f6aa4131`), Add Activity modal (`eaaf8683`). PM-420 4d — plan-fit nudge: `evaluate_plan_fit()` SQL-function cron (`vyve-evaluate-plan-fit` 04:00 UTC, jobid 32) + gold banner + carryover modal (`ec8bcffa`). All member-facing surfaces + the cron done. **PM-425..431 (28 May) closed the rest: Movement V2 is ~100% functionally complete.**

**Movement V2 — DONE (PM-425..431, 28 May):**
- ⚠️ **Dexie wpc PK→id bump (PM-425 `fb5e91e9`) — REVERTED PM-436 (`e44b2357`)** — re-keying the existing store jammed the iOS upgrade → noop shim → blank rings/habits/progress on every v21 device (§23.83). Reverted to `member_email` PK. Multi-plan-local mirror deferred; re-do via a NEW id-keyed store + device test (Option B, below), never re-key.
- ✅ **Picker → portal standard + instant start (PM-426 `030bab25`, PM-427 `a6c6b431`)** — theme.css/nav.js/cache-first paint; `startPlan` mirrors the new active row into Dexie so the home page reflects the switch instantly; Just Steps start fixed (`plan_duration_weeks` NOT NULL → sentinel `0`).
- ✅ **Render all plan shapes (PM-429 `74e3cb6d`, PM-431 `326532d4`)** — `renderPlan` routes any sessionless movement plan to the state layer; was bailing to no-plan for Just Steps then locked_ramp (§23.81 hard rule). prog-card names locked-ramp plans.
- ✅ **Live baseline recompute (PM-428 `7d2bc123`)** — `recompute_step_baselines()` SQL fn + cron jobid 33 (04:10 UTC); picker slider ceiling/default fixed. Modal opacity + editable Just Steps target (PM-430 `ee9ba078`).
- Post-trial backlog (not blocking): Return to Movement plan still INACTIVE pending Phil clinical sign-off; prompt-pool copy is Claude placeholder pending Lewis sign-off; Achievements-style UI polish pass on the movement ring.

**Movement V2 state matrix reference** (`<body data-mv-state="N">` on movement.html): 1=structured+HK, 2=structured+no-HK, 3=Just Steps+HK, 4=Just Steps+no-HK, 5=no plan. CSS-only show/hide off the body attr. Branch on `programme_json.surface==='movement'` (NOT category — spec drift, see changelog). plan_type values are `just_steps` / `locked_ramp`.

**Original Thursday pickup paragraph (still valid background):** Two binaries are with the platforms. iOS 1.4 build 3 sitting "Waiting for Review" in App Store Connect (auto-release on approval, 24–48hr expected). Android 1.0.5 versionCode 50 — needs confirmation Dean clicked "Send 1 change for review" from Play Console publishing overview (Pending #1 below). Assume approved by the time the next session opens; otherwise unblock by clicking through.

The session-opening question is the architectural decision PM-411 Item 1 Bug 3 / PM-413 Pending #5 left unresolved: capacitor.config.json is hybrid dev-loop + LiveUpdate today. Members on iOS 1.4 / Android 1.0.5 will be dev-loop mode, not bundled, contradicting §23.42. Real call between (a) proper bundled mode with @capawesome/capacitor-live-update on the prod channel, (b) accept dev-loop for trial cohort, (c) other. Talk-first; this drives the Mac-local sync work below.

Once doctrine is locked, do the Mac-local audit and selective commit per PM-413 Pending #2 — diff `~/Projects/vyve-capacitor` against remote `7a54c876`, curate legitimate ship-state changes (capacitor.config.json, build.gradle 1.0.5/code 50, Info.plist iPad orientation restore, package.json + lockfiles for @capawesome/capacitor-live-update@8.2.2, Android #0D2B2B background, regenerated mipmaps, Xcode/SPM lockfiles), atomic-commit. While there, knock out the small ones: .gitignore `www/` entry per the vyve-capacitor-mac-sync playbook (PM-413 Pending #4), Mac-local junk cleanup (literal `.git`/`.github` files + 7 .bak files at vyve-capacitor root, PM-413 Pending #3), and the keystore + password `Weareinthis2026!` into 1Password (P0 launch blocker — if Dean's Mac is wiped before keystore + password are co-located, the Android app is un-shippable for the lifetime of `co.uk.vyvehealth.app`).

Then PM-411 Bug B — the surgical 30–45min one. Workout selection doesn't update until reload. PF-7 Dexie stale-read race in `workouts-programme.js` L78–89. activateProgramme correctly clears cache + nulls programmeData + cacheRow + calls loadProgramme, but loadProgramme's Dexie-first path calls criticalHydrate un-awaited then reads Dexie immediately — Dexie still has the OLD plan because sync hasn't propagated. Single-file fix: await criticalHydrate, OR skip Dexie on cache-bust contexts, OR invalidate Dexie row before reading.

Post-binary the canonical NEXT pointer is the second pass of "in-app feature completeness" from PM-401.b: monthly check-in credit gap (real bug, members get zero credit today — see Infra / Data bucket), then certificates → leaderboard parity, then Your Journey scope decision (design talk before build). Bug A architectural (movement plan structurally homeless, 4-6h) sequences after trial data lands. Bug C (Browse Library runtime error) needs device console — schedule when Dean's at a Mac.

## PARKED — PM-411 — Body-hub overhaul Bug A/B/C

(Item 1 Bundle-prep CLOSED in PM-413. Item 2 + schema-architecture note preserved verbatim below.)

Surfaced during deanonbrown2@gmail.com end-to-end onboarding walk this session.

**Bug A — Architectural (4-6h, post-trial)**: Movement plan structurally homeless. exercise.html hero CTA L350 hardcoded `href="workouts.html"` ignoring programme category. Every workout_plan_cache row has `category: null` — no branching surface exists. Fix needs:
- programme_library category backfill
- Onboarding EF v37 writes category into workout_plan_cache
- exercise.html branches: `category === 'movement'` → movement.html, else → workouts.html
- movement.html consumes + displays programme card at top (currently only has session-logging pills + Mark as Done button)

Note: movement.html L440-486 already filters `category === 'movement'` — it's READY to consume categorised plans, nothing's writing them.

**Bug B — Surgical (30-45min, Thursday)**: Workout selection doesn't update until reload. PF-7 Dexie stale-read race in workouts-programme.js L78-89. activateProgramme flow correctly clears cache + nulls programmeData + cacheRow + calls loadProgramme, but loadProgramme's Dexie-first path calls criticalHydrate UN-AWAITED then reads Dexie immediately — Dexie still has OLD plan because sync hasn't propagated. Single-file fix: await criticalHydrate, OR skip Dexie on cache-bust contexts, OR invalidate Dexie row before reading.

**Bug C — Surgical (30-60min, needs device console)**: Browse Library tab broken at runtime. Static check clean — switchTab + loadLibrary + DOM + RLS + 30 programmes + CATEGORY_LABELS + loadPausedPlans all present. Failure is runtime JS error swallowed by outer try/catch. Candidates: getJWT undefined for new accounts, VYVEData API drift, async error in renderLibrary first-paint.

### Schema-architecture note (banked, not codifying solo)

`workout_plan_cache` has TWO contradictory UNIQUE indexes:
- `workout_plan_cache_member_email_key` UNIQUE on `(member_email)` — blocks multi-row
- `workout_plan_cache_one_active_per_member` UNIQUE on `(member_email) WHERE is_active=true` — assumes multi-row design

workout-library EF v13 paused-plan logic at L60-84 likely never works correctly — upserts at L98-110 `onConflict: 'member_email'` silently overwrite the previous plan due to the broader UNIQUE constraint. Empirically confirmed on test account: 1 row only, no paused Movement preserved after swap to Strength. Promotes to §23 on second contradictory-UNIQUE occurrence.

## PM-420 — Movement V2 build (PM-418 was the spec lock; PM-419 collided on Focus card fix)

**Spec locked PM-418, 27 May 2026.** Full design captured in `playbooks/movement-v2-spec.md`. Build chats read playbook once and execute — no design re-derivation needed.

**Scope.** Closes PM-411 Bug A (movement plan structurally homeless — category backfill + branching) and goes well beyond it. Adds the four-plan library (Just Steps / Foundation / Distance Builder / Return to Movement), state-aware `movement.html` (5 render variants), HK history pull at consent (90-day baseline via Capgo `queryAggregated`), plan-fit nudge intelligence, manual-step support for non-HK members, and full ecosystem wire (bus + Supabase + 10-surface completeness test).

**New §23 hard rules:** §23.78 (PM-420 step 4a-pre-1) — CHECK constraint write-surface audit before adding to pre-existing tables (earned the hard way via the production movement quick-log outage). The engagement-aggregator triple-occurrence rule originally slated as §23.78 — every new activity table requires touching SQL function + JS twin + dirty-mark trigger + tile renderers + charity reconcile + 10-surface completeness test — still needs its own number; will land as §23.79 when codified.

**Build sequence (7 steps, ~6-7 Claude-assisted sessions):**

1. ✅ **SHIPPED PM-419** — `programme_library.surface` column added (CHECK in `workouts` / `movement`), backfilled `workout_plan_cache.programme_json.surface` from existing plan_type. Onboarding EF v85 stamps surface. Naming: used `surface` not `category` (overrode playbook).
2. ✅ **SHIPPED PM-420 step 2** — Four movement plans seeded into `programme_library`: Just Steps (active), Sedentary Reset Foundation (active), Distance Builder (active), Return to Movement (INACTIVE pending Phil clinical sign-off). Prompt copy is Claude-drafted placeholder pending Lewis sign-off.
3. ✅ **SHIPPED PM-420 step 3** — HK 90-day baseline history pull at consent. EF `pull-baseline-steps` v1 deployed (verify_jwt:true). `healthbridge.js` v0.7→v0.8 with `pullBaselineHistory()` runs 3×30-day Capgo windows, fire-and-forget from `connect()`. Members get `baseline_steps_p50/p25/p75` + `baseline_source` + `baseline_activity_band` stamped in 2-3s post-Allow.
4. **IN PROGRESS PM-420 step 4a-pre-1** ✅ + **step 4a-pre-2** PARTIAL:
   - ✅ `movement_activities` extended (7 new columns: display_name, manual_steps, counts_for_charity, hk_native_uuid, hk_promoted_to, prompt_kind, metadata JSONB)
   - ✅ Source CHECK loosened to accept legacy 'manual' + new vocabulary (hot-fix from §23.78 trap — see master.md)
   - ✅ `manual_step_estimates` new table with RLS + email-lc trigger
   - ✅ Onboarding EF v86 deployed (deactivate-old + insert-new pattern, replaces upsert-via-on_conflict)
   - ❌ PENDING: `workouts-session.js` line 850 site patch (`&is_active=eq.true` filter add) — staged at `/home/claude/site/staging/`
   - ❌ PENDING: drop `workout_plan_cache_member_email_key` migration (full unique on member_email)
   - ❌ PENDING: `movement.html` V2 page build (the big mockup → production translation, est ~2 sessions)
5. Plan-picker page with smart sort by baseline fit + Just Steps slider with safeguards (1.3× warning, 1.5× cap) + adaptive toggle (~1 session) — folds into 4a-page since picker IS one of the 5 render variants
6. `evaluate-plan-fit` daily cron at 04:00 UTC + nudge banner UI + plan-up acceptance flow preserving streak + certificate state. Plan-down is NOT automatic — Phil-shaped human check-in only (~1 session)
7. Wire & subscribers audit per §23.78 (engagement aggregator triple-occurrence rule, not the new §23.78 CHECK-audit rule) — repo-wide grep for `movement:*` / `body:*` / `movement_activities` / `workout_plan_cache` / `baseline_steps_p50` subscribers, update SQL function + JS twin (`compute_engagement_components_v2` + `computeEngagementComponentsV2`), update `v_active_days`, add dirty-mark triggers on new tables, update charity reconcile (`charity_total_reconcile_and_heal()`), run 10-surface completeness test (~0.5-1 session). Also: `movement.html` quick-log writes should switch source literal `'manual'` → `'manual_log'` so the loose CHECK can be tightened back.

**Phil sign-off gates before ship:**
- Return to Movement plan content (audience overlaps post-injury / post-pregnancy)
- Just Steps slider max caps (especially Return to Movement plan max)
- Plan-fit nudge copy variants for each source plan
- HK consent disclosure copy addition

**Lewis copy gates:**
- 5 Today's Movement prompt text lines (one sentence each, generic — no curated content)
- 4 plan descriptions (library cards)
- Plan-fit nudge copy variants per source plan

**Out of scope for v1** (documented in playbook §13): Office Worker Mobility as distinct plan (folded into Foundation), automatic plan-down, adaptive bumps for manual-estimate members, curated mobility video content (post-trial), Android Health Connect (parked until device), real-time plan-fit evaluation (cron-driven correct starting shape).

**Mockup ship target:** `/mnt/user-data/outputs/movement-v1-final-spec.html` from the PM-418 session — source of truth for visual design. Playbook source of truth for logic.

**Dependencies:**
- Sequenced AFTER PM-413 (iOS 1.4 / Android 1.0.5 store approval) — trial cohort first
- Sequenced AFTER PM-411 Bug B (workout-library Dexie stale-read race) — Thursday-grade surgical fix
- Reads playbook `playbooks/movement-v2-spec.md` as single-read spec reference

## PARKED — Original PM-411 carry-over notes preserved

## Pre-bundle / app store (PM-413 follow-ups)

**Play Console final click (P0, confirm next session).** Confirm `In review` not `Draft` on play.google.com/console for VYVE Health Android. If still `Draft`, click `Send 1 change for review` from publishing overview.

**vyve-capacitor remote sync (P0 Thursday).** Mac local diff against remote `7a54c876` and atomic-commit per playbook — full file list in NEXT FOCUS above. Per the vyve-capacitor-mac-sync playbook, run BEFORE any new native edits to avoid layering more uncommitted state.

**capacitor.config.json doctrine — RESOLVED (PM-569, 9 Jun 2026): bundled mode (option a) chosen and SHIPPED.** iOS 1.7 is LIVE with `server.url` REMOVED → iOS members are bundled/frozen, updatable only via a Capawesome OTA (app `f9961f66-eb66-4102-b1c5-f9b2c7baeebf`, prod channel `89e12796`). Android bundled 1.0.6 vc51 still in review (Android members stay server.url-live until it lands). **P0 NATIVE PRIORITY — verify ONE Capawesome OTA push end-to-end (`--rollout 0.1` canary).** `live-update.js` OTA wiring shipped (PM-600) and bundled in iOS 1.8 + Android 1.0.7 (both submitted 11 Jun 2026, PM-602, in review). Once 1.8 approved + installed on a real non-Dean device: trigger `ota-deploy.yml` from vyve-site GitHub Actions (rollout=10), watch Capawesome Logs, verify vbb marker updates on cold start. That closes §23.106. Hard pre-Sage gate. Also outstanding: vyve-capacitor REMOTE is BEHIND Mac-local — the 1.5/1.6/1.7 ship-state is uncommitted on remote (latest remote commit PM-560 splash); curate + atomic-commit Mac-local → remote per PM-413 Pending #2.

**Mac local junk cleanup (P3).** `--exclude=.git` + `--exclude=.github` literal files (typo from earlier session) + 7 `.bak-pf14b` / `.bak2` / `.bak3` / `.bundled-backup` files at vyve-capacitor repo root. Tidy-up, not a blocker.

**`.gitignore` `www/` entry per playbook (P1).** Currently `www/` is rsync'd in from vyve-site but not committed. Not yet a problem because remote has no www/, but codify before the audit-and-curate pass to avoid accidentally committing 171 sync'd files.

**Keystore + password `Weareinthis2026!` into 1Password (P0 launch blocker).** Dean's 30s manual. Path `~/Projects/vyve-capacitor/android/app/keystore/vyve-release-key.jks`, PKCS12, alias `vyve-key`, SHA1 `CC:48:EA:AF:C1:47:ED:43:20:63:4F:FF:07:99:79:20:55:7D:23:B9`. If Mac is wiped before keystore + password are co-located, Android app is un-shippable for the lifetime of `co.uk.vyvehealth.app` on Play Store.

**CLOSED PM-954 (1 Sep 2026): `vyve-capacitor` git repo existed and was current all along — canonical checkout now `~/vyve-capacitor-git`.** Original escalation kept for history: Cumulative uncommitted edits across `build.gradle`, `variables.gradle`, `keystore.properties`, `local.properties` over iOS + Android ships sit in unversioned working tree. `.bak-pf14b` / `.bak-pf14b-android` / `.bak` files are the only history. Any accidental file overwrite is unrecoverable.

**iOS encryption answer (banked).** Dean chose "Standard encryption" against research-backed advice of "None of the algorithms" for HTTPS-only apps (per Apple Developer Forums + official guidance). Standard requires export compliance documentation if Apple ever audits. Likely fine; if Apple asks for export docs post-approval, that's the audit trail.

**iOS App Review notes scan (P2, next submission).** Residual "PWA-based" framing was caught and rewritten this session; per §23.20 the product is no longer a PWA. Future submissions: scan App Review Notes for inherited PWA / "thin web wrapper" / "web shell" framing that could trigger Apple Guideline 4.2. Always describe as "native iOS app built with Capacitor wrapper".

**R8/ProGuard enable (P3, post-trial polish).** Play Console Warning 3 of 4 from PM-413 — no deobfuscation file. Future build improvement; makes crash stack traces more readable.

**Native debug symbols (P3, post-trial polish).** Play Console Warning 4 of 4 — no native code debug symbols uploaded. Same shape as ProGuard, harder to debug native crashes if they happen.

**Debug strip gating (P1, pre-next-binary, escalation pending home-surface verification).** PM-310 always-on live-tracker debug strip across the 8 `*-live.html` shells (via `session-live.js`) + `?debug=tracker` mind-video strip (via `mind.html`). Gate behind `localStorage.getItem('vyve_debug_tracker') === '1'`. **mind.html surface CLOSED PM-424 (`9300a0bf`)** — the PM-324 strip was leaking onto Today's Focus meditation play because PM-418 gated the open-time call but an ungated 1s poller re-inserted it; now guarded at `renderDebugStrip()` entry behind `vyve_dev_panel_unlocked`. **Remaining: the 8 `*-live.html` session-live strips** (PM-409.b gating still wants a device-walk confirm) + any other surface where `player-tracker.js` mounts a strip — apply the same function-entry guard pattern. PM-315 CSP fix for YT IFrame API still not validated on device walk — either validate first and remove strip, or keep flag-gated until next walk. Settings Reset Achievements + Force Refresh App buttons stay (intentional trial-period support tools). **PM-415 brain park (27 May): Dean flagged tracker debug visible on "main section" (likely index.html), suggesting the gating leak extends beyond the 8 live shells PM-409.b covered. First Thursday action: device-walk index.html with NO debug flags set, confirm whether debug strip renders. If yes, scope broadens to a full-surface sweep of anywhere `player-tracker.js` loads or `renderDebugStrip()` output is rendered. Promotes to P0 pre-bundle-rollout if confirmed (members on 1.4/1.0.5 would see this).**

**Native binary candidates for next cut.** When cutting binaries again, bundle: bundled exercise thumbnails + workout plan templates (audit total weight first — hundreds of images adds real MB), R8/ProGuard, native debug symbols, View Transitions API wire (PM-394 plan item — lower priority since snapshot-first paint already solves visible flicker; one chat as polish).

## Body hub

**Bug B (Thursday) + Bug A (post-trial) + Bug C (device-console session).** See PARKED block above. Bug A architectural sequence: programme_library category backfill → Onboarding EF v37 writes category into workout_plan_cache → exercise.html branches on category → movement.html consumes categorised plan.

**Exercise demo video coverage fill (P1, post-trial).** 129 of 297 rows in `workout_plans` have NULL `video_url` (43%). After PM-346 wired picker thumbnails to open the fullscreen player, members see dimmed thumbs on those rows = "no preview available". Honest but visible. Action: `GROUP BY plan_type` for NULL ratio first, then prioritise filming/sourcing. Likely distribution: PPL + Upper/Lower have high coverage; Home Workouts and Movement & Wellbeing have the gaps.

**PM-308 — Cardio walking removal + per-member custom kinds.** Specced fully (Dean's next ask, reading-pass + design-check before build). Cardio's fixed kind list shrinks to running / cycling / swimming / rowing / other (walking moves out — lives in Movement now). Plus: members add their own kinds ("Football" → persists for that member forever). Schema: new `member_cardio_kinds` table (member-scoped UUID PK, `member_email`, `kind_label`, `display_order`, `created_at`, UNIQUE on `(member_email, lower(kind_label))`, RLS member-scoped). Dexie SCHEMA_V16: `member_cardio_kinds: 'id, member_email, [member_email+kind_label], display_order'`. `cardio.cardio_type` value space: fixed values lose walking; member-defined as `custom:{slug}`. `cardio.html` UX: pill row with 4 fixed + N custom + "+ Add" pill at end. Cleanup: remove dead `r => r.logged_via !== 'movement'` filters in cardio.html / cardio-history.html / workout-history.html (no-ops post-PM-307). Estimate 2-3 sessions Claude-assisted.

**Exercise.html audit Commit 7 — Browse library prefetch (open).** workouts-library.js is NOT Dexie-wired (zero `VYVELocalDB`/`hydrate`/`prefetch`; 4 raw `fetch()`; has its own localStorage cache layer). Add background prefetch so the exercise library is warm by the time the member taps in. Dean's words: loaded in the background, already there on click.

**Walk-note persistence in the movement quick-logger.** The walk branch of movement.html `logMovement()` writes a `cardio` payload (`cardio_type:'walking'`, `duration_minutes`, `distance_km`, `client_id`) that does NOT carry the member's note. Non-walk branch persists `workout_name: sessionName`. Result: walk rows in Recent Movement + movement-history.html render the type-name ("Walk") as title, never the member's label. Fix: add the note to the walk's cardio payload (confirm cardio's free-text column name first — cardio.html's logger likely uses one). The mvlog render already shows `r.workout_name||r.notes||'Walk'` so walk titles light up automatically once populated. Not trial-blocking.

**Per-set save path (`tickSet` / exercise_logs) — same `await writeQueued` shape as PM-148 (latent).** workouts-session.js ~L421: the per-set save does `await VYVEData.writeQueued(...)` — identical network-blocking pattern PM-148 fixed in `completeWorkout`. Not reported as slow by Dean, but same latent bug — will hang the set-tick under backend load. Apply the same optimistic-first treatment when next touching the set path.

## Mind hub

**PM-319 mind tracker follow-ups.**
- Achievement evaluator subscribe to `mind:viewed`. Right hook for "X minutes of meditation/sleep/visualisation across a week" or "completed N visualisation sessions" metrics. Carries `watch_seconds`, `completed`, `kind`, `pct_watched`. Distinct from `mind:logged` which fires for every mind-pillar activity including journal/affirmations/breathwork.
- Backfill old `duration_seconds:30` rows. Pre-PM-319, every mind_activities row was stamped `duration_seconds=30` regardless of actual watch. If a future drop-off-curve analysis needs them treated as "at least 30s watched", a one-time `UPDATE mind_activities SET watch_seconds = duration_seconds WHERE watch_seconds IS NULL AND ref_id IS NOT NULL` would backfill. Defer until Achievements / analytics demand it.
- Sister-page consolidation. meditation.html, sleep.html, visualisation.html, breathwork.html all duplicate the PM-180/PM-183 setTimeout pattern mind.html retired. Each a candidate for the `VYVEPlayerTracker({mode:'mind'})` swap. Follow-up sprint — mind.html is the highest-impact target and shipped.
- Migration name mismatch (cosmetic). `pm315_mind_activities_watch_tracking` filename doesn't match canonical PM-319 brain entry. Migration names are append-only in Supabase. Accepted drift, no action.

**Background audio play for video catalogue (parked, post-trial decision).** YouTube IFrame embeds disallow audio-only/background playback per ToS — pauses on `visibilitychange:hidden`, WKWebView suspends timers on background, embed self-pauses by design. Three real options post-trial:
1. Tell members to get YouTube Premium. Zero engineering, zero risk, solves nothing for retention.
2. Self-host audio. Re-encode each video's audio track, upload to Supabase Storage, build native HTML5 `<audio>` player wired with `MediaSession` API + Capacitor background-media-controls plugin. iOS `UIBackgroundModes: [audio]` in Info.plist + Android `FOREGROUND_SERVICE_MEDIA_PLAYBACK` permission + foreground service manifest entry. Lock-screen controls + scrubber free with MediaSession. New iOS / Android store submission required.
3. Build audio-first content for mind/sleep tracks — drop video format for surfaces where audio is the product (HAVEN meditations, breathwork, sleep wind-downs, visualisation). Record audio-first, host on Supabase Storage, same player infra as (2). Cleanest end state.

Decision deferred until trial engagement data tells us which mind content members actually want to background. Folds with the post-trial podcast audio player bundle — shared Capacitor plumbing + Supabase Storage pattern + MediaSession wire. When either ships, the other is a 1-day follow-on.

**HAVEN clinical sign-off (Phil-blocked).** HAVEN persona built and live but not promoted widely pending Phil's clinical sign-off review. Conor Warren on HAVEN since 15 April.

**breathwork.html phase transitions (haptics adoption).** `VYVEHaptics.light()` on each inhale→hold→exhale→hold boundary. One-line add when next touching the page.

## Connect hub

**PM-197 Profile identity campaign — remaining work after PM-242 partial ship.** Read-side identity rendering on connect-feed.html is LIVE (connect.html Recent Check-Ins surface removed PM-720 — hub identity read-side no longer applicable). `profile.js` helper exists. `connect-feed-preview` EF v2 enforces anonymous-coupling server-side. Three avatar states render: initials text / uploaded photo / V-mark for anonymous (uses `/logo-mark.png`). `vyve_identity_<email>` cross-member localStorage directory operational.

Still pending:
- Leaderboard avatars — blocked on additive `email` column return from `get_leaderboard()` Postgres RPC. Next-session full-repo audit before careful RPC change.
- Settings UI for changing privacy mode — members currently set `display_name_preference` via direct DB only; no member-facing toggle. Distribution at PM-242 ship: 16 anonymous, 3 full_name, 1 initials. Needs Settings page surface to let trial members pick.
- Curated avatar library (`avatar_kind`, `avatar_id` columns + ~12 SVGs). Full campaign owns. PM-242 ships only `avatar_url` photo + initials + V-mark anonymous path.
- Connect first-load modal for first-time display-name + avatar pickers (PM-197 Thread 1). Design locked, not shipped. First-time on connect.html with `members.connect_onboarded_at` null: single modal "Welcome to Connect. How would you like to appear?" with display name picker + avatar picker + Save/Skip. Defaults: first name + curated V-badge so skip path produces sensible identity. Modal does not gate Connect; page paints with defaults applied behind it.
- Photo upload architecture (PM-197 Thread 2). Capacitor `@capacitor/camera` plugin → native picker → base64/file URI → client-side 256×256 JPEG crop → upload to `member-avatars` Supabase Storage bucket (public-read, write-restricted to authenticated member writing own avatar) → write URL to `members.avatar_url`. Same shape WhatsApp / Slack / Instagram use.
- GDPR Article 17 bucket cleanup in erasure pipeline.

**PM-211 — Single source of truth for live sessions (post-launch, 1-2 sessions).** Live-session schedule data lives in three places that disagree: `sessions-data.js` (vyve-site, recurring), `service_catalogue` (Supabase, stale since PM-190.d), `calendar_occurrences` (Supabase, materialised). Move to one recurring-pattern source in Supabase + materialiser keeping `calendar_occurrences` topped up. Lewis edits one row, calendar + sessions page + Live This Week reflect within minutes. Delete `sessions-data.js`. Recommended: extend `service_catalogue` (add `schedule_days TEXT[]`, `tags TEXT[]`, `live_url`, `replay_url`; backfill from sessions-data.js shape). New EF `materialise-calendar-occurrences` + Sunday 03:00 UTC pg_cron + manual re-trigger endpoint for on-edit invalidation. sessions.html data binding migrates from `VYVE_SESSIONS` to `VYVELocalDB.service_catalogue`. CATALOGUE_INVALIDATION_KEY bump. Realistic timing: first 1-2 weeks post-launch. **PM-463 progress:** connect Live This Week carousel is now calendar_occurrences-driven too (mirrors home PM-453/455/456) — sessions-data.js is now cold-boot fallback ONLY on BOTH home + connect. Remaining for full retirement: recurring-pattern source + materialiser + sessions.html binding migration + delete sessions-data.js.

- **Session notes — production rollout (from PM-467 prototype).** Prototype live on podcast-live.html only (`cfg.notes`). To productionise: (1) member-scoped `session_notes` Supabase table + RLS so notes sync/persist across devices and attach to the replay (currently localStorage-only, device-local); (2) `@capacitor/share` on the native build (prototype uses `navigator.share`); (3) Lewis sign-off on member-facing copy; (4) flip `notes:true` across the other 7 live shells once approved. Reminder: there is NO iOS API to write directly into Apple Notes — the share sheet is the route (PM-467 changelog).

**PM-213b — Live check-in form variants (post-trial).** New `live_checkin_submissions` table (UUID PK, `member_email`, `occurrence_id` → calendar_occurrences ON DELETE SET NULL, `variant` text NOT NULL, `answers` jsonb, `client_id` UUID, `created_at`). Plus `calendar_occurrences.checkin_variant` column so Lewis chooses which variant per check-in. checkin-live.html bespoke shape on top of PM-251 shell — embed question form inside `sl-tab-content` Info panel (or add 4th Check-in tab). Optimistic-first submit per §23.39. ~2 sessions Claude-assisted when prioritised.

**PM-251c — Chat unlock (v1.1 feature flag flip, ships first).** Locked behind `COMING_SOON_TABS = true` in `session-live.js`. Tabs render with lock icon + "Coming soon". Flip flag + port legacy chat code (was in pre-PM-251 engine; not retained in new engine to keep v1 surface clean). Chat-only: ~2 hour session (port + wire + device test). Q&A scope NOW lives in PM-416 (promoted to first-class upvote-ordered feed with admin moderation surface) — PM-251c flips chat tab only, leaves Q&A tab locked until PM-416 ships.

**PM-416 — Live session question submission + instructor feed (post-PM-251c, 3-4 sessions Claude-assisted).** Member-side Q&A tab in `session-live.html` with composer (280-char limit) + upvote-ordered feed of all questions for the occurrence (all-visible per Dean's call). Instructor-side `live-questions.html` in `vyve-command-centre` (admin.vyvehealth.co.uk) per Dean's call: two-column Pending/Answered layout, ≥18px type, Realtime sub with subtle new-arrival flash, optional Producer-mode toggle exposing "Highlight for Lewis" star (defers operator-model decision to post-pilot).

Schema: `session_questions` (UUID PK, occurrence_id FK → calendar_occurrences ON DELETE CASCADE, member_email, question_text 3-280 CHECK, status pending/answered/dismissed, answered_at, dismissed_at, upvote_count, client_id, created_at) + `session_question_upvotes` (composite PK (question_id, member_email), trigger keeps upvote_count synced). RLS: members see pending+answered (never dismissed), INSERT own with 5-pending-per-occurrence cap via trigger, UPDATE service-role only via question-moderate EF. Self-upvote prevented at trigger level.

Two new EFs both verify_jwt:true: `question-submit` v1 (validates occurrence broadcast_state IN ('live','starting'), enforces 5-pending cap, inserts, optimistic-first per §23.39) + `question-moderate` v1 (admin-gated, updates status). Both publish `live:question:created` / `:answered` / `:dismissed` / `:upvoted` via Realtime broadcast per §23.41-§23.50 bus discipline.

Dexie SCHEMA bump: `session_questions` member-scoped on occurrence_id, 24h TTL, pullable ordered by upvote_count DESC + created_at ASC.

Build order: (1) schema migration + RLS + trigger, (2) two EFs, (3) Dexie wire + sync.js PULLABLE, (4) `session-live.html` Q&A tab flip + composer + feed + upvote interaction + Realtime sub, (5) `live-questions.html` in vyve-command-centre — admin gate + occurrence picker + two-column feed + moderate actions + producer-star path, (6) bus events both sides, (7) device walk per PM-304 on real broadcast (schedule occurrence with starts_at=now()), (8) Lewis copy pass on composer placeholder + empty state + 280-char warning.

Risks: rate limiting tight (5→3 after first session if needed), no profanity filter v1 (post-trial: wordlist in EF, no AI moderation), withdraw vs answer race (EF returns current state, client reconciles), Realtime quota fine on Pro for 1000 viewers on one channel with 5-10s polling fallback ready, producer-star vs answered two-state UI needs device-walk validation.

Cross-refs: PM-251c (chat ships first, proves Q&A tab architecture), PM-211 (calendar_occurrences source-of-truth — FK target), PM-197 (member identity rendering on question feed avatars), §23.41-§23.50 (bus discipline).

Rejected alternatives (banked for future, see changelog PM-416): embedded Zoom/Teams in WKWebView (wrong tool, vendor-hostile to embedding), Daily.co/LiveKit/Agora real-time video embed (~$0.004/participant/minute viable but wrong product shape — broadcast Q&A is 1-to-many with text, not many-to-many video). Group therapy two-way video handled out-of-app via Zoom button → `window.open(url, '_system')` → native Zoom (separate ~1-evening ship, Phil clinical sign-off blocked).

**PM-251b — Instructor backfill on `service_catalogue.default_host_*`.** Single SQL UPDATE per category as instructor confirmed. PM-251 backfilled `default_host_name='Lewis Vines'` + `default_host_role='Co-Founder, VYVE Health'` on all 8 `type='live_session'` rows as placeholder. Real identities to populate: Mindfulness (Phil — gated on clinical sign-off review of voice), Workouts (Calum), Weekly Check-In (Vicki?), Group Therapy (Phil), Events & Run Club (Lewis), Education & Experts (TBD per guest), Podcast (Lewis). Photos: upload 512×512 JPEG to a `host-photos` Supabase Storage bucket (public-read, similar shape to `member-avatars`), then UPDATE `default_host_photo_url`. 15 minutes once instructors confirmed.

**At-risk-streak push notifications (PM-309 follow-up, Lewis-blocked on copy).** Dean's ask: 19:00/20:00/21:00 BST push "you're about to lose your X-day Elite streak" when member at-risk. Definition: streak ≥ 3 AND no activity today AND under grace-day rule yesterday IS in active-day set. Requires: Capacitor push-notifications plugin status check, new cron ~19:00 UTC enumerating at-risk members (member_home_state.overall_streak_current ≥ 3 AND last_activity_at::date < CURRENT_DATE), native push dispatch path decision (APNs/FCM direct from EF via REST, or third-party service), Lewis copy with variants for streak length brackets (3-6, 7-13, 14-29, 30+), Settings.html notification toggle row. Scope ~2-3 hour build once Lewis has copy + Capacitor plugin confirmed.

**Live viewer count on session pages.** Display only when 20+ viewers. Requires `liveBroadcasts.list?part=statistics` polling. Quota trivial (8 active broadcasts × hourly = 192 units/day) but UX adds complexity. Defer.

## Home / engagement

**Engagement Score v2 — follow-ups owed after PM-295 partial ship.**
1. Default flip + v1 cleanup. After 24-48hr v2 device verification, swap default so `engagement.html` redirects to v2 (or rename v2 over v1). Drop v1 `compute_engagement_components` + `compute_engagement_score` SQL functions, v1 `member_home_state` columns (`engagement_score`, `engagement_recency`, `engagement_consistency`, `engagement_variety`, `engagement_wellbeing`), v1 JS `computeEngagementComponents`. One Supabase migration + one vyve-site commit. Single session.
2. Activity Breakdown grid rebadge (Dean's call PM-295). Today: Habits / Body / Mind / Connect / Check-ins. Target: **Habits / Mind / Body / Cardio / Check-ins**. Remove Connect + sessions-bound card, split Body into Body (workouts only) + Cardio (cardio sessions only). Pillar rows above stay as-is — they reflect what the score is computed from. Breakdown is a member-facing slice and can differ.
3. Three-tab shell (Dean's design call PM-295). Engagement page becomes `[ Score ] [ Progress ] [ Achievements ]` sticky tabs. Score tab = current engagement-v2.html (post grid rebadge). Progress tab = charity mechanic + 5-track milestone progress per §17 + §11A. Achievements tab = port v1 trophy-cabinet block verbatim (full overhaul is the separate Achievements campaign).
4. `live_checkin_submissions` Dexie registration. Add to db.js SCHEMA_V14 + version chain + makeTable consumer + sync.js PULLABLE entry (member-scoped, week_start.gte 12-week lookback, ordered week_start.desc). Sequences with the live check-in form build.
5. `re-engagement-scheduler` v11 push thresholds. Three thresholds reading new `engagement_score_v2` + `engagement_pillars_touched_7` columns: Soft slide (Score <75 first time in 14 days, 7d cooldown), Pillar gap (Score <65 for 3 days, names empty pillar, 5d cooldown), Re-engagement (Score <55 for 7 days, routes into A/B/C1/C2/C3 + push, 14d cooldown). Lewis owns 5 pillar-gap notification copy variants. Build after 24-48hr v2 verification + default flip.
6. Lewis copy pass — Engagement v2 voice. All v2 band copy (Powerhouse / Strong week / Building / Quiet week titles + subs), 5 pillar explainer popovers, "How your score works" sheet body, 6 pillar empty-state hints. Shipped as functional placeholder. One copy commit.
7. v2 30-day strip — tap-day-to-expand activity detail. Parked post-soft-launch. Tap a day on 30-day chip-row → bottom sheet shows what was logged that day. Defer until soft-launch data shows curiosity-tap is real behaviour.
8. `/page-docs/` continued backfill per §11B. First batch beyond engagement.md: index.md, habits.md, exercise.md, mind.md, connect.md, then focus pages.

**PM-358 — Achievement tier curve + naming overhaul (P1, post-trial, gated on Lewis's v1 copy review landing first).**
1. Naming unit-blind. "Five Cardio Banked", "Three Workouts In", "Twenty-Five Strong" lose the noun. Every title must contain the unit explicitly. "Five Cardio Sessions" not "Five Cardio Banked". Bulk find-and-replace across all 528 rows.
2. Tier curve dies mid-game. Current bulk-count metrics use `1, 3, 5, 10, 25, 50, 100, 250, 500, 1000, 2500, 5000, 10000`. From tier 5 each step is ≥2× previous — gap goes 15 → 25 → 50 → 150 → 250 → 500 → 1500 → 2500 → 5000. 25→100 dead zone exactly where engagement-fatigue is highest. Proposed: `1, 3, 5, 10, 20, 35, 50, 75, 100, 150, 250, 500, 1000, 2500, 10000`. 15 tiers vs current 13. Member hits tiers every 1-2 weeks through month 3, every ~3 weeks through month 6, monthly into year one, rare prestige beyond. Apply to streak-adjacent bulk-count metrics. Streak metrics (3/7/14/30/60/100/200/365/...) leave as-is. Time-based (workout_minutes_total, cardio_minutes_total, mind_minutes_total) same fix scaled to minutes. HealthKit metrics already differently scaled — review separately. volume_lifted_total leave alone.
- Sequencing: Lewis returns v1 CSV with per-tier copy approved → bulk-UPDATE per-tier title/body/copy_status='approved' → curve overhaul → v2 CSV → Lewis sign-off → bulk-UPDATE final. Doing curve change AFTER per-tier copy lands means copy work isn't wasted on tiers that survive (tier 1-5 mostly overlap with current).

**"You've been away" Score copy state (small ship, ~20min, Lewis-blocked on copy).** PM-301 considered but not built: copy-only state on Score band detecting members who haven't logged anything for 5+ days. Override `scoreBandSub` text with welcome-back framing: "Welcome back. Your floor's still at 50, your streaks held what they could, here's where to start." Check `engagement_days_since_last_log` ≥5 in `renderScore()`. No structural change. Lewis voice pass on actual copy required. Defer until member-side report of "I came back from holiday and the page felt cold" confirms problem is real.

**Engagement v1 redirect file removal (post-PM-305, small commit ~early June).** PM-305 left `/engagement.html` as 2KB redirect file. Three things before deleting entirely: (1) confirm via service-worker logs no devices hitting `/engagement.html` directly — should all go to `/engagement-v2.html`, (2) remove `/engagement.html` from sw.js precache list, (3) delete file. Earliest sensible: ~1 week after PM-305 ship.

**Build banner restoration (broken since PM-256, <15min when picked up).** `?debug=build` should show build banner with vbb-marker + sw cache key. HTML element + JS wire still in index.html (L1077-1110 inspected at PM-293) but banner doesn't appear. Most likely: CSS specificity issue from PM-256 rewrite overriding `style.display = 'block'`, OR localStorage flag check at L1086 silently failing. Not a launch blocker — diagnostic affordance lost. Run `?debug=build` in Safari with DevTools, fix spec collision or JS hook.

**Home Today's Habits paints inactive rows as if active (backlog, low priority).** test1@test.com had 4 active member_habits rows in Supabase; settings showed "4 habits currently assigned" correctly; Home Today's Habits showed 8 cards — 4 active + 4 with `active=false`. Hypothesis space: localStorage cache painted before sync.js's `replaceForMember` ran most recently, OR different join path on home (daily_habits joined to habit_library showing any habit ever logged), OR PM-291 habits-on-home wiring reading different Dexie store. Not launch-blocking — fresh members at trial launch have no accumulated `active=false` rows.

**Engagement-v2 cold load slow-path investigation (post-trial).** PM-399 snapshot makes the LOADING hang visually invisible but `computeHomeStateFromDexie` 10-table heavy read still takes 1-30s on memory-pressured devices. Profiling pass post-trial to see where the time goes.

**Lucide icon swap across nav.js (Dean call PM-295).** Replace hand-rolled inline SVG icons in `nav.js` with Lucide (https://lucide.dev, MIT, ~1500 icons). Affects bottom-nav 5 icons (Home, Body, Mind, Connect, More) + More-menu entries. Inline chosen Lucide SVG paths only for icons in use — no CDN, no `lucide.min.js` dependency. Self-contained, can land any session.

**Real per-track progress on My Progress rings (post-pillar-realignment).** PM-257 paints 5 rings with `PROGRESS_TRACKS` placeholder zero values. Wire `home-state-local.js` compute functions into renderPills. Each ring `value/target` reads from member's monthly count. Mapping: Hydration → daily_habits where habit_title matches hydration; Movement → workouts + cardio combined; Mind → mind_activities; Nutrition → daily_habits where habit_title matches nutrition; Sessions → session_views. Target 30 for all (matches certificate milestone). Sequence AFTER pillar realignment (rings → Habits/Body/Mind/Connect/Check-ins) to avoid wiring math twice.

**PM-256 follow-ups (home redesign deferred items).**
- Habit icon redesign campaign. Current `iconForHabit(habit_title)` is 12-keyword regex match with 🌿 fallback. Add `icon TEXT NULL` column to `habit_library`. Backfill 30 current rows with curated emoji or SVG glyph (Dean's call on emoji vs commissioned SVG). Replace `iconForHabit()` with `habit.icon || iconForHabit(title)` fallback. Dexie `member_habits.allFor` already pulls full row — add `icon` to select. ~1 hour emoji / ~3-4 sessions SVG.
- Fractional-ring v2 for Progress Pills. Add `target_unit TEXT NULL` + `target_value NUMERIC NULL` to `habit_library`. Examples: `walk_8000_steps` → unit `steps`, target 8000. Backfill ~10-15 habits with meaningful numeric targets. New `getHabitProgress(habit, memberEmail)`: HealthKit-backed for steps/sleep/active_energy; water-backed for hydrate; time-backed for breathwork/meditation; boolean fallback otherwise. Ring math `Math.min(1, current / target)`. ~3-4 hours. Bundle into one trip through habits.html + index.html + habit_library migration with the icon redesign.
- AI-generated daily focus (post-trial). New table `daily_focus_log(member_email, focus_date, persona, time_of_day, content_json, generated_at)` UNIQUE `(member_email, focus_date, time_of_day)`. New Anthropic EF `generate-daily-focus` mirroring weekly check-in recs. Trigger from index.html boot if no row exists. Cost ~3 gens × 14 days × 30 members = 1260 calls/month at trial scale (~£3-5/month Sonnet 4).
- Tagline `time_of_day` column for TOD-aware rotation. Add `time_of_day TEXT NULL` to `taglines` (NULL = applies to all TODs). Lewis adds 4-6 per TOD via Supabase Studio. Replace `pickTagline()` with Dexie read filtered to current TOD or NULL, local-midnight-anchored day-index rotation. ~30min.
- Mood trend visualisation for Lewis (out-of-scope trial, marker for PM-214 admin console build).

**PM-257 follow-ups.**
- Focus carousel re-consideration. PM-257 ships single Today's Focus card that changes content by hour boundary. Worth a device review now that it's live — Dean previously stuck between carousel (3 cards) and single-shifting-card; my argument for single won. If Dean wants carousel after device review: restructure `.focus-card` to `.focus-carousel` container with 3 cards always visible, swipeable, active card derives from TOD on first render. ~45min, pure UI restructure, no data layer change.

## Settings / profile

**Settings UI for changing privacy mode (PM-197 follow-up — Profile identity).** See Connect hub. Members currently set `display_name_preference` via direct DB only.

**Capacitor + theme parity for nav-injected pages.** Bottom-nav opacity split on Connect (PM-405) shipped — verify no inverse drift on other surfaces during the next light-mode pass.

## Onboarding

**Onboarding EF v37 writes category into workout_plan_cache (PM-411 Bug A architectural prereq).** See Body hub.

**Persona welcome copy spot-check in `persona_welcome_copy` (Lewis-blocked).** Dean finalises lines via UPDATE in Supabase Studio; no vyve-site commit required. HAVEN short-circuit + SAFE_ECHO_GOALS whitelist + `{name}` interp all stay code-side as safety logic. `FALLBACK_COPY_TABLE` const carries cold paint. Schema-level work for PF-13 hydration copy finalisation is done — only sweep-the-fallback-to-match remains when Lewis spot-checks.

**In-App Tour (PF-23, post-bundle P1).** Elevated from "V2 post-launch blocked on Lewis copy" to next-up P1 after bundle ships (PM-404 brain close). Modal step-through (option a) confirmed for v1. Walks members through home dashboard (score ring + streak), first habit log, first workout log, first cardio log (with HealthKit consent prompt on iOS), first session watched, first weekly check-in. Each step ends with the member tapping the actual log button — earns first-tier achievement at each step + the `tour_complete` achievement on completion. Persistence via `members.tour_completed_at` + "Restart tour" in Settings. Skip path required. Dependencies: Lewis copy + screenshot approval. ~1-2 sessions, mostly UI.

## Admin / Command Centre

**Portal Admin UI for `calendar_occurrences` (sequenced after PM-215 cron + ~1 month of operational learnings via manual pasted-timetable path).** Spec defined in `playbooks/live-sessions-operations.md`. Build with usage data, not without. Repo `VYVEHealth/vyve-command-centre`, new page at `/calendar` or `/sessions`. MVP scope ~1 session Claude-assisted: list view of upcoming occurrences, add new session form (8 fields per intake spec, category dropdown of 8 canonical strings — no free text), edit form (locked once `starts_at <= now()`), cancel (sets `active=false`, warns if `youtube_broadcast_id IS NOT NULL`). Phase 2 ~half session: bulk add for recurring patterns. Does NOT do: YouTube broadcast management UI, Riverside integration, host photo upload (paste URL), replay management.

**Broadcast schedules UI (PM-402 follow-up).** Manual broadcast UI + cron rails shipped. Out-of-scope v1 parked: scheduler creation UI in Command Centre (rails ship dark per Dean's call); "pool of quotes" infrastructure for morning quotation (Lewis decides content shape); custom audience JSON editor (power user surface).

**Android FCM banners on broadcast push (standing backlog).** APNs end-to-end live for broadcast push. Android needs FCM credentials audit + dispatch path wire.

## Native / Capacitor

**Bundled exercise thumbnails + workout plan templates into Capacitor build (own scoped vyve-capacitor session).** Dean's direction: when app downloaded from store, build contains exercise thumbnails AND workout plan data — on-device from first launch. Distinct from sw.js precaching — commits thumbnails into vyve-capacitor as bundled assets shipping inside the binary. Videos NOT bundled (network-only). Workout pages reference local bundled asset path with network URL as fallback. First size up total thumbnail weight — hundreds of images may need compression before bundling. Pairs with vyve-capacitor git init.

**Android Health Connect wiring (PM-411 Item 1 Bug 7 carry-over).** AndroidManifest + Gradle SDK + healthbridge.js Android branch. Plugin present per package.json. Android FCM credentials audit alongside.

**Capgo HealthKit live-polling upgrade on Movement (v2 follow-up).** Deferred. Current pull is per-session; live-polling would surface walk credit in near-real-time.

**iOS background HealthKit sync (parked future vision).** Capgo 8.4.7 exposes zero background primitives. Architectural path is companion Swift Capacitor plugin (~400 lines) alongside Capgo. ~4-5 build sessions + 1 week device soak + App Store review cycle. Unpark signals: member feedback naming background sync specifically; enterprise pilot requirement.

**Nutrition/MFP reads via HK (parked).** Capgo 8.4.7 exposes no dietary types. Would need plugin fork/PR. Separate plan at `plans/nutrition-healthkit.md` when sequenced. Unblocks water habit auto-tick and MFP-native nutrition totals.

**PM-250 follow-up — Wire @capacitor/browser for external links inside the app.** Plugin already installed (`@capacitor/browser ^8.0.3` in vyve-capacitor/package.json). Wiring deferred from PM-250. ~30min. PM-250 suppressed long-press "Open in Safari" preview via `-webkit-touch-callout: none`, but tap on `<a href="https://example.com/...">` inside WKWebView still navigates IN the WebView (replacing app's web shell). Members visually trapped. Route external links through `Browser.open()` → SFSafariViewController on iOS / Chrome Custom Tab on Android. Audit first: grep external links, `window.open`, `location.href`, `target="_blank"`. Implement via delegated click handler in new `external-links.js` (or fold into nav.js) — Capacitor environments only via `window.Capacitor.isNativePlatform()` guard. `window.Capacitor.Plugins.Browser.open({url, presentationStyle: 'popover'})`. Ships in vyve-site (client-side JS) not vyve-capacitor — plugin already in binary. Sweep target per §23.59 for stale PWA-era assumptions (any "Add to home screen" / `beforeinstallprompt` handler / stale sw.js urlsToCache from removed pages).

## Infra / Data

**PM-417.b — HealthKit distance backfill patcher (P2, ~1.5 sessions Claude-assisted).** PM-417 forward-fix shipped to vyve-site `2e6ffb46` — healthbridge.js now captures `totalDistance` + `totalEnergyBurned` from Cap-go workout samples going forward. Backfill remains: ~6 months of HealthKit-sourced `cardio` rows have `distance_km IS NULL` for Lewis (and any other HK-connected member). New EF `backfill-hk-distance` (verify_jwt:true, admin-gated). Client-side re-pull required since HealthKit data lives on-device — server has native_uuid but cannot re-query alone. Flow: EF returns list of NULL-distance workout `native_uuid`s for member → client calls Capgo `queryWorkouts` in 30-day windows (max 365 days per MAX_SAMPLE_AGE_DAYS) → posts `{native_uuid, totalDistance, totalEnergyBurned}` back to EF → EF merges into `member_health_samples.metadata` AND updates `cardio.distance_km` via `promoted_id` link in transaction. Trigger from vyve-command-centre per-member, OR auto-trigger via new `member_health_connections.needs_backfill BOOLEAN` flag picked up on next normal pull_samples. Edge cases: workout deleted on device → leave NULL; third-party app workouts without totalDistance → leave NULL (document, don't engineer around). No native bundle change needed — client JS only, picks up via WKWebView refresh.

**PM-145 — `platform-alert` v10 + `alert-digest` EF + cron-noise cleanup (P0, design locked PM-403.b, build pending fresh chat).** ESCALATED 16 May (PM-149). On 16 May the `platform-alert` v8 storm drained the nano-tier 60-connection Postgres pool and caused a site-wide login outage. Mitigated by deploying `platform-alert` v9 — a deliberate no-op (instant 200, no DB/Brevo/push). Consequence: platform error monitoring has been OFF since 16 May. With bundle widening cohort to all members on approval, this is now blocking-grade P0.

Design locked PM-403.b. See changelog. Summary:
- `platform-alert` v10 — server-only replacement of v9. Fingerprint `(type, normalised_endpoint, member_email)`. Severity recalibration with 14-endpoint write-path list. Circuit breaker 20/60s → 429 for 5min. skeleton_timeout counter-in-details pattern. Writes only to `platform_alerts` — push_subscriptions write dropped entirely. <50ms 200-return. Client (vyveMonitor IIFE in auth.js L927-999) untouched.
- `alert-digest` EF + pg_cron — three slots 08:00/14:00/20:00 UTC. Always sends. Subject differentiates issues vs all-quiet. Per-fingerprint Claude Sonnet 4 diagnosis (4096 tokens, cap 10/email, EF source + last-3-commits context, hardcoded endpoint→file map). Anthropic failure degrades gracefully — email always sends. Brevo `team@vyvehealth.co.uk` to deanonbrown@hotmail.com + lewisvines@hotmail.com, tag `alert-digest`.
- Cron noise cleanup — `charity_total_reconcile_and_heal()` literal swap warning→info; `habit-reminder-daily` + `streak-reminder-daily` JSON-literal fix on the `current_setting('app.service_role_key', true)` pattern; sweep `cron.job` for shared-pattern siblings.

Order in build chat: cron noise (10 min) → platform-alert v10 → alert-digest EF + cron → manual digest invocation confirms all-quiet email lands → Session B walk.

Deferred from current scope: boot_id + app_version client injection (Stage 2, auth.js v3, post-bundle OTA). Command Centre alerts panel (Stage 5, post-bundle). Pattern detection EF (Stage 4, post-bundle).

**Monthly check-in credit gap (P0 own session, Lewis-gated).** `monthly_checkins` has NO counter trigger and NO charity trigger — a monthly check-in currently earns ZERO credit. Bus event `monthly_checkin:logged` should exist (achievements evaluator handlers `monthly_checkins_completed`, `monthly_avg_improved` reference per PM-342). Check what's actually firing. Convert increment-style counters (`increment_cardio_counter`, `increment_workout_counter`, `increment_habit_counter`, `increment_checkin_counter`, `charity_count_*`) to the stateless recompute pattern used by `update_cert_sessions_count` — drift-prone (no recount on DELETE). Recompute for check-in track must UNION `wellbeing_checkins` and `monthly_checkins`. Backfill `cert_*_count` columns. Per-track cap numbers (cardio/workouts/sessions credit first 2/day; habits + check-ins credit first 1/period) are a product decision — confirm with Lewis, don't change unilaterally. `activity_dedupe` replay (months of historically-discarded activity) — Dean's steer: fix-forward, treat replay as separate.

**index.html duplicate `posthog.init` cleanup (PM-408 follow-up, P3).** Lines 1043-1046 contain inline `posthog.init` running immediately (no defer) setting `posthog.__SV=1`. auth.js's deferred init at line 8 then no-ops on existing stub. Net effect: session_recording config in auth.js (100% sampling, `maskAllInputs:true`, `maskInputOptions.email:false`) NEVER applied on index.html. Single-ship fix: delete inline init, test in fresh Chrome incognito with DevTools (confirm `window.posthog` defined within first 200ms, session recording active, no console errors). If green, ship; if not, align inline init config with auth.js. ~4-file atomic ship. Verification via PostHog "Session replays" tab. Home page recordings missing is noticeable observability gap but not blocking trial.

**§23.65 forward-sweep audit (envelope-trusted subscribers).** PM-293 fixed home `habit:logged` subscriber to be envelope-aware. Same pattern applies to any cross-page subscriber watching `<event>:logged` from a publishing surface using fire-and-forget Dexie writes (§23.39 default). Audit signal: `grep -nE "VYVEBus\.subscribe\(['\"][a-z_]+:logged" *.html *.js`. For each match, check whether subscriber's only state-update path is a `loadX()`/`fetchX()` Dexie re-read. If yes, upgrade to envelope-aware per §23.65. Surfaces likely to need it: engagement.html (subscribes to most `:logged` for score recompute), exercise.html (hub surface counts). Connect + mind already swept in PM-390. Lower priority than home fix.

**PostgREST upsert hardening forward-sweep — keepalive + on_conflict audit (paired with §23.65 sweep).** PM-296 + PM-298 hardened `settings.html saveHabits` only. Every other un-awaited PostgREST write needs same two hardening points: `keepalive: true` on fetch options, and `?on_conflict=<cols>` + `resolution=merge-duplicates` (or `ignore-duplicates`) wherever the table has a UNIQUE constraint that could be hit by re-add path. Audit signal: `grep -nE "supaFetch\(['\"]/[a-z_]+.*method:\s*'(POST|PATCH)'" *.html *.js` for un-awaited or IIFE-wrapped calls. High-suspicion surfaces: `settings.html savePersona`/`saveGoal`/custom habit create/delete, cardio.html, workouts.html, mind activity writes, wellbeing-checkin.html (UNIQUE on `(member_email, iso_week)` — will re-submit 409?), nutrition_logs, weight_logs (already has upsert — confirm). 1-2 sessions Claude-assisted.

**PM-289 follow-up — Optimistic-write reconciliation watchdog (§23 candidate, hold for second occurrence).** PM-289 found `connect_checkins` POSTs silently failing in iOS WKWebView for days — optimistic Dexie write held UI in confident "posted" state with no diagnostic surface, even though server never received the row. Read paths have §23.46 ("paint truth, not placeholders") to keep them honest; write paths have no equivalent. Worth a §23 hard rule once pattern recurs: every optimistic-first write must have reconciliation watchdog that, within N seconds of optimistic Dexie write, verifies row exists server-side and surfaces visible failure banner if not. PM-289 only fixes iOS race (keepalive + awaited navigation) — if POSTs still fail in wild after this ship, new `console.error` on 4xx will show what's actually rejecting. Hold §23 rule until one more occurrence confirms failure mode generalises.

**`workout_plan_cache` contradictory UNIQUE indexes (banked, §23 candidate on second occurrence).** See PARKED PM-411 schema-architecture note. Two contradictory UNIQUE indexes (`workout_plan_cache_member_email_key` blocks multi-row; `workout_plan_cache_one_active_per_member` assumes multi-row). workout-library EF v13 paused-plan logic at L60-84 silently never works correctly. Promotes to §23 on second occurrence.

**Auth-shape gotcha (banked PM-402, §23 candidate on second occurrence).** Post-key-rotation `SUPABASE_SERVICE_ROLE_KEY` env in EFs is the new `sb_secret_*` publishable shape; `send-push` v13's `Bearer` equality check needs JWT-format `LEGACY_SERVICE_ROLE_JWT` instead. First runner deploy got `401 UNAUTHORIZED_INVALID_JWT_FORMAT`; both new EFs redeployed at v2 with corrected env. Pattern reference: `achievement-earned-push` v2 source. Promotes to §23 on second occurrence.

**Upsert-only sync EFs need reconciliation step (banked PM-410, §23 candidate on second occurrence).** `refresh-replay-videos` v1 was upsert-only; deletable upstreams left stale rows. v2 added reconciliation step. Promotes on second occurrence.

**Cross-visit dwell accumulation (tracking.js, post-launch low priority).** tracking.js v9 dwell accumulator resets on page unload — `visitStartTime` is in-memory; only `baseMinutes` (server `minutes_watched`) survives. Two sub-60s visits to same session don't sum. Anti-farm correct; wrong if we want to credit genuinely-interrupted viewing. Fix options if pursued: (a) create row early marked unqualified and accumulate server-side, or (b) track cumulative minutes per (category, date) in Dexie across page loads. Defer until post-launch + evidence it matters.

**tracking.js outbox wiring (§23.10 hardening candidate, post-launch).** tracking.js is critical activity-write path with NO outbox — direct fetch, `session:viewed:failed` to a bus nobody surfaces, no retry beyond in-visit heartbeat. Member loses connection at 60s mark and leaves → loses legit session view. vyve-offline.js has the outbox infrastructure; tracking.js was never wired. Wire writes through offline outbox. Not launch-blocking.

**NEW FEATURE — `page_visits` owned visit/dwell analytics (post-launch, ~2 sessions).** Owned, queryable record of page visits + time-on-page, Dexie→Supabase, SEPARATE from PostHog (PostHog stays as deep web-analytics/replay layer). New `page_visits` table (`member_email`, `page`, `entered_at`, `duration_seconds`, `activity_date` — one row per visit). Small shared tracker on every portal page captures entry on load + duration on `pagehide`/`visibilitychange` with `keepalive` on final beacon for iOS WKWebView. Local-first to Dexie table, background-drain via `_sync_queue`. Value: owned analytics, re-engagement triggers ("hasn't opened in 5 days"), employer insight, future member-facing "your week" view.

**NEW FEATURE — `session_schedule` table + live-session minute-windowing (post-launch, ~1.5-2 sessions).** Live-session minutes only count during actual broadcast window (e.g. 09:00-09:30 session: member on page 09:15-11:00 credited 15 min, not 105). Two pieces: (1) FOUNDATION — `session_schedule` table (`category`, `day_of_week` or date, `start_time`, `end_time`) — schedule currently only text on sessions.html. Also unblocks real "live now" home slot and real "Coming Up This Week" block. (2) tracking.js clamps live-session minutes to `overlap(visit_window, broadcast_window)`; replays unaffected. Caveat: tracking.js measures page dwell not video play-state — "present during window" is strong proxy but not true watch-tracking; true play-state needs YouTube iframe API (V2). Pairs with future minutes-based session goals.

**Local-sunset-aware hub hero rotation (future-vision, parked).** Today's three-state photo swap (morning / afternoon / night) driven by §23.55 pre-paint inline IIFE on `getHours()` with hardcoded boundaries 05-11 / 11-19 / 19-05. "19:00 = night" is wrong for half the user base half the year. London June: sunset ~21:30 (night appears 2.5hr early). Edinburgh December: sunset 15:40 (afternoon persists ~3.5hr past dark). Stockholm winter: night at 14:45. Recommended path: cached lat/lng + NOAA solar calculation. Geocode onboarding "Where are you based" → lat/lng at onboarding-EF time, persist to `members.lat`/`members.lng`. `member-dashboard` returns alongside payload, cached localStorage. Pre-paint IIFE reads cached lat/lng → NOAA solar position algorithm (~40 lines pure JS, zero deps) → today's sunrise + sunset → picks photo. <1ms, zero network, zero permissions. Falls back to 19:00 boundary if absent. ~2 hours single long session. Stripe-country fallback for ~10% geocoding failures (free-text "Where are you based" → typeahead OR country centroid). Unparks if user base reaches extreme-latitude markets, or post-trial premium-feel polish.

**Schema audit — every member-data table has `updated_at TIMESTAMPTZ` + `BEFORE UPDATE` trigger (pre-bundle / offline-correctness gate, parked post-binary).** Catalogue tables too (delta-pull depends). Add where missing in one-shot migration.

**Idempotency audit (pre-bundle / offline-correctness gate).** Every write surface generates `client_id` UUID client-side at write time. Server respects as dedupe key. mind_activities = gold standard. Verify: workouts / cardio / daily_habits / exercise_logs / custom_workouts / exercise_swaps / weight_logs / nutrition_logs / weekly_scores / wellbeing_checkins / monthly_checkins / session_views / replay_views.

**Airplane-mode device walk (pre-bundle / offline-correctness gate).** Dean's iPhone with server.url and network killed at OS level. Open every page in order. Record render behaviour (renders / spinner / empty / broken / honest offline). Anything broken or empty (when data exists in Dexie) = P0 fix. Pairs with `_sync_queue` drain hardening (drainer wakes on app launch, drains pending writes before letting user create new ones, handles ordering, resilient to individual row failures, tested against simulated 2-week-offline queue).

**Cold-start-no-network UX (pre-bundle / offline-correctness gate).** Login screen detects no-connection state, shows honest message ("VYVE needs internet for first sign-in. After that, the app works offline.").

**Fan-out-on-focus pattern (pre-bundle / offline-correctness gate).** Capacitor `App.addListener('appStateChange')` triggers incremental delta-pull when app returns to foreground. Per-table `last_sync_timestamp` stored in Dexie `_sync_meta`. `where updated_at > [last_sync_timestamp]`.

**✅ Certificates re-pillaring — SHIPPED PM-435 (2 Jun).** Done as FIVE Your Journey buckets (habits/mind/body/connect/checkins), NOT the originally-planned three Mind/Body/Connect — certs now mirror the Journey Progress tab exactly via `get_certificate_buckets()`. `pillar` column + legacy grandfather + silent backfill + certificate-checker v24 all shipped & verified. **Open follow-ups:** (1) **🎨 Certificate VISUAL DESIGN pass — `certificate.html` / `certificates.html` are functionally correct but placeholder-quality; need a proper premium design (Dean flagged 2 Jun).** (2) Mind persona "The Anchor" is a placeholder pending Lewis sign-off — he can also rename Warrior/Explorer to cleaner pillar names if wanted. (3) Goal-target re-pillaring (non-cert half of PM-159) still open. Folded into Phase 3 pillar realignment of the original PM-184 bundle-ready campaign. **PM-415 brain park (27 May): Dean flagged this as under-surfaced in the platform audit doc — the existing 5-track certs (Architect/Warrior/Relentless/Elite/Explorer) rate 4/5 in current state which masks that they don't reflect the strategic 3-pillar positioning we use in sales conversations. Audit-doc rewrites should rank certificate re-pillaring around #11-13 in priority remediation, not bury inside a 4/5 certificates rating. Timing stays post-trial because trial members earning current-track certs this month is fine (new tracks grandfather old earns).** Canonical entry — duplicate at L360 in post-trial section is a back-reference only.

**Continue Watching UI tile (PM-294 follow-up, post-soft-launch).** `replay_video_views_member_last_updated` index already powers the query — `SELECT youtube_video_id, watch_seconds, title FROM replay_video_views WHERE member_email = X AND completed = false ORDER BY last_updated_at DESC LIMIT 3`. UI shape: horizontal carousel on replays.html above playlist tiles, three cards showing "Resume from MM:SS" with thumbnail + remaining time. Tap = mountPlayer with `startSeconds` via YT IFrame API. Mockup-first. Sequence after soft-launch trial data confirms carousel is wanted — premature before knowing whether members care about resume vs restart.

**Per-instructor drop-off analytics (PM-294 follow-up, internal-only).** `host_name` denormalised on every `replay_video_views` row. Simple SQL `GROUP BY host_name, ROUND(watch_seconds * 100.0 / NULLIF(total_seconds, 0)) ORDER BY 2` gives drop-off curves. Consumer surface: internal-only dashboard tile in Command Centre (Lewis/Calum/Phil). Not member-facing. Build after first month of trial-scale data (~30 watches per instructor). Single EF + dashboard tile, no schema change.

**Per-category cumulative watch-time achievements (PM-294 follow-up, post-trial).** New metrics in `achievement_metrics` — `yoga_minutes_watched`, `mindfulness_minutes_watched`, `workouts_minutes_watched`, one per active category — tiers at 5/10/15/30/60/120/300 cumulative minutes (placeholder, design against real distribution after ~1 week trial data). Evaluator sums `watch_seconds` from Dexie's `replay_video_views` (PM-294) AND `session_live_views` (PM-304), filtered by `category`, divided by 60. Cumulative across all play sessions. Trigger event-driven: replay tracker publishes `replay:viewed` every 30s of accumulated playback; live tracker publishes `live:viewed` with identical payload. Achievements subscriber on both events re-evaluates per-category sum from Dexie and stamps `member_achievements` if tier crosses — instant unlock toast on device, no server round-trip. Existing nightly sweep stays as cross-device consistency safety net.

**Replay-aware charity math (Lewis-blocked).** Currently 30-activities-per-month charity counter reads legacy `replay_views` (page-presence attribution, dormant since PM-235). PM-294's `replay_video_views` is more honest signal — members who actually watch vs who happened to be on page. Question for Lewis: switch charity math to new table, or stay on legacy to avoid breaking 30-activities denomination mid-trial? Sub-question: minute-weighted charity attribution (1 charity month per N total watch-minutes rather than per N completed views) possible via `SUM(watch_seconds)` but different mechanic — Lewis's call. Park until post-trial review.

**PM-315 device-walk validation + PM-316+ state-machine fix.** PM-315 CSP fix for YT IFrame API not yet device-validated. Hypothesis-confirmed by PM-311 diagnostics but not walked. Resumption procedure documented (schedule test `calendar_occurrences` row with `starts_at = now() + 30s`, invoke `session-publish` via pg_net, Dean force-refreshes app, starts Riverside stream, verify debug strip shows `ytLoaded: true` / `playerConstructed: true` / `lastState: -1 → 3 → 1` / `watchSeconds: 0 → 30+` / `hasFirstWrite: true`, confirm row in `session_live_views`). PM-316+ bug: state machine resolves LIVE strictly on clock-time `row._start <= now < row._end`. If YouTube `enableAutoStart=true` flips broadcast live BEFORE scheduled `starts_at`, page sits in PRE_ROLL even though YouTube is broadcasting. Tracker doesn't attach. Workaround per memory #23: test rows `starts_at = now()` or `now+30s`, never `now+10min`. Architectural fix: state machine queries YouTube broadcast `lifecycleStatus`. Three options — cron-driven flag on `calendar_occurrences` (zero client-side YouTube API exposure, 5-min cron lag) / client-side YouTube embed status probe via `YT.Player.getPlayerState()` (real-time, requires iframe earlier than current PRE_ROLL CSS hides it) / hybrid. Pick after sketch session, likely option 2.

**Engagement-v2 recompute fan-in retrofit (low urgency, wait-for-glitch).** Sibling to index.html `_rerenderHome` retrofit. Bus subscribers currently fan out individually; consolidate into a single recompute on writes-batch.

**Affirmations per-day-cap design issue (parked).** Identified in PM-379 audit. Defer.

**`session-rp.js` playlist ID lookup migration to Dexie catalogue (PM-390 follow-on, banked).** Currently each rp shell exposes `window.VYVE_SESSION.playlistId` inline; session-rp.js reads it directly. When `replay_playlists.youtube_playlist_id` changes for any slug, every rp shell needs manual edit. Move to Dexie lookup via slug — mirrors PM-377/PM-378/PM-384 pattern, lets Lewis manage replay playlists from Supabase Studio. Promotion: next time `replay_playlists` row needs to change AND any rp shell touched.

**Periodic rp-shell drift audit (PM-390 follow-on, banked).** events-rp drifted to standalone 17.6KB page without being caught. Worth periodic grep across `*-rp.html` for byte-count outliers — any rp shell >5KB probably drifted from canonical. Could be one-shot playbook entry.

**Refactor onboarding v74 to call `generate-workout-plan` EF.** Remove ~120 lines of inline duplicated logic. EF has richer implementation (dedicated programme-overview step, better prompts, cleaner video enrichment) than onboarding's inline copy. ~2hr, zero-risk if deployed atomically.

**In-app onboarding fallback.** Simplified questionnaire inside portal for members with no workout plan (~3-4hr).

**Onboarding resilience: save-answers-first.** Progressive answer saving + error screen (~2-3hr).

**Replay 3 throwaway EFs (PM-410 cleanup).** `replay-inventory-tmp`, `replay-archive-tmp`, `replay-ghost-cleanup-tmp` still ACTIVE in Supabase but dormant. Dean to delete via Supabase dashboard when convenient.

**EF cleanup of one-shot patchers (recount needed).** ~32 still-ACTIVE candidates per the 9 April security audit only partially actioned: `seed-library-1`, `seed-library-2`, `seed-b1`, `create-ai-decisions-table`, `setup-ai-decisions`, `setup-member-units`, `trigger-owen-workout`, `trigger-callum-workout`, `thumbnail-audit`, `thumbnail-upload`, `thumbnail-batch-upload`, `generate-stuart-plan`, `send-stuart-reset`, `monthly-checkin-test`, `run-monthly-checkins-migration`, `run-migration-monthly-checkins`, `resend-welcome`, `delete-housekeeping`, `send-test-welcome`, `send-test-push`, `inspect-members-schema`, `create-test-member`, `add-exercise-stream`, `force-cache-refresh`, `update-brain-changelog`, `debug-cert-content`, `debug-show-file`, `test-html-render`, `smoketest-ach-push`. Keep `ban-user-anthony` if ban workflow still in use. Composio doesn't expose delete-EF tool — needs Supabase CLI/dashboard. [VERIFY: confirm against current Supabase EF list before action.]

## Lewis-blocked

**Connect "This Week's Challenge" content (PM-415 brain park, 27 May).** Connect hub challenge surface is built end-to-end — `connect-challenge.html` page, `weekly_challenges` + `weekly_challenge_participation` tables live since PM-186/187 (22 May), `connect-challenge-summary` EF renders summary on connect.html. What's missing is a real challenge row in the database. Lewis hasn't seeded one. Same shape as the Replays content refill — engine is ready, content is the gap. Lewis adds first challenge via Supabase Studio (single INSERT into `weekly_challenges`) and the surface lights up. Recommend writing 4-6 weeks of challenge content ahead so the surface stays populated without Lewis touching it weekly.

**Engagement v2 voice copy pass (Engagement Score v2 #6).** All v2 band copy + 5 pillar explainer popovers + "How your score works" sheet body + 6 pillar empty-state hints.

**Re-engagement push thresholds copy (Engagement Score v2 #5).** 5 pillar-gap notification copy variants (one per pillar).

**Achievement tier curve overhaul v1 CSV (PM-358).** v1 per-tier copy review must land first. Then bulk-UPDATE per-tier title/body/copy_status='approved', then curve overhaul → v2 CSV → Lewis sign-off → bulk-UPDATE final.

**At-risk-streak push copy.** Concise, action-oriented, no emojis ("Your 11-day streak is at risk. Log anything to keep it alive."). Multiple variants for streak length brackets (3-6, 7-13, 14-29, 30+).

**Persona welcome copy spot-check in `persona_welcome_copy`.** Dean finalises lines via UPDATE; sweep-the-fallback-to-match when Lewis spot-checks.

**Mind v1 Lewis copy review.** affirmations / journal / breathwork seed content. `COPY_LEWIS_REVIEW` tags throughout.

**PF-13 hydration COPY_TABLE finalisation.** 23 entries tagged `COPY_DEAN_FINAL` in `/hydration.js`. ~30-45 min Dean writing time.

**In-App Tour copy + screenshot approval.**

**Calorie-target habit (PM-286.x).** New habit in `habit_library` auto-ticking when within daily calorie target. Wording is clinically sensitive — "stay within calorie allowance" frames as restriction (problematic for RIVER/HAVEN), "hit your protein and calorie targets" frames as performance (fine for NOVA/SAGE/SPARK fat-loss or muscle-gain). Phil sign-off mandatory. Persona-conditional assignment logic doesn't exist — needs new rule in onboarding/recommendation flow ("never auto-assign to RIVER/HAVEN regardless of goal_focus"). Autotick rule via `habit_library.health_rule jsonb` doesn't currently support `nutrition_logs` totals — existing autotick is HealthKit-driven. New evaluator code path or extend existing.

**Live check-in form variants (PM-213b) Phil sign-off on mindfulness variant.** Phil-blocked.

**B2B volume tier definition (pre-first-enterprise-contract).** Dean + Lewis.

**Annual pricing discount % decision.**

**5 disabled Make tasks — keep or remove decision.**

**Public launch comms draft.**

**HAVEN clinical sign-off (Phil-blocked).** Formally decide: approve as-is or gate pending professional review. HAVEN actively being assigned (Conor Warren, 15 April).

**Replay-aware charity math (Lewis-blocked, Infra/Data above).**

**Re-engagement copy review.** Bulk-approve subjects + body copy across the A/B ladder (A_48h/96h/7d/14d, B_3d/7d/14d/30d). Current copy carried forward from v7 staticBodies + AI persona overlay; structurally correct but not yet copy-passed post-rewrite.

**Login auth pages copy review.** Bulk approval gate before next round of polish.

## Calum-blocked (external dependency)

**Workout Engine v2 (parked 27 April 2026, awaiting Calum's filled inputs pack).** Architecture decided: deterministic engine (filter → score with context weights → rank → fill slots) replaces AI exercise selection. AI only for programme name/rationale (Sonnet 4) + Layer 2 reviewer (Haiku 4.5). Drops cost ~30× AND raises quality by encoding Calum's expertise as data. Stages on restart: import 203+67=270 scored exercises into `exercise_scoring` table + name normalisation layer → deterministic engine in TS in `generate-workout-plan` v12 behind feature flag → persist new onboarding fields (`priority_muscle`, `session_length_workouts` columns on `members`) + onboarding EF v78→v79 → code Calum's 20 QA scenarios as automated tests → shadow mode ~50 onboardings → cutover after Calum sign-off → Movement & Wellbeing engine (Dean's call: separate path from workout engine). Inputs pack drafted: `VYVE_Inputs_Pack_for_Calum.docx` (13-page structured questions doc) + `VYVE_Exercise_Scoring_Gap.xlsx` (67-exercise gap list in Calum's format).

## Post-trial / post-launch

**PM-358 tier curve + naming overhaul.** See Home / engagement bucket. After Lewis v1 copy review lands.

**Achievements badge artwork upgrade.** Current SVG generator covers 4 shapes × 4 tints in `svgTrophy()`. Future upgrade via AI image gen (Gemini + Claude art direction with VYVE brand grade). Drop-in replacement of `svgTrophy()` calls. Data layer doesn't change.

**Index.html dashboard slot — latest unseen / closest inflight achievement tier.** ~1 session low risk (reads existing `getMemberAchievementsPayload()` output). Phase 3 sub-task confirmed unstarted PM-3.

**Background audio play for mind/sleep tracks.** See Mind hub. Post-trial decision once engagement data confirms which content members want to background.

**Continue Watching tile.** See Infra / Data.

**Per-instructor drop-off analytics.** See Infra / Data.

**Per-category cumulative watch-time achievements.** See Infra / Data.

**Page_visits owned analytics + session_schedule windowing.** See Infra / Data.

**Local-sunset-aware hero rotation.** See Infra / Data.

**Profile identity remaining work.** See Connect hub (avatar library, Settings UI, Connect first-load modal, photo upload architecture, GDPR cleanup).

**In-App Tour (PF-23).** See Onboarding.

**View Transitions API wire (PM-394 plan item).** Lower priority since snapshot-first paint already solves visible flicker; ship as polish item for cross-tab nav animation. One chat.

**Persistent-shell SPA migration (Option 3 from PM-394, architecturally-correct answer).** 8-11 hours / 3 sessions of Claude-assisted work with full week headroom. Post-binary parked.

**Achievement tier-threshold rework (PM-3 future vision).** Several ladders feel sparse at upper end (e.g. habits jumping 100 → 250 → 500 → 1000). Surgical add-tiers-between-existing-thresholds — preserves existing earned `member_achievements` rows, preserves Lewis-approved tier copy via `copy_status='approved'` gate. Workflow: audit 327 tiers identifying worst-spaced ladders → draft new in-between tier copy → bulk-approval doc to Lewis → SQL migration adding rows with `CASE WHEN copy_status='approved'` protection. Estimated 2 sessions. Trigger: real cohort feedback that next-tier-too-far is hurting engagement, or part of broader Achievements polish. Folded into PM-358 above.

**Locked / mandatory habits model (PM-153 follow-on).** Habits VYVE assigns to a member — monthly theme habits, autotick HealthKit set — should be MANDATORY. Member cannot remove. CAN add their own habits on top and remove the ones added themselves. `removable` boolean on `member_habits`, set false where `assigned_by IN ('admin','onboarding','autotick','theme_update')` and true where `assigned_by = 'self'`. Settings picker renders locked habits as un-checkboxable with "Set by VYVE" label (Lewis copy gate on label). Correct `autotick-7b` `created_by` mistag to `null` as part of same work (4 library rows, 3 members each). Not trial-blocking. Pairs with `member_habits` re-add duplicate fix (no unique constraint on `(member_email, habit_id)` — soft-delete + re-add creates second row; add unique + upsert that revives existing row).

**Certificate re-pillaring — SHIPPED PM-435.** See canonical entry in Infra / Data above. Remaining: visual design pass + Mind persona "The Anchor" Lewis sign-off.

**Accessibility — large text + WCAG pass.** Flagged by Alan 21 April (struggles to read portal at large-text iOS setting). Four-option plan at `plans/accessibility-large-text.md`. Option 1 (restore pinch-zoom) is 10min; Option 2 (in-app text-size toggle in Settings) is ~half day. Full WCAG 2.1 AA pass needed before public sector / Sage procurement.

**Onboarding "Where are you based" tightening.** Free-text → typeahead (city autocomplete) for cleaner geocoding. Pairs with local-sunset-aware rotation. OR accept ~10% failures with Stripe-country centroid fallback.

**Other "Soon" items (lower-traffic):** Load `vapid.js` on other portal pages (currently only index.html has push subscription), `certificate-checker` push notification on cert earned, Hash emails before sending to PostHog.

**Other "Later" items:** Social activity feed (spec pending Lewis sign-off on 7 product decisions), Dashboard widget customisation, Persona context modifier system, AI weekly goals Phase 2 (behavioural goals from check-in data), Weekly progress summary email (blocked on Lewis copy), PostHog / Supabase Auth identity wiring, Milestone message system, Today's Progress dot strip (blocked on Lewis copy), BIMI logo in inbox sender avatar (staged: audit SPF/DKIM/DMARC → UKIPO trademark pre-Sage contract → VMC + BIMI DNS post first enterprise contract).

**Grants & Partnerships:** National Lottery Awards for All application, The Fore grant (register June/July 2026), WHISPA research partnership (monitor May 2026 launch).

## Backlog — security & hygiene

- **Delete throwaway EF `posthog-test`** (PM-559) — used once to verify PostHog connection; retired-but-ACTIVE. Composio has no delete-EF tool; remove via Supabase dashboard.
- **Command Centre App Health build (PM-559 spec).** Spec at `/mnt/user-data/outputs/app-health-build-spec.md` (NOT in repo — re-request from Dean or rebuild from changelog PM-559). Build: `cc_app_health` cache table + `cc-app-health` EF (hourly cron, reads `POSTHOG_API_KEY`) + `app-health.html`. Errors-first ranked by members-hit; live-read `platform_alerts` for instant resolve; usage/dead-pages/load-times cached from PostHog; VYVE light+dark; reviewer-only via `is_admin()`. Unconfirmed for builder: exact numeric property name on `perf_*` events (probe before wiring load-times).
- **Command Centre data-layer migration (PM-559).** Lewis's SPA runs on localStorage; `cc-adapter.js` written but OFF; `cc_*` tables empty. Future: keep shell, wire pages to `cc_*` + Storage, RLS reviewer-vs-own visibility. Honour multi-tenancy/role dimension in schema before parallel build sessions.
- **Command Centre SMS/text alerts (PM-559, Dean-wanted).** "Drop everything" alerts via text — email insufficient. Needs Twilio wired into App Health detector EF; fire only on real threshold (e.g. serious error hitting 3+ members/hour) to stay rare. Build after dashboard reveals what qualifies.

**Security Quick Wins (from 16 April audit — long-tail).**
- Fix XSS: escape `firstName` in `index.html` before `innerHTML` rendering
- Fix `running_plan_cache` RLS: change `public_update` policy to `member_email = auth.email()`
- Fix INSERT policies on `session_chat`, `shared_workouts`, `monthly_checkins`
- Remove 3 redundant RLS policies on `members` table
- Add explicit service-role-only policies to the 7 aggregation/admin tables (document intent)

**Brain Hygiene (from 18 April reconciliation — still open).**
- Full rewrite of `brain/master.md` — accumulated schema + EF churn since last rewrite warrants own session, not patches. (Note: master.md is on a known cadence; this is a marker.)
- `monthly-checkin` integration smoke test — Deno test against throwaway test member before each deploy. The column drift that caused the 500 would have been caught by a single POST test against live schema.
- Delete `staging/onboarding_v67.ts` — stale by 7 versions (live is v74). Misleads future AI sessions.
- Archive pre-April changelog entries into `changelog-archive/2026-Q1.md` — current changelog is 22815 lines and growing unboundedly. [VERIFY: confirm current line count and timing.]
- Document user-ban workflow — `ban-user-anthony` v8 exists; anthony.clickit@gmail.com is in `auth.users` with no `public.members` row (orphan). Decide reusable pattern if bans happen again.
- Migrate `exercise.html` + `movement.html` off `#skeleton` + `#app` dual-main pattern. Both pages have `<div id="skeleton"><main>...</main></div>` wrapper before `<div id="app"><main>...</main></div>`. nav.js hardened via `document.body.prepend` so dual-main no longer breaks nav, but pattern is fragile — future utility doing `document.querySelector('main')` picks skeleton one. Single `#app` root with internal `data-state="skeleton|ready|error"`. Pair with Design System Phase D.
- Housekeeping from PM-378/PM-379 cross-repo collisions: rename `brain/staging/architecture-map-pm378.md` + sibling PM-379/PM-380 staging files to suffixed labels (`pm378b.md` etc) to disambiguate. No downstream references — single grep + file renames + single brain commit.

**Design System — Phase Roadmap (remaining).**
- Phase D: Component primitives (~2 days) — Shared `.btn`, `.card`, `.input`, `.modal-sheet` classes. Removes 72 unique button class names, 90 unique card class names.
- Phase E: Typography + spacing scale migration (~1 day) — Replace 118 unique font-size values and 264 unique padding values with `--text-*`, `--space-*` tokens.
- Future: `VYVE_Health_Hub.html` redesign + PWA linking — out of scope for Phases A-E.

**External legal / compliance markers.**
- BIMI staging (see Post-trial section).
- HealthKit submission-scope decision: submit all 7 reads, or phase to 4 (workouts + weight + steps + active_energy) with v1.1 for HR/sleep/distance.
- Privacy.html HealthKit section + Lewis sign-off + App Store Connect questionnaire (per next iOS submission).

## Recently shipped (last 7 days)

Captured here so the at-a-glance state of the recent past is one short list, not 24 SHIPPED blocks of varying length. All have changelog entries.

- **PM-413** (26 May) — iOS 1.4 build 3 + Android 1.0.5 versionCode 50 BOTH submitted to App Review. Bundle session execution end-to-end.
- **PM-412** (26 May) — iOS splash storyboard fix landed in vyve-capacitor remote `4f5f55ae`.
- **PM-411** (26 May) — Brain park / Bundle-prep prompt + Body-hub overhaul campaign documented for Thursday pickup.
- **PM-410** (26 May) — Replay catalogue wipe via YouTube archive + `refresh-replay-videos` v2 reconciliation. 33 test videos archived to private YouTube playlist. All replay-side Supabase wiped clean.
- **PM-409.b** (26 May) — Pre-bundle debug surface gating (debug strip + reset achievements + unified dev-panel flag).
- **PM-409** (26 May) — Facebook Make connection refresh scrubbed from forward-looking surfaces. Pre-bundle hygiene continuation of PM-407.
- **PM-408** (26 May) — Analytics taxonomy lands before Capacitor bundle freeze.
- **PM-407** (26 May) — Stale Lewis-blocker scrub: Brevo logo (never existed), health disclaimer (done), weekly+monthly check-in slider copy mirror (done).
- **PM-406** (26 May) — Pre-bundle offline scope fix; chart.umd.js vendored locally; PF-14c §2a violation closed.
- **PM-405** (26 May) — Bottom-nav opacity split on Connect + offline scope audit pre-bundle.
- **PM-404** (26 May) — In-App Tour PF-23 elevated to next-up P1 after bundle ships.
- **PM-403.b** (26 May) — Pre-bundle monitoring restoration design end-to-end (Sessions A+B build prompt drafted). Build executes next chat. Three-times-daily digest cadence locked, severity recalibration locked, circuit breaker threshold locked.
- **PM-402** (26 May) — Broadcast push infrastructure end-to-end. Lewis-facing manual broadcast UI in Command Centre + scheduled-push cron rails. SQL: `is_admin` RPC + `admin_broadcast_log` + `broadcast_schedules` + `resolve_broadcast_audience(jsonb)`. Two new EFs: `admin-broadcast-push` v2 + `scheduled-push-runner` v2. pg_cron job 28. Live at admin.vyvehealth.co.uk/#/broadcast.
- **PM-401.b → PM-401** (25 May) — Connect-page flicker arc closed in three atomic vyve-site ships. PM-399 Your Journey snapshot, PM-400 live carousel background-image + reaction-tap site surgical patch, PM-401 renderRecentCheckins same-layout guard. Plus PM-396/397/398 Home tab-in flicker fixes (value-mutation snapshot, layout-shift reservation, habit content-late instant-paint).
- **PM-395** (25 May) — Reaction-tap cache mutation pattern (closes tap-time own-only fallback flicker).
- **PM-393** (25 May) — Real fix to Connect tab-in flicker via inverted cache-first paint ordering.
- **PM-392** (25 May) — Connect feed prefetch + cache-first paint.
- **PM-391** (25 May) — Revert PM-390 reaction-subscriber double-mutation.
- **PM-390** (25 May) — §23.65 envelope-trusted subscriber sweep on connect.html + mind.html; engagement-v2 L1682 dead subscribe converted; events-rp.html unified onto canonical session-rp shell.
- **PM-389.b** (25 May) — Activity Breakdown tile counts on engagement-v2 rewritten Dexie-direct.
- **PM-388.b** (25 May) — Mood check-in faces swapped to canonical Lucide glyphs.
- **PM-387.b** (25 May) — JS twins of PM-386 SQL fix + Activity Breakdown tile parity for Body/Connect.
- **PM-386.b** (25 May) — Mind/Body double-counting class fixed at root.
- **PM-385.b** (25 May) — tracking.js retired from 8 *-live.html shells.
- **PM-384.b** (25 May) — checkin_questions catalogue (Tier 2.3 from PM-379 audit).
- **PM-382.b** (25 May) — body:logged aggregator publish from the three Body publishers.
- **PM-381.b** (25 May) — bus.js event-name regex loosened to 2+ colon segments.
- **PM-379.b** (25 May) — Monthly check-in recap rebuilt around canonical 4 pillars (Habits/Body/Mind/Connect) + EF v19 4-pillar activity rollup + AI prompt restructured.
- **PM-378** (25 May) — Podcast platform links catalogue: `podcast_platforms` table + podcast.html `.hero-listen` rewritten as Dexie-read with FALLBACK_PLATFORMS cold-paint.
- **PM-377** (25 May) — How-to library catalogue: `how_to_resources` table + how-to-pdfs.html / how-to-videos.html rewritten as catalogue reads.
- **PM-376** (25 May) — Bottom nav Body icon swapped dumbbell → person-standing (Lucide).
- **PM-375** (25 May) — Member Prompts system: Lewis-driven in-app questionnaires.
- **PM-372** (25 May) — `hydration.js` COPY_TABLE migrated to `public.persona_welcome_copy` (Tier 2.1).
- **PM-369** (25 May) — Haptics expansion across settings.html (13 call sites).
- **PM-367** (25 May) — Mind videos catalogue migration.
- **PM-365** (25 May) — Haptics: nutrition.html water stepper + weight log + TDEE save + log-food.html food add/delete.
- **PM-364** (25 May) — Haptics bridge swept platform-wide (haptics.js loaded on 41 activity surfaces).
- **PM-363** (25 May) — Achievements toast haptic wire at `showNext()` chokepoint.
- **PM-362.b** (25 May) — Weekly check-in recap rebuilt around 4 pillars.
- **PM-360** (25 May) — Home-page habit sibling renderer (paired with PM-359 habits.html haptic).
- **PM-359** (25 May) — Haptics: habits.html log tap + index.html long-press V-logo + settings.html theme + notification toggles.
- **PM-354/355** (25 May) — Brain whole-file overwrite hazard; §23.58 codified.
- **PM-353** (25 May) — Achievements v3 Dexie-first evaluator rollout, all 6 pillars LIVE.
- **PM-319** (25 May) — Mind activities watch tracking.
- **PM-315** (25 May) — CSP fix for YouTube IFrame API on all 8 *-live.html shells (root cause of PM-304 silent failure).
- **PM-310 → PM-312** (25 May) — Live tracker debug surface; Settings Reset Achievements + Force Refresh App buttons.
- **PM-307** (25 May) — Movement first-class. `movement_activities` table live with 24 rows migrated, RLS + triggers match `mind_activities`. movement.html rewritten single-table, movement-history.html simplified, engagement-v2.html BODY metric extended to sum workouts+cardio+movement. Bus event taxonomy locked — `movement:logged` from movement.html only.
- **PM-300/301/305** (25 May) — engagement-v2.html three-tab shell + Your Journey rename + count hero promotion + charity callout + v1 engagement.html retired (redirect-only).
- **PM-298 + PM-296** (24 May) — Settings habit-save Supabase write failure fix (keepalive + on_conflict + merge-duplicates).
- **PM-295** (25 May) — Engagement Score v2 implementation phases 1-4 (schema, v2 SQL function alongside v1, JS port with parity 72/72 on real data, new engagement-v2.html behind `?score=v2` flag, bus subscriber wiring on all score-affecting events).
- **PM-293/291/287/286/285** (24 May) — Home habit surface, cross-page sync, pot-coloured habit tiles.
- **PM-286** (24 May) — PM-215 YouTube broadcast-creation cron paired with PM-251 consumer contract. `session-publish` EF v1 + `session_categories` + `session-publish-hourly` pg_cron.
- **PM-284 + PM-283** (24 May) — Focus done-state device validation (body.is-completed auto-stamp via MutationObserver, page-reopen done-restore in shared chrome).
- **PM-278** (24 May) — `VYVEHaptics` bridge shipped.
- **PM-274 phase 1** (24 May) — Twelve `/focus/<slug>.html` pages live with shared chrome + §23.39 Dexie-write dispatch. Home carousel taps now route to functional pages.
- **PM-267b → PM-261b** (24 May) — Seven-commit hero arc brought home into full §23.55 + §23.57 hub-page compliance.
- **PM-256/257** (24 May) — Home redesign atomic ship.
- **PM-255** (24 May) — Past Sessions + My PRs Dexie wiring (promoted to standalone `personal-bests.html` + `workout-history.html`).
- **PM-251** (23 May) — Live session pages redesign: schema migration + 8-shell rewrite + 5-state engine + sessions.html hub gate end-to-end.
- **PM-250** (23 May) — Web-shell rotate-overlay deletion + vyve-capacitor commit locking portrait at OS layer.
- **PM-242** (23 May) — Profile identity partial ship: read-side identity rendering on connect-feed.html + connect.html Recent Check-Ins LIVE.
- **PM-235b** (~24 May) — Hourly replay refresh cron.
- **PM-228** (24 May) — `member-avatars` Storage bucket.
- **PM-225** (24 May) — `public.taglines` table.
- **PM-215** (24 May) — YouTube broadcast-creation cron landed (paired with PM-251 consumer).
- **PM-213** (23 May) — Live session pages redesign superseded by PM-251.
- **PM-212 + 212.1-.7** (23 May) — Podcast hub MVP + six same-day follow-ups.
- **PM-211 spec** (23 May) — Single source of truth for live sessions specced (build deferred post-launch — see Connect hub).
- **PM-210b** (23 May) — Connect calendar member UI shipped end-to-end. `calendar_occurrences` table, db.js SCHEMA_V9, sync.js plan entry + CATALOGUE_INVALIDATION_KEY bump to `pm210-calendar-occurrences`, connect-calendar.html NEW 798 lines.
- **PM-209 + PM-209.1** (22 May) — Mind hub Today's Focus tile: thumbnail fills the card; §23.52 earned.
- **PM-201/200/199/198** (22 May) — Connect hub posted-state polish + community preview EF + reaction count from Dexie + Elite hero card.
- **PM-187 + PM-186** (21 May) — Connect Phase 2: spec lock + 5 tables migrated + counters-render-truth (§23.46).

## Dexie resilience — PM-436 follow-ups (added 2026-06-02)
- **Graceful open-failure fallback** — when `db.open()` rejects / `VYVELocalDB.isEnabled()===false`, aggregate surfaces (home rings `loadPillarCounts`, habits.html) must NOT render a confident `0/30` / "no habits". Either a neutral placeholder ("—") distinguishing "unknown" from "zero", or fetch server-side pillar counts (extend `member-dashboard` EF to emit the 5 lifetime ring counts — today no server source: `member_home_state` carries old-vocab `*_this_month` + v2 engagement points, not lifetime per-pillar). Also covers the separate iOS "IndexedDB lost" (PF-30) pattern. Talk-first (read-path blast radius). §23.83 corollary.
- **`dexie_open_failed` / `dexie_idb_lost` alert** — telemetry already emitted in db.js open `.catch` (PostHog). Wire a threshold alert so this class is caught instantly, not by eye. Quick.
- **Option B — multi-plan-local mirror via NEW store** — re-do PM-425's goal (both workouts + movement wpc rows mirrored locally) via a new id-keyed store (e.g. `workout_plan_cache_v2`), repoint sync.js persist + workouts-programme.js / movement.html reads, leave the old member_email store untouched. Post-launch, with a real-iPhone upgrade pass. Never re-key the existing store (§23.83).


## Go-live tail — PM-459 (2026-06-04)
- Make the runner hands-off: leave `python3 vyve-live-runner.py` daemon running, or install `com.vyve.live-runner.plist` launchd (needs the service key in an env file). 4 Jun 07:00 is the first scheduled real air — daemon must be up by then.
- Turn OFF `session-publish-hourly` (cron jobid 27) once the daemon owns broadcast creation.
- **SECURITY: rotate the service_role key** — exposed in the assistant chat 2026-06-04. Reset Supabase JWT secret (also rotates `anon`) + update all consumers (EF secrets, runner env, clients). Disruptive — plan it, not mid-air.
- Delete the ~00:43 BST test VOD (`qOmK6vZeTKo`, Yoga Flexibility) from YouTube Studio.
- 41 stale pre-re-curation calendar rows (22 May–3 Jun) still active — cleanup.
- Content calls: type-vs-per-host thumbnail mapping; drop numeric labels on flow/flexibility titles; connect-calendar card title reads `row.name` not `session_title`.


## Go-live tail update — PM-460 (2026-06-04)
- DONE: always-on runner installed as launchd LaunchAgent `com.vyve.live-runner` + relocated to `~/vyve-live` (macOS TCC); daemon healthy (PID, exit 0).
- DONE: cron 27 `session-publish-hourly` deactivated — daemon is sole broadcast owner.
- STILL OWED: rotate the exposed service_role key (now also in `~/vyve-live/vyve-runner.env`; reload the agent after); deactivate the 40 stale PAST empty-notes rows (22 May–2 Jun); delete test VOD `qOmK6vZeTKo`; watch first unattended air 4 Jun 07:00 (also the live:true device-walk sliver); content calls (type-vs-per-host thumbs, numeric labels, connect-calendar title row.name vs session_title).
- Mac-as-box caveat: keep plugged in + lid open; real home is a 24/7 server (systemd unit in repo).


## Replay clear — PM-461 (2026-06-04)
- DONE: all replay pages wiped to clean slate — 11 test videos deleted off YouTube, all 8 category playlists emptied, DB mirror (replay_videos/replay_playlists) zeroed. Empty replays are EXPECTED until sessions air from 4 Jun.
- DONE: test VOD qOmK6vZeTKo deleted (was a separate owed item).
- CONFIRMED: runner playlist-inserts each broadcast into its category youtube_playlist_id at creation; replays backfill automatically via the refresh cron. Live pages verified non-blank + correct category targeting (§23.90 / changelog PM-461).


## Box runbook + state — PM-462 (2026-06-04)
- DONE: live box operational runbook locked — launchd daemon + `caffeinate -i` + `sudo pmset -c sleep 0` (never sleep on mains). Run rule: plugged in, lid open, screen may be black. Sessions air automatically through 2 Jul.
- DONE: 4 Jun–2 Jul schedule (112 sessions) shared with Alan + Lewis.
- OPEN: first unattended air 4 Jun 07:00 — Lewis to eyeball the live:true flip.
- OPEN (carried): rotate service_role key; deactivate 40 stale empty-notes rows; calendar regeneration before 2 Jul; content calls; move runner to a real 24/7 box.


## BACKLOG — PM-608 — Proactive health alerting (issues must reach Dean before they become issues) (2026-06-12)
**Trigger:** a workouts bug (members onboarding with NO workout_plan_cache row generated) went undetected ~2 weeks because nothing watched for it; Dean found it by accident. Root cause of the blind spot: alerting watches emails (email-watchdog) + feeds a dashboard (cc-app-health) but NOTHING verifies onboarding actually worked end-to-end. onboarding v97 alerts only fire on *synchronous* throws; the background `waitUntil` plan-gen can fail silently with a 200 already returned.
**Full build spec lives in a handoff prompt** (see this session). Two phases:
- **Phase 1 — onboarding integrity monitor** (new `onboarding-health` cron EF, ~30 min): flag any member with onboarding_complete=true >15min ago but missing any of: active workout_plan_cache row (for workouts/movement stream), member_habits, auth user / signed-in (last_sign_in_at), current-week weekly_goals, welcome email sent, or flagged writeMember_core_fallback. Dedupe via a `health_alerts` table; immediate Brevo alert per new case; daily "all clear / N open" heartbeat into alert-digest.
- **Phase 2 — app usage health monitor** (daily into alert-digest): DAM drop vs 7-day avg; zero-activity-of-a-type platform-wide >24–48h; onboarding throughput → 0 for >24h (Stripe→onboarding broken); background-job health (plans generated vs onboardings started — catches waitUntil dying); push/email send anomalies. Reuse is_dean/is_test/staff filters.
**DO FIRST:** read-only backfill — count members ALREADY broken by the workouts bug (no plan / no habits / never signed in) and remediate that backlog, not just stop future cases.
  - ✅ **PARTIAL (PM-615, 15 Jun):** cardio plan-less gap closed — `ensure_foundation_running_plans()` reconciler (cron jobid 47, */5) assigns "Starter: Your First 5K" to any onboarded cardio member with no active plan; backfilled Paul Skipper + Kieran Day. Pattern extensible to other streams / no-habits cases.
  - ✅ **DONE (PM-617):** Consent-version stamping gap closed — server-side trigger `trg_default_consent_versions` fills `privacy_version`/`health_consent_version`=`'v1.0'` (fill-null-only) on any consent write, all clients/builds; 9 v1.0-gate rows backfilled; residual 0 (§23.119). RIDER: consent-gate.html should also write the two version fields at source on the next vyve-site build (gate stamps `terms_version` but omits these).
**Existing machinery to extend, not rebuild:** alert-digest EF (crons 29/30/31), platform-alert EF, email-watchdog (cron 16), cc-app-health (cron 38).

## BACKLOG — PM-607 follow-ups (onboarding resilience) (2026-06-12)
- Consider making members.weight_unit / height_unit NULLABLE (belt-and-braces) so a missing unit can never hard-fail regardless of code. v97 already coerces to 'kg'/'cm' in code (§23.117).
- Generalise the writeMember core-fallback principle (§23.118): audit other member-facing writes for all-or-nothing inserts that could lock a member out on one bad optional field.
- Welcome-email coach-voice rewrite into onboarding EF — AFTER Lewis approves the two mockups (welcome-email-v2-mock.html, coach-voices-comparison.html). HAVEN copy pre-written + Phil clinical sign-off, not AI-generated.
- Shaun Baker (shaunbaker122qa@gmail.com): password set to Mario123! via set-member-password — he should sign in directly with email + that password, NOT tap reset links.


## Partner onboarding — modules deferral (PM-659, 2026-06-22)

- **HELD migration awaiting Phil + Lewis sign-off:** `HELD_partner_modules_deferred.sql` (pasted in PM-659 chat) makes `compute_partner_onboarding_pct` + `assert_partner_golive` deferral-aware so a `modules_deferred` partner can reach 100% and go LIVE without the Safeguarding/GDPR assessment. Compliance posture decision (partner delivers live member sessions pre-safeguarding) — Phil (clinical) + Lewis (commercial/legal) must approve. Until applied, go-live BLOCKS deferred partners (correct; 0 live today). Apply trigger = first real deferred partner reaching go-live, OR the Video-modules/Education steps returning.
- **When modules return — re-enable + back-date:** add `3,4` back into `ACTIVE` in `partner-onboarding.html` (indirection handles the rest); client stops sending `modulesDeferred`; then `UPDATE partner_onboarding_progress SET steps=(steps-'modules_deferred')||'{"videos_watched":false,"safeguarding_passed":false,"gdpr_passed":false}' WHERE steps->>'modules_deferred'='true';` (gate reverts to full 8-step; they must complete for real).
- **Legacy partner rows:** 3 `partner_partners` rows (0 live) onboarded under the OLD faked-gate EF (v3 and earlier) show `safeguarding_passed/gdpr_passed=true` despite the gate never being honestly enforced; NOT `modules_deferred`-marked. If any are real partners (vs test rows), decide whether to re-flag / require real assessment. Verify identity before acting.
- **EF honesty note:** `partner-onboarding` EF before v4 hardcoded all go-live gates true at submit. v4 fixed this. Any analysis of partner onboarding completeness pre-PM-659 should treat those gate booleans as unreliable.

### Partner referral link — follow-ups (PM-682)
- **CC `partner-portal.html` Earnings "share this link" shows the WRONG link.** It renders `partner_partners.payment_link_url` (the bare £20 Stripe link) with "£10 off pre-applied" text — both now wrong. Should show the branded `https://www.vyvehealth.co.uk/join/<slug>` URL (that's what mints the discounted session). Quick patch to the Earnings render in `partner-portal.html`.
- **Deactivate the unused Men Together Stripe payment link** `plink_1Tlycv...` (bare £20, no longer referenced — the EF mints sessions now). Stop creating payment-link objects for new partners (§23.127; playbook updated).
- **`partner_partners.payment_link_url` / `payment_link_id` vestigial** — nothing reads them for the redirect. Harmless; consider dropping in a later cleanup.
- **Optional instant community-join** (currently ~15 min via reconcile cron #52): wire `welcome.html` to read `?partner=<slug>` and have the `onboarding` EF write the `partner_memberships` row from the `cs` (checkout session id) already in the success_url — leaves the earnings webhook untouched. Only if 15-min lag proves insufficient. The `?partner=` tag already rides the URL, currently inert.
- **`stripe-reconcile` cron #52 jobname still `stripe-reconcile-nightly`** but now runs `*/15`. Cosmetic; rename if it bothers.
