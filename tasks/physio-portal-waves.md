# Physio portal — wave map (PM-1207, 12 Sep 2026)

*Trigger: "physio wave N" / "start physio wave N" / "physio backend wave N". Load this file + `tasks/physio-rehab-spec.md` + `prompts/session-physio-rehab-s2.md`, then start from the wave's brief. Estimates are Claude-assisted sessions.*

| Wave | Status | Sessions | Scope |
|---|---|---|---|
| **0 — Portal split** | **SHIPPED 12 Sep 2026 (PM-1208/1209, CC `13d08aa7` + `6c27724d`)** — `src/portal/` + `tools/build-portals.js`, `physio-portal.html`, preview sheet in both libraries, `coach_exercises.library` shelves + coach opt-in; shared = REST + auth + library only (roster/messaging/nav close over the client workspace, §23.333); physio messaging moves to Wave 3 | 1–2 | Break `coach-portal.html` into a shell + shared modules (auth, REST layer, roster, library + video, messaging) + coaching modules; create `physio-portal.html` as a thin page on the same shared modules with the rehab sidebar (Patients · Plans · Library · Templates · Messages · Settings). No behaviour change for Calum — byte-identical rendering proven on the CC before and after. Physio copy says physio/patient, never coach/client. **Also in Wave 0 (Dean, 12 Sep evening):** (a) **inline video play in the exercise library, both portals** — a preview sheet off the card's play button: native `<video>` for a file, YouTube embed for a link, name + cues beside it (today only direct mp4s play; YouTube rows show a thumbnail and nothing else — no player exists in the file); add `youtube-nocookie.com` to the CC page's frame allow-list. (b) **Separate libraries:** `coach_exercises.library` text CHECK (`strength`,`rehab`) DEFAULT `strength`; coach portal reads `strength` + own rows, physio portal reads `rehab` + own rows; a coach can opt the rehab set in via a settings toggle, never by default; the RMP demo rows get `library='rehab'` in the same migration. Schema talk-first with Dean before it runs. |
| **1 — Mockup** | **SHIPPED 12 Sep 2026 (PM-1210, CC `f793dc9b`, `docs/mockups/physio-rehab-mockup.html`) — awaiting Dean → Lewis → PT** | 1 | Three clickable screens on the CC design tokens, dark first: (a) physio plan builder, (b) patient Rehab tab with a session and its post-session monitor sheet, (c) physio patient view with Tracking + Monitor populated from a fortnight of seeded data. Standalone HTML; no schema, no code paths. Gate: Dean → Lewis → the PT partner. |
| **2 — Plan builder** | **SHIPPED 12 Sep 2026 (PM-1211, CC `8764a5b8`; migrations `pm_physio_w2_*`)** — `capabilities.rehab` gate, `rehab_plans` + `rehab_plan_items` (soft-remove) + both log tables + `rehab_apply_plan` → wpc `surface='rehab'`; builder in `physio/plans.js` with templates (`coach_templates` kind `rehab_plan`) and + Add patient over the existing invite/trial rail; Lewis still owes `trial_days` | 1–2 | Talk-first schema: `rehab_plans` (name, patient, partner, start, duration_weeks, note, safety_netting, red_flags, monitor jsonb), plan items with the rehab prescription (hold_s, reps, times_daily, rest_s, days_per_week, both_sides), plan-level templates by phase/diagnosis. Builder in `physio-portal.html`; add-patient → VYVE invite via the partner trial rail (Lewis: duration). |
| **3 — Patient side** | **SHIPPED TO MAIN 12 Sep 2026 (PM-1213, vyve-site `2f89d129`, vbb 643 — NOT OTA'd; Dean's dev-shell check owed)** — `rehab.html` as a Body stream (Dean: no nav/Home change), guided timer player, logs, monitor sheet, reminders, `vyve-countdown.js`, SCHEMA_V29; Lewis strings + Phil copy open | 2 | Rehab tab in vyve-site (hidden without an active plan): plan hydrate into Dexie (merge, never wipe — §23.43), session player driven by the rehab prescription, done/skipped + like per exercise, once-daily monitor sheet from the plan's selected metrics, question-to-physio into the coach thread, push at the prescribed times of day, streak. Main → Dean's dev shell first; OTA only on his word. |
| **4 — The loop** | **SHIPPED 12 Sep 2026 (PM-1215, CC `8bdef863`, migration `pm1214_rehab_alerts`, cron 78)** — `physio/patient.js` (Plan · Tracking · Monitor, to-date adherence ring, dot grid, liked/disliked, per-metric charts, questions + Reply), `physio/messages.js` (physio thread over `coach_messages`), `rehab_alerts` + nightly `rehab_alert_scan()` (pain rising / adherence low, Phil placeholder thresholds) surfaced as banner / pills / thread cards, never into the patient's thread (§23.337); Dean's deploy check owed | 1 | Physio patient view: Plan · Tracking (done vs skipped, liked vs disliked) · Monitor (line per metric). Adherence = done ÷ (times daily × days per week × duration). Alert into the physio's thread on pain rising / adherence dropping for 2–3 weeks (thresholds: Phil). |
| **5 — Library depth** | **SHIPPED 12 Sep 2026 (PM-1216, CC `d65ab026`; migration `pm_physio_w5_subcategory`)** — `coach_exercises.subcategory` + chips under a region in the library and the picker (both portals, shared slice), By condition entry = `rehab_plan` templates grouped by `payload.diagnosis` / ordered by `payload.phase` with Add-a-phase, template save form + grouped Templates view + Details re-tag, `physio/print.js` print sheet (browser Print → Save as PDF) from builder and patient view; demo rows keyword-backfilled (dies with them). Dean's deploy check owed | 1 | `subcategory` column + sub-filter under body regions (Flexion, Extension, Rotation, Stretching, Strengthening, Advice…); diagnosis-led entry (condition → curated set); phased templates; PDF plan export as the paper fallback. |
| **6 — Later** | PARKED | — | AI draft plan (Anthropic EF over the tagged library), advice sheets as plan items, PROMs, translation, multi-practitioner clinics. |

**Cross-cutting gates.** Lewis: trial duration, product name, every patient-facing string. Phil: safety netting, red flags, monitor wording, alert thresholds. Dean: schema talk-first, CC deploy checks, OTA.

**Constraints.** The RMP demo rows (`source_slug LIKE 'rmp:%'`, partner `a23478c8`) are a demo with a ~12 Oct 2026 tear-down; nothing in any wave depends on them. Product video = own recordings on own storage, never YouTube (§23.331). `coach-portal.html` split trigger also stands on its own: do it when the file passes ~1.2MB regardless of physio (§23.332).
