**PM-1217 (2026-09-12, 23:55–00:40): CALUM REVIEW R1 SHIPPED — coach portal, CC `e456db20`. No migration, no EF, no vyve-site change.**

**Input.** Calum's first feedback batch on the coach portal (the PM-1212 seed gave him the ten demo clients to review against). Full list, triage and batch plan in `tasks/coach-portal-calum-review.md` (trigger: "calum review" / "calum batch N"). He reviewed Programmes, Weekly workouts, Day templates, the Exercise Library and Profile; more chunks to come.

**The bug.** "Assign to…" on every workout template card said "No clients yet" while he had ten clients plus himself. `w3Clients()` (`134-w3-workout-depth.js`) selected `first_name,last_name` from `coach_clients` — the columns are `invited_first_name`/`invited_last_name`, PostgREST returned 400, `catch(e){ w3ClientsCache = []; }` turned it into an empty state. Live since PM-985 (3 Sep) and never caught because Batch assign uses `roster` (select=*) and the "N clients" badges on template cards — the same cache — read as "unassigned" for everyone, which looked plausible. Fix: select the real columns, map `first_name`/`last_name` onto each row so the three modal renderers are untouched (workout assign, nutrition assign ×2 in `140-w4-pm986.js`, plus the badge counts). §23.338.

**The "massive images".** Template rows render `w3ThumbImg(first exercise, 'w3-exthumb')`; the only CSS for that class was `.de-row .w3-exthumb` (builder rows), so in the plans list the YouTube `hqdefault` painted at native size. Base rule added in `132-shared-library.js` (`56×40`, the `.de-row` override keeps `40×30`).

**The rest.** Quick view (the eye) removed from both render sites — Preview stays, `w3QvOpen` left in place; Group → **Superset** with a title hint ("same letter = back-to-back"); "▶ marks exercises…" reworded so the line doesn't open with the glyph (he read it as "Mark's Exercises"); `#pl-title` on the plans card + `PL_TITLES` per kind ("Programmes", "Day templates", …) instead of "My plans & forms" everywhere; on the sidebar deep links (`kindsel:*`) `go()` leaves exactly one kind chip visible and active — that was his "tab that takes you to the page you're on" — the new slice hides the row when only one chip is visible (section links with several kinds keep it); `input[type=date]` calls `showPicker()` on click (capture-phase delegate, try/catch — Chrome/Safari only open on the icon otherwise); `.cp-side` scrollbar on the theme tokens (`scrollbar-color` + webkit). New slice `320-calum-r1-pm1217.js` after `310`; `physio-portal.html` byte-changed by the two shared CSS additions only.

**Proof.** `build-portals --check` + `smoke-portals` green; SQL against Calum's partner (`3d552455…`): 10 assignable rows, `invited_first_name` populated. Ten files md5-perfect at `e456db20`. **Not device-checked** — Cloudflare deploy check owed.

**Not fixed in r1.** Horizontal overflow on his laptop — the sidebar-scrollbar remark says his viewport is ≤840 CSS px (the burger breakpoint), i.e. a small laptop at high display scaling; nothing in the shell has a fixed width above that, so it needs his screenshot + `innerWidth` before hunting.

**Open.** Dean: Cloudflare CC deploy check — as Calum (or Dean's `a23478c8`): Workouts › Weekly workouts → card thumbnails small, no eye button, title reads "Weekly workouts", no lone chip; Assign to… lists the ten clients; Clients › a client › Goals → click the date field → picker opens. Calum: screenshot of the side-scroll + browser width; the four call items (Weekly Workouts naming, Goals "Target", blocks vs Kahunas, duplicate-vs-edit-in-place). Batch 2 (builder ergonomics) next when Dean says.

---

