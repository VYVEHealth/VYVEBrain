**PM-1218 (2026-09-13, 00:45–01:20): COACH PORTAL — LAYOUT CENTRED ON WIDE SCREENS + NOTIFICATION REMINDERS STAMPED HONESTLY. CC `f358d457`. No migration, no EF, no vyve-site change.**

**Layout (Dean's 27" vs 15" screenshots).** `.cp-content` had `max-width:1500px; margin:0 auto` in the base styles and then `margin-left:228px` in the sidebar block — the second rule wins, so the column pinned hard left of the sidebar and a 2560px monitor showed ~830px of dead space on the right, while a 15" window (< 1500px) happened to fill. `shared/head-styles.html`: `.cp-content{margin-left:max(228px, calc(228px + (100% - 228px - 1720px) / 2))}` centres the column in the space beside the sidebar; cap raised 1500 → 1720. Dashboard: the Latest messages card moved inside `.dash-2col` as `.dash-msgs-card` (full-row below 1700px, third column at `min-width:1700px` with `1.45fr 1fr 1fr`). Both portals get the centring (shared shell); the ≤840 burger rules are untouched. Ruled out: stretching edge to edge — width caps are deliberate for reading width; browser zoom is the lever for "more on screen".

**Notifications (Dean: every row highlighted and stamped 12 Sep 19:44 when he clicked the bell).** Two synthesised reminder kinds — trial ending (`060-leads-cal-notifs.js`) and goal due (`200-tz-w1-pm1075.js`) — were pushed with `t: new Date().toISOString()`, the page-load time. Consequences: they sorted above everything, every one read "just now", `fresh = e.t > seen` was always true so they were always highlighted, and the badge (10 = 8 trial + 1 goal + 1 real) could never clear — "Mark all read" set `seen` to now and the next load stamped them later than that. Fix: a reminder is stamped with the moment it became due — trial: `trial_ends_at − 5d` (or `trial_ends_at` once ended), goal: `target_date − soon_days`, both clamped to now — and carries a `when` label ("Ends Tue 15 Sep", "Due today") that the feed row (`200` `nfRender`, the live one) and the bell row (`110` `bellRender`) show in place of a clock time. Real events still show their time. The bell click now marks seen after rendering (`110`): new rows are highlighted on the open that reveals them, the badge drops, the next open is calm; "Mark all read" stays for the feed page. `localStorage` last-seen mechanism unchanged (§19 PM-958e).

**Proof.** `build-portals --check` + `smoke-portals` green; seven files md5-perfect at `f358d457`. **Not device-checked.** Dean: reload the coach portal on the 27" — column centred with balanced margins, three dashboard panels across; bell → the eight "Trial ends in N days" rows read "Ends <date>" and sit under today's real events, badge clears after the open. Calum's laptop side-scroll still needs his screenshot.

---

