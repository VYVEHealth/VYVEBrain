**PM-1220 (2026-09-13, 02:00): EXERCISE LIBRARY MOCKUP — CC `bddf35c1`, `docs/mockups/exercise-library-mockup.html` (unlinked, standalone, 24KB, dark first on the CC tokens). Nothing built; a picture for Dean to react to.**

**What it shows.** Two clickable modes over the same rail, grid and cards. (1) **Library page** — left rail: All (1,455) · My exercises · ★ Favourites · Recently used, then collapsible groups with live counts: **Muscle group** (Quads 148 · Shoulders 129 · Abs & core 96 · Chest 85 …), **Equipment** (Barbell 180 · Dumbbell 144 · Bodyweight 136 · Cable 95 …), **Stretching & mobility** (All 478 · Knee 51 · Hip 29 · Ankle 24 · Nerve glides 57 · Senior 72 …). Toolbar: search, Video any/has/missing, "VYVE video first", sort A–Z / most used / newest, + New exercise. Card: 16:10 thumbnail with ★, source badge (VYVE / Stretch / Mine in gold), ▶ badge when a video exists, name, "Quads · Kettlebell" meta, Preview · Duplicate (Edit on own rows). (2) **Picking into a session** — same page plus a right column "Push A · Session 1 of 4" that fills as Add is tapped (default tracking shown: 3 × 10, or 30s hold for a stretch), Cancel / Done · N. Preview sheet: video area, cues, tracking + primary/secondary muscle tags, Add to session (picker only), Duplicate to my library, Favourite. Light/dark toggle in the bar. jsdom pass: rail filters, stretch group, picker add, search, sheet — zero errors.

**Data finding that shapes the build.** The stock taxonomy is dirty: `category` mixes casing (`glutes` 22 + `Glutes` 10), compounds (`Quads / Glutes / Cardio`, `Core / Obliques`, 14 such rows) and `Legs`/`Full Body`; `equipment` the same (`dumbbell` 123 + `Dumbbells` 16 + `Dumbbell` 4, `body only` 111 + `Bodyweight` 24, `Cables`/`cable`/`Cable Machine`). That is the root of Calum's "muscle filter returns random results" (#17) as much as the fuzzy `muscle_volumes` map. Batch 2 starts with a normalisation migration (one casing, one primary muscle, compounds split into primary + `muscle_volumes`), because a rail with counts is only as good as the counts. Also: 974 of 977 stock rows have an `image_url`, only 111 have a video — cards lean on the image, ▶ only where a video exists.

**Open.** Dean: open `admin.vyvehealth.co.uk/docs/mockups/exercise-library-mockup.html` (Cloudflare deploy), click through both modes, say what's wrong. Decisions the mockup asks for: (a) rail groups = muscle / equipment / stretching — or should stretching sit under muscle group as regions; (b) "Recently used" — worth a query, or drop; (c) Duplicate-vs-edit-in-place on stock rows (Calum's call item) — the mockup keeps Duplicate; (d) picker = the same page with a column, or a sheet over the builder. Build order once approved: normalisation migration → shared library slice (`132-shared-library.js` grows the rail + grid, both portals) → picker column in the day builder (#11) → thumbnails in the Weekly workout view (#12) → search on the template lists (#14).

---

