**PM-1239 (2026-09-13): RUNNING WAVE 0 SHIPPED — SCHEMA + DETERMINISTIC PACE ENGINE LIVE, SERVER ONLY.**

Eight `run_*` tables (migration `pm1239_run_wave0_schema`) and **`run-engine` v3** (`verify_jwt:false`, internal-key or member-JWT gated). Nothing member-facing; `running_plan_cache` (7 rows) and `member_running_plans` (16 rows) untouched and still serving, per the §0 blast radius — Wave 3 retires them, not this.

**Catalogue:** `run_plan_templates` (goal, target distance, week + days ranges, `mode` block|rolling, level band) · `run_session_templates` · `run_template_steps` (block warmup/main/cooldown, ordinal, run|walk|rest, distance **or** duration, band reference, target-vs-limit, repeat group + count) · `run_template_weeks` (template × week × day slot → session + volume multiplier + phase + long-run flag). **Member:** `run_member_profile` (fitness score, up to two race inputs with source, five bands, volume/difficulty knobs, injury flag, effort mode, units, load estimates, confidence) · `run_member_plans` (one active per member, partial-unique `client_id` per §23.10) · `run_member_sessions` · `run_member_steps` (band reference **plus** a resolved pace snapshot, so Wave 6 re-baselining rewrites paces without rewriting structure). RLS: member tables own-rows on `auth.email()`, catalogue authenticated-read and only where `is_active` — proven by claims simulation (owner 1/24/57/1, stranger 0/0/0/0, and 0 templates while the fixture stays inactive). All four member tables registered `purge` in `gdpr_table_policy` (`pm1239_run_wave0_gdpr_register`).

**Three calls made at the foundation.** (1) **Everything stores SI** — metres, seconds, seconds per km; miles and min/mi are a rendering choice, never a stored one. (2) **`mode` = block | rolling with `end_date` nullable**, so the open rolling-vs-fixed question for General training is a per-template flag in Wave 1, not a schema commitment now. (3) **Engine in TypeScript, not SQL** — pure functions, no `Date.now()`, no randomness, no I/O: Daniels-class VDOT from a race, the inverse velocity solve, five bands as fractions of vVDOT (easy .72–.62, long .70–.60, threshold .88–.83, interval 1.00–.95, rep 1.06–1.02, shifted ±2% by the difficulty knob), Riegel equivalents, and template + params → dated sessions. Two race times blend 0.4 short / 0.6 long and stamp confidence `high`; one stamps `medium`.

**Proven by fixture, live, not locally:** two identical `build` invocations through `pg_net` returned **byte-identical 23,977-char responses (md5 `3b31b4b8`)**; no key and a wrong key both 401. Fixture 6-week 5k block (`fixture-5k-improver`, every catalogue row `is_active=false` so it can never be served or pollute Calum's Wave 1 batch): 25:00 5k → VDOT 38.3, threshold 5:24–5:44/km, 163.8km over six weeks ramping 26.5 → 35.0 with a recovery week 4 and a taper week 6. `persist` wrote 1 plan / 24 sessions / 57 steps / 1 profile onto a demo member and was deleted after — residue zero.

**Corrected mid-session:** the first day-selection rule put the easy run on Saturday in front of a Sunday long run. Any four-of-seven week has an adjacency; the rule now spaces days evenly *forward* from the long-run day (4 + Sunday → Mon/Wed/Fri/Sun), so the adjacency lands after the long run. §23.346 banks the other one: `VYVE_INTERNAL_KEY` is **not** in the EF environment — internal callers present `x-vyve-internal-key` and the EF resolves it through the service-role-only `vyve_internal_key()` RPC (crisis-scan v2.2's pattern).

**NEXT: Wave 1 — the template library.** Still open on Dean: Calum's authoring surface (CC page vs migration + sheet), which goals ship first, rolling-vs-fixed for General. Lewis: strings. Phil: the injury question.

---

**PM-1238 (2026-09-13, 22:20): RUNNING REBUILD — BLAST RADIUS PINNED (doc only).**

Dean's scope check answered in writing as §0 of `tasks/cardio-running-waves.md`, ahead of the numbered sections so it is the first thing any session reads. **Replaced at Wave 3 and not before:** `running-plan.html` (soft-kill only — unlinked, file kept, one-line restore), the running path's `anthropic-proxy` call (the EF itself stays, other features use it), and `running_plan_cache` / `member_running_plans`, both of which stay live until a working replacement ships — new work lands in new tables alongside. **Untouched:** `cardio.html`, `cardio-history.html`, nav, bottom bar, Home, and every other pillar; the plan lives inside Body as a stream like Physiotherapy (PM-1213). **One deliberate crossing:** Wave 4's completion tick inserts a `workouts`/`cardio` row through the existing promotion mechanism so runs count for streak, charity and achievements — an insert, not a shared-schema change, and PM-150's caps-apply-to-manual-only rule means promoted rows bypass the cap. **One pointer moves at Wave 3:** `members.exercise_stream = 'cardio'` lands on the new surface instead of `running-plan.html`. Reversible.

---

**PM-1237 (2026-09-13): PRICING CORRECTION — £20 IS THE ANCHOR, NOT RETIRED.**

PM-1236 recorded the £20 list price as retired. Wrong, and corrected in place. **Dean: £20 is the full list price and stays — precisely so the real £10 can be marketed as a half-price rate.** Nobody pays £20 in practice except a walk-up signup straight off the website. So the split is: **£20 = anchor, marketing reference and B2B ROI comparator; £10 = the number to plan revenue, the £90k VAT threshold and partner payouts on.** Corporate £10/seat or lower.

**The payout engine is already correct on this** — §23.128 stamps `partner_memberships.subscription_value` = NET PAID, never the £20 list, so a £10 member pays the partner £5, not £10. The tier-coupon ladder (§23.129) is the same mechanism: partners discount off the £20 list, and the partner is paid on what landed. Nothing to fix in code. The partner agreement still needs the PM-1236 wording change (50% of **Net Revenue**, inclusive of any partner VAT) — that is a VAT/fee-allocation fix, not a price fix.

**[LEWIS] Misleading-price exposure:** an always-on "50% off" needs a genuine reference price under the CPRs / ASA rules — the £20 has to have been a real, available price for a meaningful period, and a permanent discount that never ends is the risk. Safer framings: "£10/month introductory rate", time-boxed offers, or presenting £20 as the standard and £10 as the founder/partner rate. Lewis's call before the £10 framing goes on the marketing site.

---

**PM-1237 (2026-09-13, 22:00): RUNNING PLAN REBUILD OPENED — `tasks/cardio-running-waves.md`, trigger "running wave N". No code.**

Runna torn down end to end (web onboarding ×2 + the paid app on a trial). **They do not generate plans — a curated template library is parameterised by a ~14-screen all-enum wizard, which is why theirs is instant and ours is an AI call in the critical path.** Session model confirmed: Warm-Up / Session / Cool Down blocks, numbered steps, distance + pace band or limit, inline rest, RUN/REST tags, outdoor/treadmill toggle — the shape WorkoutKit and the Garmin Training API both consume. One race time → fitness score → five bands (they take a second, longer-distance time for accuracy); injury and ability answers set named knobs (Volume, Difficulty) rather than the plan itself. AI appears only as narration (timed-release briefing, "why does this session matter", a named founding coach). Locked for us: deterministic engine, Calum-authored templates, bands not figures, HealthKit/Health Connect pre-fill of mileage and fitness at signup (their structural blind spot — they have no data on a new member), effort/HR fallback, and **no in-app GPS in v1** — completion via the existing health pipeline, with Garmin cloud → WorkoutKit → GPS tracking parked as Wave 7 in that order. Waves 0–7 plus the open decisions (Calum's authoring surface, which goals ship first, rolling-vs-fixed for General training) are in the task doc. Wave 0 = schema + pace engine, server-only, nothing member-facing. Gates: Lewis strings, Phil the injury-history question, Calum templates and progression. Pricing, alongside today's PM-1236 reset: Runna £15.99/mo or £99.99/yr for running alone vs VYVE £10/mo for the platform.

---

**PM-1236 (2026-09-13): PRICING RESET TO £10 + VAT POSITION WORKED OUT (no code).**

**Dean: the £20 list price is retired — pretty much every membership is £10/month from here, B2B £10/seat or lower.** §3 updated. The £20 new-signup Stripe link and the £20/£15/£10 partner tier-coupon ladder (§23.129) now describe a product that no longer exists — Lewis sign-off before either is re-cut.

**VAT, settled from first principles this session:**

- **Not registered, nowhere near.** Threshold is £90k of **turnover, not profit**, on a rolling 12 months — about £7,500/month of billings. Live truth today: no paying B2C at all (1 canceled paid, 25 comp, 1 enterprise seat, rest trials). At £10/month that is ~750 members, or ~750 B2B seats, before it bites.
- **Direction of travel.** You charge VAT on what you sell (output tax) and reclaim it on what you buy (input tax). A VAT-registered customer reclaims what they pay *us*; nobody reclaims VAT on revenue they receive.
- **Crossing is a cliff edge and forward-only** — revenue earned before the registration date is never taxed retrospectively. *Backward test:* exceed £90k over the previous 12 months → notify HMRC within 30 days of that month-end, registered from the 1st of the following month (~2 months' lead). *Forward test:* expect to bill >£90k in the next 30 days **alone** → registered from day one of that window. **A first enterprise contract invoiced annually up front trips the forward test on its own; monthly billing does not — bill the first enterprise deal monthly.**
- **B2B contracts must say "exclusive of VAT".** Employers reclaim, so £10 + VAT costs them nothing net and VYVE still keeps £10. A contract silent on VAT + later registration = HMRC treats the figure as VAT-inclusive and VYVE eats 1/6th per seat for the life of the deal. B2C is the opposite: the price point can't move, so registration is a straight 16.7% haircut on consumer revenue.
- **Partner share must be "50% of Net Revenue"** — amounts actually received from the member less VAT, less payment-processing fees, less any app-store commission — **and inclusive of any VAT the partner must charge**. Fixed-£ or share-of-gross means every external cost comes out of VYVE's half only; VAT-inclusive wording means a partner who registers for VAT doesn't suddenly cost 20% more than one who hasn't. Needs a VAT-registered flag on `partner_partners` + a statement branch; self-billing then requires an HMRC self-billing agreement (partner VAT number, "self-billing", re-signed annually) or registered partners invoice manually. **This closes the partner-statement "VAT position / self-billing clause" open item.**
- **Ruled out:** voluntary registration (Anthropic/Supabase are reverse-charge, almost no input VAT to recover against a 16.7% B2C loss); Flat Rate Scheme (limited-cost business → 16.5% of gross, no saving, loses input recovery); medical-care exemption (needs care by a registered professional, not a platform subscription) and sport exemption (needs a body managed on a voluntary basis — paid directors kill it). CIC status buys procurement points, not tax relief; corporation tax applies as to any company.
- **Threshold counts more than member revenue:** the full gross counts even where half goes to a partner (VYVE is principal, the share is a cost), and reverse-charge purchases from overseas suppliers count toward it too.

**[LEWIS]** £10 list-price sign-off; "exclusive of VAT" on the B2B contract template + price list; partner-agreement rev-share clause re-worded. **[DEAN]** rolling-12-month turnover tripwire on the CC finance page (amber £70k, red £85k) — not built.

---

**PM-1235 (2026-09-13, 07:45): ROWS ARE ROWS AGAIN — CC `d7a2027c` (coach-portal.html md5 `df7fa420`).**

Dean's Day templates screenshot after PM-1234: thumbnail on its own line, name under it, the "unassigned" chip stretched full width, buttons below — every row twice the height. Not the grid view: the pager showed an item with `el.style.display = ''`, which **strips the row's inline `display:flex`** and lets it fall back to block. The rows carry their layout inline (every renderer writes `style="display:flex;…"`), so a DOM-level pager must remember and restore that value (`data-lt-disp`) rather than blanking it. §23.345. Grid view re-cut on Dean's note — thumbnail to the side, name and actions beside it, one compact card (flex-wrap card, 64×48 thumb, tighter buttons). Harness: shown rows read `display:flex`, hidden `none`, page 2 the same.

---

