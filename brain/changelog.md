**PM-1223 (2026-09-13, 03:00–04:10): CALUM BATCH 2 PART 1 SHIPPED — LIBRARY V2 + PICKER OVER THE BUILDER. CC `0bbcbeda` (coach-portal.html md5 `aa22cf29`; physio untouched); migration `pm1223_exercise_taxonomy_normalise`. No EF, no vyve-site change.**

**Taxonomy (migration).** Two immutable SQL helpers, `vyve_norm_muscle(text)` and `vyve_norm_equipment(text)`, and one UPDATE over the stock rows: `category` collapses to one vocabulary (Quads · Shoulders · Abs & core · Chest · Hamstrings · Triceps · Biceps · Lats · Mid back · Glutes · Calves · Lower back · Forearms · Traps · Adductors · Abductors · Neck · Full body · Other; compounds like "Quads / Glutes / Cardio" take their first part; Stretching untouched), `equipment` to Barbell · Dumbbell · Bodyweight · Cable · Machine · Kettlebell · Bands · Medicine ball · Swiss ball · Foam roller · EZ bar · Box · TRX · Other (plus a handful of honest leftovers from the stretch rows — Towel, Wall, Chair, Spiky Ball). Own rows: casing only where the value already matched the vocabulary; a coach's free text stays theirs. Index `(library, category, equipment)`. Before: 36 category values / 32 equipment values on 977 rows; after: 20 / 18 on 1,455. `muscle_volumes` (PM-985) untouched — it is the secondary-muscle map, keys are its own.

**Library v2 (`330-library-v2-pm1223.js`, coach page only — physio keeps v3 until it gets the same slice).** `exv2Ensure()` builds a two-column shell once inside the existing Exercise Library card and *moves* the existing toolbar, count, ▶ line and `#ex-list` into the right column — no markup edit, every id keeps working. Left rail with live counts from `cexRows`: All · My exercises · VYVE library · ★ Favourites, then collapsible **Muscle group**, **Equipment**, **Stretching & mobility** (regions = `subcategory` of `category='Stretching'`, "All stretches" on top). The rail drives the *hidden* `#ex-f-cat` / `#ex-f-eq` selects and `exScope`, so sub-chips (PM-1216), the video select, staff bulk tools and every other consumer keep working; `exv2SetSel()` adds a missing `<option>` so a rail pick never depends on `exLoad` having filled the lists. Head row: title per pick, **Sort A–Z / newest** (`created_at` added to `W3_SEL`), **Video any / has / missing** chip (drives the hidden select). `exRender` **SHADOWED** (v4 = v3 + `fav` scope + sort + ★ star on every card + pick mode + "Show 60 more" instead of the hard 60 cap). Favourites live in `coach_ui_prefs.fav_exercises` (§23.237) via `w0savePrefs`; loaded alongside rows in the wrapped `exInit`; `exLoad` wrapped to repaint the rail whenever rows reload (go() calls it for the workout sections).

**The picker (Dean's PM-1222 decision).** `renderDayEditor` (live one is 260's) is wrapped: a **☰ Choose from library** button lands beside `+ Warm up exercise`, `+ Exercise` and `+ Cool down exercise`. `exv2PickerOpen(wrap, label, defaults)` moves `#exv2-wrap` (rail + grid) into a `.w3-modal.exv2-modal` sheet (1400px, 92vh), cards switch to **+ Add / ✓ Added**, picks show as chips in the header, **Done · add N** calls `addDayRow` per pick with the block's defaults (`duration` type + default seconds carried for timed exercises), dispatches the row's `change` so PM-985's default-prefill and `w3RowDecorate` run, refreshes the volume strip, and moves the library back to its home node. Cancel / backdrop just moves it back. Typed entry via the old buttons is untouched.

**Calum #13 (per-row tracking).** The Reps / Secs label on any builder row is now a toggle (dotted underline, title hint): flips `w3RowSetMode`, swaps the value to `30` / `8-12`, refreshes volume. `collectDay` already reads `dataset.w3type`, so a treadmill run switched to seconds saves as `type:'duration'` with no contract change. Distance / calories wait on `programme_json` (Batch 3).

**Proof.** `build-portals --check` + `smoke-portals` green. Harness (jsdom, slice + stubs over the real `#view-exercises` markup, 150 rows): rail 11 items with counts → Quads narrows 60 → 38, hidden select follows → Favourites scope 2 → star toggle saves `{fav_exercises:[…]}` and narrows to 1 → Stretching › Hip sets `exSubSel` → sort newest → Show more 60 → 120 → three Choose buttons injected → picker moves the wrap into the sheet → two picks, "Done · add 2" → two `addDayRow` calls into `.de-main`, sheet closed, wrap back home, volume refreshed → Reps/Secs toggle flips type and value. Three files md5-perfect at `0bbcbeda`. **Not device-checked.**

**Open / next (Batch 2 part 2).** Thumbnails in the Weekly workout view (#12), search + sort on the three template lists (#14), builder as a page (#15), same slice on the physio page. **Dean deploy check:** Exercise Library → rail with counts, Quads → cards narrow, ★ on a card → Favourites count moves; Workouts › Day templates → New → ☰ Choose from library → pick two → Done → rows appear; click "Reps" on a row → "Secs". New rule §23.340.

---

