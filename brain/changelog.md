**PM-1221 (2026-09-13, 02:40): PROGRAMME BUILDER MOCKUP — CC `f49648d8`, `docs/mockups/programme-builder-mockup.html` (unlinked, standalone, 28KB). Nothing built.**

Dean walked Kahunas' Create a Program flow on screen (chooser → detailed builder → day tabs → Add an Exercise drawer) and asked for the same shape mocked on our tokens. Two screens: (1) **New programme** chooser — Simple (typed text, ticked-done in the app, no tracking) · **Detailed** (the builder) · Upload a PDF (greyed "later"). (2) **Builder as a page** (Calum #15 — not a card appended under the list): title, tags, overview; **day tabs** with a **Mon–Sun / Session 1, 2, 3…** toggle (Calum #19) and **+ Add day → Create new · Import a day template · Import a weekly workout · Add rest day** (Calum #20 + Kahunas parity); rest day renders as a plain card; per day: name, Save day as template, Duplicate, Delete; **blocks** Warm up / Workout / Cool down with add-a-block (Conditioning · Mobility · Core · Finisher) (Calum #18); exercise rows with thumbnail, ▶ where a video exists, a **tracking select on the row** (sets × reps / time / distance / calories) so a treadmill run is never locked to reps (Calum #13), a prescription box that follows the type, rest, a **superset letter** toggle, note, remove; instructions to the client. **Choose exercise drawer** on the right: search first (name / muscle / equipment), chips All · VYVE · Mine · ★ Favourites · + New, result rows with tracking default and source badge, Add drops it straight into the block that opened the drawer; block-aware defaults (Warm up lists stretches/timed/cardio first, Cool down stretches first); **"Browse the library instead"** expander with the muscle / stretching rails from PM-1220 — so it works whichever way Calum answers. jsdom pass: chooser → builder, 4 tabs, drawer 32 → "chest press" 4 → Add lands in Workout, rest day, template import adds 3 rows, session-mode labels, cool-down puts a stretch first; zero errors.

**Open question Dean is taking to Calum:** in Kahunas a coach just searches by name when adding an exercise and never sees a library page, so the picker may not need the PM-1220 rail at all. The drawer is built search-first with the rail folded away for exactly that reason; PM-1220's library page stays for managing exercises (own videos, favourites, duplicates) either way.

**Build implications noted, not decided:** our `program` template today is phases of day templates; Kahunas' shape is days-in-programme with day templates as an import source — the builder above assumes the latter, which is a `programme_json` contract change on both sides (Batch 3). Blocks are new structure `workouts-session.js` must render. Simple programmes = a new `kind` or a `program` with text-only days.

---

