**PM-1228 (2026-09-13, 05:45): DRAWER ROUND 2 — VIDEO ON TOP, BLANK ROW GONE, MINUTES, AND VYVE'S CARDIO VIDEOS WIRED. CC `afd0d5b1` (coach-portal.html md5 `8b6cb975`); migration `pm1228_cardio_videos_wired`.**

Dean on the drawer: "that actually looks amazing" — then four things. (1) **Video preview opened behind the drawer** — the preview sheet is a `.w3-modal` at z-index 970 and the drawer sat at 975; drawer now 960, so the sheet plays on top. (2) **The blank typed row was still there** — PM-1226 tested `day.exercises.length`, but a new day's payload is `{ exercises: [{}] }` (length 1). Now tests for a *named* exercise; every unnamed row is removed on a new day. The "or type a name" link is gone too ("it should just say add exercises"); custom exercises come from + New exercise in the drawer. (3) **"Where are our cardio videos?"** — VYVE's own recordings were in Storage (`exercise-videos/` + `exercise-thumbnails/`: assault-bike, incline-walking-treadmill, jogging-treadmill, recumbent-bike, rowing-machine, ski-erg, sled, stair-master, stationary-bike, walking-treadmill) and wired to nothing — not `workout_plans`, not `coach_exercises`. Migration attaches them as `media_url` (+ thumbnail as `image_url`) to the PM-1224 Cardio rows by slug, gives the reps-style stock Sled Push the sled recording, and turns the existing stock "Recumbent Bike" row into a timed Cardio row with its video. 10 Cardio rows now play a VYVE video inline; the other 14 (skipping, battle ropes, swimming…) still wait for a recording. (4) **Minutes.** A duration row's label cycles **Reps → Secs → Mins**; in Mins the input shows minutes (`data-w3unit='min'`), `collectDay` is wrapped to hand the base collector seconds and put the minutes back, so the contract stays `duration_seconds`. A drawer pick with `default_duration_seconds ≥ 120` opens in minutes ("20"), and saved rows with a long duration display in minutes on reopen; a plank stays in seconds.

**Proof.** Harness: new day renders 0 rows, 0 links, 3 drawer buttons labelled "+ Add exercises"; drawer z 960; a 1,200s cardio pick lands as `min / Mins / 20`, `collectDay` yields `dur:1200` and the input still reads 20; label cycle min → reps (8-12) → secs (30) → min; reopening a day with `{45s, 1200s}` shows `sec:45 min:20`. Build/smoke green; two files md5-perfect at `afd0d5b1`.

**Open.** Dean: hard refresh → new day has no row → + Add exercises → Cardio in the rail → Rowing Machine card has ▶, click the thumbnail → video plays over the drawer → + Add → Done → row reads Mins 20. Programmes still to be pinned.

---

