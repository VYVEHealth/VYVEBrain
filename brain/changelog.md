**PM-1224 (2026-09-13, 04:30): WEEKLY WORKOUTS GET THE REAL BUILDER + CARDIO IN THE LIBRARY. CC `d2203364` (coach-portal.html md5 `76aeeb0d`); migration `pm1224_cardio_stock`. No EF, no vyve-site change.**

**Why Dean saw nothing.** His screenshot was Weekly workouts › + New: a bare Exercise / Sets / Reps / Rest / Notes row with a native datalist of *member-library* names (10 Minute Manifestation Meditation, 15 Mins of Yoga… — `workout_plans.exercise_name` via `loadExerciseNames`, the `ex-names` list). That editor is the PM-955 original, `addWSession` in `050-plans.js`, never upgraded: no tempo/RIR/superset, no timed rows, no volume strip — and PM-1223's picker and Reps/Secs toggle hook `renderDayEditor`, which only Day templates and programme days used. So the deploy had landed; Weekly workouts simply weren't on that builder. Calum's "Weekly workouts show names without images" (#12) is the same root.

**Fix (`340-weekly-editor-pm1224.js`).** `addWSession` SHADOWED: a session is a name + Remove session row and a `.w-day` container rendered by `renderDayEditor(sess)` — the same rows as a Day template (coach-library datalist `cex-names`, ensured/filled after `exLoad`), the same ☰ Choose from library sheet, Reps/Secs toggle, superset letters, tempo/RIR, the volume strip, and (via 260's editor) the regular / circuit / interval type panel. `plCollect` wrapped: for kind `workout` it runs `collectDay` per session and returns `{ sessions:[ { name, exercises, warmup, cooldown, … } ], instructions }` — same top-level shape as before, rows gain the optional fields `coach_build_program_json` already reads for sessions (checked: `sessions`, `warmup`, `duration_seconds`, `tempo`, `group`, `exercise_id` all in its source). Existing weekly templates open unchanged (their rows have the subset). The old `ex-names` datalist is left in the markup, unused.

**Cardio (Dean: "we should have cardio exercises").** 23 stock rows, `category 'Cardio'`, `exercise_type 'duration'`, sensible default seconds (Treadmill Run 20 min, Assault Bike 10, Jumping Jacks 3…), cues, `source_slug 'vyve:cardio:<slug>'`, no videos. `uq_coach_ex_stock_name` (lower(name)) skipped Sled Push / Mountain Climbers (exist as reps rows) and "Air Bike" is the stock abs exercise, so the machine is "Assault Bike" and the timed sled is "Sled Push — Timed". The rail shows Cardio 23 under Muscle group.

**Proof.** `build-portals --check` + smoke green; wiring harness: two sessions → two `renderDayEditor` calls, `cex-names` created, `plCollect` returns one populated session with `name` merged and instructions, other kinds fall through to the original. Three files md5-perfect at `d2203364`. **Not device-checked.**

**Open.** Dean: reload (hard refresh — Cloudflare Pages is usually live within a minute of the push; if the page still shows the old row, it's the browser cache) → Weekly workouts › + New → a session now has ☰ Choose from library beside + Exercise, rows carry Sets · Reps/Secs · Tempo · Rest · RIR · Superset · Cue; Exercise Library → Cardio 23. "Programmes — I can't really do anything": Dean to say what he expected; the programme editor composes day templates (pick a template per slot), which with zero day templates on a fresh partner is an empty picker — likely the thing he hit. Fixing that is the PM-1221 shape (days built directly in the programme, templates as an import) — Batch 3, or a smaller fix: "+ New day here" inside the programme slot.

---

